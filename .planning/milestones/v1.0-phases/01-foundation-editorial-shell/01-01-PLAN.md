---
phase: 01-foundation-editorial-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - astro.config.mjs
  - tsconfig.json
  - .gitignore
  - .github/workflows/deploy.yml
  - src/pages/index.astro
  - public/prateek-kumar-resume.pdf
  - public/favicon.svg
autonomous: true
requirements: [PLAT-01, PLAT-02, PLAT-03, CONT-05]
user_setup: []

must_haves:
  truths:
    - "npm run build produces static dist/ output with zero framework runtime"
    - "astro.config.mjs targets the user-root domain with no base path"
    - "A GitHub Actions workflow exists that builds and deploys to Pages on push to main"
    - "The résumé PDF is downloadable from the site root path /prateek-kumar-resume.pdf"
  artifacts:
    - "astro.config.mjs"
    - ".github/workflows/deploy.yml"
    - "public/prateek-kumar-resume.pdf"
    - "public/favicon.svg"
  key_links:
    - "astro.config.mjs site + absent base -> correct asset/link resolution at user-root"
    - "deploy.yml withastro/action@v6 + deploy-pages@v5 -> auto-deploy on push to main"
    - "git remote origin -> existing p2401kumar/p2401kumar.github.io repo"
---

<objective>
Stand up the Astro 7 static project skeleton, lock the deploy-critical configuration (user-root `site`, no `base`), author the GitHub Actions Pages deploy workflow, wire this working tree's git remote to the pre-existing `p2401kumar/p2401kumar.github.io` repo, and place the two static assets that later components link to (résumé PDF, favicon). No push happens here — the first live deploy is gated in plan 01-07.

Purpose: Every later plan builds on this scaffold; getting `site`/`base` and the deploy pipeline correct before any component links exist is the single highest-leverage foundation decision (ARCHITECTURE.md Anti-Pattern 4).
Output: A buildable Astro project, a deploy workflow, a wired remote, and the résumé/favicon assets.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-editorial-shell/01-CONTEXT.md
@.planning/phases/01-foundation-editorial-shell/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Astro 7 minimal, lock config, verify toolchain</name>
  <files>package.json, package-lock.json, astro.config.mjs, tsconfig.json, .gitignore, src/pages/index.astro</files>
  <read_first>
    - .planning/phases/01-foundation-editorial-shell/01-RESEARCH.md (Standard Stack, Installation block, Pitfall 4 on the typescript@7.0.2 anomaly, State of the Art on the removed --typescript flag)
    - .planning/phases/01-foundation-editorial-shell/01-CONTEXT.md (Stack & structure decisions)
    - .planning/research/ARCHITECTURE.md (Recommended Project Structure, Anti-Pattern 4)
  </read_first>
  <action>
    Scaffold in the current directory (which already contains .git, .claude, .planning) using the current create-astro CLI with the `minimal` template and `--no-install --no-git --no-ai --yes` (do NOT pass any `--typescript` flag — it no longer exists in the current CLI; the minimal template's tsconfig.json already extends `astro/tsconfigs/strict`, which is the desired strict config — leave it as-is). Then run `npm install`. Re-verify pinned versions at install time with `npm view astro version` (expect ^7, 7.0.7-class). Author astro.config.mjs via `defineConfig` with exactly three settings: `site: 'https://p2401kumar.github.io'`, `output: 'static'`, and NO `base` key at all (user-root deploy — locked decision). Do NOT add the fontaine vite plugin here — that belongs to plan 01-02 which owns the font pipeline. Add a `.gitignore` covering `node_modules/`, `dist/`, `.astro/`, and OS/editor cruft. Guard the typescript version anomaly from RESEARCH Pitfall 4: do NOT `npm install typescript@latest`; let it resolve via Astro's own tooling, then assert the resolved version with `npm ls typescript`. Leave the scaffold's default `src/pages/index.astro` untouched (plan 01-06 replaces it with the real composition).
  </action>
  <verify>
    <automated>npm run build && grep -c "site: 'https://p2401kumar.github.io'" astro.config.mjs && [ "$(grep -v '^\s*//' astro.config.mjs | grep -c 'base:')" = "0" ] && node -e "const v=require('child_process').execSync('npm ls typescript --depth=10 --silent||true').toString(); if(/typescript@7\./.test(v)){console.error('TS resolved to unexpected 7.x — re-pin to Astro peer range 5.x');process.exit(1)} console.log('ts ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 and produces a `dist/` directory containing `index.html`
    - `astro.config.mjs` contains the literal `site: 'https://p2401kumar.github.io'`
    - `astro.config.mjs` contains NO active `base:` key (filtered grep excluding comment lines returns 0)
    - `astro.config.mjs` sets `output: 'static'`
    - `tsconfig.json` extends `astro/tsconfigs/strict`
    - `npm ls typescript` resolves to a 5.x version (NOT 7.x); if 7.x, the task fails and the executor re-pins to Astro's peer range
    - No `@astrojs/react`/`@astrojs/vue`/`@astrojs/svelte` or any UI-framework integration is present in package.json (PLAT-01: zero framework runtime)
    - `.gitignore` contains `node_modules`, `dist`, and `.astro`
  </acceptance_criteria>
  <done>Astro 7 project builds to static output, config locks user-root deploy with no base path, and the toolchain versions are sane.</done>
</task>

<task type="auto">
  <name>Task 2: Author the GitHub Pages deploy workflow and wire the git remote</name>
  <files>.github/workflows/deploy.yml</files>
  <read_first>
    - .planning/phases/01-foundation-editorial-shell/01-RESEARCH.md (Code Examples: deploy workflow YAML with the verified action trio; Pitfalls 2 and 3; Security Domain known threat patterns)
    - .planning/research/STACK.md (deploy action versions)
  </read_first>
  <action>
    Create `.github/workflows/deploy.yml` named "Deploy to GitHub Pages", triggered on `push` to `main` plus `workflow_dispatch`. Set the minimal top-level `permissions` block to exactly `contents: read`, `pages: write`, `id-token: write` (no broader scopes — mitigates T-01-01). Two jobs: a `build` job on `ubuntu-latest` using `actions/checkout@v7` then `withastro/action@v6`; and a `deploy` job that `needs: build`, declares the `github-pages` environment with `url: ${{ steps.deployment.outputs.page_url }}`, and runs `actions/deploy-pages@v5` with `id: deployment`. Use those exact major-version pins (verified in RESEARCH). Then wire this working tree to the pre-existing remote (it currently has NO remote): run `git remote add origin https://github.com/p2401kumar/p2401kumar.github.io.git` and confirm with `git remote -v`. Do NOT push, do NOT change the Pages build_type, and do NOT rename the branch here — those destructive/live-affecting steps are gated in plan 01-07 so the currently-live 2021 site is not disrupted mid-build.
  </action>
  <verify>
    <automated>grep -c "withastro/action@v6" .github/workflows/deploy.yml && grep -c "actions/deploy-pages@v5" .github/workflows/deploy.yml && grep -c "actions/checkout@v7" .github/workflows/deploy.yml && grep -c "id-token: write" .github/workflows/deploy.yml && git remote get-url origin</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/deploy.yml` triggers on push to `main` and on `workflow_dispatch`
    - Workflow uses `actions/checkout@v7`, `withastro/action@v6`, `actions/deploy-pages@v5` (exact major pins)
    - `permissions` block is exactly `contents: read`, `pages: write`, `id-token: write` — no additional write scopes (T-01-01 mitigation)
    - `git remote get-url origin` returns `https://github.com/p2401kumar/p2401kumar.github.io.git`
    - No `git push` occurred (the live 2021 site is untouched at this stage) and Pages build_type is NOT modified here
  </acceptance_criteria>
  <done>A correct, minimal-permission Pages deploy workflow exists and the local repo points at the correct existing remote, with no live-site side effects.</done>
</task>

<task type="auto">
  <name>Task 3: Place résumé PDF and favicon assets</name>
  <files>public/prateek-kumar-resume.pdf, public/favicon.svg</files>
  <read_first>
    - .planning/phases/01-foundation-editorial-shell/01-CONTEXT.md (Content data & honesty gate: résumé PDF source path and public target name)
    - .planning/phases/01-foundation-editorial-shell/01-RESEARCH.md (Security Domain: PDF metadata leakage mitigation)
    - .planning/phases/01-foundation-editorial-shell/01-UI-SPEC.md (Color tokens for the favicon mark)
  </read_first>
  <action>
    Copy `C:\Users\prateekkumar\Downloads\Prateek_Kumar_resume_4_5.pdf` into `public/prateek-kumar-resume.pdf` (this executor runs on the Windows source machine, so the file is reachable). Before committing, strip document metadata and any embedded revision/comment history from the copy to mitigate T-01-02: Python 3.12/3.13 is available on this machine, so use a pypdf pass (`py -m pip install --quiet pypdf` then a small script that reads the PDF, clears the document info / metadata dictionary, and writes it back) — or an equivalent tool if already present. Author `public/favicon.svg` as a minimal square mark using the locked palette (graphite background `#0f1216`, a single copper `#d99163` glyph or bar) — Claude's discretion on the exact mark, keep it under ~1KB, no external references. A full favicon/OG set is explicitly Phase 3 scope, so only the SVG is needed now.
  </action>
  <verify>
    <automated>test -f public/prateek-kumar-resume.pdf && test -f public/favicon.svg && node -e "const b=require('fs').readFileSync('public/prateek-kumar-resume.pdf');process.exit(b.slice(0,5).toString()==='%PDF-'?0:1)" && grep -c "#d99163" public/favicon.svg</automated>
  </verify>
  <acceptance_criteria>
    - `public/prateek-kumar-resume.pdf` exists and begins with the `%PDF-` signature (valid PDF)
    - The PDF's document-info metadata (Author/Title/Producer/comments/revision history) has been cleared or made generic (T-01-02 mitigation) — verified by re-reading the info dictionary and finding no personal revision/comment residue
    - `public/favicon.svg` exists, references the copper token value `#d99163`, and contains no external URLs
  </acceptance_criteria>
  <done>The résumé PDF (metadata-stripped) and an SVG favicon live under public/ at the paths later components and BaseLayout will reference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine -> GitHub (git/gh) | Authenticated push/remote operations via gh keyring |
| GitHub Actions -> GitHub Pages | OIDC id-token deploy, managed by GitHub infra |
| npm registry -> build | Third-party packages pulled at install time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Elevation of Privilege | deploy.yml permissions | high | mitigate | Minimal `permissions` block (`contents: read`, `pages: write`, `id-token: write`) only — Task 2 acceptance gate |
| T-01-02 | Information Disclosure | public/prateek-kumar-resume.pdf | low | mitigate | Strip PDF document metadata / revision history before commit (Task 3) |
| T-01-SC | Tampering | npm install (astro + deps) | high | mitigate | RESEARCH Package Legitimacy Audit approved all packages via direct-registry verification; no postinstall scripts confirmed; TS version anomaly guarded by `npm ls typescript` assertion (Task 1) |
</threat_model>

<verification>
- `npm run build` exits 0 with static `dist/` output
- `astro.config.mjs`: `site` set to the user-root URL, no active `base` key, `output: 'static'`
- Deploy workflow present with verified action trio and minimal permissions
- `git remote origin` points at the existing user-root repo; no push performed
- Résumé PDF (metadata-stripped) and favicon.svg present under public/
</verification>

<success_criteria>
The project builds to zero-runtime static output, the deploy pipeline and remote are wired but dormant, and the two static assets are in place — a clean foundation with no live-site side effects.
</success_criteria>

## Artifacts this phase produces (plan 01-01)

- `astro.config.mjs` — config keys: `site`, `output` (no `base`)
- `.github/workflows/deploy.yml` — workflow name "Deploy to GitHub Pages", jobs `build`/`deploy`
- `tsconfig.json` (extends `astro/tsconfigs/strict`), `.gitignore`
- `package.json` / `package-lock.json` (astro ^7)
- `public/prateek-kumar-resume.pdf`, `public/favicon.svg`
- git remote `origin` -> `p2401kumar/p2401kumar.github.io`

<output>
Create `.planning/phases/01-foundation-editorial-shell/01-01-SUMMARY.md` when done.
</output>
