---
phase: 01-foundation-editorial-shell
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/styles/tokens.css
  - src/styles/global.css
  - src/styles/fonts.css
  - public/fonts/source-serif-4-latin-500-normal.woff2
  - public/fonts/cascadia-code-latin-400-normal.woff2
  - src/layouts/BaseLayout.astro
  - astro.config.mjs
autonomous: true
requirements: [SHELL-05, PLAT-04]
user_setup: []

must_haves:
  truths:
    - "Every color/spacing/type value in the site traces to one file, src/styles/tokens.css"
    - "The hero serif and header mono render from self-hosted subsetted woff2 with no visible CLS/FOUT on reload"
    - "The right-arrow glyph in hero links renders consistently in the mono face"
    - "BaseLayout provides the html/head shell, preloads both fonts, and imports the three stylesheets once"
  artifacts:
    - "src/styles/tokens.css"
    - "src/styles/fonts.css"
    - "src/styles/global.css"
    - "public/fonts/source-serif-4-latin-500-normal.woff2"
    - "public/fonts/cascadia-code-latin-400-normal.woff2"
    - "src/layouts/BaseLayout.astro"
  key_links:
    - "tokens.css imported once in BaseLayout -> all components read var(--token)"
    - "font preload links + fontaine metrics-matched fallback -> no hero CLS (PLAT-04)"
    - "re-subset Cascadia woff2 including U+2192 -> arrow glyph renders in-family in hero links"
---

<objective>
Establish the single design-token source (`tokens.css`), the base reset/typography (`global.css`), the self-hosted font pipeline with metrics-matched fallbacks and the resolved U+2192 arrow-glyph gap (`fonts.css` + woff2 + fontaine), and the `BaseLayout.astro` shell that every page fills. This is the styling foundation the shell and content components read from.

Purpose: SHELL-05 (one token source) and PLAT-04 (self-hosted subsetted fonts, no CLS) are the two design-integrity requirements everything downstream depends on. Getting fonts right — including the verified Cascadia `→` coverage gap — protects the hero, the highest-visibility element on the page.
Output: tokens.css, global.css, fonts.css, two woff2 files, fontaine wiring, and BaseLayout.astro.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-editorial-shell/01-CONTEXT.md
@.planning/phases/01-foundation-editorial-shell/01-UI-SPEC.md
@.planning/phases/01-foundation-editorial-shell/01-RESEARCH.md
@.planning/reference/prototype-shell-and-fig01.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author tokens.css (single source) and global.css (reset + base type)</name>
  <files>src/styles/tokens.css, src/styles/global.css</files>
  <read_first>
    - .planning/phases/01-foundation-editorial-shell/01-UI-SPEC.md (Color table with exact token values; Typography roles; Spacing baseline scale)
    - .planning/phases/01-foundation-editorial-shell/01-CONTEXT.md (Design system: full token list; type roles)
    - .planning/reference/prototype-shell-and-fig01.html (the `:root` block and `body`/`a` base rules — port these values verbatim)
  </read_first>
  <action>
    Create `src/styles/tokens.css` as a single `:root` block declaring every locked token exactly (do not invent or round values): `--bg:#0f1216`, `--panel:#12161c`, `--panel2:#141a22`, `--ink:#e8ebef`, `--body:#aab2bd`, `--dim:#78818d`, `--faint:#565f6b`, `--hair:#1e242d`, `--hair2:#28303b`, `--accent:#d99163`, `--good:#57b98a`, `--amber:#d9a441`. Also declare the three font-family stack tokens verbatim from the prototype: `--mono: ui-monospace,"Cascadia Code","SF Mono",Consolas,monospace`, `--serif: "Iowan Old Style","Palatino Linotype",Palatino,Georgia,serif`, `--sans: "Helvetica Neue","Segoe UI",Arial,system-ui,sans-serif`. This file is the ONLY place hex/stack literals are declared (ARCHITECTURE.md Anti-Pattern 2). Create `src/styles/global.css` with: `* { box-sizing:border-box }`, a `body` rule (`margin:0; background:var(--bg); color:var(--body); font-family:var(--sans); -webkit-font-smoothing:antialiased; font-size:15px; line-height:1.6`), and the base `a` rules (`color:var(--ink); text-decoration:none; border-bottom:1px solid var(--hair2); transition:border-color .15s` with `a:hover{ border-color:var(--accent) }`) — all reading via `var(--token)`, never a literal. Add a visible keyboard focus style for links/buttons (`:focus-visible { outline:2px solid var(--accent); outline-offset:2px }`) per the day-one accessibility requirement.
  </action>
  <verify>
    <automated>grep -c -- "--accent:#d99163" src/styles/tokens.css && grep -c -- "--bg:#0f1216" src/styles/tokens.css && grep -c -- "--amber:#d9a441" src/styles/tokens.css && grep -c "focus-visible" src/styles/global.css && [ "$(grep -cE '#[0-9a-fA-F]{6}' src/styles/global.css)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `src/styles/tokens.css` declares all 12 color tokens with the exact locked hex values listed above
    - `src/styles/tokens.css` declares `--mono`, `--serif`, `--sans` stack tokens verbatim from the prototype
    - `src/styles/global.css` contains NO 6-digit hex literal (every color read via `var(--token)`) — grep for `#[0-9a-fA-F]{6}` returns 0
    - `src/styles/global.css` defines a visible `:focus-visible` outline using `var(--accent)`
    - body base is 15px / line-height 1.6 with `font-family:var(--sans)`
  </acceptance_criteria>
  <done>tokens.css is the sole literal source; global.css applies the base reset/type reading only tokens, with day-one focus states.</done>
</task>

<task type="auto">
  <name>Task 2: Self-host fonts, resolve U+2192, wire fontaine fallback metrics</name>
  <files>public/fonts/source-serif-4-latin-500-normal.woff2, public/fonts/cascadia-code-latin-400-normal.woff2, src/styles/fonts.css, astro.config.mjs</files>
  <read_first>
    - .planning/phases/01-foundation-editorial-shell/01-RESEARCH.md (Standard Stack font packages; Pattern 3 @font-face + fontaine; Pitfall 1 the U+2192 gap; Open Question 1 fontaine multi-family config)
    - .planning/phases/01-foundation-editorial-shell/01-UI-SPEC.md (Font faces table: Source Serif 4 @500, Cascadia Code @400, glyph subset note)
    - astro.config.mjs (current config from plan 01-01 — you will add the vite.plugins array)
  </read_first>
  <action>
    Install the fixed-weight font packages as dev deps: `npm install -D @fontsource/source-serif-4 @fontsource/cascadia-code fontaine brotli` (re-verify with `npm view` per RESEARCH). Copy `node_modules/@fontsource/source-serif-4/files/source-serif-4-latin-500-normal.woff2` into `public/fonts/` unchanged. For the mono face, RESOLVE the verified U+2192 (`→`) coverage gap (RESEARCH Pitfall 1) by DECISION: re-subset Cascadia Code so the shipped woff2 includes U+2192. Concretely: obtain a full Cascadia Code source that contains the arrow outline — prefer a locally installed `C:\Windows\Fonts\CascadiaCode*.ttf` if present, else download the OFL release from the official microsoft/cascadia-code GitHub releases — then subset it with fonttools (`py -m pip install --quiet fonttools brotli`; `pyftsubset <source> --unicodes=U+0020-007E,U+00A0,U+00B7,U+00E9,U+2013,U+2014,U+2192 --layout-features='' --flavor=woff2 --output-file=public/fonts/cascadia-code-latin-400-normal.woff2`). This subset covers the site's actual glyph set: Latin basic + digits + middot (·) + é + en/em dashes + right arrow (→). Author `src/styles/fonts.css` with two hand-written `@font-face` rules (do NOT import the fontsource package CSS): `'Source Serif 4'` weight 500 `font-display:swap` src `/fonts/source-serif-4-latin-500-normal.woff2`, and `'Cascadia Code'` weight 400 `font-display:swap` src `/fonts/cascadia-code-latin-400-normal.woff2`. Register fontaine in astro.config.mjs by adding a `vite.plugins` entry `FontaineTransform.vite(...)` (import from `fontaine`) configured to generate metrics-matched fallbacks for both faces (serif fallback `Georgia`, mono fallback `Consolas`); per RESEARCH Open Question 1 use the object/per-family config shape if a single array-based call does not correctly cover both families, and set `resolvePath` to resolve `/fonts/*` against `./public`. If, and only if, the full Cascadia source cannot be obtained on this machine, fall back to RESEARCH Pitfall 1 option (a) (accept the mono fallback stack for the arrow) and record that deviation in the SUMMARY — but the arrow must still render as a visible glyph, never a `.notdef` box.
  </action>
  <verify>
    <automated>test -f public/fonts/source-serif-4-latin-500-normal.woff2 && test -f public/fonts/cascadia-code-latin-400-normal.woff2 && grep -c "Source Serif 4" src/styles/fonts.css && grep -c "font-display:\?\s*swap\|font-display: swap" src/styles/fonts.css && grep -c "FontaineTransform" astro.config.mjs && py -c "from fontTools.ttLib import TTFont; f=TTFont('public/fonts/cascadia-code-latin-400-normal.woff2'); cmap=f.getBestCmap(); import sys; sys.exit(0 if 0x2192 in cmap else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `public/fonts/source-serif-4-latin-500-normal.woff2` and `public/fonts/cascadia-code-latin-400-normal.woff2` both exist
    - The Cascadia woff2 cmap includes U+2192 (verified via fontTools) — the `→` glyph is in-family; OR (documented fallback path only) the SUMMARY records that option (a) was used and the arrow still renders visibly
    - `src/styles/fonts.css` contains exactly two hand-authored `@font-face` rules (Source Serif 4 @500, Cascadia Code @400), each with `font-display: swap`, and does NOT `@import` any `@fontsource/*` package CSS
    - `astro.config.mjs` registers `FontaineTransform` in `vite.plugins`
    - `npm run build` still exits 0 with the fontaine transform active
  </acceptance_criteria>
  <done>Both faces are self-hosted and subsetted, the arrow-glyph gap is resolved by decision, and fontaine emits metrics-matched fallbacks to prevent hero CLS.</done>
</task>

<task type="auto">
  <name>Task 3: Author BaseLayout.astro (head shell, font preloads, style imports, slot)</name>
  <files>src/layouts/BaseLayout.astro</files>
  <read_first>
    - .planning/research/ARCHITECTURE.md (Component Responsibilities: BaseLayout responsibility; Token Flow diagram)
    - .planning/phases/01-foundation-editorial-shell/01-RESEARCH.md (Pattern 3 preload link markup)
    - .planning/phases/01-foundation-editorial-shell/01-UI-SPEC.md (Copywriting Contract for default title/description tone)
    - src/styles/tokens.css, src/styles/fonts.css, src/styles/global.css (the three files this layout imports)
  </read_first>
  <action>
    Create `src/layouts/BaseLayout.astro` as the single site shell. Frontmatter accepts optional props `title` and `description` (string) with sensible defaults matching the site's factual voice (title default e.g. "Prateek Kumar — distributed systems, cloud, applied ai"; a one-line description naming the positioning — no adjectives about the subject per the tone rule). The document: `<html lang="en">` with `<head>` containing charset utf-8, viewport `width=device-width, initial-scale=1`, `<title>{title}`, meta description, a favicon link (`rel="icon" type="image/svg+xml" href="/favicon.svg"`), and two `<link rel="preload" as="font" type="font/woff2" crossorigin>` tags for the serif and mono woff2 (above-the-fold, LCP-relevant per PLAT-04). Import the three stylesheets ONCE here (`../styles/tokens.css`, `../styles/fonts.css`, `../styles/global.css`) so token flow is one-way. Body renders a single `<slot />`. Do NOT add OpenGraph/sitemap/full meta — that is explicitly Phase 3 scope; only the basic title/description/favicon ship now.
  </action>
  <verify>
    <automated>grep -c "rel=\"preload\"" src/layouts/BaseLayout.astro && grep -c "source-serif-4-latin-500-normal.woff2" src/layouts/BaseLayout.astro && grep -c "cascadia-code-latin-400-normal.woff2" src/layouts/BaseLayout.astro && grep -c "styles/tokens.css" src/layouts/BaseLayout.astro && grep -c "favicon.svg" src/layouts/BaseLayout.astro && grep -c "<slot" src/layouts/BaseLayout.astro && npx astro check 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `src/layouts/BaseLayout.astro` imports `../styles/tokens.css`, `../styles/fonts.css`, `../styles/global.css` (each exactly once)
    - Head contains two `rel="preload" as="font" type="font/woff2" crossorigin` links pointing at the serif and mono woff2 files
    - Head contains `<html lang="en">`, charset utf-8, viewport meta, a `<title>`, a meta description, and the SVG favicon link
    - Body contains a single `<slot />`
    - No OpenGraph/sitemap/og-image markup is present (Phase 3 scope)
    - `npx astro check` reports 0 errors
  </acceptance_criteria>
  <done>BaseLayout is the single shell: it preloads both self-hosted fonts, imports the three stylesheets once for one-way token flow, and exposes a slot for pages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> build | Font + fontaine packages pulled at install time |
| self-hosted fonts -> visitor browser | Static woff2 served from public/, no CDN/third-party origin |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | @fontsource/*, fontaine, fonttools install | high | mitigate | RESEARCH Package Legitimacy Audit approved these packages via direct-registry verification; no postinstall scripts confirmed; fonts are self-hosted (no runtime CDN dependency) |
| T-01-06 | Tampering | re-subset Cascadia woff2 from external source | low | mitigate | Prefer a locally installed Windows font source; if downloading, use the official OFL microsoft/cascadia-code release only; verify the resulting cmap includes U+2192 and nothing else was injected (subset output is a clean re-encode) |
</threat_model>

<verification>
- tokens.css declares all 12 locked colors + 3 font stacks; it is the only file with literal hex/stacks (global.css has none)
- Both faces self-hosted and subsetted; Cascadia cmap includes U+2192 (or documented fallback)
- fontaine registered; `npm run build` exits 0
- BaseLayout preloads both fonts, imports the three stylesheets once, exposes a slot; `astro check` clean
</verification>

<success_criteria>
The site has one design-token source, a self-hosted subsetted font pipeline with metrics-matched fallbacks and a resolved arrow-glyph gap, and a base layout shell — satisfying SHELL-05 and PLAT-04.
</success_criteria>

## Artifacts this phase produces (plan 01-02)

- `src/styles/tokens.css` — CSS custom properties: `--bg --panel --panel2 --ink --body --dim --faint --hair --hair2 --accent --good --amber --mono --serif --sans`
- `src/styles/fonts.css` — `@font-face` for `'Source Serif 4'` (500) and `'Cascadia Code'` (400)
- `src/styles/global.css` — base reset, body/link/focus-visible rules
- `public/fonts/source-serif-4-latin-500-normal.woff2`, `public/fonts/cascadia-code-latin-400-normal.woff2`
- `src/layouts/BaseLayout.astro` — props `title`, `description`; the site shell
- `astro.config.mjs` — adds `vite.plugins` `FontaineTransform.vite(...)`

<output>
Create `.planning/phases/01-foundation-editorial-shell/01-02-SUMMARY.md` when done.
</output>