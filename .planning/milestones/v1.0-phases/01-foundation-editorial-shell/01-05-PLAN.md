---
phase: 01-foundation-editorial-shell
plan: 05
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - src/components/SystemsList.astro
  - src/components/ExperienceSection.astro
  - src/components/PatentsSection.astro
  - src/components/SkillsSection.astro
  - src/components/ContactSection.astro
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-04, CONT-06, PLAT-05]
user_setup: []

must_haves:
  truths:
    - "The selected-systems list renders 4 rows (year | mono name + description | accent metric)"
    - "Experience shows Microsoft/AWS/MathWorks/Samsung entries"
    - "Patents & publications shows 2 patents + 1 paper"
    - "Skills render as grouped mono tags with a prose intro (no skill bars)"
    - "Contact provides mailto + LinkedIn (no phone)"
    - "Layout is responsive: the systems metric column hides below 640px"
  artifacts:
    - "src/components/SystemsList.astro"
    - "src/components/ExperienceSection.astro"
    - "src/components/PatentsSection.astro"
    - "src/components/SkillsSection.astro"
    - "src/components/ContactSection.astro"
  key_links:
    - "src/data/* -> each section renders from typed data (single source, honesty gate)"
    - "640px media query -> metric column hidden, grid collapses (PLAT-05)"
---

<objective>
Build the five content-section components — SystemsList, ExperienceSection, PatentsSection, SkillsSection, ContactSection — each rendering from its typed data module, porting the prototype's section-heading idiom (lowercase mono uppercase-letterspaced labels) and the systems-row grid exactly, with the locked 640px responsive behavior.

Purpose: These deliver CONT-01..04, the section-side of CONT-06, and PLAT-05 (responsive). Section headings are quiet mono labels, not display headings — this restraint is the brand.
Output: five .astro components.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-editorial-shell/01-CONTEXT.md
@.planning/phases/01-foundation-editorial-shell/01-UI-SPEC.md
@.planning/reference/prototype-shell-and-fig01.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: SystemsList.astro (4 rows, responsive grid)</name>
  <files>src/components/SystemsList.astro</files>
  <read_first>
    - .planning/reference/prototype-shell-and-fig01.html (the `.work` section + `.row`/`.yr`/`.nm`/`.ds`/`.mt` CSS + the 640px media query — port verbatim)
    - .planning/phases/01-foundation-editorial-shell/01-UI-SPEC.md (Component type map rows; Spacing locked exceptions for .work/.row; Responsive 640px behavior)
    - src/data/systems.ts (the 4 entries)
  </read_first>
  <action>
    Create `src/components/SystemsList.astro` rendering the `selected systems` section: an `<h2>` mono label (12px, .18em uppercase, `--dim`, weight 500) reading `selected systems`, then a `.row` per entry mapped from `systems` (import from `src/data/systems.ts`). Each row is a grid `grid-template-columns:64px 1fr auto; gap:18px; align-items:baseline; padding:14px 10px; margin:0 -10px; border-top:1px solid var(--hair); transition:background .15s` with hover background `--panel2`; columns are `.yr` (mono 12px `--faint`), a middle block with `.nm` (mono 13.5px `--ink`) + `.ds` (sans 13.5px `--dim`, display block, margin-top 3px), and `.mt` (mono 12px `--accent`, white-space nowrap). Do NOT add case-study links (Phase 3). Add the locked media query at max-width 640px collapsing the grid to `52px 1fr` and hiding `.mt` (`display:none`) — this is PLAT-05. Scoped styles read only `var(--token)`.
  </action>
  <verify>
    <automated>grep -c "selected systems" src/components/SystemsList.astro && grep -c "max-width:640px\|max-width: 640px" src/components/SystemsList.astro && grep -c "systems" src/components/SystemsList.astro && npx astro check 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - Renders an `<h2>` reading `selected systems` (lowercase mono label) and one `.row` per systems entry (4 rows)
    - Row grid is `64px 1fr auto` with row-hover background `--panel2`
    - A `max-width:640px` media query collapses the grid to `52px 1fr` and sets `.mt { display:none }` (PLAT-05)
    - Rows are mapped from imported `systems` data, not hardcoded
    - Scoped styles use only `var(--token)`; `npx astro check` reports 0 errors
  </acceptance_criteria>
  <done>The selected-systems list matches the prototype and collapses correctly on mobile (CONT-01, PLAT-05).</done>
</task>

<task type="auto">
  <name>Task 2: ExperienceSection.astro + PatentsSection.astro</name>
  <files>src/components/ExperienceSection.astro, src/components/PatentsSection.astro</files>
  <read_first>
    - .planning/phases/01-foundation-editorial-shell/01-CONTEXT.md (Page structure items 5 and 6)
    - .planning/phases/01-foundation-editorial-shell/01-UI-SPEC.md (Section heading type; body/mono roles)
    - src/data/experience.ts, src/data/patents.ts
  </read_first>
  <action>
    Create `src/components/ExperienceSection.astro` with an `<h2>` mono label `experience` and one entry per `experience` item (import from data): company/role/dates as mono labels and the scope bullet(s) as sans body, following the same quiet editorial idiom as the systems section (reuse the section spacing/heading pattern; company names in `--ink`, dates in `--faint`, body in `--body`/`--dim`). Create `src/components/PatentsSection.astro` with an `<h2>` mono label `patents & publications` rendering the 2 patents and 1 publication from `patents`/`publications` data (title, identifier/grade, year as mono; description as sans). Both sections read only `var(--token)`; no metric is shown without its data-backed value. Keep headings lowercase mono (never capitalize).
  </action>
  <verify>
    <automated>grep -c "experience" src/components/ExperienceSection.astro && grep -c "patents & publications" src/components/PatentsSection.astro && npx astro check 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - ExperienceSection renders an `<h2>experience</h2>` label and 4 entries (Microsoft/AWS/MathWorks/Samsung) mapped from data
    - PatentsSection renders an `<h2>patents & publications</h2>` label, 2 patents (incl. Grade A1, IN 201631007292) and 1 publication (Android Lint paper) mapped from data
    - Both read only `var(--token)`; `npx astro check` reports 0 errors
  </acceptance_criteria>
  <done>Experience and patents sections render from data with the locked quiet-label heading idiom (CONT-02, CONT-03).</done>
</task>

<task type="auto">
  <name>Task 3: SkillsSection.astro + ContactSection.astro</name>
  <files>src/components/SkillsSection.astro, src/components/ContactSection.astro</files>
  <read_first>
    - .planning/phases/01-foundation-editorial-shell/01-CONTEXT.md (Page structure items 7 and 8; contact = mailto + LinkedIn, phone OFF-site)
    - .planning/phases/01-foundation-editorial-shell/01-UI-SPEC.md (skills grouped mono tags, no skill bars/logo walls; contact copy)
    - src/data/skills.ts, src/data/profile.ts
  </read_first>
  <action>
    Create `src/components/SkillsSection.astro` with an `<h2>` mono label `skills`, a prose intro paragraph (sans body) from `skills` data, then the 4 groups each rendered as a group label plus its tags as small mono tags (styled as quiet pill/inline mono text reading `var(--token)` colors — no proficiency bars, no percentages, no logo images). Create `src/components/ContactSection.astro` with an `<h2>` mono label `contact` (give the wrapping section `id="contact"` so the header/nav anchors resolve) containing a `mailto:p2401kumar@gmail.com` link and a LinkedIn link (from `profile.links`, `target="_blank" rel="noopener noreferrer"`). Do NOT render a phone number anywhere (explicit exclusion). Both components read only `var(--token)`.
  </action>
  <verify>
    <automated>grep -c "mailto:p2401kumar@gmail.com" src/components/ContactSection.astro && grep -c "id=\"contact\"" src/components/ContactSection.astro && grep -c "skills" src/components/SkillsSection.astro && npx astro check 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - SkillsSection renders an `<h2>skills</h2>` label, a prose intro, and 4 groups as mono tags with NO proficiency bars/percentages/logo images
    - ContactSection has a wrapper with `id="contact"`, a `mailto:p2401kumar@gmail.com` link, and a LinkedIn link with `rel="noopener noreferrer"`
    - No phone number string appears in either component
    - Both read only `var(--token)`; `npx astro check` reports 0 errors
  </acceptance_criteria>
  <done>Skills (grouped tags, no bars) and contact (mailto + LinkedIn, no phone) render from data (CONT-04, CONT-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser <- static sections | Rendered HTML from build-time data; no user input, no server |
| contact/skills external links -> third-party sites | LinkedIn navigation and mailto handoff |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Information Disclosure | ContactSection external LinkedIn link | low | mitigate | `target="_blank"` link carries `rel="noopener noreferrer"` (Task 3 gate) |
| T-01-10 | Information Disclosure | phone number exclusion | low | mitigate | Phone number is kept off-site by decision; verify no phone string is rendered in contact/skills components |
</threat_model>

<verification>
- Five sections render from typed data with lowercase mono headings
- Systems list responsive at 640px (metric column hidden)
- Skills have no skill-bar/logo-wall anti-pattern; contact has mailto + LinkedIn, no phone
- Scoped styles contain no hex literals; `astro check` clean
</verification>

<success_criteria>
All content sections render from single-sourced data with the locked editorial idiom and responsive behavior — delivering CONT-01..04, CONT-06 (section side), and PLAT-05.
</success_criteria>

## Artifacts this phase produces (plan 01-05)

- `src/components/SystemsList.astro` — `selected systems` (4 rows, 640px responsive)
- `src/components/ExperienceSection.astro` — `experience` (4 entries)
- `src/components/PatentsSection.astro` — `patents & publications` (2 + 1)
- `src/components/SkillsSection.astro` — `skills` (grouped mono tags)
- `src/components/ContactSection.astro` — `contact` (id="contact", mailto + LinkedIn)

<output>
Create `.planning/phases/01-foundation-editorial-shell/01-05-SUMMARY.md` when done.
</output>