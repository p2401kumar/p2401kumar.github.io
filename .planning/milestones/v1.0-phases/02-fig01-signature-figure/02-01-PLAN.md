---
phase: 02-fig01-signature-figure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/types.ts
  - src/data/fig01.ts
  - src/lib/fig01/tokens.ts
  - src/lib/fig01/model.ts
autonomous: true
requirements: [FIG-02, FIG-03, FIG-04]
must_haves:
  truths:
    - "Every tooltip fact shown in the figure traces to the résumé via a `source` field (CONT-07 honesty gate extended to Fig. 01)"
    - "Canvas brand colors resolve from tokens.css at runtime — no hex color literal exists anywhere in src/lib/fig01/"
    - "Fault injection selects a cell among 1–3 (never cell-0) and a degraded cell is excluded from beam routing"
  artifacts:
    - src/data/types.ts (Fig01Fact interface added)
    - src/data/fig01.ts (10 typed, source-annotated node facts)
    - src/lib/fig01/tokens.ts (cached getComputedStyle token reader)
    - src/lib/fig01/model.ts (topology, route math, beam/fault state machine)
  key_links:
    - "model.ts state shape (FigureState) is the single runtime contract that render.ts draws from and interactions.ts mutates"
    - "tokens.ts is the only bridge from tokens.css custom properties into the canvas module (getComputedStyle, read once)"
---

<objective>
Build the framework-free foundation of Fig. 01: the typed, source-annotated fact data (FIG-04), the runtime token reader that keeps canvas colors single-sourced from tokens.css, and the pure `model.ts` engine (topology, route geometry, beam-routing math, fault-injection state machine) that both `render.ts` and `interactions.ts` will consume.

Purpose: These are the leaf modules of the port. They carry no DOM/canvas dependency (except tokens.ts's one `getComputedStyle` read), so they are independently type-checkable and lock the data + state contracts every later plan builds against.

Output: `src/data/types.ts` (extended), `src/data/fig01.ts`, `src/lib/fig01/tokens.ts`, `src/lib/fig01/model.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-fig01-signature-figure/02-CONTEXT.md
@.planning/phases/02-fig01-signature-figure/02-RESEARCH.md
@.planning/phases/02-fig01-signature-figure/02-UI-SPEC.md
@.planning/reference/prototype-shell-and-fig01.html
@src/data/types.ts
@src/data/systems.ts
@src/styles/tokens.css
</context>

## Artifacts this plan produces

- `Fig01Fact` interface (in `src/data/types.ts`): `{ nodeId: string; label: string; tooltipHtml: string; accessibleName: string; source: string }`
- `src/data/fig01.ts` → `export const fig01Facts: Fig01Fact[]` (10 entries: c0, c1, c2, lb, cell0, cell1, cell2, cell3, dp, ml)
- `src/lib/fig01/tokens.ts` → `getTokens(): FigTokens`, `type FigTokens`, `rgba(triple, alpha): string`
- `src/lib/fig01/model.ts` → types `NodeId`, `NodeSpec`, `RouteGeom`, `Beam`, `FigureState`; consts `nodes`, `order`, `cells`, `routeDefs`; functions `createState()`, `computeLayout(state, W, H)`, `pAt(route, d)`, `healthyCells(state)`, `pickFaultCell()`, `injectFault(state)`, `healFault(state)`, `spawnBeam(state, fromId?)`, `advanceBeams(state, dt)`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fact data (types.ts + data/fig01.ts) and the runtime token reader (tokens.ts)</name>
  <files>src/data/types.ts, src/data/fig01.ts, src/lib/fig01/tokens.ts</files>
  <read_first>
    - src/data/types.ts (existing interface pattern + `source` honesty-gate convention to mirror)
    - src/data/systems.ts (existing typed-data-module shape + how `source` strings are written)
    - src/styles/tokens.css (the ONLY color source — the custom-property names to read)
    - .planning/phases/02-fig01-signature-figure/02-UI-SPEC.md ("Copywriting Contract" tooltip rows + "Accessibility copy" node-proxy accessible-name rows; "Color" table for which tokens the canvas uses)
    - .planning/reference/prototype-shell-and-fig01.html (lines 150–161: the `nodes` object's `tip` strings — the verbatim tooltip copy incl. the `<span class=m>` metric spans)
  </read_first>
  <behavior>
    - fig01Facts has exactly 10 entries whose nodeIds are c0, c1, c2, lb, cell0, cell1, cell2, cell3, dp, ml
    - Every entry has a non-empty `source` string tracing its metric to the résumé
    - getTokens() returns the same cached object on a second call (identity-stable)
    - rgba(triple, 0.5) formats as a valid `rgba(r, g, b, 0.5)` string from a parsed token
  </behavior>
  <action>
    Extend `src/data/types.ts` with a `Fig01Fact` interface mirroring the existing `source`-bearing pattern: `nodeId` (string), `label` (mono node label, e.g. "elb/weight-away"), `tooltipHtml` (the hover-tooltip string, may contain the `<span class="m">…</span>` metric highlight — this is developer-authored build-time content, safe for innerHTML per 02-RESEARCH.md Security Domain), `accessibleName` (plain-text, HTML-stripped equivalent for the keyboard proxy button's accessible name), and `source` (honesty-gate provenance, not rendered).

    Create `src/data/fig01.ts` exporting `fig01Facts: Fig01Fact[]` with 10 entries. Copy tooltip copy VERBATIM from 02-UI-SPEC.md "Copywriting Contract": clients (c0/c1/c2) = `smartthings clients · galaxy s21`; lb = `auto-weight-away · ` + accent span `90% capacity ops automated`; cell0..3 = `isolated failure domain · compute/storage split`; dp = `durable · idempotent · ` + accent span `reconciles legacy data`; ml = `information-theory health snapshots · ` + accent span `azure`. For `accessibleName`, use the 02-UI-SPEC "Accessibility copy" pattern `{label or role} — {tooltip fact, plain text}` (e.g. `clients — smartthings clients · galaxy s21`, `elb/weight-away — auto-weight-away · 90% capacity ops automated`, `cell-2 — isolated failure domain · compute/storage split`). Set each `source` to trace the résumé fact (reuse the exact provenance strings already used in src/data/systems.ts where they overlap: +30%/−20% cellularization, 90% capacity ops automated weight-away, info-theory azure snapshots, SmartThings/Galaxy S21). Introduce NO number not already present in systems.ts / the résumé (honesty gate — no invented telemetry).

    Create `src/lib/fig01/tokens.ts`: a `getTokens()` function that lazily reads `getComputedStyle(document.documentElement)` ONCE, caches the result in a module-level variable, and returns it on every subsequent call (02-RESEARCH.md Pattern 2 — never read per-frame). Read these custom properties: `--accent`, `--good`, `--amber`, `--ink`, `--dim`, `--panel2`, `--faint`, `--hair`. Parse each hex value into an `{ r, g, b }` triple so callers can compose alpha-varied fills. Expose the typed `FigTokens` shape and an `rgba(triple, alpha)` helper that returns a `rgba(r, g, b, alpha)` string. This is the mechanism that lets render.ts produce the beam/glow/status colors WITHOUT hardcoding any color literal.
  </action>
  <acceptance_criteria>
    - `grep -c 'nodeId' src/data/fig01.ts` returns 10 (ten fact entries)
    - `grep -c 'source:' src/data/fig01.ts` returns 10 (every fact honesty-gated)
    - `grep -nE '#[0-9a-fA-F]{3,8}' src/lib/fig01/tokens.ts` returns 0 matches (token reader holds no hardcoded color; it only reads custom-property names as strings)
    - `src/lib/fig01/tokens.ts` contains `getComputedStyle(` exactly once (read-once pattern)
    - `src/data/types.ts` contains `interface Fig01Fact`
    - `npx astro check` reports 0 errors
  </acceptance_criteria>
  <verify>
    <automated>npx astro check</automated>
  </verify>
  <done>fig01Facts has 10 source-annotated entries with verbatim tooltip copy; tokens.ts reads tokens.css once via getComputedStyle and exposes parsed rgb triples + an rgba() helper with zero hardcoded colors; astro check clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: model.ts — topology, route math, and the beam/fault state machine (pure, DOM-free)</name>
  <files>src/lib/fig01/model.ts</files>
  <read_first>
    - .planning/reference/prototype-shell-and-fig01.html (lines 149–216, 233–297: the `nodes` object, `order`, `cells`, `routeDefs`, `layout()`, `pAt`, `healthyCells`, `spawnBeam`, fault state, beam advance in `frame()` — port this math VERBATIM, only restructured/typed)
    - .planning/phases/02-fig01-signature-figure/02-UI-SPEC.md ("Node geometry", "Route/beam geometry" tables — the fractional x/y, w×h, routing style, beam speed 0.22 px/ms, tail 70px)
    - .planning/phases/02-fig01-signature-figure/02-RESEARCH.md ("Code Examples": healthyCells/pickFaultCell pure functions; Pitfall 4: strict-TS typing of node literals)
    - .planning/research/ARCHITECTURE.md (fig01 module split rationale)
  </read_first>
  <behavior>
    - healthyCells(state) returns all 4 cells when degraded is null, and 3 cells (excluding the degraded one) when a cell is degraded
    - pickFaultCell() never returns cell0 (only cell1, cell2, or cell3)
    - injectFault(state) sets state.degraded to a non-cell0 cell and does nothing if a cell is already degraded
    - healFault(state) clears state.degraded back to null
    - spawnBeam(state) pushes a beam whose route legs never traverse the currently-degraded cell
    - advanceBeams(state, dt) moves a beam forward by dt*0.22 px and, on reaching a leg end, sets the arrival node's glow to 1 and advances the leg (removing the beam after the final leg)
  </behavior>
  <action>
    Port the prototype's model/math into a typed, DOM-free `src/lib/fig01/model.ts`. Define an explicit `NodeSpec` interface with all node-kind-specific fields OPTIONAL (`glyph?`, `cell?`, `ml?`, `lab?`) and common fields required (`x`, `y`, `w`, `h`, plus computed `px`, `py`, `glow`) so every node literal type-checks under `astro/tsconfigs/strict` without `as any` or pervasive `!` (02-RESEARCH.md Pitfall 4). Do NOT copy the prototype's `tip` HTML strings into model.ts — tooltip content lives in src/data/fig01.ts; model.ts is geometry + state only.

    Reproduce the topology EXACTLY: `nodes` record keyed c0,c1,c2,lb,cell0..cell3,dp,ml with the fractional positions and sizes from the 02-UI-SPEC "Node geometry" table; `order = ['c0','c1','c2','lb','cell0','cell1','cell2','cell3','dp','ml']`; `cells = ['cell0','cell1','cell2','cell3']`; `routeDefs` = the 12 edges (3 client→lb, 4 lb→cellN, 4 cellN→dp, 1 dp→ml).

    Implement `computeLayout(state, W, H)`: sets each node's `px = x*W`, `py = y*H`, and builds each route's elbow geometry — 3-segment Manhattan path `[[x1,y1],[mx,y1],[mx,y2],[x2,y2]]` where `x1=a.px+a.w/2`, `x2=b.px-b.w/2`, `mx=(x1+x2)/2` — storing `pts`, per-segment lengths `segs`, and total `len` per route (verbatim from prototype `layout()` lines 174–180, minus the canvas-sizing DOM calls which belong to render.ts). Implement `pAt(route, d)` (pure point-at-distance sampler, prototype line 182–184).

    Implement the state machine as pure functions over a `FigureState` created by `createState()`. FigureState fields: `beams: Beam[]`, `degraded: NodeId | null`, `mouse: {x,y}`, `hoverN: NodeId | null`, `focusedNode: NodeId | null`, `intro`/`t0` timing scalars, and the `nodes`/`routes` maps. Functions: `healthyCells(state)` (filter out degraded), `pickFaultCell()` (eligible = cells.slice(1); random pick — never cell0), `injectFault(state)` (no-op if already degraded, else set degraded), `healFault(state)` (clear degraded), `spawnBeam(state, fromId?)` (build legs `from>lb, lb>healthyCell, healthyCell>dp, dp>ml`; default `from` = random client `c0/c1/c2`; when `fromId` is a specific node, dispatch from there — used by keyboard/click), `advanceBeams(state, dt)` (move `b.d += dt*0.22`, on `b.d >= route.len` set arrival node `glow=1`, advance `b.leg`, splice when done — prototype lines 261–268). Keep `focusedNode` in state so render.ts can draw a focus ring and interactions.ts can set it.
  </action>
  <acceptance_criteria>
    - `grep -nE '#[0-9a-fA-F]{3,8}' src/lib/fig01/model.ts` returns 0 matches (no color literals in the model at all)
    - `grep -c "'cell" src/lib/fig01/model.ts` shows the 4-cell topology present (cells array + routeDefs reference cell0..cell3)
    - `src/lib/fig01/model.ts` contains `export function healthyCells`, `export function pickFaultCell`, `export function spawnBeam`, `export function advanceBeams`, `export function computeLayout`, `export function pAt`
    - `src/lib/fig01/model.ts` contains no `requestAnimationFrame` and no `getContext` (model is DOM/canvas-free)
    - `src/lib/fig01/model.ts` contains no `as any` (strict typing modelled up front — Pitfall 4)
    - `npx astro check` reports 0 errors
  </acceptance_criteria>
  <verify>
    <automated>npx astro check</automated>
  </verify>
  <done>model.ts reproduces the prototype topology, elbow route geometry, and beam/fault state machine as typed, DOM-free pure functions; fault selection excludes cell0 and degraded cells are routing-excluded; astro check clean with no `as any` and no color literals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tokens.css → canvas module | CSS custom properties read at runtime via getComputedStyle (one-way, CSS is authoritative) |
| src/data/fig01.ts → tooltip DOM | Developer-authored build-time literals become tooltip content (no user input path) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | src/data/fig01.ts fact copy | low | mitigate | Keep topology/labels abstracted (cell-0..3, elb/weight-away) — no literal internal AWS service names; every number traces to the résumé (honesty gate). Verified by the 10× `source:` grep. |
| T-02-02 | Tampering (token drift) | canvas color values | low | mitigate | No hex literal permitted in src/lib/fig01/ — colors resolve from tokens.css via tokens.ts. Enforced by the hex-grep acceptance gate on tokens.ts and model.ts. |
</threat_model>

<verification>
- `npx astro check` → 0 errors after both tasks
- `grep -rnE '#[0-9a-fA-F]{3,8}' src/lib/fig01/` → 0 matches (foundation is color-literal-free)
- `grep -c 'source:' src/data/fig01.ts` → 10 (honesty gate)
- Manual: confirm `pickFaultCell` eligibility slice starts at index 1 (cell0 never faulted)
</verification>

<success_criteria>
- Four foundation files exist and type-check clean under strict TS
- fig01Facts carries 10 source-annotated entries with verbatim UI-SPEC tooltip copy
- tokens.ts reads tokens.css once and exposes token-derived rgb triples + rgba() helper with zero hardcoded colors
- model.ts is a DOM-free, `as any`-free typed port of the prototype topology + beam/fault state machine
</success_criteria>

<output>
Create `.planning/phases/02-fig01-signature-figure/02-01-SUMMARY.md` when done
</output>
