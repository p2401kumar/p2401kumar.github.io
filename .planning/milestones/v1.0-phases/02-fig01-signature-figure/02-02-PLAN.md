---
phase: 02-fig01-signature-figure
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/lib/fig01/render.ts
autonomous: true
requirements: [FIG-01, FIG-05, FIG-07]
must_haves:
  truths:
    - "On load, nodes fade+rise and routes draw-on in dependency order (staggered build-in), then ambient request beams travel client → LB → healthy cell → pipelines → ML"
    - "A single requestAnimationFrame loop drives all motion; DPR is capped at 2; there is a static-frame render path that starts no rAF loop (reduced-motion foundation)"
    - "A degraded cell renders amber AND dashed (dual-encoded), and its routes render as dashed dead-ends"
  artifacts:
    - src/lib/fig01/render.ts (rAF driver, all draw primitives, static-frame path, HiDPI layout)
  key_links:
    - "render.ts reads FigureState (from model.ts) and token colors (from tokens.ts) — it owns pixels, never state mutation except beam glow decay per frame"
    - "renderStaticFrame(ctx, state) is the reduced-motion entry point interactions.ts calls; it MUST contain no requestAnimationFrame"
---

<objective>
Port the prototype's canvas drawing into a typed `render.ts` that draws every figure primitive (dot-grid substrate + cursor lens, elbow routes with draw-on stagger, gradient beams, nodes with glyphs/labels/status-dots/glow/breathing-ring/fault-styling) and exposes two entry points: `startAnimationLoop`/`stopAnimationLoop` (the single rAF driver, FIG-01/FIG-07) and `renderStaticFrame` (one complete frame with no loop, the FIG-05 reduced-motion foundation).

Purpose: This is the single most performance-critical module (60fps floor, FIG-07). Consolidating all motion into one rAF loop and providing a genuinely separate static-frame path — rather than a loop that merely skips flourishes — is what makes the reduced-motion and lifecycle-gating work in plan 02-03 correct rather than cosmetic.

Output: `src/lib/fig01/render.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-fig01-signature-figure/02-CONTEXT.md
@.planning/phases/02-fig01-signature-figure/02-RESEARCH.md
@.planning/phases/02-fig01-signature-figure/02-UI-SPEC.md
@.planning/reference/prototype-shell-and-fig01.html
@src/lib/fig01/model.ts
@src/lib/fig01/tokens.ts
</context>

## Artifacts this plan produces

- `src/lib/fig01/render.ts` →
  - `layout(ctx, canvas, stage, state, dpr)` — HiDPI backing-store sizing + delegates geometry to `computeLayout`
  - `pathTo(ctx, route, frac)` — draw an elbow path up to a fraction (draw-on)
  - `rr(ctx, x, y, w, h, r)` — rounded-rect path helper
  - `drawGrid(ctx, state, W, H)` — dot substrate + cursor lens
  - `drawRoutes(ctx, state, tokens, T)` — routes with draw-on stagger + dead-end dashing
  - `drawBeams(ctx, state, tokens, dt)` — additive-blend gradient beams
  - `drawNode(ctx, node, rise, alpha, T, state, tokens)` — node body/glyph/label/status/glow/ring/focus/fault
  - `drawFrame(ctx, state, tokens, ts)` — one animated frame (clear → grid → routes → beams → nodes)
  - `renderStaticFrame(ctx, state, tokens)` — one complete frame at prog=1, no beams, NO rAF
  - `startAnimationLoop(ctx, state, tokens)` / `stopAnimationLoop()` — idempotent single-rAF driver

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Draw primitives — HiDPI layout, dot-grid + lens, elbow routes, gradient beams, node rendering</name>
  <files>src/lib/fig01/render.ts</files>
  <read_first>
    - .planning/reference/prototype-shell-and-fig01.html (lines 171–180 layout; 235–256 grid + routes; 258–282 beams; 299–339 drawNode + rr — port these draw calls VERBATIM in geometry, only sourcing colors from tokens.ts)
    - .planning/phases/02-fig01-signature-figure/02-UI-SPEC.md ("Canvas stage dimensions", "Node geometry", "Route/beam geometry", "Canvas-drawn text", "Color" table incl. the fault-state amber+dashed dual-encoding, "Motion & Interaction" draw-strategy row)
    - src/lib/fig01/model.ts (FigureState shape, computeLayout, pAt, route geometry it produces)
    - src/lib/fig01/tokens.ts (getTokens / rgba helper — the ONLY color source)
  </read_first>
  <behavior>
    - layout() sets canvas backing store to cssSize × min(devicePixelRatio, 2) and applies setTransform so draw calls use CSS-pixel coordinates
    - A degraded cell's node border draws amber and dashed; its routes draw dashed at low alpha (dead-end)
    - Beam drawing uses globalCompositeOperation 'lighter' during beams and resets to 'source-over' afterward
    - The ml node draws a breathing ring; a focused node (state.focusedNode) draws an accent focus ring around its rect
  </behavior>
  <action>
    Create `src/lib/fig01/render.ts` with the drawing primitives, porting the prototype's canvas geometry verbatim but sourcing ALL brand colors from `getTokens()` (never a hex literal; use the `rgba(triple, alpha)` helper for alpha-varied fills).

    `layout(ctx, canvas, stage, state, dpr)`: read `stage.getBoundingClientRect()` for W and `canvas.clientHeight` for H, set `canvas.width = W*dpr`, `canvas.height = H*dpr`, call `ctx.setTransform(dpr,0,0,dpr,0,0)`, then call model's `computeLayout(state, W, H)` to populate node px/py and route geometry (DOM sizing lives here; pure geometry lives in model). `dpr` is `Math.min(devicePixelRatio || 1, 2)` (FIG-07 DPR cap).

    `rr(ctx,x,y,w,h,r)`: rounded-rect path via arcTo (prototype lines 337–339).

    `drawGrid(ctx, state, W, H)`: 22px-pitch dot substrate, dot size 1.4×1.4, base alpha .055; when `state.mouse.x >= 0`, brighten dots within the cursor lens radius (dd < 14400) up to +.10 falloff (prototype lines 238–244). These structural grey dot fills are canvas-only values blessed by 02-UI-SPEC Color table (expressed as rgba(), never hex).

    `pathTo(ctx, route, frac)`: draw the elbow path up to `route.len*frac` (prototype 185–191) — used for route draw-on.

    `drawRoutes(ctx, state, tokens, T)`: for each route, compute draw-on `prog` from the build-in stagger (route start `500 + i*55`ms, `420`ms ease); skip if prog ≤ 0; stroke width 1px; if the route touches `state.degraded`, stroke dashed at low amber alpha (dead-end, `setLineDash([3,4])`), else the hairline route color (canvas-only structural grey per UI-SPEC, rgba, non-hex); `pathTo(...prog)` + stroke; reset line dash.

    `drawBeams(ctx, state, tokens, dt)`: set `globalCompositeOperation='lighter'`; for each beam, resolve the current route via model, draw the 70px gradient tail (transparent → accent at ~.85 alpha, accent from tokens triple) and the 2.2px head dot (lightened-accent — Claude's discretion: a lightened accent triple, or the UI-SPEC-blessed canvas highlight rgba(); NO hex), walking the elbow between tail and head precisely (prototype 269–281); reset composite to 'source-over'. Beam advance/glow state is owned by model's `advanceBeams` (called from drawFrame), not here.

    `drawNode(ctx, node, rise, alpha, T, state, tokens)`: port prototype `drawNode` (299–336) — glow halo (accent radial gradient when glow > .05), panel2 body fill (from tokens, `--panel2`), rounded-rect border whose color is amber when `node.id === state.degraded` (dashed `setLineDash([3,3])`), accent when hovered/focused/glowing, else a faint structural grey; device glyph for client nodes; mono canvas label using the UI-SPEC "Canvas-drawn text" font strings (`11px ui-monospace,Consolas,monospace` inside nodes, `10.5px` under client glyphs) with ink/dim canvas renderings; status dot good/amber; ml breathing ring `0.5 + 0.5*sin(T/1200)`. Additionally: when `state.focusedNode === node.id`, draw a 2px accent focus ring offset ~2px outward around the node rect (mirrors hover border — the on-canvas keyboard focus indicator per 02-UI-SPEC Keyboard row). Decay `node.glow *= 0.955` per animated frame (do this in drawFrame's node loop, not in the static path).
  </action>
  <acceptance_criteria>
    - `grep -nE '#[0-9a-fA-F]{3,8}' src/lib/fig01/render.ts` returns 0 matches (no hex color literal anywhere)
    - `grep -c 'getTokens' src/lib/fig01/render.ts` returns ≥ 1 (brand colors sourced from tokens.ts)
    - `src/lib/fig01/render.ts` contains `setLineDash` (fault + dead-end dual-encoding present)
    - `src/lib/fig01/render.ts` contains `'lighter'` and `'source-over'` (additive beam blend is scoped and reset)
    - `src/lib/fig01/render.ts` contains `setTransform` and `Math.min(` with `devicePixelRatio` (DPR cap 2, FIG-07)
    - `npx astro check` reports 0 errors
  </acceptance_criteria>
  <verify>
    <automated>npx astro check</automated>
  </verify>
  <done>render.ts draws grid+lens, staggered draw-on routes with amber/dashed dead-ends, additive gradient beams, and full node rendering (glyph/label/status/glow/breathing-ring/fault/focus) — all colors token-sourced, DPR capped at 2, no hex literals; astro check clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Single-rAF driver + build-in stagger + ambient spawn + renderStaticFrame (reduced-motion path)</name>
  <files>src/lib/fig01/render.ts</files>
  <read_first>
    - .planning/reference/prototype-shell-and-fig01.html (lines 232–297: the `frame()` loop — intro staging, ambient spawn timer, beam advance, heal check location, node loop, `requestAnimationFrame(frame)`)
    - .planning/phases/02-fig01-signature-figure/02-RESEARCH.md ("Code Examples": single-rAF driver with delta-time + explicit start/stop; Pattern 3 reduced-motion static path; Pitfall 2 + Pitfall 3)
    - .planning/phases/02-fig01-signature-figure/02-UI-SPEC.md ("Build-in stagger" table; "Reduced motion" table; "Performance" table — exactly one rAF loop)
    - src/lib/fig01/render.ts (Task 1's primitives this task composes)
  </read_first>
  <behavior>
    - startAnimationLoop is idempotent: calling it twice does not create a second rAF loop
    - stopAnimationLoop cancels the pending frame and leaves no rAF scheduled
    - renderStaticFrame draws all routes at full length and all nodes at rest (rise 0, alpha 1), with no beams, and schedules no requestAnimationFrame
    - During the animated loop, an ambient beam spawns only after intro (T > 1400ms) and then on a 2600–4000ms cadence
  </behavior>
  <action>
    Add the rAF driver and the static path to `render.ts`.

    `drawFrame(ctx, state, tokens, ts)`: one animated frame — establish `t0`/`T = ts - t0` on first call, compute `dt = min(ts - last, 50)`, then: clear, `drawGrid`, set `state.intro = T` (drives draw-on), `drawRoutes`, ambient-spawn gate (`if T > 1400 && ts - lastSpawn > 2600 + random()*1400 → spawnBeam(state); lastSpawn = ts`), `advanceBeams(state, dt)` + `drawBeams`, then the node loop applying the build-in stagger (node start `i*60`ms, `380`ms ease, rise `(1-prog)*8`px) and glow decay `*= 0.955`, calling `drawNode`. Do NOT put the fault heal timer here (heal is a setTimeout in plan 02-03, decoupled from rAF per Pitfall 3).

    `startAnimationLoop(ctx, state, tokens)`: guarded by a module-level `rafId` — if already running, return (idempotent); set `last = performance.now()`; schedule `tick(ts)` which calls `drawFrame` then `rafId = requestAnimationFrame(tick)`. `stopAnimationLoop()`: if `rafId !== null`, `cancelAnimationFrame(rafId)` and null it.

    `renderStaticFrame(ctx, state, tokens)`: render exactly ONE complete frame with no animation and NO requestAnimationFrame call — clear, drawGrid (no lens needed if mouse is off), draw every route at `prog = 1` (full length; dead-end dashing still applies if a cell is degraded so fault state is visible), and draw every node at `rise = 0`, `alpha = 1`, with fault/focus styling but no glow decay and no breathing-ring animation (pass a fixed T so the ring is static). This is the FIG-05 path: interactions.ts calls it at init under reduced motion and again after any state mutation (fault inject / heal / focus change). It must be reachable WITHOUT ever entering the rAF loop.
  </action>
  <acceptance_criteria>
    - `grep -c 'requestAnimationFrame' src/lib/fig01/render.ts` returns exactly 2 (one in tick's re-schedule, one in startAnimationLoop's initial schedule) — the single consolidated loop
    - The `renderStaticFrame` function body contains no `requestAnimationFrame` (reduced-motion path starts no loop) — confirmed by reading the function; the total count of 2 above accounts for both being inside the loop driver only
    - `src/lib/fig01/render.ts` contains `export function renderStaticFrame`, `export function startAnimationLoop`, `export function stopAnimationLoop`
    - `src/lib/fig01/render.ts` contains `cancelAnimationFrame` (loop is stoppable for lifecycle gating)
    - `src/lib/fig01/render.ts` contains `2600` and `1400` (ambient cadence + intro gate constants ported verbatim)
    - `npx astro check` reports 0 errors
  </acceptance_criteria>
  <verify>
    <automated>npx astro check</automated>
  </verify>
  <done>A single idempotent rAF loop drives all motion with correct build-in stagger and ambient cadence; renderStaticFrame paints one complete informative frame with zero requestAnimationFrame, giving plan 02-03 a genuine reduced-motion path; astro check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tokens.ts → render.ts | Token colors consumed for canvas fills (read-only) |
| model FigureState → render.ts | State read for drawing; render mutates only per-frame glow decay |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Tampering (token drift) | canvas brand colors | low | mitigate | No hex literal in render.ts — all brand colors via getTokens(); only UI-SPEC-blessed structural greys inline as rgba(). Enforced by hex-grep acceptance gate. |
| T-02-04 | Denial of Service (self-inflicted CPU) | rAF loop | medium | mitigate | Exactly one consolidated rAF loop (grep count = 2), DPR capped at 2; stoppable via cancelAnimationFrame for plan 02-03's visibility gating. |
</threat_model>

<verification>
- `npx astro check` → 0 errors
- `grep -nE '#[0-9a-fA-F]{3,8}' src/lib/fig01/render.ts` → 0 matches
- `grep -c 'requestAnimationFrame' src/lib/fig01/render.ts` → 2 (single loop)
- Read `renderStaticFrame` and confirm no requestAnimationFrame is reachable from it
</verification>

<success_criteria>
- render.ts draws every figure primitive with token-sourced colors and zero hex literals
- Exactly one rAF loop; DPR capped at 2; fault state dual-encoded (amber + dashed)
- renderStaticFrame provides a genuine no-loop static path for the FIG-05 reduced-motion branch
</success_criteria>

<output>
Create `.planning/phases/02-fig01-signature-figure/02-02-SUMMARY.md` when done
</output>
