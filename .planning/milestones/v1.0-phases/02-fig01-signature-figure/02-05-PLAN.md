---
phase: 02-fig01-signature-figure
plan: 05
type: execute
wave: 5
depends_on: [02-04]
files_modified: []
autonomous: true
requirements: [FIG-01, FIG-02, FIG-03, FIG-04, FIG-05, FIG-06, FIG-07]
must_haves:
  truths:
    - "The whole Fig. 01 engine builds clean and passes an aggregate source + built-output verification battery covering all three hardening gaps, the honesty gate, and the no-hex/single-rAF perf floor"
    - "The figure is live at https://p2401kumar.github.io — the served HTML contains the fig-bar chrome (fig. 01 — request path, send request, inject fault)"
  artifacts:
    - "verified dist/ build with the figure active"
    - "live GitHub Pages deploy serving the figure chrome"
  key_links:
    - "push to main triggers the withastro/action deploy.yml workflow — the established Phase 1 pattern (fast-forward only, never force-push)"
    - "the live-URL grep is the phase's outermost acceptance gate: chrome present in served HTML = FIG-01 delivered end-to-end"
---

<objective>
Run the phase's aggregate verification battery — a single sweep of source-level and built-output acceptance checks proving all three research-mandated hardening gaps (reduced-motion no-loop path, keyboard proxy controls, setTimeout self-heal), the honesty gate, the no-hex/single-rAF performance floor, and the ARIA surface are all intact — then deploy to GitHub Pages (fast-forward push to main, no force-push) and confirm the live served HTML contains the figure chrome.

Purpose: The earlier plans each verified their own slice; this plan is the goal-backward closing gate that proves the assembled figure satisfies FIG-01..07 at the built + live level. In-browser visual/interaction QA (hover tooltip render, 60fps feel, keyboard walk, reduced-motion visual) is intentionally folded into the Phase 3 polish/Lighthouse pass — a CONTEXT.md deferred item and the established Phase 1 residual-QA pattern — so this plan's gates are automated (source greps + built-output + live curl), not manual.

Output: verified `dist/` build; live deploy at https://p2401kumar.github.io.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-fig01-signature-figure/02-CONTEXT.md
@.planning/phases/02-fig01-signature-figure/02-RESEARCH.md
@.planning/phases/01-foundation-editorial-shell/01-07-SUMMARY.md
@src/lib/fig01/model.ts
@src/lib/fig01/render.ts
@src/lib/fig01/interactions.ts
@src/lib/fig01/index.ts
@src/components/Figure01.astro
</context>

## Artifacts this plan produces

- A verified `dist/` build with Fig. 01 active (no new source files — this plan is the aggregate verification + deploy gate)
- Live deploy at https://p2401kumar.github.io serving the figure chrome

<tasks>

<task type="auto">
  <name>Task 1: Aggregate verification battery — clean build + source + built-output acceptance sweep</name>
  <files>(verification only — no source files created; fix-forward only if a gate fails)</files>
  <read_first>
    - .planning/REQUIREMENTS.md (FIG-01..07 acceptance surface — the checklist this battery proves)
    - .planning/phases/02-fig01-signature-figure/02-RESEARCH.md ("Common Pitfalls" 1–4 — the failure signatures the greps below detect)
    - src/lib/fig01/render.ts, src/lib/fig01/interactions.ts, src/lib/fig01/index.ts, src/components/Figure01.astro (the assembled engine being gated)
  </read_first>
  <action>
    Run the full battery. First `npx astro check` (0 errors) and `npm run build` (exit 0). Then run the source + built-output assertions below; if any FAILS, fix-forward in the owning module (this is a gap-closure gate, not a place to relax criteria) and rebuild. Do not weaken or delete any acceptance check to make it pass.

    No-hex / token-sourcing (FIG-07 token-drift discipline): every file under `src/lib/fig01/` and `src/components/Figure01.astro` is free of hardcoded hex color literals — brand colors resolve from tokens.ts / var(--token).

    Single rAF loop (FIG-07): the animation-frame scheduling call appears only inside render.ts's loop driver (count 2) and nowhere else in `src/lib/fig01/`; DPR is capped at 2 in render.ts.

    Reduced-motion no-loop path (FIG-05): interactions.ts uses `matchMedia('(prefers-reduced-motion: reduce)')` with a change listener, and applyMotionPreference's reduced-motion branch calls renderStaticFrame and NOT startAnimationLoop (confirm by reading it); render.ts's renderStaticFrame starts no rAF loop.

    setTimeout self-heal (FIG-03/FIG-05): interactions.ts contains `setTimeout(` and `8000` for the heal timer, decoupled from rAF.

    Lifecycle gating (FIG-07): interactions.ts contains IntersectionObserver, visibilitychange, and document.hidden.

    Keyboard proxy + ARIA (FIG-06): Figure01.astro has ≥ 10 `data-node` proxy buttons, `role="img"`, an `aria-label`, and `aria-live="polite"`; the bundling `<script>` tag is bare.

    Honesty gate (FIG-04): `src/data/fig01.ts` has 10 `source:` annotations; no metric appears in the figure that is not present in src/data/systems.ts / the résumé.

    Built output (FIG-01/02/03): `dist/index.html` contains the fig-bar chrome strings and the ARIA surface, and does NOT contain a literal unresolved `../lib/fig01` import path (script bundled to a hashed `dist/_astro/*.js` asset — Pitfall 1).
  </action>
  <acceptance_criteria>
    - `npx astro check` reports 0 errors AND `npm run build` exits 0
    - `grep -rnE '#[0-9a-fA-F]{3,8}' src/lib/fig01/ src/components/Figure01.astro` returns 0 matches
    - `grep -rc 'requestAnimationFrame' src/lib/fig01/` shows render.ts = 2 and every other fig01 file = 0
    - `grep -c 'setTimeout' src/lib/fig01/interactions.ts` returns ≥ 1 AND `grep -c '8000' src/lib/fig01/interactions.ts` returns ≥ 1
    - `grep -c 'IntersectionObserver' src/lib/fig01/interactions.ts` ≥ 1, `grep -c 'visibilitychange' src/lib/fig01/interactions.ts` ≥ 1, `grep -c "prefers-reduced-motion: reduce" src/lib/fig01/interactions.ts` ≥ 1
    - `grep -c 'data-node' src/components/Figure01.astro` returns ≥ 10; file contains `role="img"` and `aria-live="polite"`
    - `grep -c 'source:' src/data/fig01.ts` returns 10
    - `grep -c 'fig. 01 — request path through a cellularized region' dist/index.html` returns 1; `grep -c 'send request' dist/index.html` ≥ 1; `grep -c 'inject fault' dist/index.html` ≥ 1
    - `grep -c '../lib/fig01' dist/index.html` returns 0 (script bundled, not unresolved)
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Clean astro check + build; every source-level hardening-gap gate (no-hex, single-rAF, reduced-motion no-loop, setTimeout heal, lifecycle gating, keyboard/ARIA, honesty) passes, and dist/index.html shows the figure chrome with the script bundled to a hashed asset.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy to GitHub Pages (fast-forward push to main) + live-URL chrome check</name>
  <files>(git/deploy operation — no source files)</files>
  <read_first>
    - .planning/phases/01-foundation-editorial-shell/01-07-SUMMARY.md (the established deploy pattern: push to main → withastro/action deploy.yml runs → gh run confirms success → curl the live URL; Pages build_type is already `workflow`)
    - .planning/STATE.md (confirms site is already live at https://p2401kumar.github.io — this is a routine incremental deploy, not a first-time migration)
  </read_first>
  <action>
    Ensure all Phase 2 plan commits (02-01..02-04, plus this plan's plan-metadata) are on local `main`, then push to origin with a FAST-FORWARD push only — `git push origin main` with NO `--force`/`--force-with-lease` (the orchestrator constraint: deploy pushes are routine now, force-push is forbidden; if the push is rejected as non-fast-forward, STOP and report rather than forcing). Pushing to `main` triggers the `deploy.yml` GitHub Actions workflow (withastro/action → actions/deploy-pages).

    Wait for the deploy run to complete: use `gh run list --workflow=deploy.yml -L 1` (and/or `gh run watch`) to confirm the latest run on `main` is `completed`/`success`. Then verify the live served HTML: `curl -s https://p2401kumar.github.io/` and confirm it contains the figure chrome — `fig. 01 — request path through a cellularized region`, `send request`, and `inject fault`. If the deploy run has not finished propagating, poll the live URL until the chrome string appears or a reasonable timeout elapses; report the run id and the curl evidence.
  </action>
  <acceptance_criteria>
    - `git push origin main` succeeds as a fast-forward (no force flag used); `git rev-parse origin/main` equals local `main`
    - `gh run list --workflow=deploy.yml -L 1` shows the latest run on `main` with status `completed` and conclusion `success`
    - `curl -s https://p2401kumar.github.io/` output contains `fig. 01 — request path through a cellularized region`
    - `curl -s https://p2401kumar.github.io/` output contains `send request` and `inject fault`
  </acceptance_criteria>
  <verify>
    <automated>curl -s https://p2401kumar.github.io/ | grep -c 'fig. 01 — request path through a cellularized region'</automated>
  </verify>
  <done>main is fast-forward-pushed to origin (no force-push); the deploy.yml Actions run completed successfully; the live https://p2401kumar.github.io served HTML contains the fig-bar chrome, sending Fig. 01 into production.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local main → origin/main | Fast-forward push triggers the trusted withastro/action deploy pipeline |
| GitHub Actions → GitHub Pages | Build artifact published to the public site (no secrets in build) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09 | Tampering (history rewrite) | git push to main | high | mitigate | Fast-forward push only — force-push is forbidden by the orchestrator constraint; on a non-fast-forward rejection the action STOPS and reports rather than forcing, preserving history/provenance. |
| T-02-10 | Denial of Service (broken deploy) | deploy.yml run | medium | mitigate | Deploy gated on a clean local build (Task 1) + explicit `gh run` success confirmation + a live-URL chrome grep before the plan is considered done. |
| T-02-SC | Tampering (supply chain) | npm dependencies | low | accept | No new runtime/dev dependency added this phase (vitest deferred — 02-RESEARCH.md [SUS] flag not worth a phase-pausing checkpoint for an optional test runner); no install task exists, so no package-legitimacy checkpoint is required. |
</threat_model>

<verification>
- `npx astro check` → 0 errors; `npm run build` → exit 0
- Full source + dist grep battery (Task 1 acceptance list) all pass
- `git push origin main` fast-forward succeeds; `origin/main` == local `main`
- `gh run list --workflow=deploy.yml -L 1` → latest run `completed`/`success` on `main`
- `curl -s https://p2401kumar.github.io/` contains the fig-bar chrome strings
</verification>

<success_criteria>
- All FIG-01..07 source + built-output gates pass in one aggregate sweep
- Fig. 01 is live at https://p2401kumar.github.io with the figure chrome present in served HTML
- Deploy performed as a fast-forward push (no force-push); deploy.yml run confirmed successful
</success_criteria>

<output>
Create `.planning/phases/02-fig01-signature-figure/02-05-SUMMARY.md` when done
</output>
