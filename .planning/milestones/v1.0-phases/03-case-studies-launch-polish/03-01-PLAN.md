---
phase: 03-case-studies-launch-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content.config.ts
  - src/content/case-studies/dynamodb-cellularization.md
  - src/content/case-studies/elb-auto-weight-away.md
autonomous: true
requirements: [CASE-01, CASE-02, CASE-03]
user_setup: []

must_haves:
  truths:
    - "The `case-studies` content collection is defined and typed; getCollection('case-studies') returns exactly 2 entries at build time (CASE-03)"
    - "A deliberately malformed case-study entry makes `npm run build` exit non-zero with a Zod validation error, and the build returns to exit 0 once the fixture is removed (CASE-03 rejection proof)"
    - "Both authored case studies carry only resume-traceable numbers — the only percentage tokens present are 30, 20, 10, 90 (CASE-01, CASE-02 honesty gate)"
  artifacts:
    - src/content.config.ts
    - src/content/case-studies/dynamodb-cellularization.md
    - src/content/case-studies/elb-auto-weight-away.md
  key_links:
    - "src/content.config.ts zod schema — validates → the two .md frontmatter entries during astro build/sync"
    - "each entry's `systemId` — must match → a name in src/data/systems.ts (dynamodb/cellularization, elb/auto-weight-away)"
    - "each metric's `source` field — traces to → the resume, extending the CONT-07 honesty gate into the collection"
---

<objective>
Establish the schema-enforced Astro Content Layer collection that backs both case studies, author the two entries under absolute fact discipline, and prove the schema rejects a malformed entry via a failing build.

Purpose: CASE-03 requires a typed collection whose schema enforces the problem -> approach (with trade-offs) -> impact structure and rejects a malformed entry. CASE-01 and CASE-02 require the authored deep-dive content itself. This plan produces the data layer; the visitor-facing pages that render it are plan 03-02.
Output: `src/content.config.ts` (collection + zod schema), two validated Markdown entries, and captured evidence that a malformed fixture fails `npm run build`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-case-studies-launch-polish/03-CONTEXT.md
@.planning/phases/03-case-studies-launch-polish/03-RESEARCH.md
@src/data/systems.ts
</context>

<artifacts_produced>
## Artifacts this plan produces

| Symbol / File | Kind | Notes |
|---------------|------|-------|
| `src/content.config.ts` | Content Layer config | `defineCollection` + `glob()` loader + zod schema; exports `collections = { 'case-studies': ... }` |
| Collection `case-studies` | typed collection | schema fields: `title`, `systemId`, `dateRange`, `standfirst`, `metrics[]{label,value,source}`, `problem`, `approach`, `tradeoffs`, `impact` |
| `src/content/case-studies/dynamodb-cellularization.md` | content entry | frontmatter-driven; `systemId: dynamodb/cellularization` |
| `src/content/case-studies/elb-auto-weight-away.md` | content entry | frontmatter-driven; `systemId: elb/auto-weight-away` |
| (temporary) `src/content/case-studies/zzz-malformed-fixture.md` | negative-test fixture | created to prove rejection, then DELETED before the plan completes — must NOT remain in the repo |

Downstream consumers: plan 03-02 (`getCollection('case-studies')` in `[slug].astro`), plan 03-03 (`standfirst` feeds the SEO description).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Define the case-studies Content Layer collection + zod schema (CASE-03)</name>
  <files>src/content.config.ts</files>
  <read_first>
    - .planning/phases/03-case-studies-launch-polish/03-RESEARCH.md (Pattern 1 for exact import surface + schema shape; Pitfall 3 for the mandatory `src/content.config.ts` location; Recommended Project Structure)
    - src/data/systems.ts (the `name` values `systemId` must correspond to; the `source` string idiom to mirror)
  </read_first>
  <action>
    Create `src/content.config.ts` at exactly the `src/` root (sibling to `src/content/`, NOT `src/content/config.ts` — the legacy path silently fails to register the collection under Astro 7 Content Layer, per RESEARCH Pitfall 3). Import `defineCollection, z` from `astro:content` and `glob` from `astro/loaders`. Define one collection named `case-studies` using a `glob({ pattern: '**/*.md', base: './src/content/case-studies' })` loader. Its zod schema REQUIRES every field: `title` (z.string), `systemId` (z.string — the corresponding src/data/systems.ts `name`, i.e. dynamodb/cellularization or elb/auto-weight-away), `dateRange` (z.string), `standfirst` (z.string), `metrics` (z.array of z.object with `label`/`value`/`source` all z.string, `.min(1)`), and the four structure fields `problem`, `approach`, `tradeoffs`, `impact` each `z.string().min(1)`. The four structure fields are the mechanism that enforces the problem/approach/impact structure (CASE-03) — required typed frontmatter fields, NOT heading-text parsing of the body (RESEARCH Anti-Patterns). Export `export const collections = { 'case-studies': caseStudies };`. No `any`, no unused locals (tsconfig astro/tsconfigs/strict).
  </action>
  <verify>
    <automated>cd "C:/Development/Dump/Portfolio" && npx astro sync && npx astro check</automated>
  </verify>
  <acceptance_criteria>
    - `src/content.config.ts` exists at the `src/` root (NOT under `src/content/`).
    - `npx astro sync` completes and generates astro:content types for `case-studies` (no collection-does-not-exist error when 03-02 later calls getCollection).
    - `npx astro check` reports 0 errors.
    - The schema declares all nine required fields; grep confirms `problem`, `approach`, `tradeoffs`, `impact` each appear as schema keys.
  </acceptance_criteria>
  <done>The typed `case-studies` collection is registered with a zod schema requiring the four problem/approach/tradeoffs/impact structure fields plus title/systemId/dateRange/standfirst/metrics.</done>
</task>

<task type="auto">
  <name>Task 2: Author both case-study entries under absolute fact discipline (CASE-01, CASE-02)</name>
  <files>src/content/case-studies/dynamodb-cellularization.md, src/content/case-studies/elb-auto-weight-away.md</files>
  <read_first>
    - .planning/phases/03-case-studies-launch-polish/03-CONTEXT.md (the fact-discipline block: the ONLY permitted resume numbers and the required general-engineering framing for the trade-off sections — read verbatim, do not paraphrase the facts)
    - src/data/systems.ts (the exact `description` strings to reuse verbatim as `standfirst`, and the exact `source` strings to mirror in metrics[].source)
    - .planning/phases/03-case-studies-launch-polish/03-UI-SPEC.md (Copywriting Contract: standfirst copy, metrics-strip labels/values, section-heading casing, voice)
  </read_first>
  <action>
    Author two Markdown entries whose frontmatter satisfies the Task 1 schema. Body may be empty or a one-line note; ALL displayed prose lives in the frontmatter section fields (03-02 renders them). Voice: first person, plain, no adjectives about self, mono artifact names — identical to the shipped site.

    dynamodb-cellularization.md: `title` a plain factual title; `systemId: dynamodb/cellularization`; `dateRange: "2023"`; `standfirst` = the systems.ts description for that row verbatim; `metrics` = two entries, one label reliability value +30% and one label p99 latency value -20%, both with source string resume-4.5 §AWS — DynamoDB cellularization (+30% reliability, -20% p99 latency). `problem` = why compute/storage segregation into isolated failure domains in US-EAST-1 was needed (blast-radius language, no invented internals). `approach` = the cellularization approach plus extending AWS SDK Java v1/v2 for the new topology. `tradeoffs` = GENERAL engineering considerations of the cellular-isolation pattern only (blast-radius reduction vs capacity-pooling efficiency, routing/placement complexity, migration sequencing) — never framed as insider Amazon specifics. `impact` = up to +30% reliability, -20% p99 latency.

    elb-auto-weight-away.md: `title` plain factual; `systemId: elb/auto-weight-away`; `dateRange: "2023"`; `standfirst` = the systems.ts description for that row verbatim; `metrics` = two entries, one label peak-hour latency value -10% and one label ops automated value 90%, both with source string resume-4.5 §AWS — ELB auto-weight-away (-10% peak-hour latency, 90% capacity ops automated). `problem` = surge traffic and affected-LB handling. `approach` = LB-management service pre-allocating dedicated capacity for anticipated traffic and automatically weighing out affected LBs. `tradeoffs` = GENERAL considerations of the weight-away pattern only (automation speed vs false-positive weigh-outs, pre-allocation cost vs surge readiness). `impact` = -10% peak-hour latency, 90% of capacity adjustments automated.

    HARD honesty gate: the ONLY percentage tokens permitted anywhere in either file are 30%, 20%, 10%, 90%. Do NOT invent any other percentage, latency figure, dollar amount, team size, date, or internal name. Non-percentage resume facts that may appear: US-EAST-1, AWS SDK Java v1/v2, p99, 2023. The back-link and cross-link that end each rendered page live in 03-02, not here.
  </action>
  <verify>
    <automated>cd "C:/Development/Dump/Portfolio" && npm run build && ! grep -hoE '[0-9]+%' src/content/case-studies/dynamodb-cellularization.md src/content/case-studies/elb-auto-weight-away.md | grep -qvE '^(10|20|30|90)%$'</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (both entries satisfy the schema).
    - `grep -hoE '[0-9]+%' src/content/case-studies/*.md | sort -u` yields exactly `10%`, `20%`, `30%`, `90%` and nothing else (no forbidden percentage tokens).
    - Each file's `standfirst` equals the corresponding src/data/systems.ts `description` string verbatim.
    - Each file has exactly the four structure fields populated; the `tradeoffs` field frames trade-offs as general engineering considerations of the pattern (no invented Amazon internals, team sizes, or dates beyond 2023).
  </acceptance_criteria>
  <done>Both case studies are authored, build-valid, and pass the percentage-token honesty gate.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the schema rejects a malformed entry via a failing build (CASE-03 proof)</name>
  <files>src/content/case-studies/zzz-malformed-fixture.md (temporary — created then deleted)</files>
  <read_first>
    - .planning/phases/03-case-studies-launch-polish/03-RESEARCH.md (Pitfall 1 "astro check silently passing on schema violations" — why the proof MUST gate on `npm run build`, not `astro check` alone; Code Examples "Negative-test fixture for schema rejection")
    - src/content.config.ts (the schema the fixture must violate — omit required fields)
  </read_first>
  <action>
    Create a deliberately malformed entry at `src/content/case-studies/zzz-malformed-fixture.md`. Use a plain non-underscore filename (NOT a leading-underscore name): the Content Layer glob loader may silently ignore `_`-prefixed files, which would defeat the negative test by skipping the fixture entirely. Give it frontmatter with `title` only and OMIT the required `systemId`, `dateRange`, `standfirst`, `metrics`, `problem`, `approach`, `tradeoffs`, `impact` fields. Run `npm run build` and confirm it exits NON-ZERO with a Zod validation error naming the missing fields (this is the CASE-03 "rejects a malformed entry" proof — the build, not astro check, is the provable signal per RESEARCH Pitfall 1). Capture the failing exit code and the error text into the SUMMARY as evidence. Then DELETE `zzz-malformed-fixture.md` and re-run `npm run build`, confirming it now exits 0. The fixture must NOT remain in the repo (it would break the real build). Do not leave any commented-out fixture behind.
  </action>
  <verify>
    <automated>cd "C:/Development/Dump/Portfolio" && test ! -f src/content/case-studies/zzz-malformed-fixture.md && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - WITH the fixture present, `npm run build` exits NON-ZERO and the error output names the missing required schema fields (rejection proven at build time, not merely astro check).
    - The fixture uses a plain (non-underscore) filename so the glob loader does not silently skip it.
    - AFTER deleting the fixture, `npm run build` exits 0 and the repo contains no file matching `src/content/case-studies/zzz-malformed-fixture.md`.
    - The captured failing-build evidence (exit code + Zod error text) is recorded in the SUMMARY.
  </acceptance_criteria>
  <done>The malformed fixture is proven to fail the build, then removed; the clean build is green again. CASE-03 rejection is demonstrated by the build exit code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo-authored content -> build | Developer-authored Markdown frontmatter crosses into the build via the Content Layer; the zod schema is the only validation boundary (there is no runtime/user input on this static site) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | case-study content collection (honesty/structure integrity) | medium | mitigate | zod schema in content.config.ts fails the build on any malformed/incomplete entry (Task 1 + Task 3 proof); percentage-token honesty gate (Task 2) blocks invented numbers, extending CONT-07 |
| T-03-02 | Information Disclosure | trade-off prose leaking confidential internals | medium | mitigate | fact-discipline action constrains trade-offs to general-pattern engineering framing; no invented Amazon internals/team sizes/dates permitted (Task 2 acceptance) |
</threat_model>

<verification>
- `npx astro sync && npx astro check` clean (schema registers, 0 type errors).
- `npm run build` exits 0 with both real entries; `getCollection('case-studies')` will return 2 entries for 03-02.
- Percentage-token honesty gate passes: only 10/20/30/90 present in the two entries.
- Malformed-fixture build fails non-zero, then passes 0 after removal (CASE-03 proof captured).
</verification>

<success_criteria>
- Typed `case-studies` collection exists at `src/content.config.ts` with the four structure fields required.
- Two fact-disciplined entries authored and build-valid (CASE-01, CASE-02 content).
- Malformed-entry rejection demonstrated via failing build and then reverted (CASE-03).
</success_criteria>

<output>
Create `.planning/phases/03-case-studies-launch-polish/03-01-SUMMARY.md` when done, including the captured failing-build evidence for the malformed fixture.
</output>