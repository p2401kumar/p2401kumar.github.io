---
phase: 04-deck-mechanics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/panels.ts
  - src/components/Panel.astro
  - src/components/PanelDeck.astro
  - src/components/DeckIndex.astro
  - src/styles/deck.css
  - src/pages/index.astro
  - src/components/SiteHeader.astro
autonomous: true
requirements: [DECK-04, DECK-05, DECK-06, DECK-07, DECK-08]

must_haves:
  truths:
    - "With no JS (or before init), the home page renders the exact v1 scrolling layout — all seven sections in order, header and footer in normal flow"
    - "All v1 section content stays in the DOM inside full-viewport Panel wrappers (never unmounted, never display-hidden)"
    - "A typed panel manifest is the single source of truth for the 7 panels (id, hash, title, label)"
    - "The mono progress index + jump list render from the manifest as real in-page anchors (keyboard reachable, work with no JS)"
    - "A first-visit hint and a view-classic/deck-view link exist in the chrome markup"
    - "Under .deck-active, header and footer are fixed frame chrome and panels position via transform only"
  artifacts:
    - path: "src/data/panels.ts"
      provides: "PanelSpec interface + ordered panels[] manifest (single source of truth)"
      exports: ["PanelSpec", "panels"]
      contains: "export const panels"
    - path: "src/components/Panel.astro"
      provides: "Thin full-viewport panel wrapper (.panel, data-panel-id, tabindex=-1, conditional id)"
    - path: "src/components/PanelDeck.astro"
      provides: "Deck shell wrapping panels + DeckIndex; imports deck.css"
    - path: "src/components/DeckIndex.astro"
      provides: "Progress index pill, jump list, first-visit hint, view-classic/deck-view links, aria-live region"
    - path: "src/styles/deck.css"
      provides: "Deck layout: transform-based panel positioning, fixed chrome, body scroll-lock, reduced-motion instant — all scoped under html.deck-active"
    - path: "src/pages/index.astro"
      provides: "Restructured composition wrapping each section in <Panel>"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/Panel.astro"
      via: "each v1 section wrapped in <Panel id=... label=...>"
      pattern: "<Panel"
    - from: "src/components/DeckIndex.astro"
      to: "src/data/panels.ts"
      via: "jump list + index rendered by mapping panels[]"
      pattern: "panels.map"
    - from: "src/components/PanelDeck.astro"
      to: "src/styles/deck.css"
      via: "import of the deck stylesheet (only bundles on pages using the deck)"
      pattern: "deck.css"
---

<objective>
Build the static, no-JS-safe foundation of the panel deck: the typed panel manifest, the deck shell components (PanelDeck / Panel / DeckIndex), the restructured `index.astro`, and the deck layout stylesheet — with ZERO runtime behavior wired yet.

Purpose: Establish the DOM contract and CSS that `deck.ts` (plan 04-02) enhances, AND deliver DECK-07's progressive-enhancement base case for free — after this plan, with no deck script loaded, the home page renders byte-for-byte as the v1 scrolling layout because every deck rule is scoped under `.deck-active`, a class only `deck.ts` will ever add.

Output: `src/data/panels.ts`, `src/components/{Panel,PanelDeck,DeckIndex}.astro`, `src/styles/deck.css`, restructured `src/pages/index.astro`, and one reconciled header anchor.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deck-mechanics/04-CONTEXT.md
@.planning/phases/04-deck-mechanics/04-UI-SPEC.md

# Chrome + composition surfaces being restructured
@src/pages/index.astro
@src/components/SiteHeader.astro
@src/components/SiteFooter.astro
@src/components/ContactSection.astro
@src/components/SystemsList.astro
@src/styles/tokens.css
@src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Panel manifest (single source of truth)</name>
  <files>src/data/panels.ts</files>
  <read_first>
    - src/data/fig01.ts and src/data/profile.ts (existing typed-data-module conventions to mirror: named interface + exported const array)
    - .planning/phases/04-deck-mechanics/04-RESEARCH.md section "Pattern: Panel manifest as single source of truth"
    - .planning/phases/04-deck-mechanics/04-UI-SPEC.md "Copywriting Contract" (jump-list labels + aria-live title casing)
  </read_first>
  <action>
    Create `src/data/panels.ts` exporting an interface `PanelSpec` with fields `id: string`, `hash: string`, `title: string`, `label: string`, and an ordered `export const panels: PanelSpec[]` with exactly these 7 entries in this order (DECK-05, DECK-04): hero / fig-01 / systems / experience / patents / skills / contact. For each entry `id === hash`. `title` is Title Case for the aria-live announcement; `label` is the lowercase-mono jump-list text per the UI-SPEC Copywriting Contract. Values — id | title | label: `hero`|`Prateek Kumar`|`hero`; `fig-01`|`Fig. 01`|`fig. 01`; `systems`|`Selected Systems`|`selected systems`; `experience`|`Experience`|`experience`; `patents`|`Patents & Publications`|`patents & publications`; `skills`|`Skills`|`skills`; `contact`|`Contact`|`contact`. Also export `export const PANEL_COUNT = panels.length`. No hardcoded panel count or id list may exist anywhere else — this module is the only source. Do NOT import anything from `src/lib/fig01/*` or a future `src/lib/nightsky/*` (the manifest is dependency-free data).
  </action>
  <verify>
    <automated>test "$(grep -c 'id:' src/data/panels.ts)" = "7" && npx astro check</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(grep -oE "id: '[a-z0-9-]+'" src/data/panels.ts | wc -l | tr -d ' ')" = "7"` (exactly 7 panel entries)
    - `grep -q "export interface PanelSpec" src/data/panels.ts` and `grep -q "export const panels" src/data/panels.ts`
    - `grep -q "hero" src/data/panels.ts && grep -q "fig-01" src/data/panels.ts && grep -q "contact" src/data/panels.ts` (ordered ids present)
    - `grep -q "export const PANEL_COUNT" src/data/panels.ts`
    - `npx astro check` reports 0 errors
  </acceptance_criteria>
  <done>panels.ts exports PanelSpec + a 7-entry panels[] manifest + PANEL_COUNT; astro check passes; no other file duplicates the panel list.</done>
</task>

<task type="auto">
  <name>Task 2: Deck shell components + index.astro restructure + header anchor reconcile</name>
  <files>src/components/Panel.astro, src/components/PanelDeck.astro, src/components/DeckIndex.astro, src/pages/index.astro, src/components/SiteHeader.astro</files>
  <read_first>
    - src/pages/index.astro (current composition — the exact section order to preserve)
    - src/components/Figure01.astro (the bare-bundling-script seam convention; do NOT add the deck script here — that is plan 04-02)
    - src/components/ContactSection.astro (already renders id="contact" — the duplicate-id hazard) and src/components/SystemsList.astro (renders id="work")
    - .planning/phases/04-deck-mechanics/04-UI-SPEC.md sections "Progress Index + Jump List", "Copywriting Contract", "Fixed chrome"
  </read_first>
  <action>
    Create three bespoke `.astro` components and restructure the page. All existing v1 section components (`Hero`, `Figure01`, `SystemsList`, `ExperienceSection`, `PatentsSection`, `SkillsSection`, `ContactSection`) pass through UNMODIFIED — they only get wrapped.

    `Panel.astro`: props `{ id: string; label?: string; emitId?: boolean }`. Renders a `<div class="panel" data-panel-id={id} tabindex="-1">` with a default `<slot />`. Emit `id={id}` ONLY when `emitId !== false`; when `emitId === false` omit the `id` attribute entirely. Use a `<div>`, not `<section>`, to avoid nested landmark/id semantics. The wrapper must carry `data-panel-id` unconditionally (deck.ts keys the change-event `detail.id` and focus target off `.panel`, never off the DOM `id`).

    `DeckIndex.astro`: imports `panels` from `../data/panels`. Renders (a) a collapsed progress-index pill showing `{NN} / {total}` zero-padded (initial `01 / 07`), (b) a jump list — one real `<a href={'#' + p.hash} data-panel-index={i}>` per panel using the lowercase `p.label` text (real anchors so the no-JS fallback keeps native in-page navigation; DECK-04, DECK-05), (c) a first-visit hint element (copy `scroll · ↓ · swipe`, DECK-06), (d) a `view classic →` link and a `deck view →` link (DECK-07), and (e) a visually-hidden `aria-live="polite"` region `<div id="deck-live" class="sr-only" aria-live="polite" aria-atomic="false"></div>` with empty initial text (DECK-03). Give the pill wrapper, jump list, hint, and each link stable hooks deck.ts can query (e.g. `.deck-index`, `#deck-index-count`, `.deck-jump`, `.deck-hint`, `.deck-view-classic`, `.deck-view-deck`). Scoped `<style>` here owns the pill/jump/hint/link VISUAL styling + show/hide toggling driven by `html.deck-active` / `html.classic-active` presence (this chrome is hidden by default so the pure no-JS page shows none of it) — this styling MAY use any CSS technique for chrome; only `.panel` hiding is transform-restricted (that lives in deck.css, task 3). Every new interactive element inherits the global `:focus-visible` rule — do not restyle focus.

    `PanelDeck.astro`: imports `../styles/deck.css` in frontmatter (so deck CSS only bundles on pages that use the deck). Renders `<div class="deck"><slot /><DeckIndex /></div>`. Do NOT add any `<script>` yet.

    `index.astro`: import `Panel`, `PanelDeck`, `DeckIndex` is transitively via PanelDeck. Keep `SiteHeader` and `SiteFooter` as direct children of `.page`, OUTSIDE `<PanelDeck>` (they are fixed frame chrome, not panels). Wrap the seven sections, in the locked order, each in a `<Panel>` inside `<PanelDeck>`: hero→`<Panel id="hero" label="hero">`, fig-01→`<Panel id="fig-01" label="fig. 01">`, systems→`<Panel id="systems" label="selected systems">`, experience→`<Panel id="experience" label="experience">`, patents→`<Panel id="patents" label="patents & publications">`, skills→`<Panel id="skills" label="skills">`, contact→`<Panel id="contact" label="contact" emitId={false}>`. The contact panel uses `emitId={false}` because `ContactSection` already renders `id="contact"` — this prevents a duplicate `id="contact"` in the DOM while keeping the deep-link anchor target intact (the inner section supplies it).

    `SiteHeader.astro`: change the nav anchor `href="#work"` to `href="#systems"` (visible text stays `work`) so it resolves to the systems panel hash in both deck mode (hashchange → manifest) and no-JS mode (anchors to the systems Panel wrapper `id="systems"`). Leave `href="#contact"` as-is (already a valid panel hash). This is a chrome href reconcile, not a content-section change — it does not violate the sections-unmodified lock.
  </action>
  <verify>
    <automated>npm run build && test "$(grep -o 'class="panel"' dist/index.html | wc -l | tr -d ' ')" = "7"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds (build green at this plan boundary)
    - `test "$(grep -o 'class="panel"' dist/index.html | wc -l | tr -d ' ')" = "7"` (all seven sections wrapped)
    - `test "$(grep -o 'id="contact"' dist/index.html | wc -l | tr -d ' ')" = "1"` (no duplicate contact id)
    - `grep -q 'id="hero"' dist/index.html && grep -q 'id="fig-01"' dist/index.html && grep -q 'id="systems"' dist/index.html && grep -q 'id="patents"' dist/index.html` (deep-link targets present)
    - `test "$(grep -o 'href="#systems"' dist/index.html | wc -l | tr -d ' ')" -ge "1"` and `test "$(grep -o 'href="#work"' dist/index.html | wc -l | tr -d ' ')" = "0"` (header anchor reconciled)
    - `grep -q 'aria-live="polite"' dist/index.html` and `grep -q 'id="deck-live"' dist/index.html` (announcement region present)
    - `grep -q 'data-panel-index' dist/index.html` (jump list is real anchors)
    - v1 content still present: `grep -q 'request path through a cellularized region' dist/index.html && grep -q 'all systems operational' dist/index.html`
    - `npx astro check` reports 0 errors
  </acceptance_criteria>
  <done>index.astro wraps all 7 sections in Panels inside PanelDeck; DeckIndex renders manifest-driven index/jump/hint/links + aria-live region; contact has exactly one id; header `#work` reconciled to `#systems`; build + astro check green; all v1 content still in dist.</done>
</task>

<task type="auto">
  <name>Task 3: deck.css — transform-based layout, fixed chrome, scroll-lock, reduced-motion (all scoped under .deck-active)</name>
  <files>src/styles/deck.css</files>
  <read_first>
    - src/styles/tokens.css (every color/spacing value must read var(--token) — zero hex literals in deck.css)
    - src/components/SiteHeader.astro + src/components/SiteFooter.astro (current padding/border to preserve when fixing them)
    - .planning/phases/04-deck-mechanics/04-RESEARCH.md sections "Body Scroll Locking" and the "Cold load" diagram
    - .planning/phases/04-deck-mechanics/04-UI-SPEC.md "Interaction & Motion Contract" (420ms, cubic-bezier(0.16,1,0.3,1), translateY(24px); fixed chrome z-index:20)
  </read_first>
  <action>
    Create `src/styles/deck.css`. EVERY selector is scoped under `html.deck-active` (or `:global(html.deck-active)` is not needed here since this is a plain global stylesheet) so the base no-JS page is completely unaffected (DECK-07). Contents:

    1. Scroll lock: `html.deck-active, html.deck-active body { height: 100%; overflow: hidden; overscroll-behavior-y: none; }` — applied to the document element because the viewport, not a wrapper, must stop scrolling.
    2. Panel positioning: `html.deck-active .panel { position: absolute; inset: 0; overflow: auto; }` with centered content (grid place-items or margin auto with a max-width column ~880px matching the v1 `.page` rhythm). Panels overflow-scroll internally so tall panels (experience/skills) stay usable. Ensure centered content clears the fixed header (top) and footer (bottom) bars via padding/inset — the fixed chrome must never occlude panel content (resolves 04-RESEARCH Open Question 2).
    3. Active/inactive state via `data-state`: `html.deck-active .panel[data-state="active"] { transform: translateY(0); opacity: 1; pointer-events: auto; }` and `html.deck-active .panel[data-state="inactive"] { transform: translateY(24px); opacity: 0; pointer-events: none; }` — hide inactive panels using ONLY transform + opacity + pointer-events. Do NOT hide any `.panel` by removing it from layout flow via the CSS box-suppression property or the visibility property, and do NOT name those hiding techniques in comments in this file (the transform-only rule is a hard acceptance gate). Reverse-direction offset is handled by deck.ts toggling a direction hook if needed; a single translateY offset is sufficient for v1.
    4. Transition: `html.deck-active .panel { transition: transform 420ms cubic-bezier(0.16, 1, 0.3, 1), opacity 420ms cubic-bezier(0.16, 1, 0.3, 1); }`.
    5. Reduced-motion (DECK-08): `@media (prefers-reduced-motion: reduce) { html.deck-active .panel { transition: none; } }` — the ONLY reduced branch (instant snap, no dimmer tier).
    6. Fixed chrome: `html.deck-active header { position: fixed; top: 0; inset-inline: 0; z-index: 20; }` and `html.deck-active footer { position: fixed; bottom: 0; inset-inline: 0; z-index: 20; }` — position change only; keep existing padding/hairline (those live in the component styles). Confirm the phase decision: SiteHeader/SiteFooter become fixed frame chrome THIS phase (LOCKED per 04-UI-SPEC "Fixed chrome").
    7. Zero hex literals — read all colors via `var(--token)`; deck.css should need few/no color declarations (mostly layout), but any it needs must be tokens.
  </action>
  <verify>
    <automated>npm run build && test "$(grep -oE 'display:\s*none|visibility:\s*hidden' src/styles/deck.css | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(grep -oE 'display:\s*none|visibility:\s*hidden' src/styles/deck.css | wc -l | tr -d ' ')" = "0"` (transform-only panel hiding — no box-suppression/visibility anywhere in deck.css)
    - `test "$(grep -oE 'translateY' src/styles/deck.css | wc -l | tr -d ' ')" -ge "2"` (panels positioned via transform)
    - `grep -q 'prefers-reduced-motion' src/styles/deck.css && grep -qE 'transition:\s*none' src/styles/deck.css` (DECK-08 instant branch present)
    - `grep -q 'position: fixed' src/styles/deck.css` and `test "$(grep -oE 'html\.deck-active (header|footer)' src/styles/deck.css | wc -l | tr -d ' ')" -ge "2"` (fixed chrome under .deck-active)
    - `test "$(grep -oE '#[0-9a-fA-F]{3,8}' src/styles/deck.css | wc -l | tr -d ' ')" = "0"` (zero hex literals outside tokens.css)
    - every deck.css selector is scoped under `html.deck-active` (manual read: no bare `.panel`/`body` rule leaks to the no-JS page)
    - `npm run build` green
  </acceptance_criteria>
  <done>deck.css positions panels via transform only (no display/visibility hiding), fixes header/footer under .deck-active, locks body scroll, snaps instantly under reduced-motion, uses zero hex literals; build green; the no-JS page is untouched because everything is .deck-active-scoped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time data → static HTML | Panel manifest content is authored (typed, build-time) and rendered into markup — no user input crosses here this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02 | Tampering/Info-disclosure | DeckIndex aria-live + jump-list text | low | mitigate | All announced/label text is sourced only from the typed `src/data/panels.ts` manifest and rendered by Astro at build time — never derived from `location.hash` or any runtime input; all résumé content is already public per the honesty gate |
| T-04-SC | Tampering | package installs | low | accept | Zero new dependencies this plan (and phase) — no supply-chain surface; nothing to audit |
</threat_model>

<verification>
- `npm run build` green; `npx astro check` 0 errors.
- `dist/index.html`: exactly 7 `.panel` wrappers, exactly one `id="contact"`, deep-link target ids present, aria-live region present, header anchor reconciled, all v1 content strings present.
- `src/styles/deck.css`: transform-only panel hiding, reduced-motion instant branch, fixed chrome, zero hex literals.
- Pure no-JS behavior: because no deck script is loaded and every deck rule is `.deck-active`-scoped, opening `dist/index.html` shows the v1 scrolling layout (DECK-07 base case).
</verification>

<success_criteria>
- Panel manifest is the single source of truth (7 entries; no duplicate list anywhere).
- All v1 sections wrapped in Panels inside PanelDeck; header/footer remain outside the panel loop.
- DeckIndex renders manifest-driven progress index, jump list (real anchors), first-visit hint, view-classic/deck-view links, and an aria-live region.
- deck.css delivers transform-based layout + fixed chrome + scroll-lock + reduced-motion instant, all scoped under `.deck-active`, zero hex literals.
- Build + astro check green; the no-JS page is byte-equivalent to v1.
</success_criteria>

## Artifacts This Phase Produces (Plan 04-01)

**New files:**
- `src/data/panels.ts` — exports: `PanelSpec` (interface), `panels` (const array, 7 entries), `PANEL_COUNT` (const)
- `src/components/Panel.astro` — props `{ id, label?, emitId? }`; renders `<div class="panel" data-panel-id id? tabindex="-1">`
- `src/components/PanelDeck.astro` — renders `<div class="deck">` + `<DeckIndex/>`; imports `deck.css`
- `src/components/DeckIndex.astro` — progress index pill, jump list (`data-panel-index` anchors), first-visit hint (`.deck-hint`), `view classic →` (`.deck-view-classic`) + `deck view →` (`.deck-view-deck`) links, `#deck-live` aria-live region
- `src/styles/deck.css` — deck layout stylesheet (all rules `.deck-active`-scoped)

**New CSS classes / hooks:** `.deck`, `.panel` (+ `data-panel-id`, `data-state="active|inactive"`), `.deck-index`, `#deck-index-count`, `.deck-jump` (+ `data-panel-index`), `.deck-hint`, `.deck-view-classic`, `.deck-view-deck`, `#deck-live`; `html.deck-active`, `html.classic-active` (state classes deck.ts will toggle in 04-02).

**Modified files:** `src/pages/index.astro` (wraps sections in Panels), `src/components/SiteHeader.astro` (`#work` → `#systems`).

<output>
Create `.planning/phases/04-deck-mechanics/04-01-SUMMARY.md` when done.
</output>
