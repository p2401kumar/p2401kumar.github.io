---
phase: 04-deck-mechanics
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/lib/nightsky/deck.ts
  - src/components/PanelDeck.astro
autonomous: true
requirements: [DECK-01, DECK-02, DECK-03, DECK-04, DECK-05, DECK-06, DECK-07, DECK-08]

must_haves:
  truths:
    - "One notched-mouse wheel tick or one trackpad swipe advances exactly one panel (delta accumulation + transition lock; no double-fire, no dead feel)"
    - "A vertical touch swipe advances/retreats one panel; horizontal gestures are left to the browser (iOS edge-back intact)"
    - "Arrow/PageUp-Down/Home-End move panels without colliding with Fig. 01; non-active panels are inert + aria-hidden; each change announces via aria-live"
    - "The jump list moves directly to any panel; the progress index reflects the current panel"
    - "Every panel has a URL hash; back/forward navigate panels; a deep-linked cold-load resolves to the correct panel with no flash of panel 0"
    - "The first-visit hint dismisses on first navigation and is remembered; view-classic disables the deck and persists; deck-view restores it"
    - "Under prefers-reduced-motion, panel changes are instant"
    - ".deck-active is added to <html> ONLY after successful init; on any pre-activation throw the page stays the v1 scrolling layout"
    - "nightsky:panel-change is dispatched on document with detail {index, id, total} on every activation"
  artifacts:
    - path: "src/lib/nightsky/deck.ts"
      provides: "Panel state machine + input wiring + hash routing + a11y + hint/view-classic persistence + change-event dispatch; returns teardown"
      exports: ["initDeck", "WHEEL_THRESHOLD", "IDLE_RESET_MS", "TRANSITION_LOCK_MS"]
      contains: "export function initDeck"
    - path: "src/components/PanelDeck.astro"
      provides: "Bare bundling <script> that imports initDeck and boots it against the .deck root"
  key_links:
    - from: "src/components/PanelDeck.astro"
      to: "src/lib/nightsky/deck.ts"
      via: "bare (attribute-less) <script> imports initDeck and calls it against the .deck root"
      pattern: "initDeck"
    - from: "src/lib/nightsky/deck.ts"
      to: "src/data/panels.ts"
      via: "imports panels[] to drive goTo, hash resolution, and aria-live text"
      pattern: "from '.*data/panels'"
    - from: "src/lib/nightsky/deck.ts"
      to: "document"
      via: "goTo dispatches CustomEvent nightsky:panel-change + history.pushState + inert/focus/aria-live"
      pattern: "nightsky:panel-change"
---

<objective>
Build `src/lib/nightsky/deck.ts` — the vanilla-TS panel state machine — and boot it from `PanelDeck.astro`. This turns the static shell from plan 04-01 into a live no-scroll deck: wheel/touch/keyboard/jump input all resolve to one-gesture-one-panel, hash routing + cold-load work, non-active panels are inert with aria-live announcements, the first-visit hint and view-classic preference persist, transitions honor reduced-motion, and the `nightsky:panel-change` contract is dispatched for Phase 5 to consume later.

Purpose: DECK-01..08 behavior. Mirrors the fig01 module conventions (typed state, `required()` DOM lookups, `wire*` functions, `init*` returns a teardown, bare bundling `<script>` seam). `nightsky/*` and `fig01/*` never import each other.

Output: `src/lib/nightsky/deck.ts` (with named tunable constant exports) + the boot script in `PanelDeck.astro`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deck-mechanics/04-CONTEXT.md
@.planning/phases/04-deck-mechanics/04-RESEARCH.md

# Engine conventions to mirror + DOM contract from plan 04-01
@src/lib/fig01/index.ts
@src/lib/fig01/interactions.ts
@src/components/Figure01.astro
@src/components/PanelDeck.astro
@src/components/Panel.astro
@src/data/panels.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: deck.ts core — state machine, hash routing, a11y, change-event, bootstrap ordering</name>
  <files>src/lib/nightsky/deck.ts</files>
  <behavior>
    - goTo(target) clamps to [0, panels.length-1]; on change it sets each panel's data-state (active/inactive), inert, and aria-hidden; moves focus to the active .panel (tabindex=-1) with preventScroll; updates #deck-live textContent to "Panel {N} of {M} — {title}"; updates the index count; dispatches nightsky:panel-change; and (unless skipHistory) pushes history state with the panel hash.
    - resolveIndexFromHash('#contact') → 6; resolveIndexFromHash('#nope') → 0; resolveIndexFromHash('#<script>') → 0 (bounds-checked, never throws).
    - initDeck resolves the initial index from location.hash and applies panel state SYNCHRONOUSLY before adding .deck-active — a cold-load to #patents paints #patents first, never panel 0.
    - popstate/hashchange resolve location.hash → index → goTo(index, { skipHistory: true }).
  </behavior>
  <read_first>
    - src/lib/fig01/index.ts (the `required()` throw-on-missing idiom + init-returns-teardown pattern to mirror exactly)
    - src/data/panels.ts (the manifest this module drives from)
    - .planning/phases/04-deck-mechanics/04-RESEARCH.md sections "Routing Architecture", "Pattern: Progressive-enhancement bootstrap ordering", "Accessibility: inert, Focus, and aria-live", and "Security note" bounds-checking
    - .planning/phases/04-deck-mechanics/04-UI-SPEC.md "Focus management" + aria-live copy "Panel {N} of {M} — {title}"
  </read_first>
  <action>
    Create `src/lib/nightsky/deck.ts`. Import `panels`, `PanelSpec`, `PANEL_COUNT` from `../../data/panels`. Do NOT import anything from `src/lib/fig01/*`.

    Module-scope tunable constants as NAMED EXPORTS (04-CONTEXT locks these as named, tunable exports): `export const WHEEL_THRESHOLD = 50` (normalized px to fire one change), `export const IDLE_RESET_MS = 150` (accumulator idle reset), `export const TRANSITION_LOCK_MS = 400` (input-ignored lock window; keep ≈ the 420ms CSS transition). Also define the touch constants (`SWIPE_DISTANCE = 40`, `SWIPE_VELOCITY = 0.3`, `SWIPE_HORIZONTAL_TOLERANCE = 60`) — task 2 uses them, but declare them here as named exports too so the whole tuning surface lives at the top of the file. Also `export const PANEL_CHANGE_EVENT = 'nightsky:panel-change'`.

    Mirror fig01's `required<T>(value, name)` throw-on-missing helper (no non-null assertions).

    Module state: `currentIndex`, the `panelEls` NodeList/array, the `liveEl`, the `indexCountEl`, and `locked` (transition lock) + `htmlEl = document.documentElement`.

    `resolveIndexFromHash(hash: string): number` — `const key = decodeURIComponent(hash.replace(/^#/, ''))`; return `panels.findIndex(p => p.hash === key)` mapped so `-1 → 0`. Never index panels[] with the raw hash; never interpolate the hash into a selector or innerHTML (V5 input-validation / T-04-01 mitigation).

    `applyPanelStates(index, { animate }): void` — for each panel el i: set `data-state = i === index ? 'active' : 'inactive'`; set `el.inert = i !== index`; toggle `aria-hidden` accordingly (`'true'` on inactive, removed on active). When `animate === false` (cold-load / reduced-motion), ensure no transition plays for the initial paint (states are set before `.deck-active` is added, so the first paint is already correct).

    `announce(index)` — `liveEl.textContent = 'Panel ' + (index + 1) + ' of ' + PANEL_COUNT + ' — ' + panels[index].title` (textContent ONLY — never innerHTML; T-04-02). Update `indexCountEl.textContent` to the zero-padded `NN / NN` string.

    `goTo(target: number, opts?: { skipHistory?: boolean }): void` — clamp target to `[0, PANEL_COUNT - 1]`; if unchanged and not a forced initial apply, return; set `currentIndex`; `applyPanelStates(currentIndex, { animate: true })`; move focus: `panelEls[currentIndex].focus({ preventScroll: true })`; `announce(currentIndex)`; dispatch `document.dispatchEvent(new CustomEvent(PANEL_CHANGE_EVENT, { detail: { index: currentIndex, id: panels[currentIndex].id, total: PANEL_COUNT } }))`; unless `opts?.skipHistory`, `history.pushState(null, '', '#' + panels[currentIndex].hash)`. Export a module-scope accessor so task 2's input handlers can read `currentIndex` and call `goTo`.

    `wireHistory()` — add `popstate` and `hashchange` listeners on `window`; both resolve `location.hash` → index and call `goTo(index, { skipHistory: true })`. Return a teardown removing both.

    `initDeck(root: HTMLElement): () => void` — the bootstrap, in EXACT order (04-RESEARCH "Progressive-enhancement bootstrap ordering"): (1) `required()` all DOM nodes — `.panel` elements (must equal PANEL_COUNT), `#deck-live`, `#deck-index-count`, jump-list anchors, hint + view links; (2) `const initialIndex = resolveIndexFromHash(location.hash)`; (3) `applyPanelStates(initialIndex, { animate: false })` + set `currentIndex = initialIndex` + `announce(initialIndex)` synchronously; (4) wire history + (task 2 will wire wheel/touch/keyboard/jump/hint/view-classic here); (5) as the LAST step, read the persisted view preference — if `deck-view-preference === 'classic'` add `html.classic-active` and do NOT add `.deck-active` (start in classic mode, deck-view link visible); otherwise `document.documentElement.classList.add('deck-active')`; (6) return a teardown that calls every wire*'s teardown. Nothing before step 5 may throw for any reason other than "JS truly cannot run" — a bad hash must degrade to index 0, never throw (else a working-JS user wrongly falls back to v1).

    Read localStorage defensively: `deck-view-preference` is only honored when strictly `=== 'classic'` (any other/absent value = default deck view); wrap reads in try/catch (private-mode / disabled storage must not throw and break init). (T-04-03.)
  </action>
  <verify>
    <automated>npx astro check && grep -q 'export function initDeck' src/lib/nightsky/deck.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function initDeck" src/lib/nightsky/deck.ts`
    - `test "$(grep -oE 'export const (WHEEL_THRESHOLD|IDLE_RESET_MS|TRANSITION_LOCK_MS)' src/lib/nightsky/deck.ts | wc -l | tr -d ' ')" = "3"` (the three named tunable constants exported)
    - `grep -q "nightsky:panel-change" src/lib/nightsky/deck.ts` and `grep -q "detail:" src/lib/nightsky/deck.ts` (change-event with detail dispatched)
    - `grep -q "history.pushState" src/lib/nightsky/deck.ts` and `test "$(grep -oE "addEventListener\('(popstate|hashchange)'" src/lib/nightsky/deck.ts | wc -l | tr -d ' ')" -ge "2"` (routing dual-listen; DECK-05)
    - `grep -q "findIndex" src/lib/nightsky/deck.ts` and `grep -q "decodeURIComponent" src/lib/nightsky/deck.ts` (bounds-checked hash resolution; T-04-01)
    - `grep -q ".inert" src/lib/nightsky/deck.ts` and `grep -q "aria-hidden" src/lib/nightsky/deck.ts` and `grep -q "textContent" src/lib/nightsky/deck.ts` (DECK-03; aria-live via textContent — T-04-02)
    - `test "$(grep -oE 'innerHTML' src/lib/nightsky/deck.ts | wc -l | tr -d ' ')" = "0"` (no innerHTML from derived strings)
    - `grep -q "preventScroll" src/lib/nightsky/deck.ts` (focus management)
    - `grep -q "classList.add('deck-active')" src/lib/nightsky/deck.ts` — and manual read confirms this line executes AFTER the initial applyPanelStates + all required() lookups (cold-load ordering; DECK-04 flash prevention, DECK-07)
    - no import from `src/lib/fig01/` (`test "$(grep -oE "from '.*fig01" src/lib/nightsky/deck.ts | wc -l | tr -d ' ')" = "0"`)
    - `npx astro check` reports 0 errors
  </acceptance_criteria>
  <done>deck.ts exports initDeck + the three named constants; goTo drives data-state/inert/aria-hidden/focus/aria-live/pushState/CustomEvent; hash resolution is bounds-checked; .deck-active is the last init step after a synchronous initial-state apply; no fig01 import; astro check green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: deck.ts input surface — wheel, touch, keyboard, jump list, hint + view-classic persistence; boot from PanelDeck</name>
  <files>src/lib/nightsky/deck.ts, src/components/PanelDeck.astro</files>
  <behavior>
    - Wheel: fine trackpad deltas accumulate across events; crossing WHEEL_THRESHOLD fires exactly one goTo, then locked for TRANSITION_LOCK_MS (momentum tail discarded); accumulator resets after IDLE_RESET_MS idle; deltaMode 1/2 normalized to px before accumulation. One notch = one panel; one trackpad swipe = one panel.
    - Touch: vertical swipe past SWIPE_DISTANCE with velocity ≥ SWIPE_VELOCITY fires one goTo (swipe up = next); horizontal-dominant gestures are ignored and not preventDefaulted (iOS edge-back intact); touchmove preventDefaults only once vertical intent is confirmed.
    - Keyboard: ArrowDown/PageDown → next, ArrowUp/PageUp → prev, Home → 0, End → last; ignored when focus is in input/textarea/select/contenteditable or a modifier is held; Space is never hijacked.
    - Jump list: clicking a [data-panel-index] anchor preventDefaults, goTo(that index) (pushes history).
    - First-visit hint: dismisses (fade) on the first successful navigation by any input, and sets deck-hint-seen so it never re-nags.
    - view-classic link: removes .deck-active, adds .classic-active, persists deck-view-preference=classic; deck-view link reverses it.
  </behavior>
  <read_first>
    - src/lib/nightsky/deck.ts (task 1 output — goTo, currentIndex accessor, the named constants, the teardown wiring point in initDeck)
    - .planning/phases/04-deck-mechanics/04-RESEARCH.md sections "Wheel Normalization" (deltaMode + accumulator + lock), "Touch Swipe" (passive strategy), and "Fig. 01 Keyboard Ground Truth" (collision-free verdict)
    - src/lib/fig01/interactions.ts lines ~238-278 (confirm wireKeyboard binds only focus/blur/click — no keydown — so the deck's keydown is collision-free; document this in a comment)
    - src/components/PanelDeck.astro + src/components/Figure01.astro (the bare attribute-less <script> bundling seam to copy)
  </read_first>
  <action>
    Extend `src/lib/nightsky/deck.ts` with the input handlers and wire them inside `initDeck` (step 4), each returning a teardown folded into initDeck's returned teardown.

    `wireWheel(root)`: register `wheel` on the deck root with `{ passive: false }` ONLY (never on window — do not degrade `/work/*` scroll). `normalizeDelta(e)` branches on `e.deltaMode` (1 → ×16px line height, 2 → ×innerHeight, default → raw px). On each event: `e.preventDefault()`; if `locked` return (momentum-tail suppression — check lock BEFORE accumulating); reset accumulator if `performance.now() - lastEventTime > IDLE_RESET_MS`; accumulate; when `|accumulator| >= WHEEL_THRESHOLD`, compute direction, reset accumulator, set `locked = true`, `goTo(currentIndex + direction)`, and `setTimeout(() => locked = false, TRANSITION_LOCK_MS)`. (DECK-01.)

    `wireTouch(root)`: `touchstart`/`touchend` `{ passive: true }`, `touchmove` `{ passive: false }`. Track start Y/X/time. In touchmove, `preventDefault()` only when `dy > dx` (vertical intent). In touchend: if `locked` return; ignore horizontal-dominant gestures (`|dx| > SWIPE_HORIZONTAL_TOLERANCE && |dx| > |dy|`); require `|dy| >= SWIPE_DISTANCE` and velocity `>= SWIPE_VELOCITY`; direction `dy < 0 ? +1 : -1`; lock + goTo + unlock like wheel. Also set `overscroll-behavior-y` reliance is CSS-side (deck.css); the touchmove preventDefault is the backstop. (DECK-02.)

    `wireKeyboard(root)`: one `keydown` on `document` (or the deck root). Guard: ignore if `e.metaKey||e.ctrlKey||e.altKey`, or if `e.target` is INPUT/TEXTAREA/SELECT or `isContentEditable`. Map ArrowDown/PageDown → `goTo(currentIndex+1)`, ArrowUp/PageUp → `goTo(currentIndex-1)`, Home → `goTo(0)`, End → `goTo(PANEL_COUNT-1)`; `preventDefault()` for handled keys; never handle Space. Add a comment citing the fig01 ground truth (interactions.ts binds only focus/blur/click — no keydown — so this is collision-free even when focus is inside Fig. 01). (DECK-03.)

    `wireJumpList(root)`: for each `[data-panel-index]` anchor, on `click` → `e.preventDefault()`; parse the index from `dataset.panelIndex`; `goTo(index)` (default history push). This is the primary jump path; the `hashchange` listener from task 1 is the safety net. (DECK-04, DECK-05.)

    `wireHint(root)`: read `deck-hint-seen` (try/catch); if already seen, hide the hint immediately. Otherwise, on the FIRST `goTo`-driven navigation (any input), fade the hint out and `localStorage.setItem('deck-hint-seen', '1')`. Implement as a one-shot: e.g., a module flag `firstNavDone` checked/set inside goTo, or a subscription the hint wires. (DECK-06.)

    `wireViewToggle(root)`: on `.deck-view-classic` click → `document.documentElement.classList.remove('deck-active')`, `add('classic-active')`, persist `deck-view-preference='classic'` (try/catch). On `.deck-view-deck` click → reverse: remove `classic-active`, add `deck-active`, persist `'deck'` (or remove the key). Both re-apply the current panel state as needed so the visible panel is coherent after the mode flip. (DECK-07.)

    Reduced-motion (DECK-08): the instant-transition behavior is delivered by deck.css's `prefers-reduced-motion` branch (transition:none) — deck.ts just toggles data-state; confirm no JS-driven animation duration is imposed that would override the CSS branch. Optionally read `matchMedia('(prefers-reduced-motion: reduce)')` to set the transition lock window to ~0 so input isn't needlessly blocked when transitions are instant (mirror fig01's `matchMedia` idiom). Keep the CSS as the source of truth for motion.

    `PanelDeck.astro`: add a single trailing bare `<script>` (no attributes — the Astro TS/import bundling seam, identical to Figure01.astro). It imports `initDeck` from `../lib/nightsky/deck.ts`, queries the `.deck` root element, and calls `initDeck(root)` inside a try/catch — a thrown error must be swallowed so `.deck-active` is never added and the page stays the v1 scrolling layout (DECK-07). Do not use a hydration directive; do not add any attribute to the script tag (any attribute opts out of bundling).
  </action>
  <verify>
    <automated>npm run build && npx astro check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "deltaMode" src/lib/nightsky/deck.ts` and `grep -q "WHEEL_THRESHOLD" src/lib/nightsky/deck.ts` and `grep -q "{ passive: false }" src/lib/nightsky/deck.ts` (wheel accumulator + non-passive; DECK-01)
    - `test "$(grep -oE "addEventListener\('(touchstart|touchmove|touchend)'" src/lib/nightsky/deck.ts | wc -l | tr -d ' ')" -ge "3"` and `grep -q "SWIPE_DISTANCE" src/lib/nightsky/deck.ts` (touch swipe; DECK-02)
    - `grep -q "keydown" src/lib/nightsky/deck.ts` and `grep -q "isContentEditable" src/lib/nightsky/deck.ts` and `grep -q "PageDown\|PageUp" src/lib/nightsky/deck.ts` (guarded keyboard; DECK-03)
    - `grep -q "data-panel-index\|panelIndex" src/lib/nightsky/deck.ts` (jump-list interception; DECK-04)
    - `grep -q "deck-hint-seen" src/lib/nightsky/deck.ts` (hint persistence; DECK-06)
    - `grep -q "deck-view-preference" src/lib/nightsky/deck.ts` and `grep -q "classic-active" src/lib/nightsky/deck.ts` (view-classic toggle + persistence; DECK-07)
    - `grep -q "matchMedia" src/lib/nightsky/deck.ts` OR manual read confirms reduced-motion handled by deck.css (DECK-08)
    - `test "$(grep -oE 'window.addEventListener\(.wheel' src/lib/nightsky/deck.ts | wc -l | tr -d ' ')" = "0"` (wheel not bound to window — perf discipline for /work/*)
    - PanelDeck.astro: `grep -q "initDeck" src/components/PanelDeck.astro` and the boot `<script>` is attribute-less (manual read) and wrapped in try/catch
    - `npm run build` green and `npx astro check` 0 errors
  </acceptance_criteria>
  <done>deck.ts wires wheel/touch/keyboard/jump/hint/view-toggle to goTo with correct passivity + guards; wheel is bound to the deck root (never window); PanelDeck boots initDeck via a bare try/catch'd script; build + astro check green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL hash → deck client state | `location.hash` (attacker-influenceable via crafted link) is resolved into a panel index that drives rendering |
| localStorage → deck client state | `deck-view-preference` / `deck-hint-seen` are read at init and drive mode/affordance state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | `resolveIndexFromHash` | low | mitigate | Resolve via `panels.findIndex(p => p.hash === decodeURIComponent(hash.slice(1)))`; `-1` (crafted/nonexistent hash) falls back to index 0; the raw hash is never used as an array index, a selector, `eval`, or `innerHTML` |
| T-04-02 | Tampering/Info-disclosure | `announce()` aria-live text | low | mitigate | Announcement text is built only from the typed `panels[].title` manifest and written via `textContent` (never `innerHTML`); no runtime input reaches the live region |
| T-04-03 | Tampering | localStorage reads at init | low | mitigate | `deck-view-preference` honored only when strictly `=== 'classic'`; all reads wrapped in try/catch so disabled/private-mode storage cannot throw and force the no-JS fallback for a working-JS user |
| T-04-SC | Tampering | package installs | low | accept | Zero new dependencies — no supply-chain surface |
</threat_model>

<verification>
- `npm run build` + `npx astro check` green.
- deck.ts: initDeck + the three named constants exported; wheel/touch/keyboard/jump/hint/view-toggle present with correct passivity/guards; hash resolution bounds-checked; aria-live via textContent; `.deck-active` added last; no fig01 import; wheel never on window.
- PanelDeck.astro boots initDeck via a bare, try/catch'd `<script>`.
- Full one-gesture-one-panel feel + cold-load + back/forward + reduced-motion + hint + view-classic are exercised by the human-verify checkpoint in plan 04-03.
</verification>

<success_criteria>
- All four input paths (wheel/touch/keyboard/jump) resolve to exactly one panel per gesture and respect the transition lock.
- Hash routing works: pushState per navigation, popstate + hashchange drive the deck, cold-load resolves to the correct panel with no flash.
- Non-active panels are inert + aria-hidden; focus moves to the active panel; every change announces via aria-live and dispatches nightsky:panel-change.
- First-visit hint and view-classic preference persist; reduced-motion transitions are instant.
- `.deck-active` is added only after successful init; init failure leaves the v1 layout.
</success_criteria>

## Artifacts This Phase Produces (Plan 04-02)

**New files:**
- `src/lib/nightsky/deck.ts` — exports: `initDeck(root: HTMLElement): () => void`, `WHEEL_THRESHOLD`, `IDLE_RESET_MS`, `TRANSITION_LOCK_MS`, `SWIPE_DISTANCE`, `SWIPE_VELOCITY`, `SWIPE_HORIZONTAL_TOLERANCE`, `PANEL_CHANGE_EVENT`. Internal: `resolveIndexFromHash`, `applyPanelStates`, `announce`, `goTo`, `wireWheel`, `wireTouch`, `wireKeyboard`, `wireJumpList`, `wireHistory`, `wireHint`, `wireViewToggle`, `required`.

**Event contract dispatched (Phase 5 consumes):** `document` CustomEvent `nightsky:panel-change` with `detail: { index: number, id: string, total: number }` on every activation.

**localStorage keys written:** `deck-hint-seen` (`'1'`), `deck-view-preference` (`'deck' | 'classic'`).

**Modified files:** `src/components/PanelDeck.astro` (adds the bare bundling `<script>` that boots `initDeck`).

<output>
Create `.planning/phases/04-deck-mechanics/04-02-SUMMARY.md` when done.
</output>
