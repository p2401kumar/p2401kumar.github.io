---
phase: 05-night-sky-scene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-night-sky-scene/spike-milkyway.html
  - .planning/phases/05-night-sky-scene/SPIKE.md
  - .planning/phases/05-night-sky-scene/spike-dpr1.png
  - .planning/phases/05-night-sky-scene/spike-dpr2.png
autonomous: false
requirements: [SKY-01]

must_haves:
  truths:
    - "A standalone Milky Way spike prototype renders the band + starfield at DPR 1 and DPR 2, is screenshotted, and is judged against RESEARCH Pattern 1's objective pass/fail bar (no visible rings/bands/seams)"
    - "SPIKE.md records a written verdict: the chosen Milky Way technique, whether the recovery ladder was needed (and which step), and any adjustment to the 4 provisional sky-token values the later engine plans must adopt"
    - "The zero-dependency scatter+gradient path is exhausted through the full recovery ladder BEFORE simplex-noise is even considered"
    - "simplex-noise@4.0.3 is installed exact-pinned ONLY if the ladder fails AND only after a blocking human legitimacy check (its audit verdict is SUS)"
  artifacts:
    - path: ".planning/phases/05-night-sky-scene/spike-milkyway.html"
      provides: "Standalone, self-contained Milky Way + starfield technique prototype at production DPR"
      contains: "canvas"
    - path: ".planning/phases/05-night-sky-scene/SPIKE.md"
      provides: "Spike verdict — final technique, recovery-ladder trace, and final/adjusted --sky-zenith/--sky-horizon/--milkyway/--star values the engine plans read_first"
      contains: "Verdict"
    - path: ".planning/phases/05-night-sky-scene/spike-dpr1.png"
      provides: "DPR-1 screenshot evidence for the banding judgment"
      contains: ""
    - path: ".planning/phases/05-night-sky-scene/spike-dpr2.png"
      provides: "DPR-2 screenshot evidence for the banding judgment"
      contains: ""
  key_links:
    - from: ".planning/phases/05-night-sky-scene/SPIKE.md"
      to: "src/lib/nightsky/starfield.ts (plan 05-03)"
      via: "05-03 reads SPIKE.md's chosen technique + final token values before writing Layer 0 generation"
      pattern: "Verdict"
  prohibitions:
    - "Do NOT run git push anywhere in this phase — the live site stays v1 until Phase 6. All commits remain local on main."
    - "Do NOT deploy, publish, or trigger the GitHub Actions Pages pipeline this phase."
    - "Do NOT write any src/lib/nightsky/* engine code in this plan — the spike is a standalone throwaway prototype in the phase directory only."
    - "Do NOT install simplex-noise (or any package) pre-emptively — installation is gated behind Task 2's blocking-human checkpoint and only if the recovery ladder fails."
---

<objective>
Run the Milky Way visual spike FIRST (the milestone's one LOW-confidence technique, locked as research-gate sequencing in 05-CONTEXT.md). Build a standalone, self-contained HTML prototype that renders ONLY the starfield + Milky Way band technique at production resolution/DPR, screenshot it at DPR 1 and DPR 2, and judge it against RESEARCH Pattern 1's explicit, objective pass/fail bar. If it fails, walk the recovery ladder (per-dot alpha+position jitter → layered multi-radius gradients → coarse second scatter pass) in order. Only if the full ladder fails does the zero-dep path give way to the pre-audited simplex-noise@4.0.3 fallback — and only behind a blocking human legitimacy checkpoint (the package's automated audit verdict was SUS). Record the verdict, chosen technique, and any adjusted sky-token values in SPIKE.md so the engine plans build on a validated foundation instead of an assumption.

Purpose: De-risk the single unproven element of the phase before any engine code exists. This is non-autonomous because it pairs a visual judgment with a conditional supply-chain checkpoint.

Output: `spike-milkyway.html`, `spike-dpr1.png`, `spike-dpr2.png`, and `SPIKE.md` (the verdict later plans read_first) — all in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-night-sky-scene/05-CONTEXT.md
@.planning/phases/05-night-sky-scene/05-RESEARCH.md
@.planning/phases/05-night-sky-scene/05-UI-SPEC.md

@.planning/reference/prototype-shell-and-fig01.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Milky Way spike prototype, judge against the objective bar, walk the recovery ladder, write SPIKE.md</name>
  <files>.planning/phases/05-night-sky-scene/spike-milkyway.html, .planning/phases/05-night-sky-scene/spike-dpr1.png, .planning/phases/05-night-sky-scene/spike-dpr2.png, .planning/phases/05-night-sky-scene/SPIKE.md</files>
  <read_first>
    - .planning/phases/05-night-sky-scene/05-RESEARCH.md "Pattern 1: Milky Way spike — objective pass/fail bar" (the PASS bar, the four FAIL indicators, and the ordered recovery ladder) and "Common Pitfalls #1"
    - .planning/phases/05-night-sky-scene/05-RESEARCH.md "Pattern 2: Star field realism" (power-law magnitude via Math.pow(random, N), HSL narrow hue band, radius+alpha coupling) and "Code Examples" (chunked-generation shape, informational only)
    - .planning/phases/05-night-sky-scene/05-UI-SPEC.md "Milky Way composition" (angle 20–25° from vertical, bright core through the right margin x:0.80–0.98, layered 3–5 passes at alpha 0.04–0.10) + "Star density & magnitude bands" table + the 4 provisional sky-token hex values in the "Sky Palette" table
    - .planning/reference/prototype-shell-and-fig01.html (the existing standalone-prototype convention to match: single self-contained HTML file, inline script/style)
  </read_first>
  <action>
    Author `spike-milkyway.html` as a single self-contained file (inline style + inline module script, no build step, no external assets) matching the existing `.planning/reference/prototype-*.html` convention. It renders ONLY the Layer-0 technique: a full-viewport `<canvas>` painting the vertical sky gradient (zenith→horizon), a power-law starfield (4 magnitude bands per the UI-SPEC density table, radius AND alpha both driven by magnitude — never uniform), and the Milky Way band composited per the UI-SPEC composition rule (20–25° from vertical, bright core through the right margin, tapering to near-zero by x:0.65). Use the 4 provisional token hex values from the UI-SPEC Sky Palette as literals inside this throwaway prototype (this file is exempt from the zero-hex doctrine — it is not shipped source). Support a DPR switch (a query param or a constant) so the same scene renders at devicePixelRatio 1 and 2 with a DPR cap of 2, and add a small on-page control or constant to toggle each recovery-ladder step so screenshots can compare.

    Render and capture: use the gstack /browse skill (or headless Chrome) to open the local file and save two PNG screenshots — `spike-dpr1.png` (DPR 1) and `spike-dpr2.png` (DPR 2) — at a realistic viewport (≈1440×900).

    Judge against RESEARCH Pattern 1's objective bar, examining the SAVED screenshots (not just the live canvas) at ≥100% zoom: continuous cloud-like density variation with NO concentric rings in any radial-gradient glow, NO visible seam/repeat where scatter density transitions, NO discrete luminance stepping, and depth/dust-lane variation surviving a desaturate-to-greyscale check.

    If any FAIL indicator is present, apply the recovery ladder IN ORDER, re-screenshotting after each: (1) add per-dot random jitter to BOTH position and alpha; (2) layer 3–4 gradients of different radii/opacities instead of one; (3) add a coarser second scatter pass of larger, dimmer dots along the banding-prone transition zone. Stop at the first step that clears the bar.

    Write `SPIKE.md` recording: the verdict (PASS zero-dep / FALLBACK REQUIRED), the exact chosen technique and its parameters, which recovery-ladder steps were tried and their effect, the DPR-1/DPR-2 screenshot references, and — critically — the FINAL values for `--sky-zenith`, `--sky-horizon`, `--milkyway`, `--star` (either "unchanged from UI-SPEC recommendation" or the adjusted hex, with a one-line reason). These final values and the chosen technique are the contract plan 05-03 consumes. If the verdict is FALLBACK REQUIRED, state which ladder step failed and why, and STOP before installing anything — proceed to Task 2.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-night-sky-scene/spike-milkyway.html && test "$(grep -o '<canvas' .planning/phases/05-night-sky-scene/spike-milkyway.html | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test -f .planning/phases/05-night-sky-scene/spike-dpr1.png && test -f .planning/phases/05-night-sky-scene/spike-dpr2.png</automated>
    <automated>test -f .planning/phases/05-night-sky-scene/SPIKE.md && test "$(grep -oiE 'verdict' .planning/phases/05-night-sky-scene/SPIKE.md | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test "$(grep -oE '\-\-sky-zenith|\-\-sky-horizon|\-\-milkyway|\-\-star' .planning/phases/05-night-sky-scene/SPIKE.md | wc -l | tr -d ' ')" != "0"</automated>
  </verify>
  <acceptance_criteria>
    - `spike-milkyway.html` is a single self-contained file with a `<canvas>` rendering the gradient + power-law starfield + Milky Way band per the UI-SPEC composition rule.
    - `spike-dpr1.png` and `spike-dpr2.png` exist and were the basis of the banding judgment.
    - The judgment applied all four FAIL indicators from RESEARCH Pattern 1 against the saved screenshots at ≥100% zoom, not a glance at the live canvas.
    - If any FAIL was found, the recovery ladder was walked in order (jitter → layered gradients → coarse scatter pass), stopping at the first passing step.
    - `SPIKE.md` records the verdict, the chosen technique + parameters, the ladder trace, and the FINAL 4 sky-token values (unchanged or adjusted) that 05-03 will adopt.
    - No src/lib/nightsky/* code was written; no package was installed.
  </acceptance_criteria>
  <done>A committed standalone spike prototype + DPR-1/DPR-2 screenshots + a written SPIKE.md verdict exist in the phase dir; the Milky Way technique is validated (or explicitly flagged FALLBACK REQUIRED) against the objective bar, and the final sky-token values are locked for the engine plans.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: CONDITIONAL supply-chain checkpoint — simplex-noise@4.0.3 legitimacy + install approval (only if SPIKE.md verdict is FALLBACK REQUIRED)</name>
  <action>
    CONDITIONAL blocking-human checkpoint. If Task 1's `SPIKE.md` verdict is PASS (zero-dep technique cleared the bar), this checkpoint is N/A — record "not engaged: zero-dep spike passed" in the summary and take NO install action. If and only if the verdict is FALLBACK REQUIRED, engage the human: the simplex-noise legitimacy audit verdict is SUS (RESEARCH "Package Legitimacy Audit"), so a human must confirm the package before any install. This checkpoint is never auto-approvable. On explicit approval, the executor runs the exact-pinned install `npm install simplex-noise@4.0.3` (exact pin, no caret), re-runs the spike using simplex-noise purely for the one-time offscreen density/alpha field (never a per-frame dependency), re-screenshots, and updates SPIKE.md's verdict + technique + package-legitimacy note. If the human rejects, SPIKE.md is updated to record the rejection and the zero-dep result stands as the shipped technique.
  </action>
  <what-built>
    Task 1's spike prototype and SPIKE.md verdict. If the zero-dep scatter+gradient technique could not clear the objective banding bar even after the full recovery ladder, the only remaining option is the pre-audited simplex-noise@4.0.3 fallback — whose automated legitimacy signal came back SUS and therefore requires human confirmation before installation.
  </what-built>
  <how-to-verify>
    ONLY if SPIKE.md verdict is FALLBACK REQUIRED:
    1. Open https://www.npmjs.com/package/simplex-noise and confirm: package exists, version 4.0.3 is published (2024-07-26), the source repo is github.com/jwagner/simplex-noise.js, MIT license, no postinstall script, and a long version history (14 versions since 2012) — i.e. a long-established single-maintainer utility, not a slopsquat (which would show days/weeks of age and near-zero history). RESEARCH's manual registry evidence corroborates this but the SUS verdict is still honored, so a human must eyeball the registry page.
    2. Confirm the intended install is the EXACT pin `simplex-noise@4.0.3` (no caret), and that it will be used only for the one-time offscreen Layer-0 pre-render, never in the per-frame path.
    3. Approve or reject.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize `npm install simplex-noise@4.0.3` (exact pin) + spike re-validation, or "reject" to keep the zero-dep technique, or "n/a" if the zero-dep spike already passed.</resume-signal>
  <done>Either: the zero-dep spike passed and this checkpoint was recorded N/A with no install; or the human explicitly approved and simplex-noise@4.0.3 was installed exact-pinned and the spike re-validated; or the human rejected and the zero-dep result stands. SPIKE.md reflects the outcome. No push/deploy performed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local dev tree | A third-party package (simplex-noise) would enter the dependency tree only via the conditional fallback |
| local spike/preview → verification | The spike renders and is judged entirely locally; nothing is published |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-SC | Tampering (supply chain) | simplex-noise@4.0.3 conditional install | high | mitigate | Automated legitimacy verdict is SUS; install gated behind Task 2's blocking-human checkpoint (never auto-approvable); verified on npmjs.com/package/simplex-noise; exact-pin only; adopted ONLY if the recovery ladder fails; never used in the per-frame path |
| T-05-01 | Elevation / accidental deploy | GitHub Pages pipeline | medium | mitigate | Explicit no-push / no-deploy prohibition; spike is a local phase-dir artifact only |
</threat_model>

<verification>
- `spike-milkyway.html` + `spike-dpr1.png` + `spike-dpr2.png` + `SPIKE.md` exist in the phase dir; SPIKE.md contains a verdict and the final 4 sky-token values.
- The banding judgment was made against the saved DPR-1/DPR-2 screenshots per RESEARCH Pattern 1's objective bar.
- simplex-noise is installed ONLY if the verdict is FALLBACK REQUIRED and ONLY after the blocking-human legitimacy checkpoint; if installed, it is exact-pinned `4.0.3`.
- No `git push`, deploy, or Pages trigger anywhere in this plan.
</verification>

<success_criteria>
- SKY-01's unproven Milky Way technique is validated (or explicitly resolved to the gated fallback) against an objective, screenshot-based pass/fail bar before any engine code exists.
- SPIKE.md is a self-contained contract: chosen technique + parameters + final sky-token values, ready for 05-03 to read_first.
- Any package adoption passed a blocking-human SUS legitimacy check and an exact pin.
- The site is NOT deployed; all commits remain local on main.
</success_criteria>

## Artifacts This Phase Produces (Plan 05-01)

**New files:**
- `.planning/phases/05-night-sky-scene/spike-milkyway.html` — standalone Milky Way + starfield technique prototype (throwaway; exempt from the zero-hex doctrine).
- `.planning/phases/05-night-sky-scene/spike-dpr1.png`, `spike-dpr2.png` — DPR-1/DPR-2 screenshot evidence for the banding judgment.
- `.planning/phases/05-night-sky-scene/SPIKE.md` — the verdict: chosen technique, recovery-ladder trace, and FINAL `--sky-zenith` / `--sky-horizon` / `--milkyway` / `--star` values (the contract 05-03 reads_first).

**Conditionally modified (only if verdict = FALLBACK REQUIRED and human-approved):**
- `package.json` / `package-lock.json` — exact-pinned `simplex-noise@4.0.3` added.

**No src/ source files, components, exports, or CSS classes are created in this plan.**

<output>
Create `.planning/phases/05-night-sky-scene/05-01-SUMMARY.md` when done.
</output>
