---
phase: 05-night-sky-scene
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - src/styles/tokens.css
  - src/lib/nightsky/tokens.ts
  - src/lib/nightsky/starfield.ts
  - src/components/NightSky.astro
  - src/pages/index.astro
autonomous: true
requirements: [SKY-01, SKY-02]

must_haves:
  truths:
    - "Exactly 4 sky tokens (--sky-zenith, --sky-horizon, --milkyway, --star) are appended to tokens.css using SPIKE.md's final values; zero hex literals exist anywhere outside tokens.css"
    - "NightSky.astro renders a fixed full-viewport canvas host behind the deck (z-index below .deck, pointer-events:none, aria-hidden) plus a hand-authored inline SVG camper silhouette with a single copper glow"
    - "index.astro mounts NightSky behind the PanelDeck; the deck and all panel content still render and function unchanged"
    - "starfield.ts generates Layer 0 (power-law starfield + Milky Way band) ONCE per size to a detached offscreen canvas via chunked idle-scheduled generation (requestIdleCallback with a setTimeout fallback), never per frame"
    - "The Milky Way technique and token values match SPIKE.md's verdict exactly (zero-dep or the approved simplex-noise fallback used only for the one-time offscreen field)"
  artifacts:
    - path: "src/lib/nightsky/tokens.ts"
      provides: "Sky-specific token bridge (sky-zenith/horizon/milkyway/star + reused accent) via shared/css-tokens.ts"
      contains: "sky"
    - path: "src/lib/nightsky/starfield.ts"
      provides: "Layer 0 generation — detached offscreen canvas, power-law starfield, Milky Way band (the spiked technique), chunked idle-scheduled"
      contains: "requestIdleCallback"
    - path: "src/components/NightSky.astro"
      provides: "Fixed full-viewport canvas host behind the deck + hand-authored SVG camper silhouette + copper glow"
      contains: "pointer-events"
    - path: "src/styles/tokens.css"
      provides: "The 4 appended sky tokens (SPIKE.md final values)"
      contains: "--milkyway"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/NightSky.astro"
      via: "index mounts NightSky behind PanelDeck as a fixed background layer"
      pattern: "NightSky"
    - from: "src/lib/nightsky/tokens.ts"
      to: "src/lib/shared/css-tokens.ts"
      via: "nightsky token bridge reuses the shared getComputedStyle/parseHex/rgba logic"
      pattern: "css-tokens"
    - from: "src/lib/nightsky/starfield.ts"
      to: "src/styles/tokens.css"
      via: "star/milkyway/sky colors are read through nightsky/tokens.ts, never hardcoded"
      pattern: "--star"
  prohibitions:
    - "Do NOT run git push anywhere in this phase — the live site stays v1 until Phase 6. All commits remain local on main."
    - "Do NOT deploy, publish, or trigger the GitHub Actions Pages pipeline this phase."
    - "Do NOT add any hex literal outside tokens.css — the camper SVG fill and glow read var(--token); canvas draws read via nightsky/tokens.ts + rgba()."
    - "Do NOT start any requestAnimationFrame loop in this plan — Layer 0 is generate-once + blit; the rAF driver arrives in 05-04."
    - "Do NOT place the canvas host above the deck or make it hit-testable — it must sit behind .deck with pointer-events:none."
---

<objective>
Build the scene shell and Layer 0. Append the 4 spike-validated sky tokens to tokens.css (values from SPIKE.md), create nightsky/tokens.ts (the sky-specific bridge over the shared css-tokens module), author NightSky.astro (a fixed full-viewport canvas host mounted behind the deck with pointer-events:none and aria-hidden, plus a hand-authored inline SVG camper silhouette with a single copper glow — Layer 1, static, never in the animation loop), mount it in index.astro behind the PanelDeck, and implement nightsky/starfield.ts: Layer 0 (power-law starfield + the Milky Way band using SPIKE.md's chosen technique) generated ONCE per size to a detached offscreen canvas via chunked, idle-scheduled generation to protect TBT. This plan produces a static, blittable background — no animation loop yet.

Purpose: SKY-01 (persistent starfield + Milky Way) and SKY-02 (camper silhouette + copper glow) — the load-bearing visual substrate the rAF engine (05-04) blits every frame and the constellations (05-05) draw on top of.

Output: appended tokens.css, `src/lib/nightsky/tokens.ts`, `src/lib/nightsky/starfield.ts`, `src/components/NightSky.astro`, and index.astro mount.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-night-sky-scene/05-CONTEXT.md
@.planning/phases/05-night-sky-scene/05-RESEARCH.md
@.planning/phases/05-night-sky-scene/05-UI-SPEC.md
@.planning/phases/05-night-sky-scene/SPIKE.md

@src/lib/shared/css-tokens.ts
@src/lib/fig01/tokens.ts
@src/styles/tokens.css
@src/styles/deck.css
@src/pages/index.astro
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append sky tokens, create nightsky/tokens.ts, author NightSky.astro (canvas host + camper SVG + glow), mount in index.astro</name>
  <files>src/styles/tokens.css, src/lib/nightsky/tokens.ts, src/components/NightSky.astro, src/pages/index.astro</files>
  <read_first>
    - .planning/phases/05-night-sky-scene/SPIKE.md (FINAL values for --sky-zenith/--sky-horizon/--milkyway/--star — use these, not the UI-SPEC provisional recommendation, if the spike adjusted them)
    - .planning/phases/05-night-sky-scene/05-UI-SPEC.md "Sky Palette" table, "Scene composition geometry" table (horizon 15%, camper clamp(160px,16vw,260px), anchor x:0.06–0.22, glow radius 40–56px peak alpha 0.55), and "Pattern 5" silhouette/glow guidance (CSS radial-gradient glow, no blur filter)
    - .planning/phases/05-night-sky-scene/05-RESEARCH.md "Pattern 5: SVG silhouette over canvas" (z-index below .deck, pointer-events:none on BOTH canvas and SVG, glow as CSS radial-gradient, not shadowBlur)
    - src/styles/tokens.css (append target — the ONLY file permitted hex literals) and src/styles/deck.css (html.deck-active .deck is position:fixed; z-index:1 — the scene host must sit below it)
    - src/pages/index.astro (the mount point — NightSky goes behind PanelDeck)
  </read_first>
  <action>
    Append exactly 4 new custom properties to tokens.css under a clearly-commented "Night-sky scene (Phase 5)" block: --sky-zenith, --sky-horizon, --milkyway, --star, using SPIKE.md's final values (fall back to the UI-SPEC Sky Palette recommendation only if SPIKE.md says "unchanged"). Do not add a fifth token — fireflies and the camper glow reuse --accent; constellation stars/links reuse --star.

    Create src/lib/nightsky/tokens.ts mirroring the thinned fig01/tokens.ts pattern: a SkyTokens interface (skyZenith, skyHorizon, milkyway, star, accent) and a getSkyTokens() that reads the root computed styles once (via shared/css-tokens.ts) and caches an identity-stable parsed set. No hex literals; token NAMES are string literals only.

    Author src/components/NightSky.astro: a fixed full-viewport host (position:fixed; inset:0; z-index:0 — below .deck's z-index:1; pointer-events:none; aria-hidden="true") containing (a) the visible `<canvas>` the engine will draw into, and (b) a hand-authored inline SVG camper silhouette positioned bottom-left on the horizon line (anchor x:0.06–0.22, width clamp(160px,16vw,260px), base on the 15%-from-bottom horizon) — a simple 2–3-shape camper-van profile (rounded-rect body + one wheel arc + one window cutout) in the site's flat primitive style, filled with var(--sky-zenith) (near-black). Add the single copper glow as a CSS radial-gradient pseudo-element/element around the window/ember point (radius 40–56px, peak alpha ~0.55) using var(--accent) — never a blur filter, never canvas. Both the canvas and the SVG carry pointer-events:none. Scope all component CSS to read var(--token) only (zero hex).

    Mount NightSky in index.astro so it renders behind the PanelDeck (a fixed background sibling ahead of .deck in DOM order or explicitly z-index:0), leaving the .page/PanelDeck/SiteFooter structure otherwise unchanged. Do not wire any script yet.
  </action>
  <verify>
    <automated>npm run build && npx astro check</automated>
    <automated>test "$(grep -oE '\-\-sky-zenith|\-\-sky-horizon|\-\-milkyway|\-\-star' src/styles/tokens.css | wc -l | tr -d ' ')" = "4"</automated>
    <automated>test "$(grep -roE '#[0-9a-fA-F]{3,8}' src/lib/nightsky src/components/NightSky.astro | wc -l | tr -d ' ')" = "0"</automated>
    <automated>test "$(grep -oE 'pointer-events\s*:\s*none' src/components/NightSky.astro | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test "$(grep -oE 'aria-hidden' src/components/NightSky.astro | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test "$(grep -oE 'NightSky' src/pages/index.astro | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test "$(grep -oE 'requestAnimationFrame' src/lib/nightsky/tokens.ts src/components/NightSky.astro | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - tokens.css gains exactly the 4 sky tokens with SPIKE.md's final values; no fifth token.
    - nightsky/tokens.ts exposes getSkyTokens() reading the shared bridge once; zero hex literals.
    - NightSky.astro is fixed/full-viewport, z-index below .deck, pointer-events:none on canvas AND SVG, aria-hidden, with a hand-authored camper silhouette (var(--sky-zenith)) and a CSS radial-gradient copper glow (var(--accent), no blur filter).
    - index.astro mounts NightSky behind the deck; build + astro check green.
    - Zero hex outside tokens.css across src/lib/nightsky + NightSky.astro; no requestAnimationFrame in this task's files.
  </acceptance_criteria>
  <done>The 4 sky tokens are live, the nightsky token bridge exists, and NightSky.astro renders a fixed, non-interactive, aria-hidden background host with the camper silhouette + copper glow behind the working deck.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement nightsky/starfield.ts — Layer 0 offscreen generation (power-law starfield + Milky Way band, chunked idle-scheduled)</name>
  <files>src/lib/nightsky/starfield.ts</files>
  <read_first>
    - .planning/phases/05-night-sky-scene/SPIKE.md (the chosen Milky Way technique + parameters to implement verbatim — zero-dep scatter+gradient, or the approved simplex-noise field for the one-time offscreen pass only)
    - .planning/phases/05-night-sky-scene/05-RESEARCH.md "Pattern 2" (power-law magnitude, HSL hue, radius+alpha coupling), "Pattern 3: Offscreen pre-render mechanics" (detached document.createElement('canvas'), requestIdleCallback + setTimeout fallback matching deadline.timeRemaining(), debounced resize regen), "Code Examples" (chunked generation shape), and "Pitfall 3" (never let a per-frame path hold the layer0 context — distinct layer0Ctx vs visibleCtx naming)
    - .planning/phases/05-night-sky-scene/05-UI-SPEC.md "Star density & magnitude bands" table (4 bands, counts scale by viewport area, floor ~350 / cap ~1200) and "Milky Way composition" (angle, right-margin core placement)
    - src/lib/nightsky/tokens.ts (getSkyTokens — the color source)
  </read_first>
  <action>
    Implement src/lib/nightsky/starfield.ts as the Layer 0 generator. Export a function that, given a target width/height/DPR (DPR capped at 2), creates a DETACHED plain canvas (document.createElement('canvas'), never appended to the DOM) sized to the backing store, and generates the full Layer-0 bitmap onto its own context (name it distinctly, e.g. layer0Ctx, so no per-frame code ever references it — Pitfall 3): the vertical sky gradient (--sky-zenith → --sky-horizon), the power-law starfield (4 magnitude bands per the UI-SPEC table; use Math.pow(random, N) to bias faint, map magnitude to BOTH radius and alpha; subtle HSL hue variation weighted toward neutral --star white), and the Milky Way band via SPIKE.md's chosen technique. Return the detached canvas (the blittable Layer-0 bitmap) plus whatever star metadata Layer 2 will need (e.g. the positions/base-alpha of the Mid/Bright subset eligible for twinkle).

    Make generation CHUNKED and idle-scheduled to protect TBT: build a queue (stars in batches, the Milky Way band as its own final chunk) and drain it across requestIdleCallback callbacks, checking deadline.timeRemaining() per unit of work; feature-detect requestIdleCallback and fall back to a setTimeout(fn,1) shim that mimics the deadline.timeRemaining() contract (Math.max(0, 50 - elapsed)). Expose the generation as an async/callback-completing operation (onDone) so the caller (scene.ts, 05-04) can blit once ready. Provide the color reads exclusively through nightsky/tokens.ts (zero hex). Do NOT start any rAF loop and do NOT draw to any visible canvas here — this module only produces the offscreen bitmap and metadata.
  </action>
  <verify>
    <automated>npm run build && npx astro check</automated>
    <automated>test "$(grep -oE 'requestIdleCallback' src/lib/nightsky/starfield.ts | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test "$(grep -oE 'setTimeout' src/lib/nightsky/starfield.ts | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test "$(grep -oE 'createElement' src/lib/nightsky/starfield.ts | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test "$(grep -oE 'requestAnimationFrame' src/lib/nightsky/starfield.ts | wc -l | tr -d ' ')" = "0"</automated>
    <automated>test "$(grep -roE '#[0-9a-fA-F]{3,8}' src/lib/nightsky/starfield.ts | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - starfield.ts generates Layer 0 to a DETACHED canvas (createElement, never DOM-attached) with a distinctly-named layer0 context, returning the blittable bitmap + twinkle-subset metadata.
    - Generation is chunked via requestIdleCallback with a setTimeout fallback matching the deadline.timeRemaining() contract; no single synchronous unit blocks TBT.
    - Starfield is power-law across the 4 UI-SPEC bands (radius AND alpha coupled to magnitude); the Milky Way band matches SPIKE.md's chosen technique exactly.
    - Colors read only via nightsky/tokens.ts; zero hex literals; NO requestAnimationFrame in this module.
    - Build + astro check green.
  </acceptance_criteria>
  <done>Layer 0 (starfield + Milky Way) generates once to an offscreen detached canvas via chunked idle-scheduled work, using the spike-validated technique and tokens, ready to be blitted by the 05-04 engine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tokens.css → canvas | The only file with color literals; the canvas reads them via getComputedStyle (no user input) |
| local build → verification | All work is local; nothing is published |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Tampering | zero-hex boundary (canvas colors) | low | mitigate | Sky colors confined to tokens.css; canvas reads via nightsky/tokens.ts + rgba(); zero-hex grep enforced across src/lib/nightsky + NightSky.astro |
| T-05-04 | Denial of Service (TBT/CLS) | Layer 0 generation blocking the main thread | medium | mitigate | Chunked idle-scheduled generation (requestIdleCallback + setTimeout fallback) keeps every unit under the 50ms TBT threshold; detached one-shot pre-render, never per-frame (Phase 4's CLS incident is the cautionary precedent) |
| T-05-SC | Tampering (supply chain) | package installs | low | accept | No new dependencies in this plan (any simplex-noise adoption already gated + installed in 05-01) |
| T-05-DEPLOY | Elevation / accidental deploy | GitHub Pages pipeline | medium | mitigate | Explicit no-push / no-deploy prohibition; local-only |
</threat_model>

<verification>
- Build + astro check green with NightSky mounted; deck + panels still render/function.
- tokens.css has exactly 4 sky tokens (SPIKE.md values); zero hex outside tokens.css across src/lib/nightsky + NightSky.astro.
- NightSky.astro is fixed, behind .deck, pointer-events:none, aria-hidden; camper SVG + CSS glow present (no blur filter).
- starfield.ts generates Layer 0 to a detached canvas, chunked/idle-scheduled, no rAF; technique + tokens match SPIKE.md.
- No `git push`, deploy, or Pages trigger anywhere in this plan.
</verification>

<success_criteria>
- SKY-01: a persistent, blittable Layer-0 starfield + Milky Way (spike-validated technique) is generated once per size off the main thread's critical path.
- SKY-02: a hand-authored camper silhouette with a single copper glow renders as a static layer behind the deck.
- The scene host is non-interactive (pointer-events:none), decorative (aria-hidden), and sits behind the working deck.
- The site is NOT deployed; all commits remain local on main.
</success_criteria>

## Artifacts This Phase Produces (Plan 05-03)

**New files:**
- `src/lib/nightsky/tokens.ts` — sky token bridge. Exports: `SkyTokens` interface + `getSkyTokens()` (cached, via shared/css-tokens.ts).
- `src/lib/nightsky/starfield.ts` — Layer 0 generator. Exports: the offscreen-generation function returning the detached Layer-0 canvas + twinkle-subset metadata; the requestIdle/ setTimeout-fallback scheduler.
- `src/components/NightSky.astro` — fixed full-viewport canvas host (behind .deck, pointer-events:none, aria-hidden) + hand-authored inline SVG camper silhouette + CSS copper glow.

**Modified files:**
- `src/styles/tokens.css` — 4 appended sky tokens (`--sky-zenith`, `--sky-horizon`, `--milkyway`, `--star`) with SPIKE.md final values.
- `src/pages/index.astro` — mounts `<NightSky />` behind the PanelDeck.

**New CSS classes/vars downstream consume:** the 4 sky tokens (→ 05-04 twinkle/fireflies, 05-05 constellations, 05-06 contrast). **New exports:** `getSkyTokens`, the Layer-0 generator + twinkle metadata (→ 05-04 scene.ts).

<output>
Create `.planning/phases/05-night-sky-scene/05-03-SUMMARY.md` when done.
</output>
