---
phase: 05-night-sky-scene
plan: 04
type: execute
wave: 3
depends_on: [05-03]
files_modified:
  - src/lib/nightsky/scene.ts
  - src/components/NightSky.astro
autonomous: true
requirements: [SKY-03, SKY-04]

must_haves:
  truths:
    - "scene.ts drives the whole scene with a SINGLE requestAnimationFrame loop: it blits the Layer-0 offscreen bitmap first, then draws only Layer-2 per-frame work (twinkle subset + fireflies) on top"
    - "The only per-frame canvas work is the twinkle subset (~5–8% of stars, Mid/Bright bands) and ≤15 fireflies (bottom-25% ground band, slow drift, copper pulse) — Layer 0 is never regenerated per frame"
    - "A pause state machine stops the loop when the tab is hidden (visibilitychange), while the fig-01 panel is active (nightsky:panel-change detail.id === 'fig-01'), and under prefers-reduced-motion"
    - "Under prefers-reduced-motion the loop NEVER starts — one static complete frame is rendered (Layer 0 + static stars at base alpha + silhouette; no twinkle, no fireflies)"
    - "scene.ts subscribes to the nightsky:panel-change document event by its literal name and NEVER imports deck.ts or any fig01 module"
  artifacts:
    - path: "src/lib/nightsky/scene.ts"
      provides: "initNightSky(root) — single rAF driver, drawFrame (Layer0 blit + Layer2), pause state machine, renderStaticFrame, resize/regen orchestration"
      contains: "requestAnimationFrame"
  key_links:
    - from: "src/lib/nightsky/scene.ts"
      to: "src/lib/nightsky/starfield.ts"
      via: "scene.ts calls the Layer-0 generator on init/resize and blits its returned bitmap each frame via visibleCtx.drawImage"
      pattern: "drawImage"
    - from: "src/lib/nightsky/scene.ts"
      to: "document nightsky:panel-change event"
      via: "scene.ts adds a document listener keyed on the literal 'nightsky:panel-change' event name to pause when id === 'fig-01' (never imports deck.ts)"
      pattern: "panel-change"
    - from: "src/components/NightSky.astro"
      to: "src/lib/nightsky/scene.ts"
      via: "NightSky.astro's module script calls initNightSky(host) on load"
      pattern: "initNightSky"
  prohibitions:
    - "Do NOT run git push anywhere in this phase — the live site stays v1 until Phase 6. All commits remain local on main."
    - "Do NOT deploy, publish, or trigger the GitHub Actions Pages pipeline this phase."
    - "Do NOT import deck.ts (or any fig01 module) from nightsky/scene.ts — subscribe to the nightsky:panel-change document event only."
    - "Do NOT let any per-frame code reference the Layer-0 offscreen context — only visibleCtx.drawImage(layer0Canvas). Layer 0 must never be regenerated inside the rAF tick."
    - "Do NOT roll per-frame dice for rare events; keep the loop to twinkle + fireflies only (link-firing arrives in 05-05, scheduled, not per-frame)."
---

<objective>
Build the scene engine on top of Layer 0. Implement nightsky/scene.ts as the single-rAF driver mirroring fig01's proven render/lifecycle shape: an initNightSky(root) entry that reads sky tokens once, generates Layer 0 via starfield.ts, and runs ONE requestAnimationFrame loop whose drawFrame blits the Layer-0 bitmap first, then draws only Layer-2 per-frame work — the twinkle subset (~5–8% of Mid/Bright stars, sine alpha wobble) and ≤15 drifting copper-tinted fireflies in the bottom-25% ground band. Add the pause state machine (the "one-active-animation rule"): pause when document.hidden, when the fig-01 panel is active (subscribing to the nightsky:panel-change document event — never importing deck.ts), and under prefers-reduced-motion; add a live matchMedia change listener. Under reduced motion, never start the loop — render exactly one static complete frame (Layer 0 + static stars + silhouette). Add debounced resize regeneration of Layer 0. Wire initNightSky from NightSky.astro's module script.

Purpose: SKY-03 (fireflies + twinkle are the ONLY per-frame canvas work; bounded idle CPU) and SKY-04 (pause on hidden / fig-01-active / reduced-motion; reduced-motion static frame).

Output: `src/lib/nightsky/scene.ts` and the NightSky.astro script wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-night-sky-scene/05-CONTEXT.md
@.planning/phases/05-night-sky-scene/05-RESEARCH.md
@.planning/phases/05-night-sky-scene/05-UI-SPEC.md

@src/lib/nightsky/deck.ts
@src/lib/nightsky/starfield.ts
@src/lib/nightsky/tokens.ts
@src/lib/fig01/render.ts
@src/lib/fig01/interactions.ts
@src/components/NightSky.astro
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement nightsky/scene.ts — single rAF driver, drawFrame (Layer0 blit + Layer2 twinkle + fireflies), resize regen, initNightSky entry</name>
  <files>src/lib/nightsky/scene.ts</files>
  <behavior>
    - initNightSky(root) reads sky tokens once, generates Layer 0 via starfield.ts, resolves the visible canvas + a DPR-capped (max 2) backing store, and returns a teardown.
    - drawFrame blits the Layer-0 bitmap via visibleCtx.drawImage FIRST, then draws Layer-2 only: the twinkle subset (Mid/Bright stars, alpha = baseAlpha + amplitude*sin(t/period + phase), unsynchronized phases) and ≤15 fireflies (bottom-25% band, 4–8px/s wander, alpha pulse 3–5s, --accent tint).
    - The offscreen Layer-0 context is never referenced inside drawFrame — only visibleCtx.drawImage(layer0Canvas). Distinct layer0Ctx vs visibleCtx naming (Pitfall 3).
    - startAnimationLoop is idempotent (guarded by a single rafId); stopAnimationLoop cancels and nulls it — exactly two requestAnimationFrame occurrences total (recursive tick + initial call), matching fig01/render.ts.
    - Debounced resize (200–300ms) regenerates Layer 0 at the new size and re-blits; the loop is not touched per resize tick.
  </behavior>
  <read_first>
    - src/lib/fig01/render.ts (startAnimationLoop / stopAnimationLoop idempotent single-rafId pattern lines ~400–420; drawFrame section structure; layered-gradient glow instead of shadowBlur; DPR cap) — the pattern to mirror, NOT import
    - src/lib/nightsky/starfield.ts (the Layer-0 generator signature + twinkle-subset metadata it returns)
    - src/lib/nightsky/tokens.ts (getSkyTokens)
    - .planning/phases/05-night-sky-scene/05-UI-SPEC.md "Ambient loop (normal motion)" table (twinkle ~40 stars period 2–4s amplitude ±0.15–0.25; fireflies 9, drift 4–8px/s, pulse 3–5s, --accent) and "Spacing" firefly/twinkle rows
    - .planning/phases/05-night-sky-scene/05-RESEARCH.md "Pattern 2" (twinkle sin formula), "Pattern 3" (debounced resize regen), "Pattern 4" (drawFrame layered structure), "Pitfall 3" (layer0Ctx vs visibleCtx)
  </read_first>
  <action>
    Implement src/lib/nightsky/scene.ts mirroring fig01/render.ts's driver shape (mirror, do not import). Module-scope a single rafId. Export initNightSky(root: HTMLElement): () => void that: queries the visible `<canvas>` from the host, gets its 2D context (named visibleCtx), reads sky tokens once via getSkyTokens(), sizes the canvas with DPR capped at 2, kicks off starfield.ts Layer-0 generation (chunked; on completion, store the returned detached layer0Canvas + twinkle metadata and blit once), and creates the Layer-2 runtime state (fireflies with position/velocity/phase; the twinkle subset drawn from the Mid/Bright star metadata). Implement drawFrame(ts): clear the visible canvas, visibleCtx.drawImage(layer0Canvas, ...) to blit Layer 0, then draw the twinkle subset (per-star sin alpha wobble via rgba(star,…)) and the fireflies (drift + soft --accent pulse). NEVER reference the offscreen layer0 context in drawFrame.

    Implement startAnimationLoop (idempotent, guarded by rafId; the recursive tick calls drawFrame then requestAnimationFrame(tick)) and stopAnimationLoop (cancelAnimationFrame + null) — exactly two requestAnimationFrame occurrences total. Implement a renderStaticFrame(): one complete frame (blit Layer 0 + draw the stars at their fixed base alpha + rely on the SVG silhouette layer) with NO loop scheduled — used by the reduced-motion and single-paint paths (the pause machine in Task 2 calls it). Implement a debounced (200–300ms) resize handler that regenerates Layer 0 via starfield.ts at the new DPR-capped size and re-blits. Return a teardown that stops the loop and removes the resize listener. Leave the pause/lifecycle wiring (visibilitychange, matchMedia, panel-change) to Task 2 — but expose the hooks it needs (e.g. an updateRunState-style gate function and the reduced-motion static path).
  </action>
  <verify>
    <automated>npm run build && npx astro check</automated>
    <automated>test "$(grep -oE 'requestAnimationFrame' src/lib/nightsky/scene.ts | wc -l | tr -d ' ')" = "2"</automated>
    <automated>test "$(grep -oE 'drawImage' src/lib/nightsky/scene.ts | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test "$(grep -oE "from ['\"][^'\"]*(deck|fig01)[^'\"]*['\"]" src/lib/nightsky/scene.ts | wc -l | tr -d ' ')" = "0"</automated>
    <automated>test "$(grep -oE 'export function initNightSky' src/lib/nightsky/scene.ts | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test "$(grep -roE '#[0-9a-fA-F]{3,8}' src/lib/nightsky/scene.ts | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - scene.ts exports initNightSky(root) returning a teardown; a single rafId guards an idempotent startAnimationLoop; requestAnimationFrame occurs exactly twice.
    - drawFrame blits Layer 0 via visibleCtx.drawImage then draws ONLY twinkle subset + fireflies; the offscreen layer0 context is never referenced per frame.
    - renderStaticFrame paints one complete frame with no scheduled loop.
    - Debounced resize regenerates + re-blits Layer 0; no deck.ts/fig01 import; zero hex.
    - Build + astro check green.
  </acceptance_criteria>
  <done>scene.ts drives a single rAF loop that blits Layer 0 and draws bounded Layer-2 work, with a static-frame path and debounced resize regen, importing no deck/fig01 module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pause state machine (hidden / fig-01-active / reduced-motion) + reduced-motion static frame + NightSky.astro init wiring</name>
  <files>src/lib/nightsky/scene.ts, src/components/NightSky.astro</files>
  <behavior>
    - The loop runs only when: tab visible AND fig-01 panel NOT active AND NOT prefers-reduced-motion. Any signal flip re-evaluates via an idempotent updateRunState.
    - visibilitychange toggles tabVisible; nightsky:panel-change sets fig01Active = (detail.id === 'fig-01'); matchMedia('(prefers-reduced-motion: reduce)') change re-branches.
    - Under reduced motion, stopAnimationLoop then renderStaticFrame — the loop never starts.
    - The pause listener for fig-01 and the (future 05-05) brighten/dim listener are independent registrations; this task registers only the pause gate.
    - NightSky.astro's script calls initNightSky(host) once on load and holds the teardown.
  </behavior>
  <read_first>
    - src/lib/fig01/interactions.ts (updateRunState lines ~316–323, applyMotionPreference ~334–347, wireLifecycle — the four-signal gate to mirror; nightsky needs visibilitychange + matchMedia + panel-change, NOT IntersectionObserver, per RESEARCH Pattern 6)
    - src/lib/nightsky/deck.ts (READ ONLY — the shipped nightsky:panel-change contract: dispatched on document with detail { index, id, total }; scene.ts subscribes by the literal event name and does NOT import deck.ts, per the orchestrator's "never imports deck.ts" rule)
    - .planning/phases/05-night-sky-scene/05-RESEARCH.md "Pattern 6: Pause-reason state machine" (the exact updateRunState shape + the two-independent-listeners note) and "Anti-Patterns" (reduced-motion is stop, not dampen)
    - .planning/phases/05-night-sky-scene/05-UI-SPEC.md "Pause / resume" + "Reduced motion" tables (which Layer-2 elements are fully OFF under reduced motion)
  </read_first>
  <action>
    In scene.ts, add the pause state machine mirroring fig01/interactions.ts's updateRunState (mirror, don't import): module-scope tabVisible = !document.hidden, fig01Active = false, and the reduced-motion matchMedia. updateRunState computes shouldRun = tabVisible && !fig01Active && !rm.matches and starts or stops the loop accordingly (idempotent). Register three listeners inside initNightSky (and remove them in teardown): document 'visibilitychange' → set tabVisible, updateRunState; document 'nightsky:panel-change' (subscribe by the literal event name — do NOT import deck.ts; type the detail inline as CustomEvent&lt;{ id: string }&gt;) → set fig01Active from (detail.id === 'fig-01'), updateRunState; rm 'change' → stopAnimationLoop, then if rm.matches renderStaticFrame else updateRunState. On init, apply the motion preference first: if rm.matches, renderStaticFrame and never start; else updateRunState. Under reduced motion the twinkle and fireflies are fully OFF (renderStaticFrame draws stars at base alpha only), per the UI-SPEC reduced-motion table — not a dampened loop.

    In NightSky.astro, add the module `<script>` that imports initNightSky, queries the host element, calls initNightSky(host) once on load, and retains the teardown (matching the codebase's single-instance-per-module init pattern; no ClientRouter is in use so a plain load-time init is correct).
  </action>
  <verify>
    <automated>npm run build && npx astro check</automated>
    <automated>test "$(grep -oE 'visibilitychange' src/lib/nightsky/scene.ts | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test "$(grep -oE 'prefers-reduced-motion' src/lib/nightsky/scene.ts | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test "$(grep -oE "nightsky:panel-change" src/lib/nightsky/scene.ts | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test "$(grep -oE "fig-01" src/lib/nightsky/scene.ts | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test "$(grep -oE "from ['\"][^'\"]*(deck|fig01)[^'\"]*['\"]" src/lib/nightsky/scene.ts | wc -l | tr -d ' ')" = "0"</automated>
    <automated>test "$(grep -oE 'requestAnimationFrame' src/lib/nightsky/scene.ts | wc -l | tr -d ' ')" = "2"</automated>
    <automated>test "$(grep -oE 'initNightSky' src/components/NightSky.astro | wc -l | tr -d ' ')" != "0"</automated>
  </verify>
  <acceptance_criteria>
    - updateRunState gates the loop on tabVisible && !fig01Active && !rm.matches (idempotent); visibilitychange, nightsky:panel-change, and matchMedia change listeners drive it and are torn down.
    - scene.ts subscribes to the literal 'nightsky:panel-change' document event (no deck/fig01 import at all) and keys the pause on detail.id === 'fig-01'.
    - Under prefers-reduced-motion the loop never starts and renderStaticFrame paints one frame (twinkle + fireflies fully OFF).
    - requestAnimationFrame still occurs exactly twice; build + astro check green.
    - NightSky.astro calls initNightSky(host) once on load.
  </acceptance_criteria>
  <done>The scene pauses on tab-hidden, fig-01-active, and reduced-motion (static frame, no loop), driven only by the nightsky:panel-change event and native signals; NightSky.astro boots the engine on load.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deck → scene (event) | scene.ts consumes the document-level nightsky:panel-change CustomEvent (internally dispatched, never URL-derived); no import coupling |
| local build → verification | All work is local; nothing is published |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Denial of Service (battery/CPU) | per-frame Layer-2 work | high | mitigate | Only twinkle subset (~5–8% of stars) + ≤15 fireflies per frame; Layer 0 blitted, never regenerated; single rAF; pause on hidden/fig-01-active/reduced-motion (one-active-animation rule) — bounds the per-frame cost |
| T-05-06 | Tampering | module-boundary coupling | low | mitigate | scene.ts subscribes to the nightsky:panel-change document event by literal name and imports NO deck/fig01 module; grep gate enforces zero deck/fig01 imports |
| T-05-SC | Tampering (supply chain) | package installs | low | accept | No new dependencies in this plan |
| T-05-DEPLOY | Elevation / accidental deploy | GitHub Pages pipeline | medium | mitigate | Explicit no-push / no-deploy prohibition; local-only |
</threat_model>

<verification>
- Build + astro check green; single rAF (exactly 2 requestAnimationFrame occurrences) in scene.ts.
- drawFrame blits Layer 0 + draws only twinkle + fireflies; offscreen context never referenced per frame.
- Pause machine covers hidden / fig-01-active / reduced-motion; reduced-motion never starts the loop (static frame).
- scene.ts subscribes to the literal 'nightsky:panel-change' event; no deck/fig01 import; zero hex.
- NightSky.astro boots initNightSky once on load.
- No `git push`, deploy, or Pages trigger anywhere in this plan.
</verification>

<success_criteria>
- SKY-03: the only per-frame canvas work is the twinkle subset + ≤15 fireflies; Layer 0 is never regenerated per frame (frame cost bounded — formal audit finalized in 05-06).
- SKY-04: the scene pauses on tab-hidden, while fig-01 is active, and under reduced motion (one static frame, no loop).
- The engine is decoupled from deck.ts/fig01 (event-only), matching the codebase module-boundary discipline.
- The site is NOT deployed; all commits remain local on main.
</success_criteria>

## Artifacts This Phase Produces (Plan 05-04)

**New files:**
- `src/lib/nightsky/scene.ts` — the scene engine. Exports: `initNightSky(root)` (returns teardown); internal single-rAF driver (startAnimationLoop/stopAnimationLoop), drawFrame (Layer-0 blit + Layer-2 twinkle + fireflies), renderStaticFrame, updateRunState pause gate, debounced resize regen.

**Modified files:**
- `src/components/NightSky.astro` — module `<script>` calling `initNightSky(host)` once on load.

**New exports/hooks downstream consume:** `initNightSky` and scene.ts's drawFrame constellation-render section (→ 05-05 wires constellation rendering into drawFrame and adds its own independent panel-change brighten/dim listener).

<output>
Create `.planning/phases/05-night-sky-scene/05-04-SUMMARY.md` when done.
</output>
