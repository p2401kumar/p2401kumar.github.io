---
phase: 05.1-celestial-extras
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/nightsky/starfield.ts
  - src/lib/nightsky/meteors.ts
  - src/lib/nightsky/scene.ts
  - scripts/verify-contrast.mjs
  - .planning/phases/05.1-celestial-extras/luminance-evidence.md
  - .planning/phases/05.1-celestial-extras/frame-cost-delta.md
  - .planning/phases/05.1-celestial-extras/lighthouse-scores.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [SKY-06, SKY-07]

must_haves:
  truths:
    - "SKY-07: A thin WANING crescent moon renders in Layer 0 (static, pre-rendered, tokens-only, procedural — --star lit crescent + --sky-horizon earthshine, NO image assets), placed in the LEFT margin clear of the Milky Way band and the content column, dim by construction — its peak luminance measured STRICTLY below the Milky Way core's peak at both 1280x800 and 1440x900"
    - "SKY-06: Occasional meteor streaks cross the UPPER-sky margins — at most ONE active at a time, cadence 20000+(rand**0.6)*25000 ms, life 0.6-1.0s, with a --star gradient trail fading tip-to-tail (head brightest) — confined entirely to the margins outside the content column, and ABSENT under prefers-reduced-motion and while the scene is paused (hidden tab / Fig. 01 active)"
    - "The reduced-motion static frame includes the moon (baked into Layer 0's blit) but NEVER a meteor; a mid-flight meteor is discarded immediately on any pause / reduced-motion state change"
    - "Idle per-frame cost is unchanged: with no active meteor the tick adds ONLY a single null-check (zero extra draws); an active meteor adds a bounded delta (one gradient trail stroke + one head arc — the same cheap primitive class as the constellation beam)"
    - "The full Phase 5 gate battery re-passes with the two additions: astro check 0 errors, build green, zero hex outside tokens.css, single-rAF invariant (scene.ts=2, starfield.ts=0, meteors.ts=0, fig01/render.ts=2), zero deck/fig01 imports in the nightsky scene modules, verify-contrast.mjs both viewports non-regressed, and local Lighthouse >=90 on both presets"
  artifacts:
    - path: "src/lib/nightsky/starfield.ts"
      provides: "Procedural waning crescent moon drawn into generateLayer0 (after the Milky Way composite) — earthshine disk + clipped even-odd crescent, tokens-only"
      contains: "MOON_RADIUS_COEFF"
    - path: "src/lib/nightsky/meteors.ts"
      provides: "Layer-2 meteor subsystem — setTimeout spawn scheduler + advance(ts)/draw(ctx,w,h)/setSuppressed/teardown hook riding scene.ts's existing single rAF tick (mirrors constellations.ts's firing-beam shape)"
      contains: "setTimeout"
    - path: "src/lib/nightsky/scene.ts"
      provides: "Meteor integration: init after constellations, advance+draw in drawFrame Layer 2d, setSuppressed wired through updateRunState (mirroring setFiringSuppressed), teardown"
      contains: "meteors"
    - path: "scripts/verify-contrast.mjs"
      provides: "New --moon CDP mode: reads the live canvas, asserts moon-bbox peak luminance < Milky-Way-core-box peak luminance at both check widths (FLAG 1), plus a --selftest moon fixture; existing --selftest + --cdp modes unchanged"
      contains: "--moon"
    - path: ".planning/phases/05.1-celestial-extras/luminance-evidence.md"
      provides: "Recorded moon-peak vs MW-core-peak luminance at 1280 + 1440 with PASS verdict (FLAG 1)"
      contains: "moonPeak"
    - path: ".planning/phases/05.1-celestial-extras/frame-cost-delta.md"
      provides: "Recorded idle (zero extra draws / one null-check) + bounded in-flight (+2 cheap draws) per-frame delta vs the 05-06 frame-cost baseline (FLAG 2)"
      contains: "in-flight"
    - path: ".planning/phases/05.1-celestial-extras/lighthouse-scores.md"
      provides: "Full gate-battery PASS/FAIL table + local Lighthouse both presets >=90 with moon + meteor active; screenshot manifest incl. moon region (FLAG 3)"
      contains: "performance"
  key_links:
    - from: "src/lib/nightsky/meteors.ts"
      to: "src/lib/nightsky/scene.ts"
      via: "initMeteors + advance/draw/setSuppressed/teardown hook called from scene.ts's existing drawFrame + updateRunState + teardown — NO new requestAnimationFrame; scheduling via setTimeout"
      pattern: "meteors"
    - from: "src/lib/nightsky/meteors.ts"
      to: "src/lib/nightsky/tokens.ts"
      via: "getSkyTokens().star for the meteor head + trail (no --milkyway, no --accent, zero hex)"
      pattern: "star"
    - from: "src/lib/nightsky/starfield.ts"
      to: "src/lib/nightsky/tokens.ts"
      via: "getSkyTokens().star (lit crescent) + .skyHorizon (earthshine) — no new token, zero hex"
      pattern: "skyHorizon"
    - from: "scripts/verify-contrast.mjs"
      to: ".planning/phases/05.1-celestial-extras/luminance-evidence.md"
      via: "the --moon mode's moonPeak<mwPeak assertion is recorded as SKY-07 dimness evidence"
      pattern: "moon"
  prohibitions:
    - "Do NOT run git push, deploy, publish, or trigger the GitHub Actions Pages pipeline anywhere in this phase — Phase 6 owns the deploy; the live site stays v1. All commits remain local on main."
    - "Do NOT add any new dependency — meteors.ts and the verify-contrast.mjs extension are zero-dep (plain TS / plain node + the browser)."
    - "Do NOT add any hex literal outside tokens.css — moon + meteor read --star / --sky-horizon via getSkyTokens(); no new sky token is expected or permitted."
    - "Do NOT add any new requestAnimationFrame anywhere. The meteor rides scene.ts's EXISTING single rAF tick; its cadence uses setTimeout, mirroring constellations.ts's firing scheduler. meteors.ts must contain the token requestAnimationFrame ZERO times (including in comments); scene.ts must stay exactly 2; starfield.ts must stay 0."
    - "The meteor must NEVER appear in a reduced-motion, paused, or static frame — renderStaticFrame must NOT draw a meteor, and setSuppressed(true) must discard any in-flight meteor. The moon MUST always be present (baked in Layer 0), including in the reduced-motion static frame."
    - "Do NOT modify the SKY-05 governor functions (columnAttenuation / starAlphaCapAt) — the moon sits entirely outside the governed column at both check widths and has its own fixed alpha ceiling."
    - "Do NOT import deck.ts or any fig01 module from the nightsky scene modules (scene/starfield/constellations/meteors/tokens) — mirror math, subscribe by literal event name only (module-boundary rule)."
    - "Do NOT stage, commit, or revert .planning/config.json — its unstaged _auto_chain_active flip is out of scope; leave it exactly as-is."
---

<objective>
Add the night sky's two final celestial touches inside the shipped Phase 5 engine's floors: a thin, dim, procedural waning crescent MOON baked into Layer 0 (SKY-07), and occasional METEOR (shooting-star) streaks that ride scene.ts's existing single rAF tick, scheduled by setTimeout, confined to the upper-sky margins, and absent under reduced-motion / pause (SKY-06). Then re-run the complete Phase 5 gate battery so the additions are proven, not assumed.

Both elements are pure canvas draws inside the existing aria-hidden scene — zero new DOM, zero new tokens, zero new dependencies, zero new rAF. The moon extends starfield.ts's generateLayer0; the meteor lives in a new src/lib/nightsky/meteors.ts (recommended over inflating scene.ts because the scheduler + spawn geometry + advance + gradient-trail draw comfortably exceed ~80 lines, and mirroring constellations.ts's self-contained handle shape keeps scene.ts's per-frame tick a thin dispatcher). Three UI-checker FLAGs are resolved concretely and numerically: FLAG 1 (a canvas-readback assertion that moon-peak luminance < Milky-Way-core-peak luminance at both check widths), FLAG 2 (recorded idle vs in-flight frame-cost delta), and FLAG 3 (moon-region screenshot evidence at 1280 + 1440, both DPRs).

Purpose: SKY-06 (shooting stars) + SKY-07 (crescent moon), the only genuinely-new items in the "shooting stars and twinkle and moon and milky way" request (twinkle + Milky Way already shipped in Phase 5). This inserted mini-phase lands before Phase 6 so the final integration/deploy verification covers everything.

Output: moon in starfield.ts, src/lib/nightsky/meteors.ts + scene.ts integration, scripts/verify-contrast.mjs --moon mode, luminance-evidence.md, frame-cost-delta.md, lighthouse-scores.md, committed moon-region + reduced-motion (+ optional mid-flight meteor) screenshots, and the requirement/roadmap/state updates.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-celestial-extras/05.1-CONTEXT.md
@.planning/phases/05.1-celestial-extras/05.1-UI-SPEC.md
@.planning/phases/05-night-sky-scene/05-UI-SPEC.md
@.planning/phases/05-night-sky-scene/05-06-SUMMARY.md

@src/lib/nightsky/starfield.ts
@src/lib/nightsky/scene.ts
@src/lib/nightsky/constellations.ts
@src/lib/nightsky/tokens.ts
@src/lib/shared/css-tokens.ts
@src/styles/tokens.css
@scripts/verify-contrast.mjs
@.planning/phases/05-night-sky-scene/frame-cost-audit.md
@.planning/phases/05-night-sky-scene/contrast-evidence.md
@.planning/phases/05-night-sky-scene/lighthouse-scores.md
</context>

<hard_invariants>
These hold across ALL tasks and are re-checked in Task 3's battery:

1. **No new rAF anywhere.** The meteor rides scene.ts's existing single tick; cadence is setTimeout (mirroring constellations.ts's firing scheduler). Counts stay: scene.ts=2, starfield.ts=0, meteors.ts=0, fig01/render.ts=2. meteors.ts must contain the token requestAnimationFrame zero times (comments included) or the single-rAF grep fails.
2. **Zero idle cost.** When no meteor is active, drawFrame does exactly one reference null-check for the meteor — no draws, no allocation, no advance math.
3. **Moon baked in Layer 0.** Drawn once inside generateLayer0 (after the Milky Way composite), so it is present in every blit — including the reduced-motion static frame — at zero per-frame cost.
4. **Meteor NEVER in reduced-motion / paused / static frames.** renderStaticFrame does not draw it; setSuppressed(true) discards any in-flight meteor and clears the pending spawn timer; reduced-motion means absent entirely (WCAG C39), not dampened.
5. **Zero hex outside tokens.css.** Moon + meteor read --star / --sky-horizon via getSkyTokens(). No new token.
6. **No new dependencies.**
7. **NEVER push to origin.** No deploy, no Pages trigger. Local-only on main.
8. **Leave .planning/config.json's unstaged _auto_chain_active flip alone** — do not stage, commit, or revert it.
9. **Do not touch the SKY-05 governor** (columnAttenuation / starAlphaCapAt) — the moon is placed outside the governed column by construction.
</hard_invariants>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Crescent moon in starfield.ts Layer 0 + verify-contrast.mjs --moon luminance assertion + recorded evidence (SKY-07, FLAG 1)</name>
  <files>src/lib/nightsky/starfield.ts, scripts/verify-contrast.mjs, .planning/phases/05.1-celestial-extras/luminance-evidence.md</files>
  <read_first>
    - .planning/phases/05.1-celestial-extras/05.1-UI-SPEC.md "MOON (SKY-07)" — the full geometry contract: the columnLeft mirror formula, the constant table (MOON_RADIUS_COEFF 0.018, MOON_RADIUS_FLOOR 12, MOON_RADIUS_CAP 22, MOON_X_MARGIN_FRACTION 0.30, MOON_Y_FRACTION 0.68, MASK_RADIUS_RATIO 1.05, MASK_OFFSET_RATIO 1.85, LIT_ALPHA 0.45, EARTHSHINE_ALPHA 0.08), the alpha ceiling 0.55, the two-point luminance-readback assertion, and the draw-order note (after the Milky Way composite)
    - src/lib/nightsky/starfield.ts — generateLayer0 (where the moon draws), the existing setTransform(dpr) CSS-pixel convention, queueMilkyWay + drainQueue (the moon draws AFTER the MW work units), getSkyTokens()/rgba usage, and the existing contentColumnEdges-style pad/half formula to MIRROR (do NOT reuse the governor's fixed COLUMN_HALF_WIDTH_PX 464 — the moon uses the deck.css 880/pad formula, same as scene.ts contentColumnEdges)
    - src/lib/nightsky/tokens.ts + src/lib/shared/css-tokens.ts — getSkyTokens().star / .skyHorizon + rgba(triple, alpha)
    - scripts/verify-contrast.mjs — the existing launchChrome / connectCdp / Cdp class / findChrome CDP plumbing (reuse it for the new --moon mode), the --selftest structure (add a moon fixture additively; keep every existing fixture and the deck.css sync check green), relativeLuminance, and argValue
  </read_first>
  <action>
    Add the moon to starfield.ts, drawn ONCE inside generateLayer0 as the final Layer-0 work unit (pushed onto the queue AFTER queueMilkyWay, so it composites after the Milky Way — the moon and MW occupy disjoint margins so order is cosmetic, but fix it here for clarity).

    First add the moon constants near the other Layer-0 constants, named exactly per the UI-SPEC table: MOON_RADIUS_COEFF 0.018, MOON_RADIUS_FLOOR 12, MOON_RADIUS_CAP 22, MOON_X_MARGIN_FRACTION 0.30, MOON_Y_FRACTION 0.68, MASK_RADIUS_RATIO 1.05, MASK_OFFSET_RATIO 1.85, LIT_ALPHA 0.45, EARTHSHINE_ALPHA 0.08. Add a small local moonColumnLeft(cssWidth) helper mirroring scene.ts's contentColumnEdges left edge: pad = clamp(18, cssWidth*0.04, 32); half = min(880, cssWidth - 2*pad)/2; return cssWidth/2 - half. Mirror, do not import (module boundary).

    Compute geometry: R = clamp(MOON_RADIUS_FLOOR, MOON_RADIUS_COEFF * min(cssWidth, cssHeight), MOON_RADIUS_CAP); moonX = MOON_X_MARGIN_FRACTION * moonColumnLeft(cssWidth); moonY = MOON_Y_FRACTION * cssHeight.

    Draw, background-safe, source-over ONLY (do NOT use destination-out on the shared layer0Ctx and do NOT punch a transparent hole in the opaque sky wash):
    1. Earthshine disk: one arc at (moonX, moonY) radius R, filled rgba(getSkyTokens().skyHorizon, EARTHSHINE_ALPHA) — the faint dark-limb glow.
    2. Lit waning crescent via a clip + even-odd fill (this yields the left-facing sliver without a scratch canvas and without erasing the sky): save the context; set a clip to the lit circle (arc at (moonX, moonY) radius R); then begin a single path containing (a) the lit-disk bounding rectangle at (moonX-R, moonY-R, 2R, 2R) and (b) the mask circle arc at (moonX + MASK_OFFSET_RATIO*R, moonY) radius MASK_RADIUS_RATIO*R; fill that path with the even-odd winding rule and fillStyle rgba(getSkyTokens().star, LIT_ALPHA); restore. Inside the lit-disk clip the even-odd rule fills rectangle-minus-mask = the thin left-edge crescent (~0.20*R at its widest); the mask offset toward +x carves the sliver on the LEFT, the conventional waning-crescent read. No rotation.

    LIT_ALPHA must never exceed 0.55. Do not apply columnAttenuation/starAlphaCapAt to the moon — it sits outside the governed column by construction (margin-clearance proven in the UI-SPEC at both check widths); its fixed LIT_ALPHA is its entire brightness governance.

    Then extend scripts/verify-contrast.mjs with a new --moon mode (planner's decision: extend this script rather than a sibling, to reuse its zero-dep launchChrome/connectCdp/Cdp plumbing — additive only; the existing --selftest fixtures, deck.css sync check, and --cdp mode stay untouched and green). The --moon mode: launch headless Chrome at a given --width/--height, navigate the LOCAL preview, wait for the scene to paint (reuse the existing readiness poll), then inject a sampler that (using window.innerWidth/innerHeight so the real inner viewport is used) computes moonX/moonY/R with the SAME formulas as starfield.ts, reads the canvas via getImageData over the moon bounding box [moonX-1.1*R, moonY-1.1*R, 2.2*R, 2.2*R] and over the Milky-Way-core box [x:0.80-0.98, y:0.30-0.60 of the viewport], computes the MAX relativeLuminance in each box (moonPeak, mwPeak), and returns both. The node side asserts moonPeak < mwPeak and prints the two values; exit non-zero if the assertion fails at the given size. Also add ONE moon fixture to --selftest (a synthetic ImageData pair proving the moonPeak<mwPeak comparison logic) so the formula is self-tested with zero deps.

    Run --moon at 1440x900 and 1280x800 against the local preview (npm run build && npm run preview) and record luminance-evidence.md: moonPeak and mwPeak at each viewport, the ratio/margin, the sample-box definitions, the reproduce commands, and a PASS verdict (moonPeak strictly < mwPeak at both). If it does NOT pass, record the shortfall and lower LIT_ALPHA within its 0.40-0.50 range (never above the 0.55 ceiling) — do not round up.
  </action>
  <verify>
    <automated>npm run build && npx astro check</automated>
    <automated>node scripts/verify-contrast.mjs --selftest</automated>
    <automated>test "$(grep -oE 'MOON_RADIUS_COEFF|MOON_X_MARGIN_FRACTION|MASK_OFFSET_RATIO|MASK_RADIUS_RATIO|LIT_ALPHA|EARTHSHINE_ALPHA|MOON_Y_FRACTION' src/lib/nightsky/starfield.ts | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test "$(grep -oE 'evenodd' src/lib/nightsky/starfield.ts | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test "$(grep -oE 'requestAnimationFrame' src/lib/nightsky/starfield.ts | wc -l | tr -d ' ')" = "0"</automated>
    <automated>test "$(grep -roE '#[0-9a-fA-F]{3,8}\b' src/lib/nightsky/starfield.ts | wc -l | tr -d ' ')" = "0"</automated>
    <automated>test "$(grep -oE '\-\-moon' scripts/verify-contrast.mjs | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test -f .planning/phases/05.1-celestial-extras/luminance-evidence.md && test "$(grep -oiE 'moonPeak|PASS' .planning/phases/05.1-celestial-extras/luminance-evidence.md | wc -l | tr -d ' ')" != "0"</automated>
  </verify>
  <acceptance_criteria>
    - starfield.ts's generateLayer0 draws a thin waning crescent moon after the Milky Way composite using the UI-SPEC constants (MOON_RADIUS_COEFF etc.), earthshine via --sky-horizon and lit crescent via --star, built with a clip + even-odd fill (source-over, background preserved — no destination-out on layer0Ctx, no transparent hole in the sky).
    - The moon reuses the deck.css 880/pad column formula (mirrored, not imported) for its left-margin placement and never touches the governor functions.
    - scripts/verify-contrast.mjs gains a --moon mode + a --selftest moon fixture; `node scripts/verify-contrast.mjs --selftest` still exits 0 with all existing fixtures + the deck.css sync check green; zero new deps.
    - luminance-evidence.md records moonPeak < mwPeak at BOTH 1280x800 and 1440x900 with the sample-box definitions + reproduce commands (or a recorded shortfall + the LIT_ALPHA adjustment within its 0.40-0.50 range).
    - starfield.ts stays 0 rAF and zero hex; build + astro check green.
  </acceptance_criteria>
  <done>The crescent moon is baked into Layer 0, dim by construction, and its dimness is PROVEN numerically: a canvas-readback assertion (moon-bbox peak luminance < Milky-Way-core-box peak luminance) passes at both check widths with committed evidence. FLAG 1 resolved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Meteor subsystem (src/lib/nightsky/meteors.ts) + scene.ts integration + frame-cost delta evidence (SKY-06, FLAG 2)</name>
  <files>src/lib/nightsky/meteors.ts, src/lib/nightsky/scene.ts, .planning/phases/05.1-celestial-extras/frame-cost-delta.md</files>
  <read_first>
    - .planning/phases/05.1-celestial-extras/05.1-UI-SPEC.md "METEOR (SKY-06)" — the full parameter table (cadence 20000+(rand**0.6)*25000 ms, life uniform(0.6,1.0)s, pathLength uniform(0.12,0.20)*cssHeight, angle uniform(20,35) deg, head radius 2.0, head peak alpha 0.95, trail length uniform(0.06,0.10)*cssHeight, trail stroke 1.4, gradient stops 0%@0.95 / 45%@0.45 / 80%@0.12 / 100%@0), the spawn-zone/exclusion construction (margins-only, both-margins-<60px skip+reschedule, side pick, spawnX/spawnY, down-and-away travel), and the scheduling contract (spawn-to-spawn cadence, gate-check against shouldRun, discard-in-flight on pause/reduced-motion, defensive skip if a fire lands while active)
    - src/lib/nightsky/constellations.ts — MIRROR this module's shape: firingSuppressed/fireTimer/activeBeam/tornDown state, clearFireTimer, scheduleNextFiring (guards: tornDown||suppressed||rm.matches||timer set), fire (spawn then reschedule), advance(ts) with the MAX_ADVANCE_DELTA_MS clamp, drawBeam (createLinearGradient tail->head + 'lighter' composite + head arc + restore to 'source-over'), setFiringSuppressed (on suppress: clearTimer + activeBeam=null), pointAtDistance, and the ConstellationHandle interface. The meteor's own contentColumnEdges mirror also lives here (marginRemapX/the pad/half formula)
    - src/lib/nightsky/scene.ts — drawFrame (add Layer 2d after constellations), initNightSky (init meteors after constellations, passing tokens + a getViewport reading layer0's css size), updateRunState (add meteorsHandle?.setSuppressed(!shouldRun) beside constellationsHandle?.setFiringSuppressed), renderStaticFrame (leave it meteor-free), teardown (meteorsHandle?.teardown()); the existing rm/tabVisible/fig01Active pause machine
    - src/lib/fig01/render.ts drawBeams (lines ~204-236) — the head/gradient-tail 'lighter' idiom to mirror (mirror, never import)
    - .planning/phases/05-night-sky-scene/frame-cost-audit.md — the exact format + method (per-frame element/draw-call table, cheap-primitive class, optional live soak) to match for the delta evidence
  </read_first>
  <action>
    Create src/lib/nightsky/meteors.ts as a self-contained Layer-2 subsystem mirroring constellations.ts's handle shape. Export initMeteors(options) returning a MeteorHandle { advance(ts), draw(ctx, cssWidth, cssHeight), setSuppressed(suppressed), teardown() }. Options mirror constellations: { tokens: SkyTokens, getViewport: () => {width,height}|null }. It reads ONLY getSkyTokens().star (head + trail), zero hex, no --milkyway, no --accent. It owns NO requestAnimationFrame — the token requestAnimationFrame must not appear anywhere in the file, comments included; scheduling is setTimeout only.

    State (module-local to initMeteors, mirroring the beam): suppressed (starts true), spawnTimer, activeMeteor|null, tornDown, lastAdvanceTs, a matchMedia('(prefers-reduced-motion: reduce)') reference for defensive guards, and a MAX_ADVANCE_DELTA_MS clamp (50, same as the beam).

    Scheduling (mirror scheduleNextFiring/fire): scheduleNextSpawn() no-ops if tornDown || suppressed || rm.matches || spawnTimer set, else sets spawnTimer = setTimeout(spawn, 20000 + (Math.random()**0.6)*25000). spawn() clears spawnTimer, returns if tornDown||suppressed||rm.matches, then (only if activeMeteor is null) attempts one spawn, then ALWAYS calls scheduleNextSpawn() (spawn-to-spawn cadence, never completion-to-spawn). If a fire lands while a meteor is still active, skip the spawn and just reschedule (defensive — cadence >=20s >> life <=1s).

    Spawn geometry (per the UI-SPEC METEOR construction, using getViewport()): compute the content column via the mirrored pad/half formula (pad = clamp(18, w*0.04, 32); half = min(880, w-2*pad)/2; columnLeft = w/2-half; columnRight = w/2+half); marginLeftPx = columnLeft, marginRightPx = w - columnRight. If both margins < 60px, skip this spawn (narrow-viewport fallback — no meteor) and reschedule. Else pick a side (50/50 when both qualify, else the qualifying side). Left side: spawnX = uniform(16, marginLeftPx-12); travel down-and-left (dx = -pathLength*cos(angle), dy = +pathLength*sin(angle)). Right side: spawnX = w - uniform(16, marginRightPx-12); travel down-and-right (dx = +..., dy = +...). spawnY = uniform(0.03, 0.20)*cssHeight. Draw parameters per spawn: pathLength = uniform(0.12,0.20)*cssHeight; life = uniform(0.6,1.0) s; velocity = pathLength/life (derived); angle = uniform(20,35) deg in radians; trailLength = uniform(0.06,0.10)*cssHeight. Store spawn origin, unit direction (dx/dy normalized), pathLength, trailLength, velocity, and d=0.

    advance(ts): dt clamped to MAX_ADVANCE_DELTA_MS; if activeMeteor, d += velocity * dt/1000; discard (activeMeteor=null) once d >= pathLength + trailLength (mirroring the beam's `d >= len + BEAM_TAIL_LEN` clean-exit, so the tail visibly clears the path end rather than popping). Recompute nothing per-frame beyond d.

    draw(ctx, cssWidth, cssHeight): if activeMeteor is null, return immediately (this is the zero-idle-cost path — one null check). Otherwise headD = min(d, pathLength); tailD = max(0, d - trailLength); head = origin + unit*headD; tail = origin + unit*tailD. Mirror fig01/constellations beam treatment: set 'lighter'; createLinearGradient(headX, headY, tailX, tailY) with stops rgba(star, 0.95)@0, rgba(star, 0.45)@0.45, rgba(star, 0.12)@0.80, rgba(star, 0)@1.0; strokeStyle = gradient; lineWidth 1.4; stroke the head->tail segment; then fill the head dot arc (radius 2.0) rgba(star, 0.95); restore composite to 'source-over'.

    setSuppressed(suppressed): mirror setFiringSuppressed — no-op if unchanged; on suppress clear the spawn timer AND set activeMeteor=null (so a paused / reduced-motion / static frame can never contain a meteor); on un-suppress call scheduleNextSpawn(). teardown(): tornDown=true, clear timer, activeMeteor=null, remove the rm listener if any.

    Integrate into scene.ts (mirroring the constellations wiring exactly): import initMeteors + MeteorHandle type from './meteors'; add a module-scope meteorsHandle. In initNightSky, after constellationsHandle is created, meteorsHandle = initMeteors({ tokens: skyTokens, getViewport: () => (layer0 ? { width: layer0.cssWidth, height: layer0.cssHeight } : null) }). In drawFrame, add "Layer 2d: meteor" AFTER the constellations block: if meteorsHandle, meteorsHandle.advance(ts) then meteorsHandle.draw(visibleCtx, w, h). In updateRunState, add meteorsHandle?.setSuppressed(!shouldRun) beside the existing constellationsHandle?.setFiringSuppressed(!shouldRun) — this is the single wiring point that suppresses spawns + discards the in-flight meteor whenever the scene is paused (hidden tab / fig-01 active) or under reduced motion, and resumes it otherwise. Leave renderStaticFrame UNCHANGED (it must never draw a meteor). In teardown, add meteorsHandle?.teardown(); meteorsHandle = null. scene.ts's requestAnimationFrame count must remain exactly 2 (do not add the token in any new comment).

    Record frame-cost-delta.md matching frame-cost-audit.md's format: an idle row (activeMeteor null -> one reference null-check, +0 draws, +0 allocation vs the 05-06 ~70-typical/<=120-worst baseline) and an in-flight row (+1 gradient stroke +1 head arc = +2 draws of the same cheap primitive class as the constellation beam, +O(1) advance math, at most one meteor at a time). State the bounded worst case (<= ~122 draws/frame while a single meteor flies) and note the meteor's on-screen duration (life 0.6-1.0s once every 20-45s -> active on a small fraction of frames). If tooling permits, add a short idle CDP soak confirming ~5.6% CPU unchanged when no meteor is active; otherwise reason from code as the audit did.
  </action>
  <verify>
    <automated>npm run build && npx astro check</automated>
    <automated>test -f src/lib/nightsky/meteors.ts</automated>
    <automated>test "$(grep -oE 'requestAnimationFrame' src/lib/nightsky/meteors.ts | wc -l | tr -d ' ')" = "0"</automated>
    <automated>test "$(grep -oE 'setTimeout' src/lib/nightsky/meteors.ts | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test "$(grep -oE '25000' src/lib/nightsky/meteors.ts | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test "$(grep -roE '#[0-9a-fA-F]{3,8}\b' src/lib/nightsky/meteors.ts | wc -l | tr -d ' ')" = "0"</automated>
    <automated>test "$(grep -oE "from './meteors'" src/lib/nightsky/scene.ts | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test "$(grep -oE 'setSuppressed' src/lib/nightsky/scene.ts | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test "$(grep -oE 'requestAnimationFrame' src/lib/nightsky/scene.ts | wc -l | tr -d ' ')" = "2"</automated>
    <automated>test -f .planning/phases/05.1-celestial-extras/frame-cost-delta.md && test "$(grep -oiE 'in-flight|idle' .planning/phases/05.1-celestial-extras/frame-cost-delta.md | wc -l | tr -d ' ')" != "0"</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/nightsky/meteors.ts exists: a self-contained setTimeout-scheduled subsystem (zero requestAnimationFrame occurrences, zero hex) mirroring constellations.ts's handle shape, implementing the locked cadence/life/pathLength/angle/trail params and the margins-only spawn construction.
    - scene.ts imports + initializes meteors after constellations, advances+draws in drawFrame Layer 2d, wires setSuppressed through updateRunState, tears down in teardown, and keeps renderStaticFrame meteor-free; scene.ts rAF stays exactly 2.
    - Suppression discards the in-flight meteor + clears the spawn timer (paused / reduced-motion / static frames are structurally meteor-free); spawns never occur under reduced motion or while paused.
    - frame-cost-delta.md records the idle delta (+0 draws, one null-check) and the bounded in-flight delta (+2 cheap draws), vs the 05-06 baseline; build + astro check green.
  </acceptance_criteria>
  <done>Occasional single meteor streaks cross the upper-sky margins with a fading --star trail, scheduled by setTimeout on scene.ts's existing tick, absent under pause/reduced-motion (in-flight discarded on state change), at zero idle per-frame cost and a bounded in-flight delta recorded as evidence. FLAG 2 resolved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Full Phase-5 gate battery re-run + moon-region + reduced-motion screenshots (FLAG 3) + requirement/roadmap/state updates</name>
  <files>.planning/phases/05.1-celestial-extras/lighthouse-scores.md, .planning/phases/05.1-celestial-extras/luminance-evidence.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/05-night-sky-scene/05-06-PLAN.md Task 2 (the proven gate-battery command shapes) and .planning/phases/05-night-sky-scene/lighthouse-scores.md (the PASS/FAIL table format + the 100/100/100/100 both-preset baseline the additions must not regress below 90; the documented #deck-* id-selector hex false positives)
    - .planning/phases/05-night-sky-scene/contrast-evidence.md (the SKY-05 baseline worst-case 8.24:1 @1440 / 8.64:1 @1280 the moon must not regress below) and the reproduce commands for verify-contrast.mjs --cdp / --moon
    - .planning/phases/05-night-sky-scene/05-06-SUMMARY.md (how the scene sign-off screenshots were captured: headless Chrome CDP, DPR1/DPR2, --force-prefers-reduced-motion for the static frame)
    - .planning/REQUIREMENTS.md (SKY-06/SKY-07 checklist rows + the phase status table) and .planning/ROADMAP.md "### Phase 5.1" (the Plans placeholder + Progress table)
  </read_first>
  <action>
    Run the complete acceptance gate under Git Bash (occurrence counting via grep -oE | wc -l | tr -d ' ' — never grep -c; boundary-aware hex via the \b-terminated pattern so #deck-* id selectors are excluded). Record a PASS/FAIL summary + Lighthouse table in lighthouse-scores.md (matching 05-06's format), noting deck + scene + moon + meteor active, local preview only, no deploy.

    1. Build + typecheck: `npm run build` (succeeds) and `npx astro check` (0 errors) with moon + meteor active.
    2. Zero-hex boundary-aware: grep the \b-terminated 3-8-digit hex pattern across src/lib/nightsky, src/lib/shared, src/components/NightSky.astro, src/data/constellations.ts, src/styles/deck.css — expect 0 (the \b excludes the documented #deck-* substring false positives; meteors.ts + the moon are covered by the src/lib/nightsky scope).
    3. Single-rAF invariant: requestAnimationFrame count scene.ts=2, starfield.ts=0, meteors.ts=0, whole src/lib/nightsky total=2, and fig01/render.ts non-regression=2 (src/lib/fig01 total=2).
    4. Zero deck/fig01 imports in the nightsky scene modules (scene/constellations/starfield/meteors/tokens): grep import lines referencing a deck/fig01/interactions/render/model module path — expect 0 (event-name subscription + mirrored math only).
    5. Contrast non-regression (SKY-05): start the preview (`npm run preview` background), run `node scripts/verify-contrast.mjs --selftest` (exit 0), then `--cdp` at 1440x900 and 1280x800 — confirm every panel's failing-regions array is empty and the global worst is >=4.5:1 (matching or exceeding the 8.24 / 8.64 baseline; the moon adds a left-margin crescent that must not push any over-sky text region below floor). Record the worst ratios.
    6. Moon dimness (SKY-07, FLAG 1): run `node scripts/verify-contrast.mjs --moon` at 1440x900 and 1280x800 — both must exit 0 (moonPeak < mwPeak). Confirm luminance-evidence.md reflects the final numbers.
    7. Local Lighthouse (moon + meteor active): run mobile (default) + desktop (--preset=desktop) headless against the local preview; extract the four category scores per run; every category >=0.90. Write them to lighthouse-scores.md (run | performance | accessibility | best-practices | seo | TBT | LCP | CLS). Record any shortfall + the responsible audit; do not silently pass. Stop the preview when done.
    8. Screenshot evidence (FLAG 3): capture moon-region stills (framing MUST include the bottom-left moon at moonX ~= 84px @1440 / ~60px @1280, moonY = 0.68*cssHeight, so moon-vs-Samsung-cluster/moon-vs-camper separation is visually judgeable) at BOTH viewports and BOTH DPRs via headless Chrome CDP (Page.captureScreenshot; DPR2 via deviceScaleFactor=2 or --force-device-scale-factor=2): 05.1-01-moon-1440-dpr1.png, 05.1-01-moon-1440-dpr2.png, 05.1-01-moon-1280-dpr1.png, 05.1-01-moon-1280-dpr2.png. Also capture 05.1-01-reduced-motion.png (--force-prefers-reduced-motion — moon PRESENT, NO meteor/twinkle/fireflies). If the moon reads cramped against the microsoft/samsung cluster, the sanctioned knob is a same-file MOON_X_MARGIN_FRACTION nudge to 0.15-0.20 in starfield.ts (a constant tweak, not a spec revision) — apply it, re-run Task 1's --moon check, and note it. Best-effort mid-flight meteor still (05.1-01-meteor-midflight.png): capture via a CDP screenshot poll while a meteor is on screen; if a live catch is impractical, force it by TEMPORARILY lowering the cadence constants in a LOCAL uncommitted edit to meteors.ts, capture, then `git checkout src/lib/nightsky/meteors.ts` to restore the locked 20-45s cadence — the committed code keeps the locked cadence. If still not capturable, note it and rely on the frame-cost delta + code-level criteria per 05.1-CONTEXT.md. Add a screenshot manifest to lighthouse-scores.md.
    9. Requirement/roadmap/state updates: in .planning/REQUIREMENTS.md flip SKY-06 and SKY-07 to complete (checklist `- [ ]` -> `- [x]` and the status-table rows Pending -> Complete). In .planning/ROADMAP.md finalize the Phase 5.1 Plans list ([x] 05.1-01-PLAN.md) and, if the phase is done, mark Phase 5.1 complete in the Progress table. Update .planning/STATE.md (current position, completed plans, and an Accumulated-Context decision line noting the moon + meteor additions).

    All git commits stay LOCAL on main. Do NOT push, deploy, or trigger Pages. Do NOT stage or revert .planning/config.json.
  </action>
  <verify>
    <automated>npm run build && npx astro check</automated>
    <automated>test "$(grep -roE '#[0-9a-fA-F]{3,8}\b' src/lib/nightsky src/lib/shared src/components/NightSky.astro src/data/constellations.ts src/styles/deck.css | wc -l | tr -d ' ')" = "0"</automated>
    <automated>test "$(grep -oE 'requestAnimationFrame' src/lib/nightsky/scene.ts | wc -l | tr -d ' ')" = "2" && test "$(grep -oE 'requestAnimationFrame' src/lib/nightsky/starfield.ts | wc -l | tr -d ' ')" = "0" && test "$(grep -oE 'requestAnimationFrame' src/lib/nightsky/meteors.ts | wc -l | tr -d ' ')" = "0"</automated>
    <automated>test "$(grep -roE 'requestAnimationFrame' src/lib/nightsky | wc -l | tr -d ' ')" = "2" && test "$(grep -roE 'requestAnimationFrame' src/lib/fig01 | wc -l | tr -d ' ')" = "2"</automated>
    <automated>test "$(grep -rlE "^import[^;]*from '([^']*/)?(deck|fig01|interactions|render|model)('|/)" src/lib/nightsky/scene.ts src/lib/nightsky/constellations.ts src/lib/nightsky/starfield.ts src/lib/nightsky/meteors.ts src/lib/nightsky/tokens.ts | wc -l | tr -d ' ')" = "0"</automated>
    <automated>node scripts/verify-contrast.mjs --selftest</automated>
    <automated>test -f .planning/phases/05.1-celestial-extras/lighthouse-scores.md && test "$(grep -oiE 'performance' .planning/phases/05.1-celestial-extras/lighthouse-scores.md | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test -f .planning/phases/05.1-celestial-extras/05.1-01-moon-1440-dpr1.png && test -f .planning/phases/05.1-celestial-extras/05.1-01-moon-1280-dpr1.png && test -f .planning/phases/05.1-celestial-extras/05.1-01-reduced-motion.png</automated>
    <automated>test "$(grep -oE '\- \[x\] \*\*SKY-06|\- \[x\] \*\*SKY-07' .planning/REQUIREMENTS.md | wc -l | tr -d ' ')" = "2"</automated>
  </verify>
  <acceptance_criteria>
    - Build + astro check green; zero-hex boundary-aware = 0; single-rAF invariant holds (scene.ts=2, starfield.ts=0, meteors.ts=0, nightsky total=2, fig01=2); zero deck/fig01 imports in the scene modules.
    - verify-contrast.mjs --selftest exits 0; --cdp at 1440 + 1280 shows zero failing regions >= baseline; --moon at 1440 + 1280 exits 0 (moonPeak < mwPeak).
    - lighthouse-scores.md records both presets >=90 x4 (or a recorded shortfall) with moon + meteor active + a screenshot manifest.
    - Moon-region screenshots (1440 + 1280, DPR1 + DPR2) and the reduced-motion still (moon present, no meteor) are committed; best-effort mid-flight meteor still or a documented code-level criterion.
    - REQUIREMENTS SKY-06/SKY-07 marked complete; ROADMAP Phase 5.1 Plans finalized; STATE updated. No push/deploy; config.json's _auto_chain_active flip untouched.
  </acceptance_criteria>
  <done>The full Phase-5 gate battery re-passes with the moon + meteor: build/check, boundary-aware zero-hex, single-rAF (incl. meteors.ts=0), no cross-boundary imports, contrast non-regressed at both viewports, moon-dimness assertion green, Lighthouse >=90 x4 both presets, moon-region + reduced-motion screenshots committed, and SKY-06/SKY-07 marked complete — nothing pushed or deployed. FLAG 3 resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local build/preview → verification | The whole battery + Lighthouse + contrast/luminance sampling + screenshots run against the local dist/preview; nothing is published |
| tokens.css → canvas draws | Moon + meteor colors confined to tokens.css (--star / --sky-horizon) read via getSkyTokens(); no user input, no new token |
| pause state machine → decorative motion | scene.ts's shouldRun gate owns whether the meteor may spawn / persist; the meteor never runs its own clock |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05.1-01 | Elevation / accidental deploy | GitHub Pages pipeline | medium | mitigate | Explicit no-push / no-deploy prohibition; all verification runs against the LOCAL preview only; Phase 6 owns the deploy; commits stay local on main |
| T-05.1-02 | Information Disclosure (moon washes out the Milky Way / light-pollution premise broken) | starfield.ts moon | high | mitigate | LIT_ALPHA capped at 0.55, placed outside the governed column; PROVEN by the --moon canvas-readback assertion (moonPeak < mwPeak) at both check widths + committed evidence (FLAG 1) |
| T-05.1-03 | Denial of Service (motion under reduced-motion / runaway animation) | meteors.ts + scene.ts | medium | mitigate | Meteor rides the existing single rAF (no new loop); setSuppressed(true) via updateRunState discards the in-flight meteor + clears the spawn timer on pause / reduced-motion (WCAG C39, absent entirely); reduced-motion screenshot proves the static frame is meteor-free |
| T-05.1-04 | Information Disclosure (meteor over content text reduces legibility) | meteors.ts spawn geometry | low | mitigate | Margins-only spawn + down-and-away trajectory by construction (never enters the content column's x-range); upper-sky y-bound clears the camper; contrast non-regression re-verified at both viewports |
| T-05.1-SC | Tampering (supply chain) | package installs | low | accept | Zero new dependencies — meteors.ts is plain TS; the verify-contrast.mjs --moon mode reuses the existing zero-dep node + built-in WebSocket CDP plumbing |
</threat_model>

<verification>
- Task 1: build + astro check green; --selftest exit 0 (incl. new moon fixture); moon constants + even-odd crescent present in starfield.ts; starfield.ts rAF=0 + zero hex; --moon mode present; luminance-evidence.md records moonPeak<mwPeak at both viewports.
- Task 2: build + astro check green; meteors.ts exists with 0 rAF + zero hex + setTimeout scheduler + cadence constant; scene.ts imports meteors, wires setSuppressed, keeps rAF=2; frame-cost-delta.md records idle + in-flight deltas.
- Task 3: build/check green; boundary-aware zero-hex=0; single-rAF (scene.ts=2, starfield.ts=0, meteors.ts=0, nightsky total=2, fig01=2); zero deck/fig01 imports in scene modules; --selftest exit 0; --cdp both viewports non-regressed; --moon both viewports exit 0; Lighthouse >=90 x4 both presets recorded; moon-region + reduced-motion screenshots committed; SKY-06/SKY-07 marked complete.
- No `git push`, deploy, or Pages trigger anywhere; .planning/config.json's _auto_chain_active flip left untouched.
</verification>

<success_criteria>
- SKY-07: a thin, dim, procedural waning crescent moon lives in Layer 0 (tokens-only, no image assets), clear of the Milky Way band and content column, with its sub-Milky-Way peak luminance proven by canvas readback at both check widths — the zero-light-pollution premise preserved.
- SKY-06: occasional single meteor streaks cross the upper-sky margins with a fading --star trail, scheduled by setTimeout on the existing tick, absent under reduced-motion and pause (in-flight discarded on state change), at zero idle per-frame cost.
- The full Phase-5 gate battery re-passes with the two additions (build/check, boundary-aware zero-hex, single-rAF incl. meteors.ts=0, no cross-boundary imports, contrast non-regressed, moon-dimness assertion, Lighthouse >=90 x4 local) and the three UI-checker FLAGs are resolved with committed numeric/visual evidence.
- The site is NOT deployed; all commits remain local on main; no new dependencies.
</success_criteria>

## Artifacts This Phase Produces (Plan 05.1-01)

**New files:**
- `src/lib/nightsky/meteors.ts` — the zero-dep, zero-rAF meteor subsystem (setTimeout scheduler + advance/draw/setSuppressed/teardown handle mirroring constellations.ts).
- `.planning/phases/05.1-celestial-extras/luminance-evidence.md` — moon-peak vs MW-core-peak luminance at 1280 + 1440 (SKY-07 / FLAG 1).
- `.planning/phases/05.1-celestial-extras/frame-cost-delta.md` — idle (zero extra draws) + bounded in-flight (+2 cheap draws) per-frame delta (SKY-06 / FLAG 2).
- `.planning/phases/05.1-celestial-extras/lighthouse-scores.md` — full battery PASS/FAIL table + local Lighthouse both presets + screenshot manifest (FLAG 3).
- Screenshots: `05.1-01-moon-{1440-dpr1,1440-dpr2,1280-dpr1,1280-dpr2}.png`, `05.1-01-reduced-motion.png`, best-effort `05.1-01-meteor-midflight.png`.

**Modified files:**
- `src/lib/nightsky/starfield.ts` — the procedural crescent moon in generateLayer0.
- `src/lib/nightsky/scene.ts` — meteor init / advance+draw (Layer 2d) / setSuppressed / teardown wiring.
- `scripts/verify-contrast.mjs` — the additive --moon CDP mode + --selftest moon fixture.
- `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md` — SKY-06/SKY-07 completion + roadmap/state updates.

<output>
Create `.planning/phases/05.1-celestial-extras/05.1-01-SUMMARY.md` when done.
</output>
