---
phase: 06-integration-launch
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - .planning/phases/06-integration-launch/06-02-lighthouse-scores-live.md
  - .planning/phases/06-integration-launch/06-02-SUMMARY.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [INTG-04]

must_haves:
  truths:
    - "The deploy proceeds ONLY after explicit user approval in chat — no git push exists anywhere before the human gate (INTG-04)"
    - "origin/main fast-forwards to local main (verified ancestor, never --force) and the GitHub Actions Pages deploy completes green"
    - "The live site at https://p2401kumar.github.io/ serves the deck + night-sky scene (view-source sanity)"
    - "Live Lighthouse >=90 in all four categories on both mobile and desktop, recorded to the phase dir (INTG-04 authoritative evidence)"
    - "Live smoke passes: /, /#fig-01, a /work/* page, and /404 all serve correctly over the live origin"
    - "INTG-01..04 marked Complete, ROADMAP Phase 6 marked complete, STATE updated, 06-02-SUMMARY.md written"
  artifacts:
    - .planning/phases/06-integration-launch/06-02-lighthouse-scores-live.md
    - .planning/phases/06-integration-launch/06-02-SUMMARY.md
  key_links:
    - "The 06-02 Task 1 blocking-human approval gate precedes any git push — the only push in the entire phase lives in Task 2, after approval"
    - "git merge-base --is-ancestor origin/main main verifies fast-forward safety before push; deploy.yml fires on push to main and publishes via actions/deploy-pages"
---

<objective>
Launch v2 to production, gated on an explicit human go decision. Present the 06-01 launch-readiness pack for the user's go/no-go; then, ONLY on explicit approval, fast-forward push to origin/main (the push IS the deploy), watch the GitHub Actions Pages run to completion, verify the live site, record live Lighthouse as INTG-04's authoritative evidence, and close out the requirements/roadmap/state.

Purpose: Replacing the live professional site is outward-facing publication — the user personally gated the equivalent v1 decision. Autonomous mode must NOT auto-resolve this gate (06-CONTEXT.md LOCKED decision).
Output: a live v2 deploy, `06-02-lighthouse-scores-live.md`, `06-02-SUMMARY.md`, and completed INTG-01..04.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-integration-launch/06-CONTEXT.md

# The go/no-go pack this plan presents (produced by 06-01)
@.planning/phases/06-integration-launch/06-01-LAUNCH-READINESS.md

# The launch pipeline (unchanged from v1) + live-evidence format precedent
@.github/workflows/deploy.yml
@.planning/phases/05.1-celestial-extras/lighthouse-scores.md

# Requirements + roadmap to close out
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: BLOCKING human go/no-go — present the launch-readiness pack and require explicit user approval before any deploy</name>
  <action>
Blocking human go/no-go gate — the executor pauses here and does NOT act autonomously. Present the 06-01 launch-readiness pack to the user in chat and request an explicit go/no-go decision. THIS CHECKPOINT REQUIRES EXPLICIT USER APPROVAL IN CHAT AND MUST NEVER BE AUTO-RESOLVED UNDER AUTONOMOUS MODE — `workflow.auto_advance` / `/gsd-autonomous` standing directives do NOT apply, because this deploy replaces the live v1 professional site (06-CONTEXT.md LOCKED decision). No `git push`, deploy, or Pages trigger may occur before the user's explicit affirmative; there is no push anywhere in this plan before Task 2. Proceed to Task 2 ONLY on an explicit affirmative from the user's own message.
  </action>
  <what-built>
06-01 produced the complete local proof that v2 is launch-ready: INTG-01 (all 7 panels unforked, transform-hidden, legible over the scene), INTG-02 (the full embedded Fig. 01 checklist re-passed — cold-load 0x0, interaction checklist, resize-while-inactive, classic, reduced-motion, one-active-animation), INTG-03 (case-study cold-load, browser Back to #systems, sitemap, leak gate), and the local INTG-04 Lighthouse pre-flight — all aggregated into `.planning/phases/06-integration-launch/06-01-LAUNCH-READINESS.md`.
  </what-built>
  <how-to-verify>
Present `06-01-LAUNCH-READINESS.md` to the user in chat: what changes live (the live professional site flips from the v1 single-scroll page to the v2 night-sky deck — this REPLACES it), the aggregate gate table, the screenshot index, the rollback plan, and the carried real-device touch checklist. State plainly that the only gate not yet proven is LIVE Lighthouse (proven post-deploy in Task 2), and that everything else is green.

THIS CHECKPOINT REQUIRES EXPLICIT USER APPROVAL IN CHAT AND MUST NEVER BE AUTO-RESOLVED UNDER AUTONOMOUS MODE. `workflow.auto_advance` / `/gsd-autonomous` standing directives do NOT apply here — this is an outward-facing publication that replaces the live v1 site, and the user personally gates it (06-CONTEXT.md LOCKED decision). Do not proceed to Task 2 on anything other than an explicit affirmative from the user's own message. NO `git push`, deploy, or Pages trigger may occur before this approval — there is no push anywhere in this plan prior to Task 2.
  </how-to-verify>
  <resume-signal>User types an explicit affirmative in chat (e.g. "approved" / "go" / "deploy"). Any hesitation, question, or "not yet" halts the phase with nothing deployed. Autonomous auto-resolution is forbidden.</resume-signal>
  <done>The user has made an explicit go/no-go decision in chat. On an explicit affirmative, Task 2 may run. On anything else (question, "not yet", silence, or an autonomous auto-advance attempt), the phase halts with nothing pushed or deployed.</done>
</task>

<task type="auto">
  <name>Task 2: Post-approval launch — fast-forward push (never --force), watch the Actions deploy, live-verify, record live Lighthouse, close out INTG-01..04</name>
  <files>.planning/phases/06-integration-launch/06-02-lighthouse-scores-live.md, .planning/phases/06-integration-launch/06-02-SUMMARY.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
Runs ONLY after Task 1's explicit user approval. If Task 1 was not explicitly approved by the user in chat, STOP — do not run any step of this task.

1. Fast-forward safety gate (BEFORE any push): run `git fetch origin` then `git merge-base --is-ancestor origin/main main`. This MUST exit 0 (origin/main is an ancestor of local main — a clean fast-forward). If it does not exit 0, STOP and report; never rebase/force to make it pass. NEVER use `git push --force` / `--force-with-lease` anywhere.

2. Deploy: `git push origin main` (plain fast-forward push — this IS the deploy; deploy.yml fires on push to main). Capture the pushed HEAD sha (`git rev-parse HEAD`).

3. Watch the Actions run to completion: find the run for the pushed sha (`gh run list --branch main --limit 5 --json databaseId,headSha,status,conclusion,workflowName`), match `headSha` to the pushed HEAD, then `gh run watch <databaseId> --exit-status` (exits nonzero if the run fails). Assert both the build and deploy jobs conclude `success`. Record the run id + conclusion.

4. Live verification (against https://p2401kumar.github.io/ — allow for CDN propagation; poll/retry a few times if the old v1 is briefly still cached):
   - View-source sanity: `curl -s https://p2401kumar.github.io/` contains the deck markup (`class="panel"`, `id="deck-live"`) and the night-sky host/module is present (the deck sentinel `nightsky:panel-change` resolves in a referenced `/_astro/*.js` chunk, and `#nightsky-canvas` / the nightsky host markup is in the HTML).
   - Live Lighthouse: run `npx lighthouse` against the live URL, mobile (default preset) AND desktop (--preset=desktop), extract all four category scores each, and WRITE them to `06-02-lighthouse-scores-live.md` (format per 05.1 lighthouse-scores.md — PASS/FAIL table + scores table, noting these are the LIVE authoritative INTG-04 numbers that replace 06-01's local pre-flight). Every category must be >=90 on both presets.
   - Live smoke over the live origin (assert each returns 200 / expected content): `/`, `/#fig-01` (deep link), one `/work/*` case study page, and `/404` (a nonexistent path serves the 404 page).

5. Failure handling (INTG-04 live Lighthouse): if ANY category is <90 on the live run, RECORD the scores and the responsible audit(s) in 06-02-lighthouse-scores-live.md, then STOP and report to the user. Do NOT auto-revert — the site is functionally verified (smoke + view-source pass); a scoring shortfall is a fix-forward decision for the user, not an automatic rollback.

6. Close out (only after live verification passes): mark REQUIREMENTS.md INTG-01, INTG-02, INTG-03, INTG-04 as Complete (both the checkbox lines and the Traceability table rows); mark ROADMAP.md Phase 6 complete (Plans count + the Progress table row -> Complete + completion date 2026-07-17); update STATE.md (current position, status, last activity, progress). Use scoped Edit for each of these existing files — never a whole-file Write. Write 06-02-SUMMARY.md (template per execution_context) capturing: the deploy run id, live Lighthouse scores, live smoke results, and the rollback path left available.

7. Final docs commit: commit the doc/evidence updates locally (`gsd_run query commit` or the GSD workflow's commit path). A SECOND fast-forward push of these docs to origin/main is acceptable and expected (the docs are not part of the built site but keep the remote's planning history current) — perform it as a plain fast-forward push (re-verify `git merge-base --is-ancestor origin/main main` first; never --force) and note in the summary that this second push is docs-only.

Do NOT modify `.planning/config.json` at any point.
  </action>
  <verify>
    <automated>
set -e
# Fast-forward safety must hold (origin/main is an ancestor of local main)
git fetch origin >/dev/null 2>&1
git merge-base --is-ancestor origin/main main
# The deploy push landed: origin/main now equals local main (fast-forward completed)
test "$(git rev-parse origin/main)" = "$(git rev-parse main)"
# Live site serves the v2 deck + scene (view-source sanity)
curl -s https://p2401kumar.github.io/ | grep -q 'id="deck-live"'
test "$(curl -s https://p2401kumar.github.io/ | grep -oE 'class="panel"' | wc -l | tr -d ' ')" -ge 7
# Live 404 serves
test "$(curl -s -o /dev/null -w '%{http_code}' https://p2401kumar.github.io/this-path-does-not-exist)" = "404"
# One case study serves live
test "$(curl -s -o /dev/null -w '%{http_code}' https://p2401kumar.github.io/work/dynamodb-cellularization/)" = "200"
# Live Lighthouse evidence recorded (authoritative INTG-04)
LH=.planning/phases/06-integration-launch/06-02-lighthouse-scores-live.md
grep -qiE 'mobile' "$LH" && grep -qiE 'desktop' "$LH"
# Requirements + roadmap closed out
test "$(grep -oE 'INTG-0[1-4].*Complete' .planning/REQUIREMENTS.md | wc -l | tr -d ' ')" -ge 4
grep -qiE 'Phase 6.*(Complete|complete)' .planning/ROADMAP.md
test -f .planning/phases/06-integration-launch/06-02-SUMMARY.md
echo "TASK 2 PASS"
    </automated>
  </verify>
  <done>After explicit user approval: origin/main fast-forwarded to local main (verified ancestor, no --force), the Actions Pages run concluded success, the live site serves the v2 deck + scene, live Lighthouse >=90 x4 on both presets is recorded as INTG-04's authoritative evidence, live smoke (/, /#fig-01, /work/*, /404) passes, INTG-01..04 are Complete, ROADMAP Phase 6 is complete, STATE is updated, and 06-02-SUMMARY.md is written. If live Lighthouse fell short, scores were recorded and the phase stopped for a user fix-forward decision without auto-reverting. config.json untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local main -> origin/main (GitHub) | The push IS the deploy and REPLACES the live v1 professional site. Crossed exactly once, in Task 2, only after Task 1's explicit human approval. |
| CDN / live origin | Post-deploy the live URL is the authoritative INTG-04 evidence surface; verified before close-out. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Elevation/Integrity | premature/auto deploy | critical | mitigate | Task 1 is a blocking-human gate that must never auto-resolve (autonomous ignored); NO push exists anywhere before it; Task 2 runs only on explicit approval. |
| T-06-02 | Tampering | force-push clobbers remote history / loses v1 | high | mitigate | `git merge-base --is-ancestor origin/main main` verified before push; plain fast-forward push only, never `--force`/`--force-with-lease`; v1.0 tag + legacy-2021 branch preserved as rollback. |
| T-06-03 | Denial of Service (credibility) | deploying a broken/regressed live site | high | mitigate | 06-01 proved every gate but live Lighthouse locally; Task 2 view-source + live smoke + live Lighthouse confirm the deploy; live <90 stops for a user fix-forward decision (no silent ship). |
| T-06-04 | Tampering | `.planning/config.json` | low | mitigate | Plan never touches config.json; the human gate's non-auto-resolution does not depend on a config flip. |
| T-06-SC | Tampering | transient `npx lighthouse` / `gh` CLI | low | mitigate | Both are transient audit/ops tools (Phase 4/5 precedent); no new dependency added to package.json. |
</threat_model>

<verification>
- No `git push` occurred before Task 1's explicit user approval (there is none in the plan text before Task 2).
- Fast-forward only: `git merge-base --is-ancestor origin/main main` exits 0 before each push; no `--force` anywhere.
- Actions run for the pushed sha concluded `success` (build + deploy).
- Live: deck + scene served (view-source), live Lighthouse >=90 x4 both presets recorded, live smoke (/, /#fig-01, /work/*, /404) passes.
- INTG-01..04 Complete; ROADMAP Phase 6 complete; STATE updated; 06-02-SUMMARY.md written; config.json untouched.
</verification>

<success_criteria>
- The deploy happened only after explicit human approval, as a verified fast-forward, and the live v2 site is up and verified.
- INTG-04 is closed with live Lighthouse evidence; INTG-01..03 are marked Complete; the milestone requirements are fully mapped and done.
- A rollback path (revert + fast-forward push; v1.0 tag; legacy-2021 branch) remains documented and available.
</success_criteria>

<output>
Create `.planning/phases/06-integration-launch/06-02-SUMMARY.md` when done.
</output>
