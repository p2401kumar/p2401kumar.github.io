---
phase: 08-glass-system
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/styles/tokens.css
  - src/styles/deck.css
  - src/components/SiteHeader.astro
  - src/components/SiteFooter.astro
  - src/components/DeckIndex.astro
  - src/layouts/BaseLayout.astro
  - src/pages/index.astro
  - .planning/phases/08-glass-system/08-02-glass-gate.md
autonomous: true
requirements: [GLS-01, GLS-02, GLS-03]
user_setup: []
must_haves:
  truths:
    - "Panels, header, footer, and the jump index render as frosted glass over the real sky (translucent fill + backdrop blur + saturate + brightness + a 1px top light edge), token-expressed as --glass-*"
    - "Glass attaches ONLY to .panel[data-state=active] (deck) / html.classic-active .panel (classic) — never a bare .panel and never a descendant of .panel"
    - "Every panel x both viewports holds >= 4.5:1 worst-case over the photo under the screenshot-sampled gate before values lock; text-dense panels tier to the protected recipe as the gate demands"
    - "Glass degrades cleanly: @supports absent -> today's opaque baseline; prefers-reduced-transparency:reduce -> solid; print strips filters"
    - "Shared chrome glass is home-scoped via body.has-sky so /work/* and /404 render exactly as today"
  artifacts:
    - src/styles/tokens.css
    - src/styles/deck.css
    - src/components/SiteHeader.astro
    - src/components/SiteFooter.astro
    - src/components/DeckIndex.astro
    - src/layouts/BaseLayout.astro
    - src/pages/index.astro
    - .planning/phases/08-glass-system/08-02-glass-gate.md
  key_links:
    - "BaseLayout hasSky prop -> body.has-sky class (index only) gates all shared-chrome glass, zero-leak by construction"
    - "The 08-01 --cdp-screenshot gate arbitrates every --glass-* value; tier-1 -> tier-2 escalation is the only response to a failing panel; tier-2 failure = stop-and-escalate"
---

<objective>
Land the full glass system: define the 13 `--glass-*` tokens exactly per the approved manifest, apply glass to the four surface families (active panel, header, footer, jump index) with correct state-scoping and degradation ladders, then run the screenshot-sampled contrast gate and escalate tiers per protocol until every surface holds >= 4.5:1 at both viewports. GLS-01 (full glass chrome) + GLS-02 (degradation) + GLS-03's arbitration (values proven before they lock).

Purpose: deliver the optical "light bending through cold night air" read while never spending the razor-thin 1280x800 legibility headroom (07-04 recorded only +0.08 pre-glass) — the gate, not taste, is the arbiter.

Output: tokens + glass CSS across 7 files + a recorded gate/escalation table.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-glass-system/08-CONTEXT.md
@.planning/phases/08-glass-system/08-RESEARCH.md
# THE token manifest, per-surface map, degradation appearances, tiering protocol, scrim decision.
@.planning/phases/08-glass-system/08-UI-SPEC.md
@.planning/phases/08-glass-system/08-01-baseline-contrast.md

# Surfaces going glass + the backdrop-root trap live here.
@src/styles/tokens.css
@src/styles/deck.css
@src/components/SiteHeader.astro
@src/components/SiteFooter.astro
@src/components/DeckIndex.astro
@src/layouts/BaseLayout.astro
@src/pages/index.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: 13 tokens + glass CSS (active-panel-only scoping, body.has-sky chrome, degradation ladders)</name>
  <files>src/styles/tokens.css, src/styles/deck.css, src/components/SiteHeader.astro, src/components/SiteFooter.astro, src/components/DeckIndex.astro, src/layouts/BaseLayout.astro, src/pages/index.astro</files>
  <action>
Paste the 13-token block into tokens.css EXACTLY per 08-UI-SPEC.md Token Manifest (lines 306-340), values verbatim: tier-1 --glass-bg rgb(255 255 255 / 0.06) / --glass-blur 12px / --glass-saturate 150% / --glass-brightness 0.90; chrome --glass-bg-chrome rgb(255 255 255 / 0.05) / --glass-blur-chrome 10px / --glass-saturate-chrome 140% / --glass-brightness-chrome 0.92; tier-2 --glass-bg-2 rgb(15 18 23 / 0.55) / --glass-blur-2 10px / --glass-saturate-2 130% / --glass-brightness-2 0.96; shared --glass-edge rgb(255 255 255 / 0.10). All values are rgb()/px/%/decimal — ZERO hex, so the zero-hex-outside-tokens floor is untouched. Reproduce the manifest's header comment including the 16px-ceiling note WITHOUT declaring a --glass-blur-ceiling token (an unused token violates the tokens.css pruning discipline; the ceiling lives as prose only). --glass-brightness is load-bearing (it darkens the captured backdrop before the white fill lightens it — the number that keeps tier-1 above 4.5:1); do not round it.

PANELS (deck.css) — extend, never replace. Leave the existing rules at :47 (positioning), :65 (`[data-state="active"]` transform/opacity/pointer-events), :71 (`[data-state="inactive"]`), :78 (transition), and the whole scrim block (:110-161, peak 0.38) UNTOUCHED. Do NOT add any unconditional `background` to `.panel` — panels have no background today (transparent + scrim), so the correct `@supports`-absent / reduced-transparency fallback is simply "no new rule matches" (08-UI-SPEC.md Degradation Appearances: no --glass-fallback-bg token). Append glass as an ADDITIVE nest: an `@supports (backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))` wrapping a `@media (prefers-reduced-transparency: no-preference)` wrapping the glass rules. Inside: a tier-1 rule matching `html.deck-active .panel[data-state="active"]` and `html.classic-active .panel` sets background var(--glass-bg); backdrop-filter blur(var(--glass-blur)) saturate(var(--glass-saturate)) brightness(var(--glass-brightness)); the identical -webkit-backdrop-filter; border-top 1px solid var(--glass-edge). Then a tier-2 override rule keyed by data-panel-id — `.panel[data-state="active"][data-panel-id="experience"]` (and patents, and skills) plus `html.classic-active .panel[data-panel-id="experience"]` (and patents, and skills) — swaps to the *-2 fill/blur/saturate/brightness tokens (keeps the shared --glass-edge). Use data-panel-id (unconditional, present in both modes per Panel.astro) so tier-2 resolves identically in deck and classic. NEVER attach any glass property to a bare `.panel {` selector or to `[data-state="inactive"]` — that is the Pitfall-1 budget-blower (inactive panels are opacity:0, never display:none, so 7 panels would blur instead of the measured 4) and the Pitfall-2 backdrop-root trap. The light edge is a plain border-top only — never a second, independently-blurred child element.

HEADER + FOOTER (SiteHeader.astro / SiteFooter.astro <style> blocks) — home-scope every new rule under `body.has-sky` (08-RESEARCH.md Pattern 2): on /work/* and /404 the body has no has-sky class so NONE of these rules match and both render exactly as today (they have no background property now — true zero visual diff, not a solid fallback). Add `body.has-sky header { ... }` (footer analogously) inside the same nested `@supports` + `@media (prefers-reduced-transparency: no-preference)` shape, using the *-chrome tokens: background var(--glass-bg-chrome); backdrop-filter + -webkit- blur/saturate/brightness with the chrome tokens; border-top 1px solid var(--glass-edge). Do not touch header/footer position, padding, type, or copy (04-UI-SPEC / 07-04 locked).

JUMP INDEX (DeckIndex.astro) — needs NO body.has-sky scope (it renders only on index; structurally cannot leak). Keep the existing unconditional base rules (.deck-index-toggle background var(--bg) + border 1px solid var(--hair2); .deck-jump background var(--bg) + border) so the @supports-absent / reduced fallback is the current pill look. Inside the same nested @supports + no-preference block, override `.deck-index-toggle` and `.deck-jump` (desktop popover AND its max-width:640px mobile sheet) to background var(--glass-bg-chrome) + the chrome backdrop-filter/-webkit pair + border-top 1px solid var(--glass-edge) (overriding only the top border; the existing hairline stays on the other three sides; rounded top corners render natively). This jewel-like pill is the reference surface — get it right.

FIG. 01 EXEMPTION — do NOT edit Figure01.astro. The `.panel[data-state="active"][data-panel-id="fig-01"]` WRAPPER receives tier-1 glass like any panel (grammar consistency), but the `.fig` card inside keeps `background: var(--panel)` (opaque, an instrument not a window) untouched — it paints opaque on top of the glass wrapper, no conflict.

PRINT — add one `@media print { .panel, header, footer, .deck-index-toggle, .deck-jump { background:none; backdrop-filter:none; -webkit-backdrop-filter:none; border-top:none; } }` (place in deck.css or global.css) to strip filters for print. Every backdrop-filter declaration site (enhancement AND print) must carry its `-webkit-backdrop-filter` twin (Pitfall 6 — Safari silently renders opaque without the prefix).

BODY SCOPING WIRING — BaseLayout.astro: add `hasSky?: boolean` to Props (default false) and set `<body class={hasSky ? "has-sky" : undefined}>`. index.astro: change `<BaseLayout>` to `<BaseLayout hasSky>`. Leave work/[slug].astro and 404.astro passing nothing (no has-sky -> zero chrome-glass leak). Scrim stays 0.38, untouched this phase (08-UI-SPEC.md Scrim Interaction: KEEP).
  </action>
  <verify>
    <!-- Region-scoped negative gate: 'backdrop-filter' legitimately appears in deck.css (active-panel + print);
         the ==0 check below is scoped to the inactive-state block only, not the whole file. -->
    <!-- planner-discipline-allow: backdrop-filter -->
    <automated>npx astro check && npm run build && test "$(grep -oE '\-\-glass-[a-z0-9-]+:' src/styles/tokens.css | wc -l | tr -d ' ')" = "13" && test "$(grep -o 'has-sky' src/layouts/BaseLayout.astro | wc -l | tr -d ' ')" != "0" && test "$(grep -o 'hasSky' src/pages/index.astro | wc -l | tr -d ' ')" != "0" && test "$(grep -oiE '#[0-9a-fA-F]{3,8}' src/styles/deck.css src/components/SiteHeader.astro src/components/SiteFooter.astro src/layouts/BaseLayout.astro src/pages/index.astro | wc -l | tr -d ' ')" = "0" && test "$(grep -oP '#[0-9a-fA-F]{3,8}(?![0-9A-Za-z_-])' src/components/DeckIndex.astro | wc -l | tr -d ' ')" = "0" && test "$(grep -o 'html.deck-active .panel {' src/styles/deck.css | wc -l | tr -d ' ')" = "3" && test "$(grep -o 'backdrop-filter' src/styles/deck.css src/components/SiteHeader.astro src/components/SiteFooter.astro src/components/DeckIndex.astro | wc -l | tr -d ' ')" = "$(( 2 * $(grep -o '\-webkit-backdrop-filter' src/styles/deck.css src/components/SiteHeader.astro src/components/SiteFooter.astro src/components/DeckIndex.astro | wc -l | tr -d ' ') ))" && test "$(grep -A3 'data-state=\"inactive\"' src/styles/deck.css | grep -o 'backdrop-filter' | wc -l | tr -d ' ')" = "0" && test "$(grep -o 'var(--glass' src/components/Figure01.astro | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <done>astro check 0, build green; exactly 13 --glass-* tokens in tokens.css (all rgb()/px/%/decimal, zero hex); zero hex outside tokens.css across every modified file (DeckIndex #deck-* ids excluded via the identifier-boundary regex); bare `html.deck-active .panel {` opener count unchanged at 3; every backdrop-filter paired with -webkit-backdrop-filter; the inactive-state block carries no backdrop-filter; Figure01.astro references no --glass token; body.has-sky wired via BaseLayout+index; scrim untouched (--selftest still green in the battery below).</done>
</task>

<task type="auto">
  <name>Task 2: The screenshot gate — run, tier-escalate per protocol, evidence</name>
  <files>.planning/phases/08-glass-system/08-02-glass-gate.md</files>
  <action>
Run the 08-01 screenshot gate against the built+preview site WITH glass live, at BOTH check widths, and drive the tiering to a pass. Build + preview, then `node scripts/verify-contrast.mjs --cdp-screenshot --url http://localhost:4321/ --width 1280 --height 800` and again at `--width 1440 --height 900`. Record a per-panel + per-chrome worst-case table into 08-02-glass-gate.md.

TIER-ESCALATION PROTOCOL (08-UI-SPEC.md Tiering, verbatim — the gate is the sole arbiter, not the density heuristic):
1. Text-density classification is the STARTING assignment already encoded in Task 1 (tier-1: hero, fig-01 wrapper, systems, contact; tier-2: experience, patents, skills; chrome/jump-index: standard chrome recipe).
2. Once real glass values are in place, the screenshot-sampled gate is the SOLE arbiter — it measures the actual GPU-composited output including the real blur kernel, which no hand calculation replicates.
3. ANY panel failing 4.5:1 worst-case at its assigned tier MOVES TO THE NEXT TIER UP (tier-1 -> tier-2), by relocating its data-panel-id into the tier-2 override group in deck.css, then re-running the gate. Watch hero especially: its 1280x800 baseline (4.58) is numerically identical to the tightest tier-2 panels with only 0.08 pre-glass headroom, and the naive-tier-1 hand math lands ~4.64 (thin) — if the gate shows it below 4.5 under tier-1, escalate hero to tier-2 immediately, no debate.
4. The jump-index pill is the single highest-risk chrome surface (bottom-right, in 07-03's deliberately-brighter galactic-core margin, never gated before). If its extended chrome sampling fails, move it to the tier-2 dark protective fill (trading some jewel-like read for contrast — contrast wins).
5. There is NO tier beyond tier-2. If tier-2's protective fill (dark ~55%-alpha near-panel fill, ~10.2 hand-math headroom) STILL fails anywhere, that is a STOP-AND-ESCALATE condition: halt, do not invent a third tier, and return the failing surface + numbers to the orchestrator per 08-UI-SPEC.md. Do not silently ship below 4.5.

Document EACH escalation you make (which surface, from which tier to which, the before/after worst-case number). Re-run the gate after each change until every over-sky region at BOTH widths is >= 4.5:1 (the mode exits 0). Then a CLASSIC-MODE spot-check: load `?view=classic` (or the view-classic escape hatch) at both widths and visually confirm glass panels stay legible against the static photo (08-UI-SPEC.md calls this the lower-rigor path; if a real cost/contrast problem shows, content-visibility:auto on off-screen .panel is the documented not-yet-applied lever — note it, do not apply speculatively). Capture evidence into a glass-evidence/ folder: fresh full-page screenshot pairs at both viewports, a jump-index pill closeup, a prefers-reduced-transparency:reduce emulated capture (via CDP Emulation.setEmulatedMedia — GLS-02 proof that glass collapses to solid), and an @supports-off simulation if feasible.
  </action>
  <verify>
    <automated>npm run build && (npm run preview >/dev/null 2>&1 & PREV=$!; sleep 5; node scripts/verify-contrast.mjs --cdp-screenshot --url http://localhost:4321/ --width 1280 --height 800 >/dev/null 2>&1; A=$?; node scripts/verify-contrast.mjs --cdp-screenshot --url http://localhost:4321/ --width 1440 --height 900 >/dev/null 2>&1; B=$?; kill $PREV 2>/dev/null; test $A = 0 && test $B = 0) && test -f .planning/phases/08-glass-system/08-02-glass-gate.md && node scripts/verify-contrast.mjs --selftest</automated>
  </verify>
  <done>The --cdp-screenshot gate exits 0 at BOTH 1280x800 and 1440x900 with glass live — every panel and every chrome surface >= 4.5:1 worst-case; each tier escalation (if any) is documented with before/after numbers; classic mode spot-checked; degradation evidence (reduced-transparency + @supports-off) captured; --selftest still green (scrim 0.38 untouched). If tier-2 failed anywhere, execution stopped and escalated instead of shipping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client browser -> CSS compositor | glass is static declarative CSS; no untrusted input crosses, no JS added |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-02 | Information Disclosure | glass translucency lowering text contrast below the WCAG legibility floor | medium | mitigate | screenshot-sampled >= 4.5:1 gate (GLS-03) arbitrates before any value locks; tier-1 -> tier-2 escalation; tier-2 failure = stop-and-escalate, never ship below floor |
| T-08-05 | Denial of Service | classic-mode glass on all 7 panels over the animating scene | low | accept | classic gets a spot-check (08-UI-SPEC.md lower-rigor path); content-visibility:auto documented as the not-yet-applied lever; deck mode (the measured path) stays at exactly 4 active surfaces |
| T-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero new dependencies; package.json untouched; no install task in this plan |
</threat_model>

<verification>
- astro check 0; build green (4 pages).
- Exactly 13 --glass-* tokens; zero hex outside tokens.css (identifier-boundary regex for DeckIndex #deck-* ids).
- Glass only on `.panel[data-state="active"]` / `html.classic-active .panel` — bare-panel opener count unchanged (3), inactive block glass-free, backdrop-filter/-webkit parity holds.
- --cdp-screenshot gate exits 0 at both widths; tier escalations documented.
- Degradation proven: reduced-transparency emulation collapses to solid; @supports/print strip filters.
- Scrim 0.38 untouched (--selftest sync check green); Figure01.astro unchanged.
</verification>

<success_criteria>
Panels, header, footer, and jump index read as frosted glass over the real sky; every surface holds >= 4.5:1 worst-case at both viewports under the screenshot gate; glass degrades cleanly through the @supports / reduced-transparency / print ladders; chrome glass is home-scoped so /work and /404 are untouched — GLS-01, GLS-02, and GLS-03's arbitration are satisfied.
</success_criteria>

<output>
Create `.planning/phases/08-glass-system/08-02-SUMMARY.md` when done.
</output>
