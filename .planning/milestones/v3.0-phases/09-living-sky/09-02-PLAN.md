---
phase: 09-living-sky
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/styles/tokens.css
  - src/lib/nightsky/tokens.ts
  - src/lib/nightsky/aurora.ts
  - src/lib/nightsky/scene.ts
  - scripts/verify-contrast.mjs
autonomous: true
requirements: [AMB-03]

must_haves:
  truths:
    - "A faint teal-white aurora (3 left-margin curtains) breathes over a ~20s cycle, alpha 0.07-0.17, hard-capped at 0.20 in code."
    - "Aurora composites OVER the moon and BEFORE clouds, and renders ZERO pixels inside the content column (hard margin exclusion)."
    - "verify-contrast.mjs --aurora asserts auroraPeak < mwPeak at BOTH viewports, observing the breathing peak by max-over-a-full-cycle sampling."
  artifacts:
    - src/lib/nightsky/aurora.ts
    - "the --aurora token in src/styles/tokens.css"
    - "the --aurora mode in scripts/verify-contrast.mjs"
  key_links:
    - "scene.ts drawFrame calls auroraHandle.advance/draw AFTER the Layer-0 blit and BEFORE the cloud draw."
    - "tokens.ts getSkyTokens reads --aurora into SkyTokens.aurora; aurora.ts fills with tokens.aurora only."
    - "aurora.ts hard-caps alpha at AURORA_ALPHA_CEILING (0.20); the gate multi-samples >= one 20s period to catch the envelope peak."
---

<objective>
Add the one sanctioned added light source (AMB-03): a breathing, noise-driven aurora of 3 curtains confined to the LEFT margin, composited over the moon and before the clouds, with a hard in-code luminance ceiling and a new `--aurora` verify mode proving its peak stays strictly below the Milky-Way core at both check viewports.

Purpose: Make the sky faintly, glacially alive over there — without ever pushing a text surface below 4.5:1 or exceeding the photo's own galactic-core brightness.

Output: the `--aurora` token, aurora.ts, the scene.ts draw wiring (over-moon / before-clouds), and the `verify-contrast.mjs --aurora` luminance gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-living-sky/09-CONTEXT.md
@.planning/phases/09-living-sky/09-RESEARCH.md
@.planning/phases/09-living-sky/09-UI-SPEC.md

# Live code this plan extends or mirrors
@src/lib/nightsky/scene.ts
@src/lib/nightsky/starfield.ts
@src/lib/nightsky/tokens.ts
@src/styles/tokens.css
@scripts/verify-contrast.mjs
@.planning/phases/09-living-sky/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add the --aurora token to tokens.css and expose it through tokens.ts</name>
  <files>src/styles/tokens.css, src/lib/nightsky/tokens.ts</files>
  <behavior>
    - The only new hex literal site-wide is the --aurora token in tokens.css.
    - getSkyTokens() returns an aurora RgbTriple sourced from the token; no consumer reads a hex directly.
  </behavior>
  <action>
This is the one sanctioned exception to the 200-230 degree hue family, documented inline.

1. In `src/styles/tokens.css`, in the "Night-sky scene" block, immediately AFTER the `--star:#eef2fa;` line, add the `--aurora` token with value `#bfe8df` and the one-line exception comment from 09-UI-SPEC's Token Manifest (a desaturated high-lightness teal-white standing in for real aurora oxygen-emission green, kept in the same low-saturation/high-lightness register as --milkyway so it reads as a palette sibling, not a new hue family; cite 09-UI-SPEC.md). Change NO other token — `--milkyway` is repurposed (not modified) as the cloud fill in 09-01, its value stays exactly `#cfd9f2`. Glass values, scrim, and photo tokens are LOCKED — do not touch them.

2. In `src/lib/nightsky/tokens.ts`, add `aurora: RgbTriple;` to the `SkyTokens` interface (after `star`), and add `aurora: readToken(styles, '--aurora'),` to the object built in `getSkyTokens()`. The cache stays identity-stable.
  </action>
  <verify>
    <automated>npm run build && npx astro check && test "$(grep -o "\-\-aurora" src/styles/tokens.css | wc -l)" -ge 1 && test "$(grep -o "aurora: readToken" src/lib/nightsky/tokens.ts | wc -l)" -eq 1 && test "$(grep -oE "#[0-9a-fA-F]{3,8}" src/lib/nightsky/tokens.ts | wc -l)" -eq 0</automated>
  </verify>
  <done>Build green, astro check clean. --aurora exists in tokens.css only; tokens.ts reads it via the token bridge with zero hex literals.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build aurora.ts (noise curtains + breathing + 4-frame throttle + left-margin exclusion) and wire it over the moon, before clouds</name>
  <files>src/lib/nightsky/aurora.ts, src/lib/nightsky/scene.ts</files>
  <behavior>
    - Three left-margin curtains with independently-undulating top edges breathe together in alpha (0.07-0.17) and height (0.49H-0.55H) over ~20s; brightness is per-frame smooth, shape repaints only every 4 frames.
    - Alpha can never exceed the hard ceiling 0.20 regardless of breathing phase.
    - Aurora renders zero pixels inside the content column (hard margin boundary), and none in the right-margin MW-core region.
    - Draw order in the live tick: Layer-0 blit -> aurora -> clouds -> twinkle. The static path renders aurora once at mid-breath (alpha 0.12, top 0.52H).
  </behavior>
  <action>
Build aurora as a handle-shaped, timer-free, rAF-free module and slot it into the single tick in the physically-honest order.

1. Create `src/lib/nightsky/aurora.ts` exporting `interface AuroraHandle { advance(ts: number): void; draw(ctx: CanvasRenderingContext2D, cssWidth: number, cssHeight: number): void; drawStatic(ctx: CanvasRenderingContext2D, cssWidth: number, cssHeight: number): void; resize(cssWidth: number, cssHeight: number): void; setShapeThrottle(everyN: number): void; teardown(): void; }` and `initAurora(options: { tokens: SkyTokens; getViewport: () => { width: number; height: number } | null }): AuroraHandle`. Owns NO rAF and NO timers; all cadence rides `advance()`.

   Noise table: a `Float32Array` of 96 samples built once as a sum of three mismatched non-harmonic sines (periods ~17/29/41 samples) normalized to [0,1], sampled with linear interpolation between entries. Rebuild on init only (size-independent).

   Placement (LEFT margin only, mirror `contentColumnEdges(cssWidth)`): x-range [8, columnLeft - 8] (1440 -> x:8-272; 1280 -> x:8-192). Below the existing `TWINKLE_MIN_MARGIN_WIDTH` (48px) available margin, render nothing (graceful empty, no special-case code). Keep the RIGHT margin (the photo's MW core and the `mwBox` comparator x:0.80-0.98) entirely clear so the luminance comparator stays representative. Vertical box: base fixed at y=0.85H (horizon); top oscillates 0.49H (bright/tall) to 0.55H (dim/short), IN PHASE with the alpha breath.

   Three curtains of varying widths/heights inside the margin box, each curtain's undulating top edge sampled from the noise table at an INDEPENDENT phase offset so the three never breathe in lockstep. Max shape step: bound the per-repaint noise-phase advance so the curtain-edge x-displacement is <= 1.5px per repaint step at the 1440 reference width.

   Breathing: define `AURORA_BREATH_MID = 0.12`, `AURORA_BREATH_AMP = 0.05`, period ~20000ms; `envelope = AURORA_BREATH_MID + AURORA_BREATH_AMP*Math.sin(ts/PERIOD*2*PI)` giving 0.07-0.17. Then `alpha = Math.min(envelope, AURORA_ALPHA_CEILING)` where `AURORA_ALPHA_CEILING = 0.20` is the HARD in-code cap the envelope can never exceed (mirrors the moon's LIT_ALPHA ceiling doctrine — this is what makes the verify-time assertion stable across breathing phases). The alpha (a pure sine) is evaluated EVERY frame (smooth). The curtain SHAPE (noise repaint / top-edge geometry) is throttled to every `shapeEveryN` frames (default 4) via a module frame counter; `draw()` composites the last-computed geometry every frame at the live alpha.

   Render each curtain with the DEFAULT source-over compositing (never the additive blend mode — globalCompositeOperation stays at its source-over default at every fill): a vertical `createLinearGradient` (bright top -> transparent bottom) clipped to the wavy top edge via a path, filled with `tokens.aurora` modulated by the live alpha and a per-stop vertical falloff (soft fade-in at the top edge, blend to zero at the 0.85H base). Use at least 4 gradient stops (no banding). Draw directly to the visible canvas (start simple; the offscreen-buffer-then-upscale optimization is deferred per research Open Question 2 — build only if 09-03's soak shows pressure).

   `drawStatic(ctx,w,h)`: render the curtains ONCE at mid-breath — alpha exactly 0.12, curtain top exactly 0.52H (the exact midpoints) — the reduced-motion fixed phase.
   `setShapeThrottle(everyN)`: set `shapeEveryN` (the mobile-ladder tier-2 seam; wired by 09-03 — default 4 here; brightness sine stays per-frame regardless).
   `resize(w,h)`: recompute curtain x-positions/widths/box for the new margin geometry (noise table persists).
   `teardown()`: drop state / any motion listener.

2. Wire into `src/lib/nightsky/scene.ts`: `import { initAurora, type AuroraHandle } from './aurora';` add `let auroraHandle: AuroraHandle | null = null;`. In `initNightSky`, `auroraHandle = initAurora({ tokens: skyTokens, getViewport: () => (layer0 ? { width: layer0.cssWidth, height: layer0.cssHeight } : null) });`. In `adoptLayer0`, call `auroraHandle?.resize(result.cssWidth, result.cssHeight);` before `renderStaticFrame()`. In `drawFrame`, insert the aurora block IMMEDIATELY AFTER the `visibleCtx.drawImage(layer0.canvas, ...)` blit and BEFORE the 09-01 cloud (Layer 1.5) block — `if (auroraHandle) { auroraHandle.advance(ts); auroraHandle.draw(visibleCtx, w, h); }` — with a comment: aurora composites OVER the moon and BEFORE clouds (09-UI-SPEC Aurora Placement; supersedes the RESEARCH diagram's tentative last-layer placement). In `renderStaticFrame`, insert `auroraHandle?.drawStatic(visibleCtx, layer0.cssWidth, layer0.cssHeight);` after the Layer-0 blit and BEFORE the cloud drawStatic. In `teardown`, `auroraHandle?.teardown(); auroraHandle = null;`.
  </action>
  <verify>
    <automated>npm run build && npx astro check && test "$(grep -o "lighter" src/lib/nightsky/aurora.ts | wc -l)" -eq 0 && test "$(grep -o "requestAnimationFrame" src/lib/nightsky/aurora.ts | wc -l)" -eq 0 && test "$(grep -oE "setTimeout|setInterval" src/lib/nightsky/aurora.ts | wc -l)" -eq 0 && test "$(grep -oE "#[0-9a-fA-F]{3,8}" src/lib/nightsky/aurora.ts | wc -l)" -eq 0 && test "$(grep -oE "AURORA_ALPHA_CEILING\s*=\s*0\.20" src/lib/nightsky/aurora.ts | wc -l)" -ge 1 && test "$(grep -o "requestAnimationFrame" src/lib/nightsky/scene.ts | wc -l)" -eq 2</automated>
  </verify>
  <done>Build green, astro check clean. aurora.ts has zero additive-composite/rAF/timer/hex; the hard 0.20 ceiling constant is present; scene.ts still owns exactly 2 rAF calls and draws aurora over the moon, before clouds; static frame renders aurora at mid-breath.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add the verify-contrast.mjs --aurora luminance gate (auroraPeak < mwPeak, both viewports, peak observed by full-cycle max-sampling)</name>
  <files>scripts/verify-contrast.mjs</files>
  <behavior>
    - --aurora samples the aurora bounding box's composited peak luminance and the photo's MW-core peak, asserting the aurora peak is strictly lower.
    - The breathing peak is provably observed by taking the MAX composited aurora luminance across a sampling window >= one full ~20s breathing period.
    - Runs at both reference viewports; exits non-zero on failure; the existing --selftest and --moon assertions are unaffected.
  </behavior>
  <action>
Extend the verifier with a sibling `--aurora` mode, mirroring `--moon` — MIRROR, do not refactor the passing moon gate.

1. Add a self-contained `sampleAuroraOnce()` browser sampler (its own inline `deviceRect`/`compositedPeakOf`/`photoPeakOf` mirroring `sampleMoonOnce`, so the locked moon path is untouched). It reads `#nightsky-canvas`, builds the photo comparator via `makePhotoCanvas`, and computes the aurora bounding box from aurora.ts's documented geometry mirrored here: x from 8 to (columnLeft - 8) using the same pad/half column formula, y from 0.49H (the highest breathing top extent) to 0.85H (base). It returns `{ auroraPeak: compositedPeakOf(auroraBox).peak, auroraPeakPixel, mwPeak: photoPeakOf(mwBox).peak, mwPeakPixel }` where `mwBox` is the SAME comparator the moon uses (x:0.80-0.98, y:0.30-0.60). Confirm the aurora box does NOT overlap `mwBox` (left-margin vs right-margin) so the ceiling check stays a real independent comparison, not a self-referential near-tie.

2. Add `async function auroraMain(args)` mirroring `moonMain`: parse `--url`/`--width`/`--height`; launchChrome, connectCdp, Page.navigate, wait on `READY_PROBE`. Then observe the breathing PEAK by MAX-over-a-full-cycle sampling: loop N samples at interval I sized so `N*I` is at least ~21s (>= the 20s breathing period) — e.g. 24 samples at 900ms — installing the sampler via `.toString()` injection (as `sampleMoonOnce` is), and tracking the running MAXIMUM `auroraPeak` across samples (mwPeak is photo-static). Because any window >= the period contains the envelope peak, the observed max provably reflects the brightest breathing moment; the in-code `AURORA_ALPHA_CEILING` (0.20) additionally bounds it so the observed peak can never silently drift above the design ceiling between runs. Assert `maxAuroraPeak < mwPeak`; log the JSON report (mode "aurora", both peaks, assertion, pass); `process.exit(1)` on failure.

3. Add the CLI branch `else if (args.includes("--aurora")) { await auroraMain(args); }` in the entry dispatch (alongside `--moon`), and extend the usage string. The gate MUST be run at BOTH viewports with explicit dimensions — do not rely on the 1440x900 default.
  </action>
  <verify>
    <automated>node scripts/verify-contrast.mjs --selftest && test "$(grep -o "async function auroraMain" scripts/verify-contrast.mjs | wc -l)" -eq 1 && test "$(grep -o "sampleAuroraOnce" scripts/verify-contrast.mjs | wc -l)" -ge 2 && npm run build</automated>
  </verify>
  <done>--selftest still passes (moon fixtures intact); auroraMain + sampleAuroraOnce + the --aurora CLI branch exist; build green. Live-gate proof (both viewports exit 0) is exercised in this plan's verification section against a running preview.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aurora render -> glass surfaces | Aurora is confined to the left margin, outside every panel/chrome footprint, to bound re-blur cost and contrast risk. |
| runtime render -> build-time gate | The luminance CEILING has no in-browser enforcement; it is enforced at verify time by the --aurora CDP gate plus the hard in-code alpha cap. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04 | Denial of Service (main-thread) | aurora.ts | medium | mitigate | Handle shape, zero owned timers/rAF; 4-frame shape throttle; grep gate asserts zero rAF/timers so it can never outlive the pause machine. |
| T-09-05 | Information Disclosure (legibility) | aurora luminance vs MW core / text | high | mitigate | Hard in-code AURORA_ALPHA_CEILING 0.20 + left-margin hard exclusion (zero pixels in the column) + --aurora gate asserting auroraPeak < mwPeak at both viewports, sampling the breathing peak via full-cycle max. |
| T-09-06 | Tampering | aurora composite mode | high | mitigate | Source-over-only fills; grep gate asserts zero additive-composite occurrences (the diagnosed 05-06 failure mode) in aurora.ts. |
| T-09-SC | Tampering (supply chain) | npm installs | n/a | accept | Zero package installs this plan (Floors: package.json untouched) — Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
Live-gate proof (build once, serve preview, then run both viewports):
- `npm run build` green; `npx astro check` 0 errors.
- Start `npm run preview`; then BOTH must exit 0: `node scripts/verify-contrast.mjs --aurora --width 1440 --height 900` and `node scripts/verify-contrast.mjs --aurora --width 1280 --height 800` (auroraPeak < mwPeak).
- Regression guard: `node scripts/verify-contrast.mjs --moon --width 1440 --height 900` and `--width 1280 --height 800` still exit 0 (aurora over the moon must not break the moon-dimness assertion); `node scripts/verify-contrast.mjs --selftest` passes.
- Early contrast smoke (aurora + clouds now both live): `--cdp-screenshot` at both viewports exits 0.
- Capture evidence PNGs of a normal 1440x900 frame (aurora glow present, left margin) and a reduced-motion 1440x900 frame (aurora at mid-breath) into `.planning/phases/09-living-sky/evidence/` — the full reduced-motion/pause proof battery is 09-03.
</verification>

<success_criteria>
- AMB-03: a breathing, noise-driven, 3-curtain aurora glows faintly in the left margin, throttled to every 4 frames, capped in code at 0.20, with peak luminance strictly below the Milky-Way core at both viewports.
- Aurora composites over the moon and before the clouds; renders zero pixels inside the content column.
- Exactly one new token (--aurora) added, in tokens.css only; single-rAF preserved (scene.ts == 2; aurora.ts == 0); zero new dependencies; no push.
</success_criteria>

<output>
Create `.planning/phases/09-living-sky/09-02-SUMMARY.md` when done.
</output>
