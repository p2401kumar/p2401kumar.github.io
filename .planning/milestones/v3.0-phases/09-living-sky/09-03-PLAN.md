---
phase: 09-living-sky
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - src/lib/nightsky/clouds.ts
  - src/lib/nightsky/aurora.ts
  - src/lib/nightsky/scene.ts
  - .planning/phases/09-living-sky/ambient-soak/ambient-soak.mjs
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [AMB-05]

must_haves:
  truths:
    - "With all four ambient systems + glass live, idle CPU stays < 10% total at ~60fps with zero long tasks."
    - "The mobile degradation ladder sheds far clouds -> throttles aurora -> drops the chromatic nudge in that order; parallax never sheds."
    - "Under reduced motion the scene renders exactly one static frame; hidden / fig01-active / reduced-motion all fully stop ambient (capture-pair identical)."
    - "Both viewports pass the screenshot contrast gate (all surfaces >= 4.5) and the aurora luminance gate; the canvas transform is 'none' at rest and mid-nudge."
  artifacts:
    - .planning/phases/09-living-sky/ambient-soak/ambient-soak.mjs
    - .planning/phases/09-living-sky/battery/
  key_links:
    - "scene.ts computes the device tier at adoptLayer0 and pushes it to cloudsHandle.setFarShed / auroraHandle.setShapeThrottle / chromaticNudgeEnabled."
    - "single-rAF counts: scene.ts == 2; clouds.ts / aurora.ts / parallax.ts == 0."
    - "REQUIREMENTS AMB-01..05 Complete + ROADMAP Phase 9 [x] recorded after the battery passes."
---

<objective>
Close AMB-05: implement the documented mobile degradation ladder, run the full closing battery with ALL ambient + glass live (re-proving the <10% idle floor and every carry-forward gate), capture the reduced-motion "beautiful still" and pause-machine proofs, and mark the phase complete.

Purpose: Prove the living sky costs what the budget projected (0.3-0.7pp), never breaks contrast or the pause machine, degrades gracefully on constrained devices, and renders exactly one static frame under reduced motion.

Output: the tier heuristic across clouds/aurora/scene, a Phase-9 ambient soak driver, the full evidence battery, and the requirements/roadmap/state close-out.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-living-sky/09-CONTEXT.md
@.planning/phases/09-living-sky/09-RESEARCH.md
@.planning/phases/09-living-sky/09-UI-SPEC.md

# Live code + the soak/gate drivers this plan extends or reuses
@src/lib/nightsky/scene.ts
@src/lib/nightsky/clouds.ts
@src/lib/nightsky/aurora.ts
@scripts/verify-contrast.mjs
@scripts/verify-banding.mjs
@.planning/phases/08-glass-system/glass-reproof/real-soak.mjs
@.planning/phases/09-living-sky/09-01-SUMMARY.md
@.planning/phases/09-living-sky/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implement the documented mobile degradation ladder (far-cloud shed -> aurora throttle -> chromatic-nudge drop; parallax never sheds)</name>
  <files>src/lib/nightsky/scene.ts, src/lib/nightsky/clouds.ts, src/lib/nightsky/aurora.ts</files>
  <behavior>
    - The tier is computed from viewport width and (feature-detected) navigator.deviceMemory, recomputed on every adopt/resize.
    - Tier 1 sheds the far cloud layer; tier 2 additionally throttles aurora shape repaint to every 8-10 frames; tier 3 additionally drops the scintillation chromatic nudge.
    - Parallax is never affected at any tier.
    - deviceMemory absence (Safari/iOS) falls back cleanly to width-only thresholds.
  </behavior>
  <action>
Centralize the tier decision in scene.ts (which already owns adopt/resize) and push it to the seams 09-01/09-02 exposed. Document the heuristic in-code and in the SUMMARY.

1. In `src/lib/nightsky/scene.ts`, add a `computeAmbientTier(cssWidth)` helper returning 0-3 from the LOCKED ladder (reuse the project's existing responsive boundaries where they line up):
   - Tier >= 1 when `cssWidth < 640` OR (`navigator.deviceMemory` present AND `<= 4`).
   - Tier >= 2 when `cssWidth < 480` OR (`deviceMemory` present AND `<= 2`).
   - Tier >= 3 when `cssWidth < 390` OR (`deviceMemory` present AND `<= 2` AND tier 2 already active).
   Feature-detect `deviceMemory` (`typeof navigator.deviceMemory === 'number'`); when absent, use width-only. In `adoptLayer0` (after `resize`, before `renderStaticFrame`), compute the tier and apply it: `cloudsHandle?.setFarShed(tier >= 1); auroraHandle?.setShapeThrottle(tier >= 2 ? 9 : 4); chromaticNudgeEnabled = tier < 3;`. Add a comment block documenting the ladder order and that PARALLAX NEVER SHEDS (it is not referenced here).

2. Confirm the 09-01 `cloudsHandle.setFarShed` seam skips the far layer in BOTH `draw` and `drawStatic`, and the 09-02 `auroraHandle.setShapeThrottle` seam only changes the shape-repaint cadence (brightness sine stays per-frame). If either seam is missing, add it now. The `chromaticNudgeEnabled` module flag from 09-01 gates the 3-star saturation nudge only.
  </action>
  <verify>
    <automated>npm run build && npx astro check && test "$(grep -o "computeAmbientTier" src/lib/nightsky/scene.ts | wc -l)" -ge 1 && test "$(grep -o "setFarShed" src/lib/nightsky/clouds.ts | wc -l)" -ge 1 && test "$(grep -o "setShapeThrottle" src/lib/nightsky/aurora.ts | wc -l)" -ge 1 && test "$(grep -o "deviceMemory" src/lib/nightsky/scene.ts | wc -l)" -ge 1 && test "$(grep -o "requestAnimationFrame" src/lib/nightsky/scene.ts | wc -l)" -eq 2</automated>
  </verify>
  <done>Build green, astro check clean. The tier helper + all three seams exist, deviceMemory is feature-detected, and single-rAF is preserved (scene.ts == 2). Parallax has no tier reference.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run the FULL closing battery (ambient soak <10%, contrast + aurora + moon gates both viewports, banding, Lighthouse, single-rAF, zero-hex, reduced-motion still, pause-machine + canvas-transform + leak proofs) and record evidence</name>
  <files>.planning/phases/09-living-sky/ambient-soak/ambient-soak.mjs, .planning/phases/09-living-sky/battery/</files>
  <behavior>
    - A Phase-9 soak driver measures ambient-off (reduced-motion) vs ambient-on (no-preference), both with glass live, and reports TOTAL and MARGINAL CPU.
    - Every carry-forward gate passes at both reference viewports with clouds + aurora live.
    - Reduced-motion and each pause condition produce byte-identical capture pairs (no motion leak); the canvas transform stays 'none'.
    - All numeric results + screenshots are written under the battery evidence dir.
  </behavior>
  <action>
Build once, serve the preview, then run every gate and record its output. Write JSON reports + PNGs under `.planning/phases/09-living-sky/battery/` and a `battery-summary.md` table. The screenshot contrast gate at BOTH viewports is the arbiter (09-CONTEXT).

1. Create `.planning/phases/09-living-sky/ambient-soak/ambient-soak.mjs` by adapting `.planning/phases/08-glass-system/glass-reproof/real-soak.mjs` (reuse its launchChrome/Cdp/connectCdp/sampleTreeCpuMs/runSoak scaffolding VERBATIM — zero new deps, node built-ins only; do NOT edit the locked 08 file). Change the two runs so glass stays live throughout and AMBIENT is what toggles: BASELINE emulates `prefers-reduced-motion: reduce` via `Emulation.setEmulatedMedia` (the canvas rAF stops -> one static frame, glass still live); AMBIENT run emulates `prefers-reduced-motion: no-preference` (all four ambient systems live + glass). Report MARGINAL = ambient - baseline (vs the 0.3-0.7pp projection) and TOTAL = ambient-run cpuPct (must be < 10%). 1440x900 DPR1, 60s per run, headless=new software raster; keep the whole-tree CPU cross-check. Update the readiness wait to also confirm the ambient handles are live.

2. Run the battery (npm run build, then npm run preview serving dist), each result saved to the battery dir:
   - SOAK: `node .planning/phases/09-living-sky/ambient-soak/ambient-soak.mjs` — assert TOTAL cpuPct < 10, fps >= ~58 (target 60.0), longTasks == 0; record MARGINAL vs 0.3-0.7pp.
   - CONTRAST GATE (arbiter): `node scripts/verify-contrast.mjs --cdp-screenshot --width 1440 --height 900` AND `--width 1280 --height 800` — both exit 0; inspect `failing[]` for experience/patents/skills (tall scroll-swept panels, Pitfall 2).
   - AURORA LUMINANCE: `node scripts/verify-contrast.mjs --aurora --width 1440 --height 900` AND `--width 1280 --height 800` — both exit 0; plus static ceiling guard `test "$(grep -oE "AURORA_ALPHA_CEILING\s*=\s*0\.20" src/lib/nightsky/aurora.ts | wc -l)" -ge 1`.
   - MOON UNBROKEN: `node scripts/verify-contrast.mjs --moon --width 1440 --height 900` AND `--width 1280 --height 800` exit 0; `node scripts/verify-contrast.mjs --selftest` passes.
   - BANDING: `node scripts/verify-banding.mjs --selftest` passes; then capture CDP screenshot crops of the cloud band + the aurora region at 1440x900 and run the comb-spike detector against those live crops (a NEW capture path — verify-banding currently targets committed image masters); assert no comb-spike over the clean threshold. Keep it bounded; save the crops as evidence.
   - LIGHTHOUSE: run both presets (mobile + desktop) locally against `npm run preview` (never the deployed site) — assert all four categories >= 90; record scores.
   - SINGLE-rAF: `test "$(grep -o "requestAnimationFrame" src/lib/nightsky/scene.ts | wc -l)" -eq 2` and `test "$(grep -oE "requestAnimationFrame" src/lib/nightsky/clouds.ts src/lib/nightsky/aurora.ts src/lib/nightsky/parallax.ts | wc -l)" -eq 0`.
   - ZERO-HEX: `test "$(grep -oE "#[0-9a-fA-F]{3,8}" src/lib/nightsky/clouds.ts src/lib/nightsky/aurora.ts src/lib/nightsky/parallax.ts src/lib/nightsky/idle-queue.ts | wc -l)" -eq 0` and confirm the only new CSS hex is `--aurora` in tokens.css.
   - SOURCE-OVER ONLY: `test "$(grep -o "lighter" src/lib/nightsky/clouds.ts src/lib/nightsky/aurora.ts | wc -l)" -eq 0`.
   - REDUCED-MOTION STILL ("the beautiful still"): via CDP `Emulation.setEmulatedMedia` prefers-reduced-motion: reduce, capture two screenshots ~2s apart and assert byte-identical (no motion leak); save the still as evidence.
   - PAUSE-MACHINE PROOF: for each of hidden-tab (Emulation/visibility), fig01-active (navigate `#fig-01`), and reduced-motion, capture a ~2s-apart pair and assert identical (ambient fully stopped) — reuse the capture-pair technique.
   - CANVAS NEVER TRANSFORMED: assert `getComputedStyle(document.querySelector('#nightsky-canvas')).transform === 'none'` AND for `.nightsky-host` at rest AND during a panel-change nudge (trigger a hash change, sample within the 420ms window).
   - LEAK GATE: assert the nightsky ambient script is not present on `/work/*` case-study routes or `/404` (reuse the prior-phase leak-gate technique against the built output).
  </action>
  <verify>
    <automated>npm run build && npx astro check && node scripts/verify-contrast.mjs --selftest && node scripts/verify-banding.mjs --selftest && test "$(grep -oE "requestAnimationFrame" src/lib/nightsky/clouds.ts src/lib/nightsky/aurora.ts src/lib/nightsky/parallax.ts | wc -l)" -eq 0 && test "$(grep -o "requestAnimationFrame" src/lib/nightsky/scene.ts | wc -l)" -eq 2 && test "$(grep -oE "#[0-9a-fA-F]{3,8}" src/lib/nightsky/clouds.ts src/lib/nightsky/aurora.ts src/lib/nightsky/parallax.ts src/lib/nightsky/idle-queue.ts | wc -l)" -eq 0 && test "$(grep -o "lighter" src/lib/nightsky/clouds.ts src/lib/nightsky/aurora.ts | wc -l)" -eq 0 && test -f .planning/phases/09-living-sky/ambient-soak/ambient-soak.mjs</automated>
  </verify>
  <done>Soak TOTAL < 10% at ~60fps / 0 long tasks (MARGINAL recorded vs 0.3-0.7pp). Contrast gate + aurora gate + moon gate pass BOTH viewports; banding selftest + live-crop spot check clean; Lighthouse >= 90 x4 both presets; single-rAF (scene 2, ambient 0), zero-hex, source-over-only all hold; reduced-motion + all three pause conditions produce identical capture pairs; canvas transform is 'none' at rest and mid-nudge; leak gate clean. Evidence written under the battery dir.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Close out — mark AMB-01..05 Complete, ROADMAP Phase 9 done, update STATE, commit docs</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <behavior>
    - All five AMB requirements read Complete; Phase 9 reads done with its plan checkboxes ticked; STATE reflects the completed phase and the measured ambient budget.
  </behavior>
  <action>
Only after the full battery passes:

1. In `.planning/REQUIREMENTS.md`, check the AMB-01..AMB-05 boxes and set their status rows to Complete.
2. In `.planning/ROADMAP.md`, mark Phase 9 `[x]`, set its Plans line to `3/3 plans complete`, and tick the plan checkboxes.
3. In `.planning/STATE.md`, record Phase 9 complete: the measured ambient TOTAL and MARGINAL CPU (vs the 0.3-0.7pp projection and the 3.9pp headroom), the new `--aurora` token, the aurora over-moon/before-clouds draw order, the mobile-ladder heuristic actually shipped, and the single-rAF/pause invariants re-proved. Note any residual tuning items for Phase 10 integration.
4. Commit the docs (`gsd_run query commit` or the workflow's commit step). NEVER push.
  </action>
  <verify>
    <automated>test "$(grep -oE "AMB-0[1-5].*Complete" .planning/REQUIREMENTS.md | wc -l)" -ge 5 && test "$(grep -o "3/3 plans complete" .planning/ROADMAP.md | wc -l)" -ge 1</automated>
  </verify>
  <done>AMB-01..05 Complete; ROADMAP Phase 9 [x] with 3/3 plans; STATE updated with the ambient budget + invariants; docs committed, not pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pause machine {hidden, fig01, reduced} -> ambient | All four systems must fully stop under every pause condition; verified by capture-pair diffs, not documentation. |
| device tier -> render load | Constrained devices must shed load in the locked order without ever shedding parallax. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-07 | Denial of Service (availability) | ambient never stops on pause | high | mitigate | Capture-pair proof for hidden / fig01-active / reduced-motion (byte-identical frames), plus single-rAF grep counts; ambient rides scene.ts's gated loop only. |
| T-09-08 | Denial of Service (constrained devices) | mobile load | medium | mitigate | Documented degradation ladder (far cloud shed -> aurora throttle -> chromatic drop) recomputed per resize; parallax never sheds; validated by narrow-width capture in the battery. |
| T-09-09 | Information Disclosure (legibility) | full composite contrast | high | mitigate | --cdp-screenshot arbiter at both viewports with all ambient live; failing[] inspected on tall scroll-swept panels. |
| T-09-SC | Tampering (supply chain) | npm installs | n/a | accept | Zero package installs this plan; the soak driver reuses existing node built-ins only (package.json/.planning/config.json untouched). |
</threat_model>

<verification>
The task <verify> blocks plus the plan's own battery ARE the verification. Summary of the gate set (all must pass, both reference viewports where applicable): build + astro check; ambient soak TOTAL < 10% / ~60fps / 0 long tasks; `--cdp-screenshot`; `--aurora`; `--moon`; `--selftest`; banding selftest + live-crop spot check; Lighthouse >= 90 x4 both presets; single-rAF (scene 2 / ambient 0); zero-hex; source-over-only; reduced-motion still + hidden/fig01/reduced pause capture-pairs identical; canvas transform 'none' at rest and mid-nudge; leak gate clean. Evidence lands in `.planning/phases/09-living-sky/battery/`.
</verification>

<success_criteria>
- AMB-05: all four ambient systems ride the single rAF + existing pause machine; the documented mobile ladder sheds load in order (parallax never sheds); reduced motion renders exactly one static frame.
- Idle CPU re-proves < 10% total with all ambient + glass live; contrast + aurora luminance gates pass both viewports; every carry-forward floor (single-rAF, zero-hex, source-over, canvas-never-transformed, leak gate, Lighthouse) re-passes.
- AMB-01..05 marked Complete, Phase 9 recorded done, docs committed; no push, no new dependencies.
</success_criteria>

<output>
Create `.planning/phases/09-living-sky/09-03-SUMMARY.md` when done.
</output>
