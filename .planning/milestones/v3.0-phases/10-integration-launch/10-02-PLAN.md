---
phase: 10-integration-launch
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - .planning/phases/10-integration-launch/10-02-lighthouse-scores-live.md
  - .planning/phases/10-integration-launch/10-02-SUMMARY.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [FLR-01, FLR-02, FLR-03, LNC-01]

must_haves:
  truths:
    - "The deploy proceeds ONLY after explicit user approval in chat — no `git push` exists anywhere before the human gate; the blocking checkpoint must NEVER be auto-resolved under autonomous mode (LNC-01)"
    - "origin/main fast-forwards to local main (verified ancestor, never --force) and the GitHub Actions Pages deploy (build + deploy jobs) concludes green (LNC-01)"
    - "The live site at https://p2401kumar.github.io/ serves the v3.0 real-sky composite: view-source shows body.has-sky, the deck + 7 panels, the sky <picture>/preload + .nightsky-host/#nightsky-canvas, the nightsky chunk, and the CC BY 4.0 credit line (FLR-02)"
    - "Live Lighthouse >=90 in all four categories on BOTH mobile and desktop, recorded to the phase dir as FLR-01's authoritative evidence (FLR-01)"
    - "Live smoke passes over the live origin: /, /#fig-01 (pause seed), /#work (alias), both /work/* case-study pages, /404, and the credit line's two links resolve (FLR-02, FLR-03)"
    - "FLR-01, FLR-02, FLR-03, LNC-01 marked Complete; ROADMAP Phase 10 marked complete + the v3.0 milestone line updated; STATE updated; 10-02-SUMMARY.md written with the checkpoint-resolution record verbatim"
  artifacts:
    - .planning/phases/10-integration-launch/10-02-lighthouse-scores-live.md
    - .planning/phases/10-integration-launch/10-02-SUMMARY.md
  key_links:
    - "The 10-02 Task 1 blocking-human approval gate precedes any git push — the ONLY push in the entire phase lives in Task 2, after the user's explicit affirmative in chat"
    - "git merge-base --is-ancestor origin/main main verifies fast-forward safety before push (currently true: ~62 commits ahead, origin/main is an ancestor); deploy.yml fires on push to main and publishes via actions/deploy-pages@v5"
    - "Live Lighthouse against https://p2401kumar.github.io/ is FLR-01's authoritative evidence — it structurally cannot be pre-verified locally, so it is the one gate deferred out of 10-01"
---

<objective>
Launch v3.0 "Real Sky" to production, gated on an explicit human go decision. Present the 10-01 launch-readiness pack for the user's go/no-go; then, ONLY on an explicit affirmative in chat, fast-forward push to origin/main (the push IS the deploy), watch the GitHub Actions Pages run to completion, verify the live site, record live Lighthouse as FLR-01's authoritative evidence, run the live smoke, and close out FLR-01..03 + LNC-01 / ROADMAP / STATE / SUMMARY.

Purpose: replacing the live professional site is outward-facing publication — the user personally gated the equivalent v1 and v2 decisions. Autonomous mode must NOT auto-resolve this gate (10-CONTEXT.md LOCKED decision: the push replaces the live v2.0 Night Sky with v3.0 Real Sky). This launch replaces a ~1-day-old v2.0 — same substance, real sky; framed honestly, no drama.
Output: a live v3.0 deploy, `10-02-lighthouse-scores-live.md`, `10-02-SUMMARY.md`, and completed FLR-01..03 + LNC-01.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-integration-launch/10-CONTEXT.md

# The go/no-go pack this plan presents (produced by 10-01)
@.planning/phases/10-integration-launch/10-01-LAUNCH-READINESS.md

# The PROVEN v2.0 gated-launch flow this plan mirrors (checkpoint language is
# the template: "REQUIRES EXPLICIT USER APPROVAL IN CHAT / MUST NEVER BE
# AUTO-RESOLVED UNDER AUTONOMOUS MODE"):
@.planning/milestones/v2.0-phases/06-integration-launch/06-02-PLAN.md

# The launch pipeline (push-to-main = deploy) + live-evidence format precedent
@.github/workflows/deploy.yml
@.planning/phases/09-living-sky/battery/battery-summary.md

# Requirements + roadmap to close out
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: BLOCKING human go/no-go — present the launch-readiness pack and require explicit user approval before any deploy</name>
  <action>
Blocking human go/no-go gate — the executor pauses here and does NOT act autonomously. Present the 10-01 launch-readiness pack to the user in chat and request an explicit go/no-go decision. THIS CHECKPOINT REQUIRES EXPLICIT USER APPROVAL IN CHAT AND MUST NEVER BE AUTO-RESOLVED UNDER AUTONOMOUS MODE — `workflow.auto_advance` / `/gsd-autonomous` standing directives do NOT apply here, because this deploy replaces the live v2.0 Night Sky with v3.0 Real Sky: outward-facing publication of the user's professional site (10-CONTEXT.md LOCKED decision). No `git push`, deploy, or Pages trigger may occur before the user's explicit affirmative; there is no push anywhere in this plan before Task 2. Proceed to Task 2 ONLY on an explicit affirmative from the user's own message.

Offer the same option shape the user chose last time — the orchestrator asks in chat:
- **launch now** — proceed to Task 2 immediately.
- **fix-listed-items-then-launch** — address specific fix-forward items first (halts this phase with nothing deployed; re-enter after the fixes).
- **hold** — everything stays local, the live site remains v2.0.
  </action>
  <what-built>
10-01 produced the complete LOCAL proof that v3.0 is launch-ready: FLR-02 (the full carry-forward regression battery re-passed on the composited build — embedded Fig. 01 36-check audit with the full ambient paused behind it, deck mechanics, no-JS classic WITH the photo, view-classic, case-study scene-free leak gate, sitemap 3 routes, /#work alias, cold-/#fig-01 pause seed, single-rAF, zero-hex, contrast/aurora/moon/banding, soak <10%), FLR-03 (reduced-motion full-stop, keyboard operability, honesty gate intact), and the local FLR-01 Lighthouse pre-flight (>=90 x4 both presets) — all aggregated into `.planning/phases/10-integration-launch/10-01-LAUNCH-READINESS.md`, plus the documented LNC-01 rollback path (tags v1.0/v2.0).
  </what-built>
  <how-to-verify>
Present `10-01-LAUNCH-READINESS.md` to the user in chat, leading with the before/after hero shots (v2-live vs v3-local) and the aggregate gate table so the call takes 60 seconds: what changes live (the live professional site flips from the v2.0 procedural night sky to the v3.0 real-sky composite — real Milky Way photo + frosted glass + living ambient — this REPLACES it), the gate table with numbers, the screenshot index (featuring the reduced-motion still), the rollback plan (tags v1.0/v2.0), and the carried real-device checklist incl. the v3 additions. State plainly that the only gate not yet proven is LIVE Lighthouse (proven post-deploy in Task 2), and that everything else is green and inside the Phase-9 reference families.

THIS CHECKPOINT REQUIRES EXPLICIT USER APPROVAL IN CHAT AND MUST NEVER BE AUTO-RESOLVED UNDER AUTONOMOUS MODE. `workflow.auto_advance` / `/gsd-autonomous` standing directives do NOT apply here — this is an outward-facing publication that replaces the live v2.0 site, and the user personally gates it (10-CONTEXT.md LOCKED decision). Do not proceed to Task 2 on anything other than an explicit affirmative from the user's own message. NO `git push`, deploy, or Pages trigger may occur before this approval — there is no push anywhere in this plan prior to Task 2.
  </how-to-verify>
  <resume-signal>User types an explicit affirmative in chat (e.g. "launch now" / "approved" / "go" / "deploy"). "fix-then-launch", "hold", any hesitation, question, or "not yet" halts the phase with nothing deployed. Autonomous auto-resolution is forbidden.</resume-signal>
  <done>The user has made an explicit go/no-go decision in chat. On an explicit affirmative (launch now), Task 2 may run. On anything else (fix-then-launch, hold, a question, "not yet", silence, or an autonomous auto-advance attempt), the phase halts with nothing pushed or deployed.</done>
</task>

<task type="auto">
  <name>Task 2: Post-approval launch — fast-forward push (never --force), watch the Actions deploy, live-verify, record live Lighthouse (FLR-01 authoritative), close out FLR-01..03 + LNC-01</name>
  <files>.planning/phases/10-integration-launch/10-02-lighthouse-scores-live.md, .planning/phases/10-integration-launch/10-02-SUMMARY.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
Runs ONLY after Task 1's explicit user approval in chat. If Task 1 was not explicitly approved by the user, STOP — do not run any step of this task.

1. Fast-forward safety gate (BEFORE any push): run `git fetch origin` then `git merge-base --is-ancestor origin/main main`. This MUST exit 0 (origin/main is an ancestor of local main — a clean fast-forward; currently ~62 commits ahead). If it does not exit 0, STOP and report; never rebase/force to make it pass. NEVER use `git push --force` / `--force-with-lease` anywhere.

2. Deploy: `git push origin main` (plain fast-forward push — this IS the deploy; deploy.yml fires on push to main). Capture the pushed HEAD sha (`git rev-parse HEAD`).

3. Watch the Actions run to completion: `gh run list --branch main --limit 5 --json databaseId,headSha,status,conclusion,workflowName`, match `headSha` to the pushed HEAD, then `gh run watch <databaseId> --exit-status` (exits nonzero if the run fails). Assert BOTH the build and deploy jobs conclude `success`. Record the run id + conclusion.

4. Live verification (against https://p2401kumar.github.io/ — allow for CDN propagation; poll/retry a few times if the old v2 is briefly still cached):
   - View-source sanity: `curl -s https://p2401kumar.github.io/` shows body.has-sky, the deck markup (`class="deck"`, `class="panel"` x>=7, `id="deck-live"`), the sky photo (`<picture>` + the `/sky/milky-way-1920.avif` preload) and the night-sky host (`class="nightsky-host"`, `id="nightsky-canvas"`), the nightsky chunk (the deck sentinel `nightsky:panel-change` resolves in a referenced `/_astro/*.js` chunk), and the CC BY 4.0 credit line.
   - Live Lighthouse: `npx lighthouse` against the live URL, mobile (default preset) AND desktop (--preset=desktop), extract all four category scores + LCP/CLS/TBT each, and WRITE them to `10-02-lighthouse-scores-live.md` (format per battery-summary.md — PASS/FAIL table + scores table, noting these are the LIVE authoritative FLR-01 numbers that replace 10-01's local pre-flight; reference family: mobile 99/100/100/100, desktop 100x4). Every category must be >=90 on both presets.
   - Live smoke over the live origin: `/` (200 + deck), both `/work/*` case studies (200), `/404` (a nonexistent path serves the 404 page). For the client-side hash routes — `/#fig-01` (deep-link cold-load inits the figure, ambient paused seed) and `/#work` (alias -> systems panel) — run a lightweight headless CDP smoke against the live URL (reuse 10-01's scratchpad driver pointed at the live origin); if a live remote CDP deep-link run is flaky under CDN propagation, record the HTTP-200 + view-source deck presence as the live floor and note that the hash behavior is proven on the byte-identical build in 10-01 (do NOT block the launch on remote CDP flakiness). Confirm the credit line's two links resolve: `curl -sI` (or -o /dev/null -w '%{http_code}') on https://noirlab.edu/public/images/noirlab2430b/ and https://creativecommons.org/licenses/by/4.0/ each return < 400.

5. Failure handling (FLR-01 live Lighthouse): if ANY category is <90 on the live run, RECORD the scores and the responsible audit(s) in 10-02-lighthouse-scores-live.md, then STOP and report to the user. Do NOT auto-revert — the site is functionally verified (smoke + view-source pass); a scoring shortfall is a fix-forward decision for the user, not an automatic rollback (10-CONTEXT.md v2 rule). If the Actions run itself FAILS, STOP and report; Pages keeps serving the previous (v2.0) deploy — no manual surgery needed.

6. Close out (only after live verification passes): mark REQUIREMENTS.md FLR-01, FLR-02, FLR-03, LNC-01 as Complete (both the checkbox lines AND the Traceability table rows). Mark ROADMAP.md Phase 10 complete (the `- [ ] **Phase 10...`  checkbox -> `[x]`, the Plans count + plan boxes, the progress/detail row -> Complete + completion date 2026-07-19) AND update the v3.0 "Real Sky" milestone line to shipped. Update STATE.md (current position, status, last activity, progress). Use scoped `Edit` for each of these existing files — NEVER a whole-file Write. Write 10-02-SUMMARY.md (template per execution_context) capturing: the deploy run id, live Lighthouse scores, live smoke results, the rollback path left available, AND the checkpoint-resolution record verbatim (the exact user affirmative from Task 1, timestamped — the audit trail that the blocking gate was human-resolved, not auto-advanced).

7. Final docs commit + one more FF push: commit the doc/evidence updates locally (`gsd_run query commit` or the GSD workflow's commit path). A SECOND fast-forward push of these docs to origin/main is acceptable and expected (docs keep the remote's planning history current) — perform it as a plain fast-forward push (re-verify `git merge-base --is-ancestor origin/main main` first; NEVER --force) and note in the summary that this second push is docs-only.

Do NOT modify `package.json`, `package-lock.json`, or `.planning/config.json` at any point.
  </action>
  <verify>
    <automated>
set -e
# Fast-forward safety must hold (origin/main is an ancestor of local main)
git fetch origin >/dev/null 2>&1
git merge-base --is-ancestor origin/main main
# The deploy push landed: origin/main now equals local main (fast-forward completed)
test "$(git rev-parse origin/main)" = "$(git rev-parse main)"
# Live site serves the v3 real-sky deck + scene (view-source sanity)
curl -s https://p2401kumar.github.io/ | grep -q 'id="deck-live"'
test "$(curl -s https://p2401kumar.github.io/ | grep -oE 'class="panel"' | wc -l | tr -d ' ')" -ge 7
curl -s https://p2401kumar.github.io/ | grep -q 'nightsky-host'
curl -s https://p2401kumar.github.io/ | grep -q '/sky/milky-way-1920.avif'
curl -s https://p2401kumar.github.io/ | grep -q 'CC BY 4.0'
# Live 404 serves
test "$(curl -s -o /dev/null -w '%{http_code}' https://p2401kumar.github.io/this-path-does-not-exist)" = "404"
# Both case studies serve live
test "$(curl -s -o /dev/null -w '%{http_code}' https://p2401kumar.github.io/work/dynamodb-cellularization/)" = "200"
test "$(curl -s -o /dev/null -w '%{http_code}' https://p2401kumar.github.io/work/elb-auto-weight-away/)" = "200"
# Live Lighthouse evidence recorded (authoritative FLR-01)
LH=.planning/phases/10-integration-launch/10-02-lighthouse-scores-live.md
grep -qiE 'mobile' "$LH" && grep -qiE 'desktop' "$LH"
# Requirements + roadmap closed out
test "$(grep -oE 'FLR-0[1-3].*(Complete|complete)' .planning/REQUIREMENTS.md | wc -l | tr -d ' ')" -ge 3
grep -qiE 'LNC-01.*(Complete|complete)' .planning/REQUIREMENTS.md
grep -qiE 'Phase 10.*(Complete|complete|\[x\])' .planning/ROADMAP.md
test -f .planning/phases/10-integration-launch/10-02-SUMMARY.md
# Checkpoint-resolution record present verbatim in the summary
grep -qiE 'approv|launch now|explicit affirmative' .planning/phases/10-integration-launch/10-02-SUMMARY.md
# No new deps / config untouched
test "$(git diff HEAD -- package.json package-lock.json .planning/config.json | wc -l | tr -d ' ')" -eq 0
echo "TASK 2 PASS"
    </automated>
  </verify>
  <done>After explicit user approval: origin/main fast-forwarded to local main (verified ancestor, no --force), the Actions Pages run (build + deploy) concluded success, the live site serves the v3.0 real-sky composite (view-source: has-sky, deck, 7 panels, sky picture/preload, nightsky host/canvas + chunk, CC BY 4.0 credit), live Lighthouse >=90 x4 on both presets is recorded as FLR-01's authoritative evidence, live smoke (/, /#fig-01 pause seed, /#work alias, both /work/*, /404, credit links resolve) passes, FLR-01..03 + LNC-01 are Complete, ROADMAP Phase 10 is complete + the v3.0 milestone line updated, STATE is updated, and 10-02-SUMMARY.md is written with the checkpoint-resolution record verbatim. If the Actions run failed, STOPPED with Pages serving v2.0; if live Lighthouse fell short, scores recorded and STOPPED for a user fix-forward decision without auto-reverting. package.json/lock and config.json untouched; never --force.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local main -> origin/main (GitHub) | The push IS the deploy and REPLACES the live v2.0 professional site. Crossed exactly once, in Task 2, only after Task 1's explicit human approval. |
| CDN / live origin | Post-deploy the live URL is the authoritative FLR-01 evidence surface; verified before close-out. |
| deploy pipeline -> package/config | A launch must add no dependency and flip no autonomous flag; package.json/lock and config.json stay byte-identical. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Elevation/Integrity | premature / auto deploy | critical | mitigate | Task 1 is a blocking-human gate that must never auto-resolve (autonomous ignored — verbatim v2 language); NO push exists anywhere before it; Task 2 runs only on the user's explicit affirmative. |
| T-10-02 | Tampering | force-push clobbers remote history / loses v2 | high | mitigate | `git merge-base --is-ancestor origin/main main` verified before EACH push; plain fast-forward push only, never `--force`/`--force-with-lease`; tags v1.0 + v2.0 on origin preserved as rollback anchors. |
| T-10-03 | Denial of Service (credibility) | deploying a broken/regressed live site | high | mitigate | 10-01 proved every gate but live Lighthouse locally; Task 2 view-source + live smoke + live Lighthouse confirm the deploy; a live <90 stops for a user fix-forward decision (no silent ship); an Actions failure stops with Pages still serving v2.0. |
| T-10-04 | Tampering | `package.json` / `.planning/config.json` | low | mitigate | Plan never touches them (verify asserts a byte-identical diff); the human gate's non-auto-resolution does not depend on a config flip. |
| T-10-SC | Tampering | transient `npx lighthouse` / `gh` CLI | low | mitigate | Both are transient audit/ops tools (Phase 4/5/7/8/9 precedent); no new dependency added to package.json. |
</threat_model>

<verification>
- No `git push` occurred before Task 1's explicit user approval (there is none in the plan text before Task 2); the blocking gate was human-resolved, recorded verbatim in the summary.
- Fast-forward only: `git merge-base --is-ancestor origin/main main` exits 0 before each push; no `--force` anywhere; origin/main == local main after the deploy push.
- Actions run for the pushed sha concluded `success` (build + deploy).
- Live: v3 composite served (view-source has-sky + deck + 7 panels + sky picture/preload + nightsky host/canvas + chunk + credit), live Lighthouse >=90 x4 both presets recorded, live smoke (/, /#fig-01, /#work, both /work/*, /404, credit links) passes.
- FLR-01..03 + LNC-01 Complete; ROADMAP Phase 10 complete + v3.0 milestone line updated; STATE updated; 10-02-SUMMARY.md written; package.json/lock + config.json untouched.
</verification>

<success_criteria>
- The deploy happened only after explicit human approval, as a verified fast-forward, and the live v3.0 site is up and verified.
- FLR-01 is closed with LIVE Lighthouse evidence; FLR-02, FLR-03, LNC-01 are marked Complete; the milestone requirements are fully mapped and done.
- A rollback path (revert + fast-forward push; tags v1.0 + v2.0) remains documented and available; Pages served v2.0 throughout if the run had failed.
</success_criteria>

<output>
Create `.planning/phases/10-integration-launch/10-02-SUMMARY.md` when done.
</output>
