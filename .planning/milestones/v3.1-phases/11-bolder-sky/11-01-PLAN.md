---
phase: 11-bolder-sky
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build-sky.mjs
  - src/components/NightSky.astro
  - public/sky/milky-way-2560.avif
  - public/sky/milky-way-1920.avif
  - public/sky/milky-way-2560.webp
  - public/sky/milky-way-1920.webp
  - public/sky/milky-way-lqip.txt
autonomous: true
requirements:
  - BOLD-01
  - BOLD-02

must_haves:
  truths:
    - "The regenerated master reads warm + bright: the amber galactic core keeps its gold, the cool arms stay blue, and the midtones are lifted out of the murk (no cool-slate wash over everything)."
    - "The core LEADS the composition and the frame is filled at 1440/1280; the 375 mobile tier shows real sky/core presence, not an empty navy gradient."
    - "No visible banding in the regenerated masters — the lifted-midtone regrade re-passes the banding gate (selftest + all four committed masters)."
  artifacts:
    - "scripts/build-sky.mjs — regraded pipeline (reduced desaturation, warmed/neutralized tint, lifted mids, reduced/removed column vignette) + recomputed crop anchor."
    - "public/sky/milky-way-{2560,1920}.{avif,webp} — regenerated masters."
    - "public/sky/milky-way-lqip.txt — regenerated LQIP data URI."
    - "src/components/NightSky.astro — object-position responsive ladder recomputed so every tier frames the core."
  key_links:
    - "build-sky.mjs grade constants -> the baked master -> NightSky.astro object-position window (the crop the visitor actually sees)."
    - "lifted midtones -> anti-banding grain amplitude (GRAIN_AMP) -> verify-banding gate: lifted mids are a NEW banding risk that crushed blacks never had."
---

<objective>
Rebuild the *grade* and *composition* of the committed sky master so the real amber
galactic core leads a warm, bright frame — killing the muddy, cool-slate, lopsided,
empty-mobile look diagnosed in the v3.0 UI review. This is a re-grade + re-crop of the
existing `build-sky.mjs` pipeline (BOLD-01, BOLD-02), NOT an engine rebuild. The grade is
baked into the master via `sharp` (the v3.0 no-runtime-filter rule holds — no CSS filters).

Purpose: The current grade is the murk culprit — desaturate x0.35 -> cool slate tint
`#93a7cf` -> black-point crush -> midtone x0.80 darken -> heavy column vignette (alpha 0.88).
It kills the real amber core and dulls the whole frame to grey-blue. The approved mockup
(Direction A "Galactic Core") reproduces its target with `brightness(1.32) contrast(1.12)
saturate(1.42)` + a warm core glow; the real build bakes that same target into the master.

Output: A regraded, recomposed set of masters + LQIP where the amber core is the one warm
event in a cool frame, the composition leads with it, and the banding gate stays green.
Evidence: before/after master crops.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-bolder-sky/11-CONTEXT.md
@.planning/10-UI-REVIEW.md

# The pipeline to rebuild + the current baked-in murk constants
@scripts/build-sky.mjs
# The object-position responsive ladder to recompute (in the <style> block)
@src/components/NightSky.astro
# The banding gate this regrade must re-pass
@scripts/verify-banding.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regrade the master pipeline — warm + bright, out of the murk</name>
  <files>scripts/build-sky.mjs</files>
  <action>
Rebuild the grade stages in build-sky.mjs so the master reproduces the approved mockup
target (per 11-CONTEXT.md recipe item 1, BOLD-01). Four coupled moves:

(1) REDUCE desaturation: raise GRADE_SATURATION from 0.35 toward ~0.6-0.7 so the galactic
core keeps its real amber/gold and the arms keep cool blue — the warm-cool contrast is the
entire point. Do not tint everything into cool slate.

(2) WARM / NEUTRALIZE the tint: the current GRADE_TINT `#93a7cf` (pale blue-slate) washes
the warm core cold. Replace it with a near-neutral or slightly warm target — or drop the
`.tint()` stage entirely and rely on the reduced-desaturation core color — so the amber
core survives. Keep any residual tint subtle. (Update the TOKEN PROVENANCE header note if
you change a value that mirrors tokens.css; the sky-* tokens themselves do NOT change this
task — only the grade constants.)

(3) LIFT midtones: MIDTONE_SCALE is 0.80 today — a darken (out = 0.8*in + 0.2*zenith). Replace
this with a midtone LIFT (bring mids UP, e.g. a mapping whose slope/gamma raises the mid/high
range) while keeping the black point anchored near --sky-zenith so shadows stay clean. The
lift lives in the mids/highs; the black-point map to --sky-zenith may stay.

(4) REDUCE or REMOVE the column vignette: VIGNETTE_ALPHA is 0.88 today. In v3.0 the vignette
existed to protect full-viewport-panel contrast — but glass is now card-scoped (post the
2026-07-19 visibility fix, on .panel-card), so the CARD is the text scrim and the sky should
read bright across the whole frame. Cut the vignette to a light touch or remove it. Contrast
is re-held by the card, proven by the gate in 11-03 — NEVER by darkening the sky.

Leave the download/integrity gate (byte length 16359276 + 4000x2000/srgb metadata), the frame
geometry solver, the seam ramp, and the encode ladder UNTOUCHED. Do not change the source
asset or its integrity constants. Do not wire this into `astro build`.

Then run `node scripts/build-sky.mjs` to regenerate the candidate previews under
.planning/phases/07-real-sky-foundation/spike-banding/ and eyeball the winner master preview.
  </action>
  <verify>
    <automated>test "$(grep -o 'GRADE_SATURATION = 0.35' scripts/build-sky.mjs | wc -l | tr -d ' ')" = "0" && test "$(grep -o 'MIDTONE_SCALE = 0.8' scripts/build-sky.mjs | wc -l | tr -d ' ')" = "0" && test "$(grep -o 'VIGNETTE_ALPHA = 0.88' scripts/build-sky.mjs | wc -l | tr -d ' ')" = "0"</automated>
    <human-check>The regenerated winner preview (spike-banding/A-*-master.jpg) reads warm + bright: amber/gold core, cool-blue arms, lifted mids — not the grey-blue murk.</human-check>
  </verify>
  <done>The three murk constants (desat 0.35, midtone darken 0.80, vignette 0.88) are gone; the regraded pipeline runs clean and the winner preview reads warm + bright.</done>
</task>

<task type="auto">
  <name>Task 2: Recompose — core leads, frame filled, mobile reads as sky; regenerate masters</name>
  <files>scripts/build-sky.mjs, src/components/NightSky.astro, public/sky/milky-way-2560.avif, public/sky/milky-way-1920.avif, public/sky/milky-way-2560.webp, public/sky/milky-way-1920.webp, public/sky/milky-way-lqip.txt</files>
  <action>
Recompose so the amber core LEADS as a bold anchor and the frame is filled (per 11-CONTEXT.md
recipe item 2, BOLD-02; mockup A target `background-position ~74% 42%`).

(A) In build-sky.mjs: retune the winner candidate's crop anchor (`coreAt` / core / theta on the
committed A-core-t20 candidate, or add a new winner candidate and set WINNER to it) so the core
sits where it reads as leading the composition rather than shoved to the dead edge — keep every
frame corner inside the distortion-safe strip (the frameLayout solver already asserts this; if a
new anchor fails to fit, the script throws — respect it, don't relax SAFE_TOP/SAFE_BOTTOM).

(B) In src/components/NightSky.astro <style>: recompute the object-position responsive ladder
(the `.sky-photo img` object-position at the base tier and each @media tier) so EVERY tier frames
the core with real sky/core presence. The mobile tier is the priority: the v3.0 build deliberately
cropped 375w to the QUIET region (`object-position: 10% 70%`) — that is why mobile read as an empty
gradient. Recompose the mobile tier so it still reads as a sky with core presence (honor the
visibility gate's 375w floors, re-checked in 11-03). Remove the `10% 70%` quiet-crop.

Then run `node scripts/build-sky.mjs` to regenerate ALL masters (2560/1920 avif+webp) and the LQIP.
The script auto-downloads the gitignored NOIRLab source if absent (integrity-gated); if the network
fetch fails it throws with the ESO-mirror note — do not silently swap sources.

Exact anchor + ladder numbers are Claude's discretion (11-CONTEXT.md) — reproduce the mockup target.
  </action>
  <verify>
    <automated>test "$(grep -o '10% 70%' src/components/NightSky.astro | wc -l | tr -d ' ')" = "0" && test -s public/sky/milky-way-1920.avif && test -s public/sky/milky-way-2560.avif && test -s public/sky/milky-way-1920.webp && test -s public/sky/milky-way-2560.webp && test -s public/sky/milky-way-lqip.txt</automated>
    <human-check>Full master preview + the 1440 window crop show the core leading and the frame filled; the mobile-tier window shows real sky/core, not a flat navy gradient.</human-check>
  </verify>
  <done>The crop anchor + object-position ladder are recomputed (old mobile quiet-crop removed); all four masters + the LQIP are regenerated and non-empty.</done>
</task>

<task type="auto">
  <name>Task 3: Re-pass the banding gate (lifted-mids risk) + commit the new masters</name>
  <files>scripts/build-sky.mjs, public/sky/milky-way-2560.avif, public/sky/milky-way-1920.avif, public/sky/milky-way-2560.webp, public/sky/milky-way-1920.webp, public/sky/milky-way-lqip.txt</files>
  <action>
Lifted midtones band MORE than crushed blacks did (recipe item 1's explicit warning) — the
anti-banding grain may now be too light. Run the banding gate and re-tune until green:

Run `node scripts/verify-banding.mjs --selftest` (clean control must PASS, banded control must
FAIL — proves the detector still works) and `node scripts/verify-banding.mjs` (the four committed
masters — top-dark-sky + seam-ramp regions must show no comb-spike fracture: runsAboveZero <= 3,
zeroGaps < 2). If any master region fails, raise GRAIN_AMP in build-sky.mjs (currently 1.5), rerun
`node scripts/build-sky.mjs` to regenerate, and re-check — iterate until both gate invocations exit 0.

Capture before/after master crops for the evidence record (the pre-regrade committed masters vs the
regenerated ones — a right-margin core crop and a mid-frame crop are enough) under
.planning/phases/11-bolder-sky/evidence/ .

Commit the regenerated masters + LQIP + build-sky.mjs + NightSky.astro. Do NOT push (Phase 12 owns
the gated deploy). Leave package.json and .planning/config.json untouched.
  </action>
  <verify>
    <automated>node scripts/verify-banding.mjs --selftest && node scripts/verify-banding.mjs</automated>
    <human-check>Before/after crop pair shows the regrade: grey-blue murk -> warm amber core with cool arms, no visible banding in the smooth dark sky.</human-check>
  </verify>
  <done>verify-banding --selftest and the masters run both exit 0; before/after crops captured; masters + LQIP + build-sky.mjs + NightSky.astro committed; nothing pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network -> build input | `build-sky.mjs` re-downloads the NOIRLab TIFF (external URL) as the master's only pixel source |
| author -> committed masters | regenerated binary masters are committed and later served to real users |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | NOIRLab TIFF re-download in `ensureSource()` | high | mitigate | Keep the existing integrity gate intact: byte-length 16359276 + 4000x2000/srgb metadata assertion refuses any swapped/corrupted source; the ESO-mirror path is documented, never silently substituted. Do NOT weaken these constants. |
| T-11-02 | Spoofing | CC BY 4.0 credit provenance | low | accept | The NOIRLab/NSF/AURA/E. Slawik/M. Zamani credit line lives in the footer (unchanged this plan); regrading pixels does not alter attribution. No action. |
| T-11-03 | Repudiation | regraded masters shipped without a look-of-record | low | mitigate | Commit before/after crops as evidence so the visual change is auditable; the banding gate output records the objective pass. |
</threat_model>

<verification>
- `node scripts/verify-banding.mjs --selftest` exits 0 (clean PASS, banded FAIL).
- `node scripts/verify-banding.mjs` exits 0 (all four committed masters green).
- The old murk constants are absent from build-sky.mjs; the old mobile quiet-crop `10% 70%` is absent from NightSky.astro.
- All four masters + LQIP regenerated and non-empty.
- package.json + .planning/config.json unchanged; nothing pushed.
</verification>

<success_criteria>
- The regenerated master reads warm + bright with the amber core leading (human eyeball on the before/after crops).
- The banding gate is green on the lifted-midtone regrade.
- Mobile object-position no longer targets the v3.0 quiet region.
- Masters + LQIP + scripts committed, origin untouched.
</success_criteria>

<output>
Create `.planning/phases/11-bolder-sky/11-01-SUMMARY.md` when done.
</output>
