---
phase: 11-bolder-sky
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - scripts/fixtures/visibility-refs/
  - src/styles/tokens.css
  - .planning/phases/11-bolder-sky/evidence/
autonomous: true
requirements:
  - BOLD-06

must_haves:
  truths:
    - "verify-visibility is re-blessed to the NEW approved stills and GREEN at all three viewports; the blur + blackout selftest controls still FAIL (gate honesty intact)."
    - "EVERY text surface reads >=4.5:1 at BOTH viewports on the brighter sky + new glass — contrast held by the CARD (fill escalated per the tier protocol if needed), NEVER by re-darkening the sky; --moon/--aurora/banding all green."
    - "Ambient soak <10%; local Lighthouse >=90 x4 both presets; single-rAF / zero-hex / leak-gate / no-cross-import invariants hold; origin still behind local (NOTHING pushed — Phase 12 owns the gated deploy)."
  artifacts:
    - "scripts/fixtures/visibility-refs/ — re-blessed reference stills (the new approved look)."
    - ".planning/phases/11-bolder-sky/evidence/ — before/after full-viewport captures at 1440 + 375."
    - "src/styles/tokens.css — CONDITIONAL: card-fill opacity escalation ONLY, if a surface dips (never a sky change)."
  key_links:
    - "brighter sky -> text contrast -> card fill opacity (--glass-bg / --glass-bg-2) = the escalation lever -> the 4.5:1 floor."
    - "re-blessed SSIM refs -> the approved mockup look (Galactic Core + liquid glass) — the anchor the visibility gate now enforces."
    - "NO push -> origin stays on the fixed-but-muddy v3.0 look until Phase 12's user-gated deploy."
---

<objective>
Prove the new bolder look on every gate — brightness INCLUDED — and produce the local
before/after evidence, without pushing (BOLD-06 local portion). The v3.0 process passed a broken
look because every gate rewarded darkness; this battery must PROVE the new look, not just the
absence of failure.

Purpose: The gate discipline is the POINT of this milestone. A brighter sky under text is the KEY
RISK — the card glass is the scrim now. If any surface dips below 4.5:1, escalate the card fill
(more opaque) per the v3.0 tier protocol. NEVER re-darken the whole sky to pass (the exact v3.0
anti-pattern that produced the muddy look); NEVER ship below floor.

Output: A green gate battery on the new look, re-blessed visibility refs, before/after captures at
1440 + 375, and an explicit assertion that origin is still behind local. Phase 12 owns the deploy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-bolder-sky/11-CONTEXT.md

# The gates + the re-bless target
@scripts/verify-visibility.mjs
@scripts/verify-contrast.mjs
@scripts/verify-banding.mjs
# The card-fill escalation lever (tokens.css only — never the sky)
@src/styles/tokens.css
# The full v3.0 re-gate battery + exact commands + last-known-good numbers
@.planning/quick/2026-07-19-sky-visibility-fix/SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-bless the visibility gate to the new look; prove it green + honest</name>
  <files>scripts/verify-visibility.mjs, scripts/fixtures/visibility-refs/</files>
  <action>
Build and serve the new look, then re-anchor the perceptual gate to it (per 11-CONTEXT.md recipe
item 6, BOLD-06). Run `npm run build`, then start `npm run preview` in the background (serves dist
at http://localhost:4321/).

FIRST, fix the stale lower-left assertion (plan-checker BLOCKER). The camper silhouette is cut
(11-02); its `.camper` container is kept for parallax but now holds only the soft warm glow. The
gate's `camperEdgeAbs` Sobel-edge floor (5.0/5.0/10.0) measured the silhouette's edges — a soft
radial glow has near-zero edge energy, so that floor is both wrong for the new design and would
falsely FAIL. Edge-energy is the wrong metric for a glow; luminance-presence is the right one.
In scripts/verify-visibility.mjs, REPLACE the `.camper`-region Sobel-edge assertion with a
WARM-GLOW-PRESENCE assertion on the same region: assert the region's warm signal — mean warm-channel
(R−B, or a warm-weighted luminance) over the glow area — is elevated above its immediate surround /
the frame base by a delta calibrated from the healthy new build (so a region that LOST its copper
glow FAILS). Rename the floor + its comments from edge → warm-glow accordingly; keep the region
keyed on `.camper` (11-02 keeps `.camper-glow` a child of `.camper`, so the glow sits inside that
box). This keeps meaningful coverage of the lower-left — it does NOT weaken the gate (a dropped floor
would; a right-metric floor does not). Do NOT lower a floor merely to pass.

Re-bless: run `node scripts/verify-visibility.mjs --bless --yes --url http://localhost:4321/` to
re-capture the blessed reference stills under scripts/fixtures/visibility-refs/ to the NEW intended
look (the brighter sky is easier to see, but the SSIM anchor MUST move to the approved look or the
gate would still be comparing against the old muddy stills). Re-blessing is a deliberate decision —
this is the sanctioned moment for it. Re-bless AFTER the metric swap so the new warm-glow baseline
is captured.

Then prove the re-blessed gate is both GREEN and HONEST:
- `node scripts/verify-visibility.mjs --gate --url http://localhost:4321/` must PASS all three
  viewports (1440x900, 1280x800, 375x812) — band range, starfield range, star count, warm-glow
  presence in the lower-left `.camper` region, aurora canvas coverage, SSIM vs the new refs.
- `node scripts/verify-visibility.mjs --selftest --url http://localhost:4321/` must PASS: the
  healthy page passes, the blacked-out control FAILS, and the blur(12) control FAILS the structure
  + star-count floors. The gate is invalid if either control passes.

Commit the re-blessed refs.
  </action>
  <verify>
    <automated>node scripts/verify-visibility.mjs --gate --url http://localhost:4321/ && node scripts/verify-visibility.mjs --selftest --url http://localhost:4321/</automated>
  </verify>
  <done>The `.camper`-region gate assertion is swapped from Sobel-edge to warm-glow luminance-presence (right metric for the new glow, coverage kept — not weakened); refs re-blessed to the new look; --gate PASS all 3 viewports; --selftest PASS (blackout + blur controls both fail correctly); script + refs committed.</done>
</task>

<task type="auto">
  <name>Task 2: Contrast >=4.5 both viewports every surface (card escalation, never sky); moon/aurora/banding</name>
  <files>src/styles/tokens.css</files>
  <action>
Prove text contrast on the brighter sky + new glass — the KEY RISK (per 11-CONTEXT.md recipe item 6).
With the preview serving http://localhost:4321/, run the screenshot contrast gate at BOTH reference
viewports:
- `node scripts/verify-contrast.mjs --cdp-screenshot --url http://localhost:4321/ --width 1440 --height 900`
- `node scripts/verify-contrast.mjs --cdp-screenshot --url http://localhost:4321/ --width 1280 --height 800`
Every over-sky text region across all 7 panels PLUS header, footer, and #deck-index must clear its
WCAG threshold (4.5 normal / 3 large). The header/footer/jump-index chrome now sits over a brighter
TOP sky — re-checked here too.

ESCALATION PROTOCOL (the whole discipline of this milestone) — if ANY surface dips below floor:
- Raise the CARD fill opacity — --glass-bg (tier-1) and/or --glass-bg-2 (tier-2) in tokens.css, more
  opaque — because the card is the scrim now. For a chrome surface, escalate the chrome token
  (--glass-bg-chrome / --glass-brightness-chrome) — tokens.css only.
- Rebuild (`npm run build`) and re-run the gate. Iterate until both viewports pass.
- NEVER re-darken the sky (build-sky.mjs grade is frozen — do not touch it). NEVER ship a surface
  below 4.5. Record any escalation (which token, old->new value, which surface forced it).

Then re-target the celestial ceilings to the new brighter core and prove them:
- `node scripts/verify-contrast.mjs --moon --url http://localhost:4321/ --width 1440 --height 900`
  and `--width 1280 --height 800` — moonPeak < mwPeak strictly (a brighter core gives MORE headroom).
- `node scripts/verify-contrast.mjs --aurora --url http://localhost:4321/ --width 1440 --height 900`
  and `--width 1280 --height 800` — max(auroraPeak) < mwPeak strictly.
- `node scripts/verify-contrast.mjs --selftest` (WCAG formula fixtures) and
  `node scripts/verify-banding.mjs --selftest` + `node scripts/verify-banding.mjs` all green.
  </action>
  <verify>
    <automated>node scripts/verify-contrast.mjs --cdp-screenshot --url http://localhost:4321/ --width 1440 --height 900 && node scripts/verify-contrast.mjs --cdp-screenshot --url http://localhost:4321/ --width 1280 --height 800 && node scripts/verify-contrast.mjs --moon --url http://localhost:4321/ --width 1440 --height 900 && node scripts/verify-contrast.mjs --moon --url http://localhost:4321/ --width 1280 --height 800 && node scripts/verify-contrast.mjs --aurora --url http://localhost:4321/ --width 1440 --height 900 && node scripts/verify-contrast.mjs --aurora --url http://localhost:4321/ --width 1280 --height 800 && node scripts/verify-contrast.mjs --selftest && node scripts/verify-banding.mjs --selftest && node scripts/verify-banding.mjs</automated>
  </verify>
  <done>Every surface >=4.5 at both viewports (held by the card — any dip escalated the card/chrome fill in tokens.css, never the sky); --moon + --aurora pass strictly both viewports; contrast + banding selftests green.</done>
</task>

<task type="auto">
  <name>Task 3: Budget + invariants + before/after evidence; commit; assert NO push</name>
  <files>.planning/phases/11-bolder-sky/evidence/</files>
  <action>
Close the battery and produce the eyeball evidence (per 11-CONTEXT.md recipe item 6, BOLD-06 local).
With the preview serving http://localhost:4321/:

- Ambient soak: run `node .planning/milestones/v3.0-phases/09-living-sky/ambient-soak/ambient-soak.mjs
  --url http://localhost:4321/` — TOTAL cpuPct must be < 10% (expect the 5-6% family), 60fps, 0 long
  tasks. (Absolute number is machine-relative per the v3.0 note — record it; the floor is <10%.)
- Local Lighthouse both presets >=90 x4: run the v3.0 local-Lighthouse method against
  http://localhost:4321/ for mobile (default) and desktop (--preset=desktop), output JSON, and assert
  all four categories >=90 in each. (Lighthouse via `npx lighthouse` is transient — NOT a package.json
  dependency; do not add it to package.json.)
- Invariant greps (all ASSERT form): single-rAF (scene.ts requestAnimationFrame == 2; aurora /
  constellations / clouds / parallax / meteors / starfield == 0); zero 6-hex outside tokens.css in
  src; the has-sky leak gate (scene markup only where intended); no cross-boundary imports between
  nightsky modules and fig01/deck.
- Before/after full-viewport captures at 1440 AND 375 into .planning/phases/11-bolder-sky/evidence/
  (the muddy v3.0 look vs the new bolder look) proving the new frame reads like the approved mockup
  (Galactic Core + liquid glass).

Commit everything (evidence + any escalation to tokens.css). Then ASSERT origin is still behind local
and confirm NOTHING was pushed: `git rev-list --count origin/main..HEAD` must be > 0. Leave
package.json + .planning/config.json untouched. Do NOT run git push (Phase 12 owns the gated deploy).
  </action>
  <verify>
    <automated>node .planning/milestones/v3.0-phases/09-living-sky/ambient-soak/ambient-soak.mjs --url http://localhost:4321/ && test "$(grep -o 'requestAnimationFrame' src/lib/nightsky/scene.ts | wc -l | tr -d ' ')" = "2" && test "$(grep -rEn '#[0-9a-fA-F]{6}' src --include='*.css' --include='*.astro' --include='*.ts' | grep -v 'tokens.css' | wc -l | tr -d ' ')" = "0" && git diff --exit-code package.json .planning/config.json && test "$(git rev-list --count origin/main..HEAD)" -gt "0"</automated>
    <human-check>End-of-phase eyeball: the before/after captures at 1440 + 375 show the muddy v3.0 look becoming the approved bolder look — a warm, core-led real sky (amber core leading, cool arms) under a premium liquid-glass card, camper cut. It reads like mockup Direction A + C's glass.</human-check>
  </verify>
  <done>Soak <10% at 60fps/0 long tasks; local Lighthouse >=90 x4 both presets; single-rAF/zero-hex/leak/no-cross-import invariants hold; before/after 1440+375 captures committed; package.json+config.json untouched; origin behind local; NOTHING pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local build -> live origin | pushing the new look would replace the live professional site (outward-facing) |
| gate refs -> gate authority | the re-blessed stills define what "visible/approved" means forever after |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-07 | Elevation of Privilege | accidental `git push` of the unapproved look to the live origin | high | mitigate | NO push in Phase 11 (Phase 12 owns the user-gated deploy). Verify asserts `origin/main..HEAD > 0` (origin behind local) and no push command is run; the live site keeps the fixed-but-muddy v3.0 look until the user approves. |
| T-11-08 | Tampering | re-blessing to a still-broken look (silently anchoring the gate to a bad frame) | high | mitigate | The --selftest (blur + blackout controls must FAIL) proves the re-blessed gate still rejects the failure modes; the end-of-phase human eyeball confirms the refs match the approved mockup before Phase 12 trusts them. |
| T-11-09 | Repudiation | contrast passing by an unrecorded sky-darkening rather than card escalation | medium | mitigate | build-sky.mjs is frozen this plan; any contrast fix is a recorded tokens.css card/chrome escalation (token, old->new, forcing surface). The sky-darkening anti-pattern is explicitly banned. |
</threat_model>

<verification>
- `verify-visibility --gate` PASS all 3 viewports; `--selftest` PASS (blur + blackout controls FAIL).
- `verify-contrast --cdp-screenshot` PASS both 1440 + 1280, every surface >=4.5 (large text >=3).
- `verify-contrast --moon` + `--aurora` PASS strictly both viewports; `--selftest` + `verify-banding` green.
- Ambient soak <10%; local Lighthouse >=90 x4 both presets.
- single-rAF (scene 2); zero-hex outside tokens.css; package.json + config.json unchanged.
- `git rev-list --count origin/main..HEAD` > 0 — origin behind local, nothing pushed.
</verification>

<success_criteria>
- The full gate battery is green on the NEW bolder look, brightness included.
- Any contrast shortfall was resolved by escalating the card/chrome fill in tokens.css — the sky was never re-darkened, no surface shipped below 4.5.
- Before/after 1440 + 375 captures prove the new look reads like the approved mockup (end-of-phase eyeball).
- Origin is still behind local; Phase 12 owns the deploy.
</success_criteria>

<output>
Create `.planning/phases/11-bolder-sky/11-03-SUMMARY.md` when done.
</output>
