---
phase: 01-foundation-editorial-shell
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/data/types.ts
  - src/data/profile.ts
  - src/data/systems.ts
  - src/data/experience.ts
  - src/data/patents.ts
  - src/data/skills.ts
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-04, CONT-07]
user_setup: []

must_haves:
  truths:
    - "All page content lives in typed TS data modules, not hardcoded in components"
    - "Every displayed metric carries a source annotation tracing it to the résumé (honesty gate)"
    - "The 4 selected systems, 4 experience entries, 2 patents + 1 paper, and grouped skills are all present as data"
  artifacts:
    - "src/data/types.ts"
    - "src/data/profile.ts"
    - "src/data/systems.ts"
    - "src/data/experience.ts"
    - "src/data/patents.ts"
    - "src/data/skills.ts"
  key_links:
    - "each data entry's `source` field -> CONT-07 honesty gate consumed by verification in plan 01-06"
    - "typed exports -> section components import them in plans 01-04 / 01-05"
---

<objective>
Author the typed TypeScript data modules that hold every piece of page content: profile (identity, bio, links), selected systems, experience, patents & publications, and skills. Each metric-bearing entry carries a `source` field tracing it to the résumé — the mechanism that enforces the honesty gate (CONT-07). These are plain typed modules, NOT Astro content collections (collections are Phase 3 for case studies).

Purpose: Single-source the content so the shell/section components (plans 01-04, 01-05) render from data, and so the honesty gate is machine-checkable. All copy and metrics are locked verbatim in 01-CONTEXT.md — treat as content, not paraphrasable.
Output: six files under src/data/.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-editorial-shell/01-CONTEXT.md
@.planning/phases/01-foundation-editorial-shell/01-RESEARCH.md
@.planning/reference/prototype-shell-and-fig01.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared types + profile.ts + systems.ts</name>
  <files>src/data/types.ts, src/data/profile.ts, src/data/systems.ts</files>
  <behavior>
    - types.ts exports interfaces with a required `source: string` field on every metric-bearing entry type
    - profile.ts exports name, tagline/eyebrow, bio, and the résumé/LinkedIn/GitHub/email URLs
    - systems.ts exports exactly 4 SystemEntry items with year, mono name, description, metric, source
  </behavior>
  <read_first>
    - .planning/phases/01-foundation-editorial-shell/01-CONTEXT.md (Copy section — verbatim header/eyebrow/thesis/bio/links; Page structure item 4 — the 4 systems rows with exact metrics; Content data & honesty gate — source field, external URLs)
    - .planning/phases/01-foundation-editorial-shell/01-RESEARCH.md (Pattern 1: typed data module with `source` field — the SystemEntry interface shape)
    - .planning/reference/prototype-shell-and-fig01.html (the four `.row` blocks — exact names, descriptions, metrics to port verbatim)
  </read_first>
  <action>
    Create `src/data/types.ts` exporting interfaces: `SystemEntry { year: string; name: string; description: string; metric: string; source: string }`; plus interfaces used by later tasks (`ExperienceEntry`, `Patent`, `Publication`, `SkillGroup`) each carrying a `source: string` where a metric/claim is made. Create `src/data/profile.ts` exporting a typed `profile` object with: `name: 'prateek kumar'`, `location: 'seattle'`, eyebrow parts (`distributed systems · cloud · ` + accent `applied ai`), the hero thesis "I build the infrastructure that intelligence runs on.", the bio line verbatim ("SDE 2 at Microsoft, on Azure's data platform. Previously cellularized DynamoDB's US-EAST-1 region at AWS and built the SmartThings notifications platform at Samsung. Two patents. M.S. CS, USC."), and a `links` object with `resume: '/prateek-kumar-resume.pdf'`, `linkedin: 'https://www.linkedin.com/in/prateek-kumar-7b11321b3'`, `github: 'https://github.com/p2401kumar'`, `email: 'p2401kumar@gmail.com'`, each with a `source`. Create `src/data/systems.ts` exporting `systems: SystemEntry[]` with exactly the 4 locked rows: 2025 `azure/health-snapshots` (metric `info-theory`), 2023 `dynamodb/cellularization` (metric `+30% rel · −20% p99`), 2023 `elb/auto-weight-away` (metric `90% ops automated`), 2019 `iot/contextual-widget` (metric `patented`) — descriptions ported verbatim from the prototype `.row` blocks, each with a `source` like `resume-4.5 §AWS — DynamoDB cellularization`. Every metric must appear in the résumé (no invented numbers).
  </action>
  <verify>
    <automated>npx astro check 2>&1 | tail -1 && grep -c "dynamodb/cellularization" src/data/systems.ts && grep -c "prateek-kumar-7b11321b3" src/data/profile.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/data/types.ts` defines `SystemEntry` (and the interfaces used by later tasks) each with a required `source: string`
    - `src/data/profile.ts` exports the thesis "I build the infrastructure that intelligence runs on." and the verbatim bio naming Microsoft/AWS/Samsung
    - `profile.links` has `resume: '/prateek-kumar-resume.pdf'`, LinkedIn `.../prateek-kumar-7b11321b3`, GitHub `.../p2401kumar`, email `p2401kumar@gmail.com`
    - `src/data/systems.ts` exports exactly 4 entries with names `azure/health-snapshots`, `dynamodb/cellularization`, `elb/auto-weight-away`, `iot/contextual-widget` and metrics `info-theory`, `+30% rel · −20% p99`, `90% ops automated`, `patented`
    - Every systems entry has a non-empty `source`
    - `npx astro check` reports 0 errors
  </acceptance_criteria>
  <done>Shared types, profile, and the 4 selected systems are typed data with source annotations.</done>
</task>

<task type="auto">
  <name>Task 2: experience.ts + patents.ts</name>
  <files>src/data/experience.ts, src/data/patents.ts</files>
  <read_first>
    - .planning/phases/01-foundation-editorial-shell/01-CONTEXT.md (Page structure items 5 and 6 — verbatim experience scope/metrics for Microsoft/AWS/MathWorks/Samsung; the 2 patents + Android Lint paper)
    - src/data/types.ts (the ExperienceEntry / Patent / Publication interfaces from Task 1)
  </read_first>
  <action>
    Create `src/data/experience.ts` exporting `experience: ExperienceEntry[]` with 4 entries in order: Microsoft SDE 2 (2024–now, Azure data platform: durable idempotent pipelines, info-theory health snapshots, auto-healing container apps); AWS SWE (2023–24, DynamoDB cellularization + SDK Java v1/v2, ELB auto-weight-away −10% peak latency); MathWorks intern (2022, Web-MATLAB low-bandwidth, JS test suite −3min); Samsung Sr. SWE (2017–20, SmartThings notifications platform, APK 150→90MB, contextual widget on Galaxy S21, multi-client RTSP). Each entry: company, role, dates, bullet(s), and a `source`. Create `src/data/patents.ts` exporting `patents: Patent[]` (IoT room identification via device-type watermarks, Grade A1, 2020; automated position & latch locking control via mobile, IN 201631007292, 2016) and `publications: Publication[]` (Android Lint tool optimization, ~30% faster dead-code/unused-resource detection), each with a `source`. All metrics must trace to the résumé.
  </action>
  <verify>
    <automated>npx astro check 2>&1 | tail -1 && grep -c "MathWorks" src/data/experience.ts && grep -c "201631007292" src/data/patents.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/data/experience.ts` exports 4 entries (Microsoft, AWS, MathWorks, Samsung) with real scope/metrics and a `source` each
    - `src/data/patents.ts` exports 2 patents (incl. Grade A1 and IN 201631007292) and 1 publication (Android Lint paper), each with a `source`
    - `npx astro check` reports 0 errors
  </acceptance_criteria>
  <done>Experience and patents/publications are typed data with source annotations.</done>
</task>

<task type="auto">
  <name>Task 3: skills.ts</name>
  <files>src/data/skills.ts</files>
  <read_first>
    - .planning/phases/01-foundation-editorial-shell/01-CONTEXT.md (Page structure item 7 — the skills groups and prose intro)
    - .planning/phases/01-foundation-editorial-shell/01-UI-SPEC.md (Supplementary: skills as grouped mono tags, no skill bars / no logo walls)
    - src/data/types.ts (SkillGroup interface)
  </read_first>
  <action>
    Create `src/data/skills.ts` exporting a short prose intro string plus `skillGroups: SkillGroup[]` with the 4 locked groups: Languages (Java, C#, Kotlin, Go, Python, JavaScript/TypeScript); Cloud & Distributed (AWS DynamoDB/EC2/ECS/EKS, Azure, Kubernetes, Docker, load balancing, auto-scaling); Data (DynamoDB, PostgreSQL, MySQL, Redis); Mobile (Android SDK, Jetpack, Kotlin, JNI). Each group is `{ label: string; tags: string[] }`. This is grouped-tag data only — no proficiency levels/percentages (skill bars and logo walls are explicit anti-patterns). No `source` metric is required here since these are capability tags, not claimed metrics, but include a `source` on the group set for provenance consistency.
  </action>
  <verify>
    <automated>npx astro check 2>&1 | tail -1 && grep -c "Cloud & Distributed" src/data/skills.ts && grep -c "Kubernetes" src/data/skills.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/data/skills.ts` exports 4 skill groups (Languages, Cloud & Distributed, Data, Mobile) as `{ label, tags }`
    - No numeric proficiency/percentage fields exist (no skill-bar data)
    - A prose intro string is exported
    - `npx astro check` reports 0 errors
  </acceptance_criteria>
  <done>Skills are grouped mono-tag data with a prose intro, no skill-bar anti-pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time data -> rendered HTML | Content consumed at Astro build; no runtime fetch, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Information Disclosure | src/data/* content | low | accept | All content is intentionally public résumé material; the only excluded PII (phone number) is kept off-site by decision (contact = email + LinkedIn only) |
| T-01-08 | Tampering | metric integrity vs résumé (honesty gate) | medium | mitigate | Every metric-bearing entry carries a `source` field tracing to the résumé; verification in plan 01-06 asserts source coverage (CONT-07) |
</threat_model>

<verification>
- All six data modules type-check clean (`astro check`)
- systems (4), experience (4), patents (2)+publication (1), skills (4 groups) present with verbatim locked copy/metrics
- Every metric-bearing entry has a `source` field (honesty gate)
- Phone number does NOT appear in any data module
</verification>

<success_criteria>
Page content is single-sourced as typed data with honesty-gate source annotations, ready for the section components to consume — satisfying CONT-01..04 and CONT-07 (data side).
</success_criteria>

## Artifacts this phase produces (plan 01-03)

- `src/data/types.ts` — interfaces `SystemEntry`, `ExperienceEntry`, `Patent`, `Publication`, `SkillGroup`
- `src/data/profile.ts` — export `profile` (name, location, eyebrow, thesis, bio, links)
- `src/data/systems.ts` — export `systems: SystemEntry[]` (4)
- `src/data/experience.ts` — export `experience: ExperienceEntry[]` (4)
- `src/data/patents.ts` — exports `patents: Patent[]` (2), `publications: Publication[]` (1)
- `src/data/skills.ts` — export prose intro + `skillGroups: SkillGroup[]` (4)

<output>
Create `.planning/phases/01-foundation-editorial-shell/01-03-SUMMARY.md` when done.
</output>