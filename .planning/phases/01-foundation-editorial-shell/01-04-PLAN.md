---
phase: 01-foundation-editorial-shell
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - src/components/SiteHeader.astro
  - src/components/Hero.astro
  - src/components/SiteFooter.astro
autonomous: true
requirements: [SHELL-01, SHELL-02, SHELL-03, SHELL-04, CONT-05, CONT-06]
user_setup: []

must_haves:
  truths:
    - "The mono header shows identity (name · seattle) and nav (work · résumé · contact)"
    - "The hero shows the eyebrow, serif thesis, bio, and three quiet links (résumé/linkedin/github)"
    - "The footer shows a green status dot + 'all systems operational' and a live Seattle clock"
    - "External links carry rel='noopener noreferrer'"
  artifacts:
    - "src/components/SiteHeader.astro"
    - "src/components/Hero.astro"
    - "src/components/SiteFooter.astro"
  key_links:
    - "profile data -> hero copy/links single-sourced"
    - "footer clock script -> Intl.DateTimeFormat America/Los_Angeles, ticks <=30s"
    - "var(--token) reads -> tokens.css single source"
---

<objective>
Build the three editorial-shell components — SiteHeader, Hero, SiteFooter — porting the approved prototype's markup, spacing, and type exactly, sourcing all copy/links from `src/data/profile.ts`, and reading every color/type value via `var(--token)`. The footer carries the only live element in Phase 1: the Seattle clock.

Purpose: These deliver SHELL-01..04 and the hero-side of CONT-05/CONT-06. The hero is the first-viewport credibility surface; it must match the locked type/spacing contract with no regression.
Output: three .astro components.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-editorial-shell/01-CONTEXT.md
@.planning/phases/01-foundation-editorial-shell/01-UI-SPEC.md
@.planning/reference/prototype-shell-and-fig01.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: SiteHeader.astro</name>
  <files>src/components/SiteHeader.astro</files>
  <read_first>
    - .planning/reference/prototype-shell-and-fig01.html (the `<header>` block + `header .id`/`header nav` CSS — port verbatim)
    - .planning/phases/01-foundation-editorial-shell/01-UI-SPEC.md (Component type map: Header identity + nav rows; Spacing locked exceptions for header padding/gap)
    - src/data/profile.ts (name, location)
  </read_first>
  <action>
    Create `src/components/SiteHeader.astro` reproducing the prototype header: a flex row (`justify-content:space-between; align-items:baseline; padding:30px 0 0`) with a `.id` mono block `<b>prateek kumar</b> · seattle` (name weight 600 color `--ink`, rest `--dim`, mono 12.5px letter-spacing .04em) and a `<nav>` (gap 22px) with three links `work` (href `#work`), `résumé` (href `/prateek-kumar-resume.pdf`), `contact` (href `#contact`) — mono 12.5px, color `--dim` hovering to `--ink`, no border. Nav casing is lowercase — this IS the brand, do not capitalize. Source name/location from `profile` where practical. Use a scoped `<style>` block reading only `var(--token)` values; ensure keyboard focus is visible (inherit the global `:focus-visible` outline, do not set `outline:none`). The `résumé` nav link and the `work`/`contact` anchors satisfy SHELL-01 and part of CONT-05/CONT-06.
  </action>
  <verify>
    <automated>grep -c "prateek kumar" src/components/SiteHeader.astro && grep -c "prateek-kumar-resume.pdf" src/components/SiteHeader.astro && grep -c "#contact" src/components/SiteHeader.astro && npx astro check 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - Header renders identity `prateek kumar · seattle` (name bold) and nav `work · résumé · contact`, all lowercase mono
    - `résumé` nav link points at `/prateek-kumar-resume.pdf`; `contact` points at `#contact`; `work` points at `#work`
    - Scoped styles use only `var(--token)` (no hex literals in the component style block)
    - No `outline:none` without a replacement focus style
    - `npx astro check` reports 0 errors
  </acceptance_criteria>
  <done>The mono header matches the prototype and provides nav to work/résumé/contact (SHELL-01, CONT-05/06 partial).</done>
</task>

<task type="auto">
  <name>Task 2: Hero.astro</name>
  <files>src/components/Hero.astro</files>
  <read_first>
    - .planning/reference/prototype-shell-and-fig01.html (the `.hero` section + `.eyebrow`/`h1`/`.sub`/`.links` CSS — port verbatim)
    - .planning/phases/01-foundation-editorial-shell/01-UI-SPEC.md (Component type map: Eyebrow, Hero thesis h1, Bio, Hero links; hero measures thesis 21ch / bio 58ch)
    - src/data/profile.ts (eyebrow, thesis, bio, links)
  </read_first>
  <action>
    Create `src/components/Hero.astro` reproducing the prototype hero (`.hero` padding `88px 0 30px`): an `.eyebrow` (mono 12px, .18em uppercase, `--dim`, with the `applied ai` fragment wrapped in `<em>` colored `--accent`, font-style normal); the thesis `<h1>` (serif weight 500, `clamp(30px,4.6vw,44px)`, line-height 1.18, letter-spacing -0.005em, `--ink`, `max-width:21ch`, `text-wrap:balance`, margin `20px 0 18px`) with the exact text "I build the infrastructure that intelligence runs on."; a `.sub` bio paragraph (`--body`, `max-width:58ch`, margin `0 0 22px`) with the verbatim bio; and a `.links` row (mono 13px, gap 20px) of three links `résumé →`, `linkedin →`, `github →`. The résumé link points at `/prateek-kumar-resume.pdf` (add `download`); linkedin/github point at the profile URLs and open in a new tab with `target="_blank" rel="noopener noreferrer"` (T-01-03 mitigation). Links use `border-bottom:1px solid var(--hair2)` hovering border to `--accent`. Source all copy/URLs from `profile`. Include the `→` glyph literally in each link label.
  </action>
  <verify>
    <automated>grep -c "intelligence runs on" src/components/Hero.astro && grep -c "applied ai" src/components/Hero.astro && grep -c "noopener noreferrer" src/components/Hero.astro && npx astro check 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - Hero renders eyebrow `distributed systems · cloud · applied ai` (accent on `applied ai`), the exact thesis, the verbatim bio, and 3 links `résumé →` / `linkedin →` / `github →`
    - The thesis is an `<h1>` using the serif face at `clamp(30px,4.6vw,44px)`, weight 500, `max-width:21ch`
    - linkedin and github links carry `target="_blank" rel="noopener noreferrer"`; résumé link has `download` and points at `/prateek-kumar-resume.pdf`
    - Scoped styles use only `var(--token)` (no hex literals)
    - `npx astro check` reports 0 errors
  </acceptance_criteria>
  <done>The hero matches the locked type/spacing/copy contract and provides the résumé/linkedin/github links (SHELL-02, SHELL-03, CONT-05).</done>
</task>

<task type="auto">
  <name>Task 3: SiteFooter.astro with live Seattle clock</name>
  <files>src/components/SiteFooter.astro</files>
  <read_first>
    - .planning/reference/prototype-shell-and-fig01.html (the `<footer>` block + `footer .st i` CSS + the `tick()` clock script — port verbatim)
    - .planning/phases/01-foundation-editorial-shell/01-RESEARCH.md (Pattern 2: footer clock as plain inline module script, no hydration)
    - .planning/phases/01-foundation-editorial-shell/01-UI-SPEC.md (Copywriting Contract: footer status + clock)
  </read_first>
  <action>
    Create `src/components/SiteFooter.astro` reproducing the prototype footer: a flex row (`justify-content:space-between; padding:48px 0 40px; border-top:1px solid var(--hair); margin-top:40px; mono 11.5px; color:--faint`) with a `.st` span containing a 6x6px `--good` status dot (`<i>` circle) + the text `all systems operational`, and a `#clock` span initially `seattle —:—`. Add a plain unattributed `<script>` (Astro processes it as a bundled module, no framework runtime) that defines `tick()` using `Intl.DateTimeFormat('en-US', { hour:'2-digit', minute:'2-digit', hour12:false, timeZone:'America/Los_Angeles' })` to set `#clock` to `seattle {HH:mm}`, calls `tick()` once, and `setInterval(tick, 30000)`. Guard against a missing element (return if `#clock` not found). Scoped styles read only `var(--token)`.
  </action>
  <verify>
    <automated>grep -c "all systems operational" src/components/SiteFooter.astro && grep -c "America/Los_Angeles" src/components/SiteFooter.astro && grep -c "setInterval" src/components/SiteFooter.astro && npx astro check 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - Footer renders a `--good` status dot + `all systems operational` and a `#clock` element
    - The clock script uses `Intl.DateTimeFormat` with `timeZone: 'America/Los_Angeles'`, 24h format, and `setInterval(tick, 30000)`
    - The script guards against a missing `#clock` element
    - Scoped styles use only `var(--token)` (no hex literals)
    - `npx astro check` reports 0 errors
  </acceptance_criteria>
  <done>The footer shows the status line and a live Seattle clock ticking at most every 30s (SHELL-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser <- static components | Rendered HTML + one inline clock script; no user input, no server |
| hero/footer external links -> third-party sites | LinkedIn/GitHub navigation from the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Information Disclosure | Hero external links (linkedin/github) | low | mitigate | `target="_blank"` links carry `rel="noopener noreferrer"` (reverse-tabnabbing) — Task 2 acceptance gate |
| T-01-09 | Tampering | inline footer clock script | low | accept | Trivial read-only DOM script (Intl format + setInterval); no external input, no eval, no network — no attack surface |
</threat_model>

<verification>
- Header/Hero/Footer match the prototype's markup, spacing, and type contract
- All copy/links single-sourced from profile data; external links have rel="noopener noreferrer"
- Clock uses America/Los_Angeles, 24h, ticks <=30s
- Scoped styles contain no hex literals; `astro check` clean
</verification>

<success_criteria>
The editorial shell (header, hero, footer) is complete, matches the locked design contract, and delivers SHELL-01..04 plus the hero-side résumé/contact links.
</success_criteria>

## Artifacts this phase produces (plan 01-04)

- `src/components/SiteHeader.astro` — mono identity + nav (work/résumé/contact)
- `src/components/Hero.astro` — eyebrow, thesis h1, bio, 3 quiet links
- `src/components/SiteFooter.astro` — status line + live Seattle clock (`tick()` inline script)

<output>
Create `.planning/phases/01-foundation-editorial-shell/01-04-SUMMARY.md` when done.
</output>