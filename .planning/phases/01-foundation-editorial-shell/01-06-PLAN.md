---
phase: 01-foundation-editorial-shell
plan: 06
type: execute
wave: 4
depends_on: ["01-04", "01-05"]
files_modified:
  - src/pages/index.astro
  - src/pages/404.astro
autonomous: true
requirements: [CONT-07, PLAT-05, SHELL-02]
user_setup: []

must_haves:
  truths:
    - "The home page composes header, hero, all content sections, and footer inside BaseLayout, in the locked order"
    - "A Fig. 01 slot is left as a comment placeholder between hero and systems (Phase 2 fills it)"
    - "The built dist/index.html contains the thesis, bio employers, all 4 systems, patents, and skills"
    - "Every rendered metric traces to a source in the data (honesty gate)"
    - "A 404 page exists in the site voice"
  artifacts:
    - "src/pages/index.astro"
    - "src/pages/404.astro"
  key_links:
    - "index.astro imports every component + BaseLayout -> the single-scroll page"
    - "Fig. 01 slot comment -> Phase 2 insertion point between Hero and SystemsList"
---

<objective>
Compose the single-scroll home page: import BaseLayout and every shell/section component and lay them out in the locked order (header, hero, [Fig.01 slot comment], selected systems, experience, patents & publications, skills, contact, footer). Add a 404 page in the site voice. Then verify the whole page builds, renders the locked copy, holds the honesty gate, and is responsive.

Purpose: This is the integration point that turns the components into a content-complete page and proves CONT-07 (honesty), SHELL-02 (thesis in built output), and PLAT-05 (responsive) at the page level before the live deploy.
Output: index.astro, 404.astro, and a passing local build.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-editorial-shell/01-CONTEXT.md
@.planning/phases/01-foundation-editorial-shell/01-UI-SPEC.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: index.astro composition + 404.astro</name>
  <files>src/pages/index.astro, src/pages/404.astro</files>
  <read_first>
    - .planning/research/ARCHITECTURE.md (index.astro composes Header/Hero/.../Footer; component boundaries)
    - .planning/phases/01-foundation-editorial-shell/01-UI-SPEC.md (Section order; Error state 404 recommended copy)
    - src/layouts/BaseLayout.astro (props + slot contract)
    - src/components/SiteHeader.astro, Hero.astro, SystemsList.astro, ExperienceSection.astro, PatentsSection.astro, SkillsSection.astro, ContactSection.astro, SiteFooter.astro
  </read_first>
  <action>
    Replace the scaffold's `src/pages/index.astro` with the real composition: import `BaseLayout` and all eight components, wrap the page inside `<BaseLayout>` (passing a title/description if desired), and render inside a `.page` container (max-width 880px, padding `0 clamp(18px,4vw,32px)` — the locked prototype page frame) in this exact order: `SiteHeader`, `Hero`, an HTML comment placeholder `<!-- Fig. 01 slot — Phase 2 -->` between Hero and SystemsList (render nothing here now), `SystemsList`, `ExperienceSection`, `PatentsSection`, `SkillsSection`, `ContactSection`, `SiteFooter`. Create `src/pages/404.astro` using BaseLayout with the recommended factual copy: a mono `404` label, one line `this route doesn't exist.`, and a single quiet link `back to work →` pointing at `/`. No apology copy, no humor.
  </action>
  <verify>
    <automated>grep -c "Fig. 01 slot" src/pages/index.astro && grep -c "SystemsList" src/pages/index.astro && grep -c "SiteFooter" src/pages/index.astro && grep -c "back to work" src/pages/404.astro && npx astro check 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/index.astro` wraps content in `BaseLayout` and renders the 8 components in the locked order inside a `.page` container
    - A `<!-- Fig. 01 slot — Phase 2 -->` comment sits between Hero and SystemsList (nothing rendered there)
    - `src/pages/404.astro` renders `404`, `this route doesn't exist.`, and a `back to work →` link to `/`
    - `npx astro check` reports 0 errors
  </acceptance_criteria>
  <done>The home page composes the full editorial shell + content sections in the locked order; a 404 page exists.</done>
</task>

<task type="auto">
  <name>Task 2: Build, render, and honesty-gate verification</name>
  <files>src/pages/index.astro</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 1 Success Criteria)
    - .planning/phases/01-foundation-editorial-shell/01-CONTEXT.md (honesty gate: every metric has a source)
    - src/data/systems.ts, src/data/experience.ts, src/data/patents.ts (the source fields)
  </read_first>
  <action>
    Run `npm run build` and assert the produced `dist/index.html` contains the locked, first-viewport-critical strings: the thesis "I build the infrastructure that intelligence runs on.", the bio naming Microsoft/AWS/Samsung, all four system names (`azure/health-snapshots`, `dynamodb/cellularization`, `elb/auto-weight-away`, `iot/contextual-widget`), the `patents & publications` and `skills` headings, and the `all systems operational` footer text. Assert the honesty gate (CONT-07): grep the data modules to confirm the count of `source:` annotations is at least the count of metric-bearing entries (systems 4 + experience 4 + patents/publications 3) so no displayed metric is unsourced. Confirm responsiveness structurally: `dist` CSS includes the `max-width:640px` rule that hides the metric column (PLAT-05). Do not fix component internals here — if an assertion fails, the failure points back to the owning component plan.
  </action>
  <verify>
    <automated>npm run build && grep -rc "intelligence runs on" dist/index.html && grep -rc "dynamodb/cellularization" dist/index.html && grep -rc "all systems operational" dist/index.html && grep -rlc "640px" dist/</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `dist/index.html` contains the exact thesis string and the bio (Microsoft/AWS/Samsung named)
    - `dist/index.html` contains all 4 system names, the `patents & publications` and `skills` section headings, and `all systems operational`
    - Honesty gate: `source:` annotation count across data modules >= number of metric-bearing entries (>= 11); no metric rendered without a source
    - A `max-width:640px` responsive rule is present in the built CSS (PLAT-05)
  </acceptance_criteria>
  <done>The built site is content-complete, matches the locked copy, holds the honesty gate, and is responsive — ready to deploy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time composition -> dist/ HTML | Page assembled at build; no runtime input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Tampering | honesty gate (unsourced metric) | medium | mitigate | Task 2 asserts `source:` coverage >= metric-entry count; an unsourced metric fails the build gate (CONT-07) |
| T-01-11 | Information Disclosure | dist/ output contents | low | accept | dist/ contains only intentionally-public content; no secrets, no `.planning`/`.claude` in the build output |
</threat_model>

<verification>
- index.astro composes all components in the locked order with the Fig.01 slot comment
- `npm run build` exits 0; dist/index.html contains thesis, bio employers, all sections, footer status
- Honesty gate holds (source coverage); responsive rule present
- 404 page present in site voice
</verification>

<success_criteria>
A content-complete, honesty-gated, responsive static site builds locally with all locked copy present — satisfying the page-level portion of SHELL-02, CONT-07, and PLAT-05, ready for the live deploy in plan 01-07.
</success_criteria>

## Artifacts this phase produces (plan 01-06)

- `src/pages/index.astro` — the single-scroll home page composition (8 components + Fig.01 slot comment)
- `src/pages/404.astro` — factual 404 page
- `dist/` — built static output (verified content-complete)

<output>
Create `.planning/phases/01-foundation-editorial-shell/01-06-SUMMARY.md` when done.
</output>