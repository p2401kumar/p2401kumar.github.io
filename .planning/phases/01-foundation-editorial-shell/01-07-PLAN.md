---
phase: 01-foundation-editorial-shell
plan: 07
type: execute
wave: 5
depends_on: ["01-06"]
files_modified: []
autonomous: false
requirements: [PLAT-02, PLAT-03, SHELL-02, SHELL-04]
user_setup: []

must_haves:
  truths:
    - "The new Astro site is live at https://p2401kumar.github.io (root, no base path)"
    - "The live page serves the thesis string, not the old 2021 content"
    - "GitHub Pages deploys automatically from the GitHub Actions workflow on push to main"
    - "The hero renders with no visible CLS/FOUT and the footer clock ticks live"
  artifacts:
    - "A successful GitHub Actions 'Deploy to GitHub Pages' run"
    - "Live deployment at https://p2401kumar.github.io"
  key_links:
    - "push to main -> Actions build+deploy -> Pages serves dist/ at user-root"
    - "Pages build_type=workflow -> Actions deployment supersedes the legacy 2021 branch build"
---

<objective>
Ship the site live. The target repo `p2401kumar/p2401kumar.github.io` ALREADY EXISTS with unrelated 2021 content and a legacy branch-based Pages build currently serving at https://p2401kumar.github.io/ — this is a deviation from the original assumption that the repo was empty. This plan gates the destructive replacement behind a decision checkpoint, then switches Pages to the GitHub Actions source, pushes the new site to `main`, watches the deploy to success, and verifies the live URL serves the new thesis.

Purpose: This is Phase 1 Success Criterion 1 — the site must end LIVE at https://p2401kumar.github.io. It proves PLAT-02 (auto-deploy) and PLAT-03 (user-root link/asset resolution) against the real deployed URL.
Output: a live site + a green Actions run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-foundation-editorial-shell/01-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Checkpoint: Confirm replacing the existing 2021 p2401kumar.github.io repo content</name>
  <action>Decision gate. Do NOT proceed to the switch/push task until the user selects one of the options below; the next task branches on that choice (plain replace vs. back up the 2021 content first).</action>
  <decision>Replace the existing p2401kumar/p2401kumar.github.io repo content (2021 site + legacy branch Pages) with the new Astro site, via a force-push to main and a switch to GitHub Actions as the Pages source.</decision>
  <context>
    Ground truth verified during planning: the repo `p2401kumar/p2401kumar.github.io` exists (public, default branch `main`, last pushed 2021-11-10, NOT empty) and Pages is currently `build_type: legacy` (branch `main` `/`), live at https://p2401kumar.github.io/. This working tree is on branch `master` with unrelated history (one commit) and origin was wired in plan 01-01. Publishing the new site requires overwriting the 2021 `main` history (a force-push) and switching Pages to the Actions build. This is destructive to the 2021 content. STATE.md/PROJECT.md record the intent to deploy the new portfolio at this exact user-root URL, but the pre-existing content was not anticipated, so explicit confirmation is required before overwriting.
  </context>
  <options>
    <option id="replace">
      <name>Replace (recommended)</name>
      <pros>Delivers the locked user-root URL with no base-path risk; the 2021 content is superseded by design</pros>
      <cons>Overwrites 2021 main history (recoverable only via GitHub's ref logs / any local clone)</cons>
    </option>
    <option id="backup-then-replace">
      <name>Back up the 2021 content first, then replace</name>
      <pros>Preserves the old site (e.g. tag/branch `legacy-2021` or a local clone) before force-push</pros>
      <cons>One extra step</cons>
    </option>
  </options>
  <resume-signal>Reply "replace", "backup-then-replace", or describe an alternative (e.g. a different repo).</resume-signal>
</task>

<task type="auto">
  <name>Task: Switch Pages to Actions, push to main, and watch the deploy</name>
  <files>(no source files — git/gh operations)</files>
  <read_first>
    - .planning/phases/01-foundation-editorial-shell/01-RESEARCH.md (Pitfall 2 repo naming; Pitfall 3 Pages source must be "GitHub Actions"; Code Examples: gh api pages build_type)
    - .github/workflows/deploy.yml (the workflow that must run)
  </read_first>
  <action>
    Honor the checkpoint decision. If "backup-then-replace", first preserve the 2021 content (e.g. `git fetch origin main` then create a `legacy-2021` tag/branch from `origin/main`, or clone it aside) before any force-push. Switch the Pages source from legacy branch to GitHub Actions: `gh api repos/p2401kumar/p2401kumar.github.io/pages -X PUT -f build_type=workflow`. Bring the local branch in line with the workflow trigger: rename `master` to `main` (`git branch -m master main`). Push, replacing the unrelated 2021 history: `git push -u origin main --force`. This push triggers the "Deploy to GitHub Pages" workflow. Watch it to completion: get the latest run id (`gh run list --workflow=deploy.yml -L 1`) and `gh run watch <id> --exit-status` until it succeeds; if it fails, read the logs (`gh run view <id> --log-failed`) and resolve (common causes: Pages source not yet `workflow`, Node floor, missing lockfile — all covered by prior plans).
  </action>
  <verify>
    <automated>gh api repos/p2401kumar/p2401kumar.github.io/pages -q .build_type && git rev-parse --abbrev-ref HEAD && gh run list --workflow=deploy.yml -L 1</automated>
  </verify>
  <acceptance_criteria>
    - Pages `build_type` is `workflow` (GitHub Actions source) — `gh api .../pages -q .build_type` returns `workflow`
    - The local branch is `main` and has been pushed to `origin/main`
    - The most recent "Deploy to GitHub Pages" Actions run has concluded with status success
  </acceptance_criteria>
  <done>Pages is on the Actions source and the deploy workflow has run green on push to main (PLAT-02).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify the live site at https://p2401kumar.github.io</name>
  <action>Human verification gate. Run the automated live check first (curl for the thesis string), then confirm the visual/behavioral items below in a browser before signing off.</action>
  <what-built>The new Astro portfolio, deployed live at https://p2401kumar.github.io via GitHub Actions, replacing the legacy 2021 site.</what-built>
  <how-to-verify>
    1. Automated live check: run `curl -s https://p2401kumar.github.io/` and confirm the output contains the exact string `I build the infrastructure that intelligence runs on.` (this is Phase 1 Success Criterion 1 and proves PLAT-03 user-root resolution). If GitHub's CDN is still serving a cached 2021 copy, wait ~1 minute and retry.
    2. Open https://p2401kumar.github.io/ in a browser and do a slow/hard reload: the hero thesis must appear with NO visible layout shift or font flash (PLAT-04 CLS check — RESEARCH Assumption A2).
    3. Confirm the three hero links `résumé →` `linkedin →` `github →` render the `→` arrow as a clean glyph consistent with the mono text (RESEARCH Pitfall 1 — the resolved arrow gap).
    4. Confirm the footer shows the green `all systems operational` dot and a live `seattle HH:mm` clock that matches Pacific time.
    5. Resize to ~360px wide: layout stays intact and the systems-list metric column hides (PLAT-05).
    6. Click `résumé →` and confirm the PDF downloads/opens.
  </how-to-verify>
  <resume-signal>Type "approved", or describe any issue (CLS, arrow glyph, clock, responsive, or a stale 2021 page).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine -> GitHub (push/api) | Authenticated destructive push + Pages config change via gh keyring |
| GitHub Actions -> GitHub Pages | OIDC id-token deploy |
| visitor browser <- live static site | Public read-only static assets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering / Denial | force-push over existing 2021 repo history | medium | mitigate | Blocking decision checkpoint before force-push; optional `legacy-2021` backup path offered; the 2021 content is superseded by design |
| T-01-01 | Elevation of Privilege | Actions deploy permissions | high | mitigate | Workflow uses only `contents:read`, `pages:write`, `id-token:write` (set in plan 01-01); no secrets added |
| T-01-05 | Information Disclosure | .planning/.claude in public repo source | low | accept | Only `dist/` is served to visitors (not repo source); planning docs are the user's own career material — flagged for the user's awareness, no action required in Phase 1 |
</threat_model>

<verification>
- Pages `build_type` = `workflow`; deploy workflow run succeeded on push to main
- `curl https://p2401kumar.github.io/` returns HTML containing the thesis string
- Human-verified: hero no-CLS, arrow glyph clean, live clock, 360px responsive, résumé download
</verification>

<success_criteria>
The site is LIVE at https://p2401kumar.github.io serving the new content-complete portfolio (Phase 1 Success Criterion 1), auto-deployed via GitHub Actions (PLAT-02) with correct user-root resolution (PLAT-03).
</success_criteria>

## Artifacts this phase produces (plan 01-07)

- A green "Deploy to GitHub Pages" GitHub Actions run
- Pages configured with `build_type: workflow`
- Live deployment at https://p2401kumar.github.io (new site replaces legacy 2021 content)
- (optional) `legacy-2021` backup ref if the backup path was chosen

<output>
Create `.planning/phases/01-foundation-editorial-shell/01-07-SUMMARY.md` when done.
</output>