---
phase: 02-fig01-signature-figure
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - src/lib/fig01/interactions.ts
  - src/lib/fig01/index.ts
autonomous: true
requirements: [FIG-02, FIG-03, FIG-04, FIG-05, FIG-06, FIG-07]
must_haves:
  truths:
    - "Hovering a node shows its real production fact; clicking a node (or pressing send request) dispatches a beam through the full topology"
    - "Injecting a fault narrates the sequence in an aria-live log and self-heals after ~8s via a setTimeout timer that is NOT tied to the rAF loop"
    - "Under prefers-reduced-motion the rAF loop never starts (only renderStaticFrame runs), yet fault injection still works and narrates; the rAF loop pauses when the figure is offscreen or the tab is hidden"
    - "Every node is reachable by keyboard through native proxy buttons with a visible on-canvas focus indicator, and there is no keyboard trap"
  artifacts:
    - src/lib/fig01/interactions.ts (pointer, buttons, log, reduced-motion branch, lifecycle gating, keyboard proxy)
    - src/lib/fig01/index.ts (public initFig01 orchestrator + teardown)
  key_links:
    - "applyMotionPreference() is the branch point: rm.matches → renderStaticFrame only (no startAnimationLoop); the FIG-05 acceptance bar"
    - "updateRunState() gates the loop on intersecting && !document.hidden && !rm.matches (FIG-07 lifecycle)"
    - "the fault heal timer is a setTimeout (FIG-03/FIG-05), never a per-frame time comparison inside rAF (Pitfall 3)"
---

<objective>
Wire the runtime: pointer hover/click, the send/fault buttons, the timestamped aria-live event log, and — the genuine engineering of this phase — the three hardening gaps the prototype lacks: (1) a real `prefers-reduced-motion` branch that starts no rAF loop yet keeps fault injection functional, (2) dual-gate rAF lifecycle pausing (IntersectionObserver + visibilitychange), and (3) keyboard access via native proxy `<button>`s per node with an on-canvas focus indicator and no keyboard trap. Expose it all through `initFig01(root)`.

Purpose: FIG-02/03/04 are ports; FIG-05/06/07 are net-new hardening that the reference prototype does not implement. This plan is where the accessibility and performance floors become real behavior, backed by source-level acceptance criteria (a browser-only visual pass is folded into Phase 3's polish/Lighthouse pass — a CONTEXT.md deferred item).

Output: `src/lib/fig01/interactions.ts`, `src/lib/fig01/index.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-fig01-signature-figure/02-CONTEXT.md
@.planning/phases/02-fig01-signature-figure/02-RESEARCH.md
@.planning/phases/02-fig01-signature-figure/02-UI-SPEC.md
@.planning/reference/prototype-shell-and-fig01.html
@src/components/SiteFooter.astro
@src/lib/fig01/model.ts
@src/lib/fig01/render.ts
@src/lib/fig01/tokens.ts
@src/data/fig01.ts
</context>

## Artifacts this plan produces

- `src/lib/fig01/interactions.ts` →
  - `wirePointer(state, canvas, stage, tipEl, redraw)` — hover hit-test → tooltip; canvas click → spawnBeam
  - `wireButtons(state, sendBtn, faultBtn, logEl, redraw, scheduleHeal)` — send/fault handlers
  - `writeLog(logEl, msg, cls?)` — timestamped, newest-first, trim-to-2 log writer
  - `wireKeyboard(state, proxyButtons, tipEl, redraw)` — per-node focus/activate → tooltip + spawnBeam + on-canvas focus ring
  - `applyMotionPreference(state, ctx, tokens, redraw)` — reduced-motion branch point
  - `updateRunState()` — IntersectionObserver + visibilitychange rAF gate
- `src/lib/fig01/index.ts` → `initFig01(root: HTMLElement): () => void`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pointer + buttons + timestamped aria-live event log + setTimeout self-heal</name>
  <files>src/lib/fig01/interactions.ts</files>
  <read_first>
    - .planning/reference/prototype-shell-and-fig01.html (lines 202–229: send/fault handlers, `log()` writer, pointer mousemove hit-test + tooltip positioning, canvas click → spawnBeam; lines 284–286: the heal check to relocate into a setTimeout)
    - .planning/phases/02-fig01-signature-figure/02-UI-SPEC.md ("Copywriting Contract" log/tooltip rows incl. exact strings + `.hl`/`.ok` classes; "Log timestamp format"; "Log retention" max 2 lines)
    - src/components/SiteFooter.astro (the exact Intl.DateTimeFormat America/Los_Angeles pattern to reuse for log timestamps)
    - src/lib/fig01/model.ts (spawnBeam, injectFault, healFault, healthyCells, hoverN/mouse state fields)
    - src/data/fig01.ts (tooltipHtml per node for the hover tooltip)
  </read_first>
  <behavior>
    - Hovering within a node's hit box (|mouse.x−px| < w/2+6 and |mouse.y−py| < h/2+8) sets that node's tooltipHtml into the tip element and positions it; leaving clears it
    - Clicking the canvas while hovering a node dispatches a beam from that node
    - send request logs `request dispatched → region` and dispatches a beam; inject fault logs the amber `.hl` fault line and disables the fault button
    - After ~8000ms a degraded cell heals: healFault runs, the `.ok` recovery line logs, and the fault button re-enables
    - The event log keeps at most 2 lines, newest first, each prefixed with an HH:mm:ss America/Los_Angeles timestamp
  </behavior>
  <action>
    Create `src/lib/fig01/interactions.ts` starting with the pointer, button, and log wiring, taking a `redraw()` callback (so the same handlers work in both animated and reduced-motion modes — under reduced motion `redraw` calls `renderStaticFrame`, under animation it is a no-op because the loop already repaints).

    `writeLog(logEl, msg, cls?)`: create a `<div>` (optional class `hl` for fault / `ok` for recovery), prefix with an HH:mm:ss 24h `America/Los_Angeles` timestamp using the SAME `Intl.DateTimeFormat` shape as SiteFooter.astro's clock, `logEl.prepend(...)`, and trim to the newest 2 children (prototype lines 202–207). The aria-live announcement is handled by the markup wrapper (plan 02-04 sets `aria-live="polite"` on the log container) — this writer only appends text so the live region announces it.

    `wirePointer(state, canvas, stage, tipEl, redraw)`: on `stage` mousemove, translate to canvas coords, hit-test all nodes with the 6px/8px hit-slop, set `state.hoverN`; when hovering a node with a fact, set `tipEl.innerHTML` to that node's `tooltipHtml` from fig01Facts (developer-authored content — safe per 02-RESEARCH.md Security Domain; do NOT wire this to any external/user input) and position via `tx = clamp(px − tipEl.offsetWidth/2, 8, W − tipEl.offsetWidth − 8)`, `ty = py − h/2 − 38`; on mouseleave clear hoverN + hide tip; on canvas `click`, if `state.hoverN` is set, `spawnBeam(state, state.hoverN)`. Call `redraw()` after hover changes so the hovered node's border repaints under reduced motion.

    `wireButtons(state, sendBtn, faultBtn, logEl, redraw, scheduleHeal)`: send → `spawnBeam(state)` + `writeLog(logEl, 'request dispatched → region')` + `redraw()`; fault → if already degraded return, else `injectFault(state)`, `writeLog(logEl, `${state.degraded} fault injected → weighed away · traffic rerouting · p99 stable`, 'hl')`, disable the fault button, `redraw()`, and `scheduleHeal()`.

    `scheduleHeal` (define here, used by wireButtons): a `setTimeout(..., 8000)` — NOT an rAF-driven `performance.now()` comparison (02-RESEARCH.md Pitfall 3) — that runs `healFault(state)`, `writeLog(logEl, `${cell} recovered · weight restored`, 'ok')`, re-enables the fault button, and calls `redraw()`. This must live outside the rAF loop so a fault injected under reduced motion still self-heals and narrates.
  </action>
  <acceptance_criteria>
    - `src/lib/fig01/interactions.ts` contains `setTimeout(` for the heal timer, and contains no `requestAnimationFrame` (heal + all wiring are rAF-decoupled)
    - `src/lib/fig01/interactions.ts` contains `America/Los_Angeles` (log timestamps match the footer clock)
    - `src/lib/fig01/interactions.ts` contains the exact strings `request dispatched → region`, `fault injected → weighed away · traffic rerouting · p99 stable`, and `recovered · weight restored`
    - `src/lib/fig01/interactions.ts` contains `8000` (self-heal window)
    - `grep -nE '#[0-9a-fA-F]{3,8}' src/lib/fig01/interactions.ts` returns 0 matches
    - `npx astro check` reports 0 errors
  </acceptance_criteria>
  <verify>
    <automated>npx astro check</automated>
  </verify>
  <done>Pointer hover/click, send/fault buttons, and a timestamped trim-to-2 log are wired with verbatim copy; the fault self-heal is a setTimeout decoupled from rAF; no requestAnimationFrame and no hex literal in interactions.ts; astro check clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reduced-motion branch (matchMedia, no rAF) + dual-gate rAF lifecycle (IntersectionObserver + visibilitychange)</name>
  <files>src/lib/fig01/interactions.ts</files>
  <read_first>
    - .planning/phases/02-fig01-signature-figure/02-RESEARCH.md (Pattern 3 reduced-motion first-class path; Pattern 5 dual-gate lifecycle; Pitfalls 2 + 3)
    - .planning/phases/02-fig01-signature-figure/02-UI-SPEC.md ("Reduced motion" table incl. the media-query change listener; "Performance" pause-conditions row)
    - src/lib/fig01/render.ts (startAnimationLoop, stopAnimationLoop, renderStaticFrame — the entry points this task branches between)
    - src/lib/fig01/interactions.ts (Task 1's redraw/scheduleHeal wiring this task feeds)
  </read_first>
  <behavior>
    - At init with prefers-reduced-motion active, startAnimationLoop is never called — only renderStaticFrame runs
    - Toggling the OS reduced-motion setting mid-session re-branches live (change listener), starting or stopping the loop accordingly
    - When reduced motion is active, the redraw() callback repaints via renderStaticFrame after any state change (fault inject, heal, hover, focus)
    - The rAF loop runs only when the figure is intersecting AND the tab is visible AND reduced motion is off; it stops when any of those is false
  </behavior>
  <action>
    Add the motion-preference branch and the lifecycle gate to `interactions.ts`.

    Reduced motion: `const rm = matchMedia('(prefers-reduced-motion: reduce)')`. Define `applyMotionPreference(state, ctx, tokens, redraw)` that first calls `stopAnimationLoop()`, then branches: if `rm.matches`, call `renderStaticFrame(ctx, state, tokens)` and START NO LOOP; else call `updateRunState()` (which may start the loop subject to visibility). Register `rm.addEventListener('change', ...)` so a live OS toggle re-branches (02-UI-SPEC Reduced-motion row; Pitfall 2). Define the module `redraw()` used by Task 1's wiring as: `if (rm.matches) renderStaticFrame(ctx, state, tokens)` else no-op (the animated loop already repaints) — this is why fault injection stays functional under reduced motion (the setTimeout heal + redraw path is identical in both modes; Pitfall 3).

    Lifecycle gating: track `let intersecting = false`. Create an `IntersectionObserver(([entry]) => { intersecting = entry.isIntersecting; updateRunState(); }, { threshold: 0 })` observing the stage element (pause as soon as zero pixels visible; resume on any). Add `document.addEventListener('visibilitychange', updateRunState)`. Define `updateRunState()`: `const shouldRun = intersecting && !document.hidden && !rm.matches; if (shouldRun) startAnimationLoop(ctx, state, tokens); else stopAnimationLoop();` — startAnimationLoop is idempotent so repeated calls are safe. Also create a `ResizeObserver(() => layout(...))` on the stage so container-driven resizes (font-load reflow) re-layout the canvas, then redraw (02-RESEARCH.md Pattern 6). Return the created observers so index.ts can include them in teardown.
  </action>
  <acceptance_criteria>
    - `src/lib/fig01/interactions.ts` contains `matchMedia('(prefers-reduced-motion: reduce)')`
    - `src/lib/fig01/interactions.ts` contains `renderStaticFrame` and, within the `rm.matches` branch of applyMotionPreference, does NOT call `startAnimationLoop` (reduced motion starts no loop — the FIG-05 bar; confirm by reading applyMotionPreference)
    - `src/lib/fig01/interactions.ts` contains `IntersectionObserver`, `visibilitychange`, and `document.hidden` (dual-gate lifecycle, FIG-07)
    - `src/lib/fig01/interactions.ts` contains `ResizeObserver`
    - `src/lib/fig01/interactions.ts` contains `addEventListener('change'` (live motion-preference toggle)
    - `npx astro check` reports 0 errors
  </acceptance_criteria>
  <verify>
    <automated>npx astro check</automated>
  </verify>
  <done>applyMotionPreference renders a static frame and starts no rAF loop under reduced motion (with a live change listener); updateRunState gates the single loop on intersection + tab visibility + motion preference; fault injection stays functional and narrated in the reduced-motion path; astro check clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Keyboard proxy-button wiring (per-node focus/activate, on-canvas focus ring, no trap) + initFig01 orchestrator</name>
  <files>src/lib/fig01/interactions.ts, src/lib/fig01/index.ts</files>
  <read_first>
    - .planning/phases/02-fig01-signature-figure/02-RESEARCH.md (Pattern 4 native-button proxy controls; Open Question 1 recommendation = sr-only buttons at node coords; Open Question 2 = teardown for hygiene)
    - .planning/phases/02-fig01-signature-figure/02-UI-SPEC.md ("Keyboard" table: sequential tab order, node-proxy accessible name, focus ring, no keyboard trap; "Accessibility copy" node-proxy action row)
    - .planning/research/ARCHITECTURE.md (initFig01(root) signature + Figure01.astro ↔ lib/fig01 DOM boundary)
    - src/lib/fig01/model.ts (state.focusedNode field, spawnBeam(state, fromId))
    - src/lib/fig01/render.ts (renderStaticFrame + the focus-ring draw keyed on state.focusedNode)
    - src/lib/fig01/interactions.ts (Task 1 + 2 wiring this task orchestrates)
  </read_first>
  <behavior>
    - Each of the 10 node proxy buttons, when focused, sets state.focusedNode, shows that node's tooltip at its coordinates, and triggers a redraw so the on-canvas focus ring appears
    - Activating a proxy button (Enter/Space, native button) dispatches a beam from that node
    - Blurring a proxy button clears state.focusedNode and hides the tooltip
    - initFig01(root) returns a teardown function that removes listeners and disconnects observers; calling it stops the rAF loop
  </behavior>
  <action>
    Add `wireKeyboard(state, proxyButtons, tipEl, redraw)` to `interactions.ts`. `proxyButtons` is the NodeList of `button.node-proxy` elements Figure01.astro renders (plan 02-04), each carrying a `data-node` id. For each button: on `focus`, set `state.focusedNode = id`, position + show `tipEl` at that node's px/py using the SAME positioning math as hover (reuse the pointer tooltip logic), and call `redraw()` so render draws the on-canvas accent focus ring (state.focusedNode); on `blur`, clear `state.focusedNode`, hide tip, `redraw()`; on `click` (native Enter/Space activation), `spawnBeam(state, id)` + writeLog `request dispatched → region` + `redraw()`. Do not intercept Tab/arrow keys — native sequential tab order over the buttons satisfies FIG-06 with no keyboard trap (02-UI-SPEC Keyboard row; sequential over roving-tabindex per Claude's Discretion). The buttons' accessible names come from the markup (fig01Facts.accessibleName); interactions may additionally update a button's accessible text with a `· degraded, rerouting` suffix when its node is the faulted cell (mirrors the log narration for screen-reader users).

    Create `src/lib/fig01/index.ts` exporting `initFig01(root: HTMLElement): () => void` (signature per ARCHITECTURE.md). It: queries the canvas, stage, tip, log, send/fault buttons, and `.node-proxy` buttons from `root`; null-checks each (Pitfall 4 — no bare `!`); gets the 2D context; reads `getTokens()`; `createState()`; runs `layout(...)` once (HiDPI sizing + computeLayout); writes the startup log line `region healthy · 4 cells · ambient traffic`; wires pointer, buttons, keyboard; then calls `applyMotionPreference(...)` (which either starts the gated loop or renders one static frame). It attaches the IntersectionObserver/visibilitychange/ResizeObserver/matchMedia listeners and returns a teardown that stops the loop, removes the document/window listeners, and disconnects the observers (teardown is for hygiene/testability; the plain-MPA site tears down on navigation anyway — 02-RESEARCH.md Open Question 2).
  </action>
  <acceptance_criteria>
    - `src/lib/fig01/interactions.ts` contains `node-proxy` and `data-node` (proxy-button wiring bound by node id)
    - `src/lib/fig01/interactions.ts` contains `focusedNode` (drives the on-canvas focus indicator)
    - `src/lib/fig01/interactions.ts` wireKeyboard binds `focus`, `blur`, and activation handlers (grep shows `'focus'` and `'blur'`)
    - `src/lib/fig01/index.ts` contains `export function initFig01` returning a teardown (contains `return () =>` or an equivalent returned function)
    - `src/lib/fig01/index.ts` contains the startup log string `region healthy · 4 cells · ambient traffic`
    - `src/lib/fig01/index.ts` contains no `!` non-null assertion on a `querySelector`/`getElementById` result (null-checked per Pitfall 4) and `grep -nE '#[0-9a-fA-F]{3,8}' src/lib/fig01/index.ts` returns 0
    - `npx astro check` reports 0 errors
  </acceptance_criteria>
  <verify>
    <automated>npx astro check</automated>
  </verify>
  <done>Keyboard proxy buttons expose every node's fact + beam dispatch with a visible on-canvas focus ring and no keyboard trap; initFig01(root) orchestrates tokens/state/layout/wiring/motion-branch and returns a teardown; astro check clean with null-checked DOM lookups.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fig01Facts → tip innerHTML | Developer-authored tooltip HTML rendered via innerHTML (build-time content, no user-input path) |
| DOM events → figure state | User pointer/keyboard events mutate simulation state only (no network, no storage) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering / Information Disclosure (DOM XSS) | tip `innerHTML` | medium | mitigate | Tooltip HTML originates ONLY from the closed, typed src/data/fig01.ts module (no form, URL param, or fetch). Action explicitly forbids wiring innerHTML to any external/user input; event-log lines use textContent-appended nodes. Flag in code review if fig01.ts is ever sourced from anything but a hand-authored literal. |
| T-02-06 | Denial of Service (CPU/battery) | rAF loop lifecycle | medium | mitigate | Loop gated by IntersectionObserver + visibilitychange + reduced-motion; reduced motion starts no loop at all. Enforced by acceptance greps for the gating APIs. |
</threat_model>

<verification>
- `npx astro check` → 0 errors
- `grep -c 'requestAnimationFrame' src/lib/fig01/interactions.ts` → 0 (all wiring rAF-decoupled)
- `grep -c 'setTimeout' src/lib/fig01/interactions.ts` → ≥ 1 (heal timer)
- `grep -c 'IntersectionObserver' src/lib/fig01/interactions.ts` → ≥ 1; `grep -c 'visibilitychange'` → ≥ 1
- Read applyMotionPreference: the `rm.matches` branch calls renderStaticFrame and never startAnimationLoop
- `grep -rnE '#[0-9a-fA-F]{3,8}' src/lib/fig01/interactions.ts src/lib/fig01/index.ts` → 0
</verification>

<success_criteria>
- FIG-02/03/04 wired (dispatch, fault narration + setTimeout heal, hover tooltip) with verbatim copy
- FIG-05 met: reduced motion starts no rAF loop, fault injection still narrates and heals
- FIG-06 met: every node keyboard-reachable via native proxy buttons, visible focus ring, no trap
- FIG-07 met: single loop gated on intersection + tab visibility + motion preference
- initFig01(root) is the single public entry point and returns a teardown
</success_criteria>

<output>
Create `.planning/phases/02-fig01-signature-figure/02-03-SUMMARY.md` when done
</output>
