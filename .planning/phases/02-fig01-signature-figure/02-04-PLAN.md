---
phase: 02-fig01-signature-figure
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified:
  - src/components/Figure01.astro
  - src/pages/index.astro
autonomous: true
requirements: [FIG-01, FIG-06]
must_haves:
  truths:
    - "The figure renders between Hero and SystemsList on the home page with the fig-bar chrome (fig. 01 — request path…), send request + inject fault buttons, canvas, event log, and caption"
    - "The canvas exposes role=img + an aria-label; the event log is an aria-live=polite region; 10 native sr-only proxy buttons (one per node) sit in topology tab order"
    - "The figure's script is a bare <script> that Astro bundles src/lib/fig01/index.ts into (no framework island, no client: directive)"
  artifacts:
    - src/components/Figure01.astro (markup chrome + scoped .fig CSS + bare bundling script + 10 proxy buttons)
    - src/pages/index.astro (Fig. 01 slot filled with <Figure01 />)
  key_links:
    - "the bare <script> tag is the Astro bundling seam — any attribute on it opts out of TS/import bundling (02-RESEARCH.md Pattern 1)"
    - "proxy buttons carry data-node ids + accessibleName text that interactions.ts (plan 02-03) binds focus/activate handlers to"
---

<objective>
Build `Figure01.astro` — the bespoke markup shell (fig-bar title + two buttons, canvas stage with tooltip overlay, event log, caption, and 10 visually-hidden per-node proxy buttons) with the `.fig` chrome CSS ported verbatim from the prototype and a bare bundling `<script>` that boots `initFig01` — then fill the Fig. 01 slot in `index.astro`.

Purpose: This is the accessibility-and-composition surface. The canvas semantics (role/aria-label), the aria-live log, and the native keyboard proxy buttons live here as real DOM; the bare-script decision (02-RESEARCH.md Pattern 1, correcting CONTEXT.md's wording) is what makes `src/lib/fig01/*.ts` a bundled, type-checked module rather than an unresolved import.

Output: `src/components/Figure01.astro`, modified `src/pages/index.astro`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-fig01-signature-figure/02-CONTEXT.md
@.planning/phases/02-fig01-signature-figure/02-RESEARCH.md
@.planning/phases/02-fig01-signature-figure/02-UI-SPEC.md
@.planning/reference/prototype-shell-and-fig01.html
@.planning/phases/01-foundation-editorial-shell/01-06-SUMMARY.md
@src/pages/index.astro
@src/components/SiteFooter.astro
@src/styles/global.css
@src/data/fig01.ts
@src/lib/fig01/index.ts
</context>

## Artifacts this plan produces

- `src/components/Figure01.astro` — the figure component (chrome markup + scoped CSS + bare `<script>` + 10 `.node-proxy` buttons)
- `src/pages/index.astro` — `<Figure01 />` inserted at the Fig. 01 slot, import added

<tasks>

<task type="auto">
  <name>Task 1: Figure01.astro — chrome markup, ported .fig CSS, ARIA surface, proxy buttons, bare bundling script</name>
  <files>src/components/Figure01.astro</files>
  <read_first>
    - .planning/reference/prototype-shell-and-fig01.html (lines 36–61 + 79–80: the `.fig`, `.fig-bar`, `.fig-actions`, `.btn`, `.fig-stage`, `canvas`, `.tip`, `.fig-log`, `.fig-cap` CSS + the 640px canvas-height override — port VERBATIM; lines 97–111: the `<figure>` markup structure + button ids `send`/`fault` + `#tip`/`#log`/caption)
    - .planning/phases/02-fig01-signature-figure/02-UI-SPEC.md ("Copywriting Contract" — fig-bar title, button labels, caption, all verbatim; "Accessibility copy" — canvas aria-label, node-proxy accessible names, aria-live; "Typography"/"Color" chrome type map)
    - .planning/phases/02-fig01-signature-figure/02-RESEARCH.md (Pattern 1 bare-script bundling + the CONTEXT.md wording correction; Pattern 4 proxy-button markup)
    - src/components/SiteFooter.astro (existing bare-`<script>` component precedent + scoped-`<style>` idiom)
    - src/styles/global.css (existing `:focus-visible` treatment the proxy buttons inherit; confirm no `.visually-hidden` exists yet)
    - src/data/fig01.ts (fig01Facts — looped in frontmatter to render the proxy buttons)
  </read_first>
  <action>
    Create `src/components/Figure01.astro`. In the frontmatter, `import { fig01Facts } from '../data/fig01'`.

    Markup (port the prototype `<figure class="fig">` structure verbatim, minus the inline `style` attribute): a `.fig-bar` with the title span — bold `fig. 01` + ` — request path through a cellularized region ` + a faint `· live` span (verbatim from 02-UI-SPEC Copywriting Contract) — and a `.fig-actions` group holding `<button class="btn" id="send">send request</button>` and `<button class="btn warn" id="fault">inject fault</button>`. Then a `.fig-stage` containing: the `<canvas id="c">` with `role="img"` and the verbatim `aria-label` from 02-UI-SPEC "Accessibility copy" (the "Diagram: three client devices route through a load balancer…" string); the `<div class="tip" id="tip" aria-hidden="true">` tooltip overlay; and the 10 per-node proxy buttons rendered by mapping over `fig01Facts` — each `<button class="node-proxy visually-hidden" data-node={fact.nodeId}>{fact.accessibleName}</button>` in topology order (fig01Facts is already ordered c0,c1,c2,lb,cell0..3,dp,ml). After the stage: `<div class="fig-log" id="log" aria-live="polite">` (empty; interactions.ts fills it) and the `.fig-cap` caption span with the verbatim caption ("The pattern I shipped at AWS: …").

    Scoped `<style>`: port the prototype's `.fig` / `.fig-bar` / `.fig-actions` / `.btn` (+ `.btn.warn` + `:disabled`) / `.fig-stage` / `canvas` / `.tip` / `.fig-log` (+ `.hl`/`.ok`/keyframes) / `.fig-cap` rules verbatim, including the `@media (max-width:640px){ canvas{ height:300px } }` override — every color already reads `var(--token)` so no change is needed. Add a `.visually-hidden` rule for the proxy buttons: absolutely positioned, clipped/zero-size so they have no visual footprint but remain focusable (NOT `display:none`/`aria-hidden`) — buttons stay in the accessibility tree and tab order. The `.node-proxy:focus-visible` state inherits the global `:focus-visible` accent outline; the meaningful visible focus indicator is drawn on the canvas by render.ts (state.focusedNode), so the proxy buttons themselves need no extra visible chrome.

    Script: add a bare `<script>` tag with NO attributes (02-RESEARCH.md Pattern 1 — this is what makes Astro bundle and type-check the TypeScript imports; any attribute on the tag opts out of that pipeline, which is the CONTEXT.md wording correction). Inside: `import { initFig01 } from '../lib/fig01/index'`, resolve the figure root element (the `.fig` element — e.g. `document.currentScript`'s parent, or `document.querySelector('.fig')`, null-checked), and call `initFig01(root)`.
  </action>
  <acceptance_criteria>
    - `grep -c '<script>' src/components/Figure01.astro` returns 1 AND `grep -cE '<script [^>]' src/components/Figure01.astro` returns 0 (the bundling script tag is bare — no attributes; Astro bundling seam intact)
    - `grep -c 'node-proxy' src/components/Figure01.astro` shows the proxy buttons present; `grep -c 'data-node' src/components/Figure01.astro` returns ≥ 10 (one per node via the fig01Facts map)
    - `src/components/Figure01.astro` contains `role="img"`, `aria-label`, and `aria-live="polite"`
    - `src/components/Figure01.astro` contains the verbatim strings `send request`, `inject fault`, and `request path through a cellularized region`
    - `src/components/Figure01.astro` contains `initFig01` and `import { fig01Facts }`
    - `grep -nE '#[0-9a-fA-F]{3,8}' src/components/Figure01.astro` returns 0 (chrome CSS reads var(--token); tooltip bg uses a token, not a hex)
    - `npx astro check` reports 0 errors
  </acceptance_criteria>
  <verify>
    <automated>npx astro check</automated>
  </verify>
  <done>Figure01.astro renders the full fig-bar/canvas/log/caption chrome with ported .fig CSS, role=img + aria-label canvas, aria-live log, 10 sr-only data-node proxy buttons from fig01Facts, and a bare bundling script calling initFig01 — no hex literals, astro check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Fill the Fig. 01 slot in index.astro and confirm the built page renders the figure chrome</name>
  <files>src/pages/index.astro</files>
  <read_first>
    - src/pages/index.astro (the `<!-- Fig. 01 slot — Phase 2 -->` comment between `<Hero />` and `<SystemsList />` — the exact insertion point)
    - .planning/phases/01-foundation-editorial-shell/01-06-SUMMARY.md (the composition convention: index.astro is the single integration point; component import order)
    - src/components/Figure01.astro (the component created in Task 1)
  </read_first>
  <action>
    Edit `src/pages/index.astro`: add `import Figure01 from "../components/Figure01.astro";` alongside the other component imports, and replace the `<!-- Fig. 01 slot — Phase 2 -->` comment line with `<Figure01 />` in the same position (between `<Hero />` and `<SystemsList />`), preserving the locked page order. Change nothing else in the composition.

    Then build and confirm the figure chrome is present in the emitted HTML and that Astro bundled the figure script into a hashed JS asset (Pattern 1 / Pitfall 1 — the built HTML must NOT contain a literal unresolved `../lib/fig01` import path).
  </action>
  <acceptance_criteria>
    - `grep -c 'Figure01' src/pages/index.astro` returns 2 (import + usage)
    - `grep -c 'Fig. 01 slot' src/pages/index.astro` returns 0 (placeholder comment replaced)
    - `npm run build` exits 0
    - `dist/index.html` contains `fig. 01 — request path through a cellularized region`, `send request`, and `inject fault`
    - `dist/index.html` contains `role="img"` and `aria-live="polite"` on the figure surface
    - `dist/index.html` does NOT contain the literal string `../lib/fig01` (script was bundled, not left as a raw import — Pitfall 1); a hashed `.js` asset exists under `dist/_astro/`
    - `npx astro check` reports 0 errors
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>index.astro renders <Figure01 /> at the locked slot; npm run build exits 0 and dist/index.html contains the fig-bar chrome + ARIA surface with the figure script bundled to a hashed asset (no unresolved import path).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Astro build → static HTML/JS | Figure01.astro prerendered at build time; bare script bundled to a hashed asset (no SSR) |
| public dist output | Served HTML contains only intentionally-public figure chrome + facts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Tampering (bundling bypass) | bare `<script>` tag | medium | mitigate | Any attribute on the script tag opts out of Astro's TS/import bundling (Pitfall 1). Enforced by the bare-tag grep gate + the dist `../lib/fig01` negative check confirming the import was bundled. |
| T-02-08 | Information Disclosure | dist/index.html figure content | low | accept | Built output contains only public résumé-traceable facts + abstracted topology labels — no secrets, no internal service names (matches T-02-01 discipline). |
</threat_model>

<verification>
- `npx astro check` → 0 errors
- `npm run build` → exit 0
- `grep -c '<script>' src/components/Figure01.astro` → 1; `grep -cE '<script [^>]' src/components/Figure01.astro` → 0 (bare tag)
- `grep -c 'data-node' src/components/Figure01.astro` → ≥ 10 (proxy buttons)
- `grep -c 'fig. 01 — request path through a cellularized region' dist/index.html` → 1
- `grep -c '../lib/fig01' dist/index.html` → 0 (bundled, not unresolved)
</verification>

<success_criteria>
- Figure01.astro renders the full chrome + ARIA surface + 10 keyboard proxy buttons, with verbatim copy and no hex literals
- The bare bundling script boots initFig01 and is emitted as a hashed asset (not an unresolved import)
- index.astro composes <Figure01 /> at the locked slot; the built home page shows the fig-bar chrome
</success_criteria>

<output>
Create `.planning/phases/02-fig01-signature-figure/02-04-SUMMARY.md` when done
</output>
