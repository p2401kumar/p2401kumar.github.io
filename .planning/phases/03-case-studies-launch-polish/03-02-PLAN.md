---
phase: 03-case-studies-launch-polish
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/pages/work/[slug].astro
  - src/components/SystemsList.astro
autonomous: true
requirements: [CASE-01, CASE-02]
user_setup: []

must_haves:
  truths:
    - "Visitor can click the dynamodb/cellularization and elb/auto-weight-away rows in the systems list and land on /work/{slug} (CASE-01, CASE-02)"
    - "Each case-study page renders the mono H1, serif standfirst, a metrics strip with source annotations, the problem -> approach (incl. trade-offs considered) -> impact sections, and a back-to-work + cross-link block"
    - "Each case-study page includes the site header and footer with 'all systems operational' — the every-page contract from Phase 1's verification gap"
  artifacts:
    - src/pages/work/[slug].astro
    - src/components/SystemsList.astro
  key_links:
    - "[slug].astro getStaticPaths — getCollection('case-studies') → one static route per entry, params.slug = entry.id"
    - "SystemsList row — <a href='/work/{slug}'> → the corresponding case-study page (only the 2 rows with entries)"
    - "[slug].astro — composes → BaseLayout(title,description) + SiteHeader + SiteFooter"
---

<objective>
Build the visitor-facing case-study pages and turn the two matching systems-list rows into links, delivering the readable deep-dives (CASE-01, CASE-02).

Purpose: Plan 03-01 produced the schema-validated content. This plan renders it into two pages structured problem -> approach (with trade-offs considered) -> impact, composed with the full site shell (header + footer, per the Phase 1 every-page lesson), and wires the entry points from the systems list.
Output: `src/pages/work/[slug].astro` (one template, both routes) and a modified `SystemsList.astro` where exactly the two case-study rows become links.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-case-studies-launch-polish/03-UI-SPEC.md
@.planning/phases/01-foundation-editorial-shell/01-VERIFICATION.md
@src/data/systems.ts
@src/components/SystemsList.astro
@src/pages/404.astro
</context>

<artifacts_produced>
## Artifacts this plan produces

| Symbol / File | Kind | Notes |
|---------------|------|-------|
| `src/pages/work/[slug].astro` | dynamic route | `getStaticPaths` from `case-studies`; renders both `/work/dynamodb-cellularization` and `/work/elb-auto-weight-away` |
| `dist/work/dynamodb-cellularization/index.html` | built page | must contain `<header`, `<footer`, "all systems operational" |
| `dist/work/elb-auto-weight-away/index.html` | built page | same every-page contract |
| `src/components/SystemsList.astro` (modified) | component | the 2 matching rows gain an `<a href="/work/{slug}">` wrapper; other 2 rows unchanged |

Depends on: plan 03-01 `case-studies` collection. Consumed by: plan 03-03 (sitemap includes these 2 routes) and plan 03-04 (live-URL checks).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Create the case-study page template src/pages/work/[slug].astro (CASE-01, CASE-02)</name>
  <files>src/pages/work/[slug].astro</files>
  <read_first>
    - .planning/phases/03-case-studies-launch-polish/03-RESEARCH.md (Pattern 2 "getStaticPaths + render() in [slug].astro" — note entry.id is the route key, there is no reserved slug field; Pitfall 4 "missing SiteHeader/SiteFooter on new pages")
    - .planning/phases/03-case-studies-launch-polish/03-UI-SPEC.md (the Supplementary "Case-study page section order", Spacing Scale + Typography tables for the article, metrics strip, section headings, cross-link block)
    - src/pages/404.astro (the exact `.page` container pattern + BaseLayout + SiteHeader + SiteFooter composition to mirror)
    - src/components/SiteFooter.astro (confirms the "all systems operational" string the every-page grep checks for)
  </read_first>
  <action>
    Create `src/pages/work/[slug].astro`. In frontmatter: `export async function getStaticPaths()` calls `getCollection('case-studies')` and maps each entry to `{ params: { slug: entry.id }, props: { entry } }` (entry.id is the filename-derived id, e.g. dynamodb-cellularization — RESEARCH Pattern 2; do NOT expect a reserved `slug` frontmatter field). Read `const { entry } = Astro.props`. Sections are frontmatter string fields (03-01 schema), so render them directly — no render()/<Content /> needed; split each section string on blank lines into paragraphs.

    Compose EXACTLY like 404.astro: `<BaseLayout title={entry.data.title} description={entry.data.standfirst}>` wrapping a `.page` container that renders `<SiteHeader />`, then a `<main>` article, then `<SiteFooter />`. Passing `description={entry.data.standfirst}` ensures the per-page meta is correct once 03-03 wires the SEO partial into BaseLayout. Header/footer are MANDATORY (Pitfall 4 / 01-VERIFICATION.md every-page gap — this is a pre-encoded acceptance criterion below).

    Article structure per UI-SPEC section order: mono H1 = `entry.data.systemId` (the artifact name, e.g. dynamodb/cellularization); serif standfirst = `entry.data.standfirst`; a metrics strip mapping `entry.data.metrics` to `label: value` pairs where each value carries a native `title={metric.source}` attribute (the source annotation — no custom tooltip widget, per UI-SPEC); then the three sections with lowercase mono headings `problem`, `approach`, `impact`; inside the approach section, after the approach prose, render a `trade-offs considered` sub-heading followed by the `tradeoffs` prose. End with a cross-link block: `back to work →` linking to `/#work`, and `next: {otherSystemId} →` linking to the other case study's `/work/{otherId}` (compute the "other" entry from the full collection). Use the trailing `→` glyph idiom.

    Scoped `<style>`: reuse the `.page` rule verbatim from 404.astro (max-width 880px, clamp padding). All colors via `var(--token)` — zero hex literals (SHELL-05 gate). Apply the UI-SPEC values: H1 mono `clamp(22px,3.4vw,32px)` weight 500 color --ink; standfirst serif 20px color --body max-width 58ch; metrics strip with `border-top`/`border-bottom: 1px solid var(--hair)`, values color --accent, labels --dim uppercase; section headings mono 12px .18em uppercase --dim; sub-heading mono 12px .1em uppercase --faint; prose --body; cross-link block `border-top: 1px solid var(--hair)` mono 13px --ink with accent hover.
  </action>
  <verify>
    <automated>cd "C:/Development/Dump/Portfolio" && npm run build && grep -l "all systems operational" dist/work/dynamodb-cellularization/index.html dist/work/elb-auto-weight-away/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 and produces `dist/work/dynamodb-cellularization/index.html` AND `dist/work/elb-auto-weight-away/index.html`.
    - Both built pages contain `<header`, `<footer`, and the literal `all systems operational` (every-page contract — pre-encoded from 01-VERIFICATION.md).
    - Each page contains its mono H1 artifact name, the standfirst, a metrics strip whose values carry a `title=` source attribute, and the three headings problem/approach/impact plus the `trade-offs considered` sub-heading.
    - Each page has a `back to work →` link to `/#work` and a `next: ... →` cross-link to the other case study.
    - Zero hex color literals in the scoped style block (grep for `#` hex codes returns none; all colors are `var(--...)`).
  </acceptance_criteria>
  <done>Both case-study routes build with the full shell, correct article structure, metrics strip with source annotations, and back/cross links.</done>
</task>

<task type="auto">
  <name>Task 2: Link the two matching systems-list rows (CASE-01, CASE-02 entry points)</name>
  <files>src/components/SystemsList.astro</files>
  <read_first>
    - src/components/SystemsList.astro (the existing `.row` markup, grid, and `.row:hover` idiom that must stay unchanged)
    - src/data/systems.ts (the 4 `name` values; exactly 2 — dynamodb/cellularization, elb/auto-weight-away — get links)
    - .planning/phases/03-case-studies-launch-polish/03-UI-SPEC.md (Copywriting Contract row-links entry: only the two case-study rows become links; hover idiom unchanged; other two stay plain text)
  </read_first>
  <action>
    Modify `SystemsList.astro` so exactly the two rows that have a case study become links, keeping the other two (azure/health-snapshots, iot/contextual-widget) as plain text. Make it data-driven, not hardcoded: in the frontmatter, `getCollection('case-studies')` and build a Map from `entry.data.systemId` to `entry.id` (the route slug). In the row map, if the map has `entry.name`, wrap the existing row content in `<a href={"/work/" + slug}>` (root-relative — user-root deploy, no base prefix per RESEARCH Project Constraints); otherwise render the row exactly as today. Do NOT change `.row` grid/spacing/`.row:hover` background or the 640px collapse — only add an anchor wrapper on the two linked rows. The anchor must not introduce a competing underline: reset its `border-bottom`/color so the row hover idiom (panel2 background) remains the only hover affordance, and keep the whole row clickable.
  </action>
  <verify>
    <automated>cd "C:/Development/Dump/Portfolio" && npm run build && grep -c 'href="/work/' dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - Built `dist/index.html` contains exactly 2 `href="/work/` occurrences: `/work/dynamodb-cellularization` and `/work/elb-auto-weight-away`.
    - The other two rows (azure/health-snapshots, iot/contextual-widget) render with NO `/work/` link.
    - `.row` grid, spacing, `.row:hover`, and the 640px `.mt` collapse are byte-identical to before (only an anchor wrapper added on the 2 linked rows).
    - Linkage is derived from the collection (a new case study would auto-link its row), not a hardcoded slug list.
  </acceptance_criteria>
  <done>The two case-study rows are clickable entry points into their pages; the systems list is otherwise visually unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| collection data -> rendered HTML | Developer-authored frontmatter is interpolated into `[slug].astro`; Astro auto-escapes interpolated values by default — no user/runtime input exists on this static page |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Tampering | case-study page markup (metric `title` source strings, section prose) | low | accept | Content is developer-authored in the repo, not user-submitted; Astro's default templating auto-escapes all interpolated frontmatter values — no injection surface introduced |
| T-03-04 | Spoofing | broken/incorrect `/work/{slug}` link targets | low | mitigate | Links are derived from `getCollection` (slug = entry.id) rather than hand-typed strings, so a row only links to a real built route; the exactly-2-links build grep confirms |
</threat_model>

<verification>
- `npm run build` exits 0; both `/work/*` routes emit `index.html`.
- Every-page contract holds: both pages contain `<header`, `<footer`, `all systems operational`.
- `dist/index.html` has exactly 2 `href="/work/` links; other rows unlinked.
- Zero hex literals in the new scoped style block.
</verification>

<success_criteria>
- Two readable case-study pages exist with the full shell and problem/approach(+trade-offs)/impact structure (CASE-01, CASE-02).
- The systems list links exactly the two rows that have case studies, hover idiom unchanged.
</success_criteria>

<output>
Create `.planning/phases/03-case-studies-launch-polish/03-02-SUMMARY.md` when done.
</output>