---
phase: 03-case-studies-launch-polish
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - src/components/SEO.astro
  - src/layouts/BaseLayout.astro
  - astro.config.mjs
  - package.json
  - package-lock.json
  - public/robots.txt
  - public/og/og-default.png
  - scripts/render-og.mjs
  - og-source.svg
autonomous: true
requirements: [PLAT-06]
user_setup: []

must_haves:
  truths:
    - "Every page (home + both case studies) ships title/description/canonical + OpenGraph (og:title/description/url/image/type) + twitter:card meta, and references the favicon and the OG image (PLAT-06)"
    - "A static 1200x630 OG image exists at public/og/og-default.png and og:image resolves to an absolute https://p2401kumar.github.io URL"
    - "The generated sitemap lists exactly 3 URLs (home + 2 case studies), excludes /404, and robots.txt points at sitemap-index.xml (PLAT-06)"
  artifacts:
    - src/components/SEO.astro
    - public/og/og-default.png
    - scripts/render-og.mjs
    - og-source.svg
    - public/robots.txt
  key_links:
    - "BaseLayout <head> — imports → SEO.astro so every page inherits the meta (PLAT-06 every-page)"
    - "SEO.astro — new URL(ogImage, Astro.site) → absolute og:image URL"
    - "astro.config.mjs sitemap({filter}) — excludes → the /404 route; robots.txt — points at → /sitemap-index.xml"
---

<objective>
Ship full SEO/OpenGraph coverage on every page, one static OG image, a 3-page sitemap, and a robots.txt — closing PLAT-06.

Purpose: PLAT-06 requires every page to carry title/description/OG meta plus a static OG image, favicon, and sitemap. The SEO partial lives in BaseLayout (the single shell every page uses), so home and both case-study pages inherit it automatically. The OG image is one hand-authored SVG rasterized to PNG via the sharp already vendored by astro:assets — no new runtime dependency. The only new package is `@astrojs/sitemap` (pinned 3.7.3, already legitimacy-cleared in RESEARCH.md's audit table).
Output: `SEO.astro`, an OG image + its build-time rasterizer script, the sitemap integration, and robots.txt.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-case-studies-launch-polish/03-RESEARCH.md
@.planning/phases/03-case-studies-launch-polish/03-UI-SPEC.md
@src/layouts/BaseLayout.astro
@astro.config.mjs
</context>

<artifacts_produced>
## Artifacts this plan produces

| Symbol / File | Kind | Notes |
|---------------|------|-------|
| `src/components/SEO.astro` | head partial | Props {title, description, ogImage?}; emits title/description/canonical/OG/twitter meta |
| `src/layouts/BaseLayout.astro` (modified) | layout | imports SEO into `<head>`, delegates title/description to it, keeps favicon + font preloads |
| `og-source.svg` | source asset | hand-authored 1200x630 OG art (repo root, not served) |
| `scripts/render-og.mjs` | build-time script | sharp SVG->PNG rasterizer; rerunnable, retained in repo |
| `public/og/og-default.png` | static asset | 1200x630 committed OG image |
| `astro.config.mjs` (modified) | config | adds `@astrojs/sitemap` integration with a /404 `filter` |
| `public/robots.txt` | static asset | Allow all + `Sitemap:` line pointing at sitemap-index.xml |
| `@astrojs/sitemap@3.7.3` | devDependency | the ONE new package this phase; generates sitemap-index.xml + sitemap-0.xml at build |

Depends on: plan 03-02 (the 2 case-study routes must exist for the "exactly 3 URLs" sitemap proof). Consumed by: plan 03-04 (live-URL SEO/Lighthouse checks).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: SEO head partial wired into BaseLayout (PLAT-06 meta, every page)</name>
  <files>src/components/SEO.astro, src/layouts/BaseLayout.astro</files>
  <read_first>
    - .planning/phases/03-case-studies-launch-polish/03-RESEARCH.md (Pattern 3 "Canonical URL + OG meta via Astro.site/Astro.url" for the exact `new URL(Astro.url.pathname, Astro.site)` idiom and meta tag set; Don't-Hand-Roll canonical/OG URL construction)
    - src/layouts/BaseLayout.astro (current `<head>`: it already emits `<title>` + `<meta description>` + favicon + font preloads — SEO must take over title/description to avoid duplicate tags)
  </read_first>
  <action>
    Create `src/components/SEO.astro` with `interface Props { title: string; description: string; ogImage?: string }` defaulting `ogImage = '/og/og-default.png'`. Compute `const canonicalURL = new URL(Astro.url.pathname, Astro.site)` and `const ogImageURL = new URL(ogImage, Astro.site)` (absolute URLs from the configured `site` — do NOT string-concatenate, per RESEARCH Don't-Hand-Roll). Emit exactly: `<title>{title}</title>`, `<meta name="description" content={description}>`, `<link rel="canonical" href={canonicalURL}>`, `<meta property="og:title" content={title}>`, `<meta property="og:description" content={description}>`, `<meta property="og:url" content={canonicalURL}>`, `<meta property="og:image" content={ogImageURL}>`, `<meta property="og:type" content="website">`, `<meta name="twitter:card" content="summary_large_image">`.

    Modify `src/layouts/BaseLayout.astro`: import SEO and render `<SEO title={title} description={description} />` inside `<head>`, and REMOVE BaseLayout's own `<title>` and `<meta name="description">` lines so they are not duplicated (BaseLayout already has `title`/`description` props with the current defaults — keep those defaults as the fallback passed into SEO). Keep the favicon `<link rel="icon">` and both font preload links in BaseLayout unchanged. Update the stale scope-note comment in BaseLayout that says OpenGraph/sitemap are "explicitly Phase 3 scope — do not add them here" (that Phase 3 is now).
  </action>
  <verify>
    <automated>cd "C:/Development/Dump/Portfolio" && npm run build && test "$(grep -c 'og:image' dist/index.html)" = "1" && test "$(grep -c 'og:image' dist/work/dynamodb-cellularization/index.html)" = "1" && test "$(grep -c 'og:image' dist/work/elb-auto-weight-away/index.html)" = "1"</automated>
  </verify>
  <acceptance_criteria>
    - All three built pages (`dist/index.html`, both `dist/work/*/index.html`) contain exactly one each of: `og:title`, `og:description`, `og:url`, `og:image`, `og:type`, `twitter:card`, `rel="canonical"`.
    - `og:image` and `og:url` are ABSOLUTE URLs beginning `https://p2401kumar.github.io` (from Astro.site), not root-relative or duplicated.
    - Each page has exactly ONE `<title>` and ONE `<meta name="description">` (no duplication from BaseLayout + SEO).
    - The favicon `<link rel="icon" ... href="/favicon.svg">` is present on all three pages.
    - Case-study pages show their own title/standfirst as title/description (BaseLayout receives them from 03-02's `[slug].astro`).
  </acceptance_criteria>
  <done>Every page ships complete, non-duplicated SEO/OG/twitter meta with absolute OG/canonical URLs and the favicon.</done>
</task>

<task type="auto">
  <name>Task 2: Author the OG image (SVG) + sharp rasterizer -> public/og/og-default.png (PLAT-06)</name>
  <files>og-source.svg, scripts/render-og.mjs, public/og/og-default.png</files>
  <read_first>
    - .planning/phases/03-case-studies-launch-polish/03-RESEARCH.md (Pattern 4 "Build-time OG image rasterization with sharp" for the exact script shape; Open Question 2 — sharp native binary is manifest-confirmed but unproven on this machine, so the script must run and fail loudly; Environment Availability fallback: hand-commit a PNG if sharp fails)
    - .planning/phases/03-case-studies-launch-polish/03-UI-SPEC.md (Supplementary "OG image composition" table: ground, dot-grid substrate, cell/route motif in --accent, name + thesis text, accent budget; Copywriting Contract OG image text)
    - src/styles/tokens.css (exact hex values to hardcode INTO the SVG — the SVG is outside the token system, so read the literal values: --bg #0f1216, --panel2 #141a22, --accent #d99163, --ink #e8ebef, --body #aab2bd)
  </read_first>
  <action>
    Author `og-source.svg` at 1200x630 reusing the Fig.01 aesthetic per UI-SPEC: `--bg` (#0f1216) full-bleed ground with a low-opacity 22px dot-grid substrate; a simplified cell/route motif in the lower-right third (2-3 rounded rects in `--panel2` connected by one elbow route stroke in `--accent` at reduced weight); the text `Prateek Kumar` (serif, `--ink`, large, upper/center-left) and beneath it `I build the infrastructure that intelligence runs on.` (`--body`) — nothing else competing. Accent budget <5% (the route line + at most one short underline), never a fill. Because the SVG is rasterized outside the site's self-hosted-font pipeline, use a broadly-available serif/sans family in the SVG (or embed the woff2 as a base64 @font-face) so text renders — Claude's discretion; the token colors, however, must be the exact literals from tokens.css.

    Create `scripts/render-og.mjs`: a standalone Node ESM script that imports `sharp`, reads `og-source.svg`, and writes a 1200x630 PNG to `public/og/og-default.png` (use `.resize(1200, 630).png()`); after writing, read back the PNG metadata and log `width`/`height` so the run proves the output dimensions. Do NOT wire this into `astro build` — it is a one-time/rerunnable script mirroring the font-subsetting convention (RESEARCH Anti-Patterns). Run `node scripts/render-og.mjs` to produce and commit the PNG. If sharp fails to execute on this machine, fall back to rasterizing the SVG with any external tool and committing the 1200x630 PNG directly (CONTEXT-approved path) — but retain the script and SVG in the repo either way.
  </action>
  <verify>
    <automated>cd "C:/Development/Dump/Portfolio" && node scripts/render-og.mjs && test -f public/og/og-default.png</automated>
  </verify>
  <acceptance_criteria>
    - `public/og/og-default.png` exists and is a valid PNG sized exactly 1200x630 (the script logs width=1200 height=630, or an external-tool export of the same dimensions is committed).
    - `og-source.svg` and `scripts/render-og.mjs` are both retained in the repo (rerunnable).
    - The OG image uses only the locked token hex values for color; text reads `Prateek Kumar` + the thesis line, nothing else.
    - `scripts/render-og.mjs` is NOT imported by astro.config.mjs / the Astro build graph.
  </acceptance_criteria>
  <done>A committed 1200x630 OG PNG exists, produced by a retained, rerunnable sharp script (or an approved hand-export fallback).</done>
</task>

<task type="auto">
  <name>Task 3: Install @astrojs/sitemap (pinned), wire the /404-excluding filter, add robots.txt (PLAT-06)</name>
  <files>package.json, package-lock.json, astro.config.mjs, public/robots.txt</files>
  <read_first>
    - .planning/phases/03-case-studies-launch-polish/03-RESEARCH.md (Package Legitimacy Audit — @astrojs/sitemap is Approved via manual npm-view override, add the lightweight npm view confirmation as cheap insurance; Code Examples "Sitemap integration with 404 excluded" + "robots.txt"; Pitfall 2 — verify the filter actually excludes /404 in the built XML, do not trust config alone)
    - astro.config.mjs (the existing defineConfig with `site`, `output`, and the FontaineTransform vite plugin — the sitemap integration is added alongside, the vite/Fontaine config stays untouched)
  </read_first>
  <action>
    Supply-chain insurance first (T-03-SC): run `npm view @astrojs/sitemap version` and confirm it reports `3.7.3` before installing (the package is already legitimacy-cleared in RESEARCH.md's audit table — official withastro/astro monorepo, no postinstall script; this is a cheap re-confirm, not a blocking gate). Then `npm install --save-dev @astrojs/sitemap@3.7.3` (pin the exact version).

    Modify `astro.config.mjs`: import `sitemap from '@astrojs/sitemap'` and add `integrations: [ sitemap({ filter: (page) => !page.endsWith('/404/') && !page.endsWith('/404') }) ]` to the existing `defineConfig` — leave `site`, `output: 'static'`, and the entire `vite.plugins` FontaineTransform block exactly as-is. The `filter` excludes the 404 route so the sitemap covers exactly home + 2 case studies (CONTEXT "exactly 3 pages").

    Create `public/robots.txt` (none exists yet): `User-agent: *` / `Allow: /` / blank line / `Sitemap: https://p2401kumar.github.io/sitemap-index.xml`.

    After `npm run build`, open `dist/sitemap-0.xml` and confirm it contains exactly 3 `<url>` entries and zero `/404` matches (RESEARCH Pitfall 2 — the filter has a documented history of not filtering; verify the built XML, not the config).
  </action>
  <verify>
    <automated>cd "C:/Development/Dump/Portfolio" && npm run build && test "$(grep -c '<url>' dist/sitemap-0.xml)" = "3" && ! grep -q 404 dist/sitemap-0.xml && test -f dist/robots.txt</automated>
  </verify>
  <acceptance_criteria>
    - `npm view @astrojs/sitemap version` reported 3.7.3 and package.json pins `@astrojs/sitemap` at `3.7.3` (exact, not a caret range that could drift).
    - `dist/sitemap-0.xml` contains exactly 3 `<url>` entries (home + the 2 case-study routes) and zero `/404` matches (Pitfall 2 verified against built XML).
    - `dist/robots.txt` exists and its `Sitemap:` line points at `https://p2401kumar.github.io/sitemap-index.xml`.
    - `astro.config.mjs` still has the FontaineTransform vite plugin and `site`/`output` unchanged; only the sitemap integration was added.
  </acceptance_criteria>
  <done>The ONE new dependency is pinned and legitimacy-confirmed; the sitemap covers exactly 3 URLs excluding /404; robots.txt references it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local build | Installing `@astrojs/sitemap` pulls third-party code into the build toolchain (the only supply-chain surface this phase adds) |
| frontmatter -> meta tags | Developer-authored title/description flow into `<meta>`; Astro auto-escapes interpolated attribute values |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-SC | Tampering | `@astrojs/sitemap` npm install (supply chain) | high | mitigate | Already legitimacy-cleared in RESEARCH.md Package Legitimacy Audit (Approved via manual npm-view override: official withastro/astro monorepo, 83 versions, no postinstall script). Re-confirm `npm view @astrojs/sitemap version` == 3.7.3 immediately before install and pin the exact version. Not auto-approvable to a lower bar; no separate blocking human checkpoint required given the prior clearance. |
| T-03-05 | Spoofing/Tampering | OG/canonical meta tag injection via frontmatter | low | accept | All meta values are developer-authored in the repo (not user-submitted); Astro templating auto-escapes interpolated values; URLs built with `new URL(...)` not string concat |
| T-03-06 | Tampering | sitemap silently including /404 or wrong route set | medium | mitigate | Verify the BUILT `dist/sitemap-0.xml` has exactly 3 `<url>` and no /404 (Pitfall 2) rather than trusting the filter config |
</threat_model>

<verification>
- All three pages carry non-duplicated SEO/OG/twitter meta with absolute URLs + favicon.
- `public/og/og-default.png` is a valid 1200x630 PNG from a retained script.
- `dist/sitemap-0.xml` has exactly 3 `<url>`, no /404; `dist/robots.txt` points at the sitemap index.
- `@astrojs/sitemap@3.7.3` pinned; astro.config.mjs Fontaine/vite config intact.
</verification>

<success_criteria>
- PLAT-06 fully delivered: every page has title/description/OG meta + favicon; one static OG image; 3-URL sitemap; robots.txt.
- No scope-reduced placeholders; the single new dependency is pinned and legitimacy-confirmed.
</success_criteria>

<output>
Create `.planning/phases/03-case-studies-launch-polish/03-03-SUMMARY.md` when done.
</output>