---
phase: 03-case-studies-launch-polish
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - .planning/phases/03-case-studies-launch-polish/03-LIGHTHOUSE.md
autonomous: false
requirements: [PLAT-07]
user_setup: []

must_haves:
  truths:
    - "The two new case-study routes are live on GitHub Pages, return 200, and each contains its thesis/standfirst plus the site header and footer with 'all systems operational'"
    - "The home page (and a case-study page) scores Lighthouse >= 90 in Performance, Accessibility, Best Practices, and SEO on the LIVE deployed URL (PLAT-07)"
    - "Residual visual QA deferred from Phases 1-2 (hero CLS/font-flash, arrow glyph, footer clock ticking, 360px collapse, Fig.01 send/fault/heal/keyboard) is confirmed against the live site"
  artifacts:
    - .planning/phases/03-case-studies-launch-polish/03-LIGHTHOUSE.md
  key_links:
    - "push to main — .github/workflows/deploy.yml → GitHub Pages live URL (existing pipeline, auto-picks up new routes)"
    - "live URL — npx lighthouse (headless Chrome) → 4-category scores >= 0.90 recorded in 03-LIGHTHOUSE.md"
    - "live URL — gstack /browse or human-verify checklist → residual visual QA sign-off"
---

<objective>
Deploy the completed site, certify Lighthouse >= 90 across all four categories on the live URL, and close the residual visual QA deferred from Phases 1-2 — the final gate before launch (PLAT-07).

Purpose: This is the last plan of v1. Everything is built (case studies, SEO, OG, sitemap). This plan pushes to production, runs the formal Lighthouse audit against the live deployed URL with a fix-forward budget, and resolves the in-browser visual checks that headless tooling could not run in earlier phases — via automation if it starts, otherwise via the single permitted human-verify checkpoint.
Output: A live, verified site; `03-LIGHTHOUSE.md` recording all category scores; and a SUMMARY that surfaces the post-launch reminder to retire the old /home repo.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-case-studies-launch-polish/03-RESEARCH.md
@.planning/phases/03-case-studies-launch-polish/03-CONTEXT.md
@.planning/phases/01-foundation-editorial-shell/01-VERIFICATION.md
</context>

<artifacts_produced>
## Artifacts this plan produces

| Symbol / File | Kind | Notes |
|---------------|------|-------|
| `.planning/phases/03-case-studies-launch-polish/03-LIGHTHOUSE.md` | audit record | Performance/Accessibility/Best-Practices/SEO scores for home + a case-study page on the live URL |
| (live) `/work/dynamodb-cellularization/`, `/work/elb-auto-weight-away/` | deployed routes | 200 on GitHub Pages, header/footer/status present |
| post-launch reminder | SUMMARY note | old p2401kumar.github.io/home repo retirement (milestone-wrap task, not built here) |

Depends on: plans 03-01..03-03 (all content + SEO + sitemap must be in the build). Final plan of the v1 milestone.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Fast-forward deploy + live-URL route verification</name>
  <files>(no source changes — git push + live curl checks; may edit source only if a check fails)</files>
  <read_first>
    - .planning/STATE.md (deploy pipeline is the existing withastro/action deploy.yml on push to main; user-root deploy at https://p2401kumar.github.io, no base path)
    - .planning/phases/01-foundation-editorial-shell/01-VERIFICATION.md (the live-curl verification method already used for Phase 1/2 deploys — header/footer/'all systems operational' grep on live HTML)
  </read_first>
  <action>
    Confirm the working tree is clean and all of 03-01..03-03's commits are on `main`. Fast-forward push to `origin main` only (no force). Watch the `deploy.yml` GitHub Actions run to completion with `gh run watch` (or poll `gh run list --workflow=deploy.yml -L 1`) and confirm it is completed/success before running live checks. Then curl the live URL: both `/work/dynamodb-cellularization/` and `/work/elb-auto-weight-away/` must return 200 and each live HTML must contain its H1 artifact name + standfirst, `<header`, `<footer`, and `all systems operational` (the every-page contract, live-verified not just in dist). Also confirm `/sitemap-index.xml`, `/robots.txt`, and `/og/og-default.png` each return 200 on the live URL. If any check fails, diagnose and fix-forward (re-commit, re-push, re-watch) within this plan.
  </action>
  <verify>
    <automated>curl -sf https://p2401kumar.github.io/work/dynamodb-cellularization/ | grep -q 'all systems operational' && curl -sf https://p2401kumar.github.io/work/elb-auto-weight-away/ | grep -q 'all systems operational' && curl -sf -o /dev/null https://p2401kumar.github.io/sitemap-index.xml && curl -sf -o /dev/null https://p2401kumar.github.io/og/og-default.png</automated>
  </verify>
  <acceptance_criteria>
    - The `deploy.yml` run for this push is completed/success (confirmed via gh) before any live check runs.
    - Live `/work/dynamodb-cellularization/` and `/work/elb-auto-weight-away/` return 200 and contain `<header`, `<footer`, `all systems operational`, and their own H1 + standfirst.
    - Live `/sitemap-index.xml`, `/robots.txt`, and `/og/og-default.png` each return 200.
    - Push was fast-forward (no force-push); working tree clean.
  </acceptance_criteria>
  <done>The full site — home, both case studies, sitemap, robots, OG image — is live on GitHub Pages and the new routes pass the live every-page contract.</done>
</task>

<task type="auto">
  <name>Task 2: Lighthouse >= 90 audit on the live URL, recorded, with fix-forward (PLAT-07)</name>
  <files>.planning/phases/03-case-studies-launch-polish/03-LIGHTHOUSE.md</files>
  <read_first>
    - .planning/phases/03-case-studies-launch-polish/03-RESEARCH.md (Environment Availability — Chrome at the default Windows path, `npx lighthouse` v13.4.0 resolvable; Pitfall 5 — CHROME_PATH fallback; Summary — npx lighthouse primary, PageSpeed Insights API v5 keyless secondary fallback)
    - .planning/phases/03-case-studies-launch-polish/03-CONTEXT.md (Lighthouse gate decision + fix-forward budget: common suspects meta description, contrast on --dim/--faint text, image sizing)
  </read_first>
  <action>
    Run `npx lighthouse` against the LIVE home URL `https://p2401kumar.github.io/` for all four categories, JSON output, headless (e.g. `npx lighthouse https://p2401kumar.github.io/ --quiet --chrome-flags=--headless=new --output=json --output-path=.planning/phases/03-case-studies-launch-polish/lighthouse-home.json`). If Chrome auto-detection fails with a CHROME_PATH error, set `CHROME_PATH` to `C:\Program Files\Google\Chrome\Application\chrome.exe` and retry; if local Chrome cannot launch at all, fall back to the PageSpeed Insights API v5 (keyless) for the same four categories. Also audit one case-study page (e.g. `/work/dynamodb-cellularization/`). Read `categories.performance/accessibility/best-practices/seo .score` from the JSON and record all scores (as percentages) into `03-LIGHTHOUSE.md` with the audit method, URL, and timestamp.

    FIX-FORWARD: if any category on the home page is below 0.90, diagnose and fix within this phase (likely suspects per CONTEXT: missing/short meta description, color-contrast on `--dim`/`--faint` text, unsized/oversized images), commit, re-deploy (re-push, re-watch deploy.yml green), and re-run the audit until all four home-page categories are >= 0.90. Record the final passing scores.
  </action>
  <verify>
    <automated>cd "C:/Development/Dump/Portfolio" && npx lighthouse https://p2401kumar.github.io/ --quiet --chrome-flags=--headless=new --output=json --output-path=.planning/phases/03-case-studies-launch-polish/lighthouse-home.json && test -f .planning/phases/03-case-studies-launch-polish/03-LIGHTHOUSE.md</automated>
  </verify>
  <acceptance_criteria>
    - `03-LIGHTHOUSE.md` records Performance, Accessibility, Best Practices, and SEO scores for the live home page, all >= 90, plus the audit method (npx lighthouse or PSI fallback), URL, and date.
    - A case-study page was also audited and its scores recorded.
    - If any home-page category was initially < 90, the fix, re-deploy, and re-audit are documented and the final recorded scores are all >= 90 (PLAT-07 met via fix-forward, not by lowering the bar).
  </acceptance_criteria>
  <done>The live home page scores Lighthouse >= 90 in all four categories, recorded in 03-LIGHTHOUSE.md; any shortfall was fixed-forward within the phase.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Residual visual QA on the live site — browser automation first, human-verify fallback</name>
  <action>Attempt in-browser visual QA against the live URL FIRST via the gstack /browse skill (per the user's global instruction to use /browse for all web browsing; never the claude-in-chrome tools). If /browse or the built-in browser pane starts, drive the how-to-verify checklist below, capture evidence, and record results in the SUMMARY so this checkpoint is a confirm-only sign-off. Only if browser tooling fails to start (the known Phase 1-2 risk on this machine) does a human run the checklist manually. This is the ONLY permitted human checkpoint in this phase.</action>
  <what-built>
    The complete live site: home (hero + Fig.01 + systems list with 2 new case-study links), both case-study pages, footer clock, SEO/OG meta, and sitemap — all deployed to https://p2401kumar.github.io.

    Automation-first: the executor MUST first attempt in-browser visual QA against the live URL using the gstack `/browse` skill (per the user's global instruction to use /browse for all web browsing; never the claude-in-chrome tools). If /browse (or the built-in browser pane) starts, drive the checklist below, capture evidence (screenshots/DOM assertions), record results in the SUMMARY, and this checkpoint becomes a confirm-only sign-off. Only if browser tooling fails to start on this machine (the known Phase 1-2 risk) does this fall through to a human doing the checklist. This is the ONLY permitted human checkpoint in this phase.
  </what-built>
  <how-to-verify>
    Open https://p2401kumar.github.io/ and check, on the live site:
    1. Hero: the serif thesis renders with NO visible layout shift (CLS) or font-flash (FOUT) on load.
    2. The `→` glyph in the hero/quiet links and the `back to work →` / `next: ... →` case-study links renders cleanly (not a tofu box).
    3. Footer: the Seattle clock shows a real time and visibly ticks (updates within ~30s).
    4. Resize to 360px width: the systems list, hero, and case-study pages collapse cleanly with no horizontal scroll or overlap.
    5. Fig.01 interactions: `send request` animates a beam client->LB->cell->pipelines->ML; `inject fault` degrades a cell (amber/dashed) + logs + self-heals after ~8s; keyboard Tab reaches every control with a visible focus ring and no keyboard trap.
    6. Case-study pages: click a linked systems row -> lands on /work/{slug}; H1 (mono artifact name), standfirst, metrics strip (hover a value shows the source via native title tooltip), problem/approach(+trade-offs considered)/impact sections, and back/cross links all render; header + footer present.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe the specific issues to fix-forward.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo -> GitHub Pages | `git push` to main triggers the deploy.yml Actions workflow that publishes static output; the workflow already uses minimal permissions (contents:read, pages:write, id-token:write) |
| external audit tooling -> live site | `npx lighthouse` / PageSpeed Insights read the public live URL out-of-band; no write path, no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Tampering | deploy pipeline (push -> Pages) | low | accept | Existing verified deploy.yml (withastro/action@v6 + deploy-pages@v5, minimal permissions); fast-forward-only push, no force; no pipeline changes this plan |
| T-03-08 | Denial of Service | keyless PageSpeed Insights API fallback rate limits | low | accept | Only a handful of manual audits; primary path is local `npx lighthouse` against the public URL; fallback is occasional-use only (RESEARCH Assumptions A2) |
</threat_model>

<verification>
- deploy.yml run completed/success; both /work/* routes live (200) with the every-page shell.
- Live home page Lighthouse >= 90 in all four categories, recorded in 03-LIGHTHOUSE.md (fix-forward applied if needed).
- Residual visual QA confirmed (automated via /browse, or human-verified checklist).
- SUMMARY surfaces the post-launch reminder: retire the old p2401kumar.github.io/home repo.
</verification>

<success_criteria>
- The site is live and launch-ready; PLAT-07 met on the live URL across all four Lighthouse categories.
- The Phase 1-2 deferred in-browser visual QA is closed.
- As the final v1 plan, the SUMMARY records the milestone-wrap reminder to retire/redirect the old /home repo.
</success_criteria>

<output>
Create `.planning/phases/03-case-studies-launch-polish/03-04-SUMMARY.md` when done. Include the recorded Lighthouse scores, the residual-QA result, and the post-launch /home-repo-retirement reminder for the milestone wrap-up.
</output>