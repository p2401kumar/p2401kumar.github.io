---
phase: 3
slug: case-studies-launch-polish
status: draft
shadcn_initialized: false
preset: none
created: 2026-07-17
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **Source note (AUTO MODE — formalization, not design):** The design system is fully locked across Phases 1–2 (`01-UI-SPEC.md` shell contract, `02-UI-SPEC.md` figure contract) and is live in production (`src/styles/tokens.css`, `src/styles/global.css`, `src/components/SystemsList.astro`, `src/pages/404.astro`). This document **extends** those contracts to cover Phase 3's new surfaces — case-study article pages, their entry links in the systems list, and the static OG image — and never contradicts them. No new visual system is introduced. Values marked **"Claude's discretion (recommendation)"** below fill true gaps (article-length typography, blockquote/pull-metric treatment, OG image pixel composition) with conservative extensions of the existing token set — every color, font, and spacing value used is one already declared in `tokens.css` or the Phase 1 baseline scale. Canonical sources: `01-UI-SPEC.md`, `02-UI-SPEC.md`, `03-CONTEXT.md`, `src/styles/tokens.css`, `src/components/SystemsList.astro`, `src/pages/404.astro`, `.planning/reference/prototype-shell-and-fig01.html`.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — same as Phases 1–2. No shadcn/component library; explicit project-wide exclusion (STACK.md "What NOT to Use": no Tailwind, no UI framework). |
| Preset | not applicable |
| Component library | none — case-study pages are bespoke `.astro` files (`src/pages/work/[slug].astro`), composed from existing `BaseLayout` + `SiteHeader` + `SiteFooter` plus new case-study-specific markup/styles. OG image is a hand-authored SVG rasterized to PNG via `sharp` (build-time script, no runtime component). |
| Icon library | none. No new icons this phase. Cross-links and back-links reuse the existing literal-arrow-glyph idiom (`résumé →` pattern: mono text + trailing `→` character, no SVG). |
| Font | Reuses Phase 1's two self-hosted faces exactly — serif display (`Source Serif 4`) for standfirst/prose, mono (`Cascadia Code`) for artifact-name H1s, metrics, and labels. Sans body (system stack) for prose paragraphs, matching the existing `.sub`/`.ds` body-copy role. No new font is introduced. |

---

## Spacing Scale

Phase 1's baseline scale (`xs:4 / sm:8 / md:16 / lg:24 / xl:32 / 2xl:48 / 3xl:64`) applies to all new Phase 3 spacing. The `.page` shell (`max-width:880px`, `padding:0 clamp(18px,4vw,32px)`) and header/footer spacing are **already fully specified in `01-UI-SPEC.md`** — port verbatim, do not re-derive (confirmed identical in `src/pages/404.astro`, the precedent for a non-home page composed of `BaseLayout` + `SiteHeader` + `SiteFooter`).

### New case-study page spacing — Claude's discretion (recommendation), conservative extension of the locked scale

| Element | CSS | Value | Rationale |
|---------|-----|-------|-----------|
| `<main>` / article wrapper | `padding` | `88px 0 20px` (top matches `.hero`'s `88px` top padding — same "first content after header" rhythm as the home page) | Reuses Phase 1's hero top-offset exactly rather than inventing a new value |
| Case-study H1 (mono artifact name) | `margin` | `0 0 16px` | `md` — new element, so it sits on the baseline grid (one step below the hero's hand-tuned `h1`→`.sub` gap of 20px) |
| Standfirst (serif, beneath H1) | `margin` | `0 0 32px`; `max-width: 58ch` (same measure as `.sub`) | `32px` (xl) separates standfirst from the metrics strip — a full section-level break, since the metrics strip is a distinct visual block, not more running prose |
| Metrics strip | `padding` | `16px 0`; `border-top: 1px solid var(--hair)`; `border-bottom: 1px solid var(--hair)` | Reuses `--hair` hairline exactly as `.row` and `.fig` borders do; `16px` (md) vertical padding — a compact data block, not a prose block |
| Metrics strip | `gap` between metric items | `32px` desktop, wraps to `16px` row-gap under 640px | `xl`/`md` — matches `.links` gap doctrine (20px was hand-tuned for 3 short links; metrics are wider `label: value` pairs, so bumped one step to `32px` for breathing room, still on-scale) |
| Metrics strip → prose (first section) | `margin-top` | `32px` | Matches strip's own bottom spacing — symmetric block |
| Section heading (`Problem` / `Approach` / `Impact`) | `margin` | `48px 0 12px` (top), i.e. `2xl` above, `12px` below (on-grid; deliberately near `.work h2`'s locked hand-tuned `10px` without inheriting the exception into a new element) | `48px` matches "major section break" per the baseline scale table — problem/approach/impact are the article's top-level structure, equivalent weight to home page's `experience`/`patents`/`skills` section breaks (which use `64px`/`3xl` `.work` top-padding as their break) |
| Sub-heading ("Trade-offs considered") | `margin` | `24px 0 12px` | `lg` above, `12px` below (on-grid) — a subsection within Approach, one step down from the `48px` top-level break |
| Prose paragraph | `margin` | `0 0 16px` | `md` — standard paragraph rhythm, matches body line-height (1.6) without excessive whitespace between short paragraphs |
| Cross-link / back-link block (page end) | `margin-top` | `48px`; `padding-top` `24px`; `border-top: 1px solid var(--hair)` | `2xl` top margin marks it as a distinct closing block (same weight as a major section break); reuses `.footer`'s `border-top: 1px solid var(--hair)` hairline idiom scaled down |
| Systems-list row → case-study link | no new spacing | `.row` markup/CSS unchanged | Only the two now-linked rows (`dynamodb/cellularization`, `elb/auto-weight-away`) gain an `<a>` wrapper around the existing row content; `01-UI-SPEC.md`'s locked `.row` spacing/hover idiom applies unmodified |

Exceptions: **all NEW Phase 3 values above are on the 4px baseline grid.** Two off-grid values appear on case-study pages only by inheritance from Phase 1's locked hand-tuned exceptions table (user-approved prototype idiom, documented in `01-UI-SPEC.md`): the `.page` shell padding lower bound (`clamp(18px,…)`) and the unchanged `.row` gap (`18px`) on the systems list rows that gain links. These are ported verbatim per the lock — they are Phase 1 exceptions, not new Phase 3 spacing decisions, and changing them would regress the approved shipped design. No 44px touch-target exception needed — no icon-only controls are introduced this phase (cross-links and back-links carry mono text labels, same as Phase 1's hero links).

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body (prose) | 15px (inherits global `body`) | 400 | 1.6 |
| Label (mono, metrics/source tags) | 12px | 400 | 1.4 |
| Heading (section, mono uppercase — Problem/Approach/Impact) | 12px | 500 | 1.4 |
| Display (case-study H1, mono artifact name) | `clamp(22px, 3.4vw, 32px)` | 500 | 1.2 |

### New case-study type map — Claude's discretion (recommendation), built from Phase 1's locked component type map, no new values invented

| Element | Font | Size | Weight | Letter-spacing | Color |
|---------|------|------|--------|-----------------|-------|
| Case-study H1 (mono artifact name, e.g. `dynamodb/cellularization`) | mono | `clamp(22px, 3.4vw, 32px)` | 500 | −0.005em | `--ink`; `line-height:1.2`. Sized below the home hero's serif thesis (`clamp(30,4.6vw,44)`) since this is a page-level heading, not the site's primary thesis — same visual family as `.row .nm` (mono, `--ink`) promoted to display scale, per `03-CONTEXT.md` "the systems-list idiom promoted to page scale" |
| Standfirst (serif sentence beneath H1) | serif | `20px` | 400 | — | `--body`; `line-height:1.4`; `max-width:58ch`. One step below the home `.sub` in weight/role — same face (`--serif`) as the hero thesis but at prose scale, signaling "this is the article's framing sentence," not a second display headline |
| Metrics strip: label | mono | 11px | 400 | .06em, uppercase | `--dim` |
| Metrics strip: value | mono | 15px | 500 | — | `--accent` (reuses `.row .mt`'s accent-metric idiom exactly — same role, promoted to a standalone strip) |
| Metrics strip: source annotation (tooltip/footnote marker) | mono | 11px | 400 | — | `--faint`; revealed via native `title` attribute or an inline parenthetical, not a new tooltip widget (see Claude's discretion note below) |
| Section heading (`problem` / `approach` / `impact`) | mono | 12px | 500 | .18em, uppercase | `--dim` — identical to `.work h2` (`selected systems`) treatment, lowercase-only casing preserved |
| Sub-heading (`trade-offs considered`) | mono | 12px | 400 | .1em, uppercase | `--faint` — one step quieter than the top-level section heading, signals subsection without competing |
| Prose paragraph | sans | 15px | 400 | — | `--body`; inline links `--ink` with `border-bottom:1px solid var(--hair2)` (identical to global `a` rule) |
| Cross-link / back-link | mono | 13px | 400 | — | `--ink`, underline `border-bottom:1px solid var(--hair2)`, hover border → `--accent` (identical to Hero's `résumé →` link idiom, incl. trailing `→` glyph) |

**Metrics strip source annotations — Claude's discretion (recommendation):** `03-CONTEXT.md` calls for `source` tooltips/annotations on each metric but does not specify the exact UI. Recommend the simplest mechanism consistent with the site's "no new interactive widgets" restraint: a native `title` attribute on each metric value (`<span class="mv" title="resume-4.5 §AWS — DynamoDB cellularization...">+30% rel</span>`) — zero new markup, zero new JS, works today with keyboard/mouse hover via the browser's native tooltip, and the underlying zod-schema `source` field is still present in the data for anyone reading the collection source. Do not build a custom hover-card component for this — that would be new interactive surface for a phase whose stated boundary is "launch polish," not new UI patterns.

---

## Color

Dark-only palette, identical token set to Phases 1–2 — **no new hex values are introduced in Phase 3.** Every color below reads from `src/styles/tokens.css` via `var(--token)`.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `--bg: #0f1216` | Case-study page background (identical to home) |
| Secondary (30%) | `--panel2: #141a22` | Row hover background (existing `.row:hover` rule, now applies to the two linked rows); no new panel surface introduced on the case-study page itself — it stays a flat prose page, consistent with the editorial-restraint doctrine |
| Accent (reserved) | `--accent: #d99163` | **Reserved for:** metrics-strip values (`.mv`), cross-link/back-link hover border, OG image accent motif element (cell/route line). Never a fill color, never body/standfirst text. |
| Destructive | `--amber: #d9a441` | Not used this phase — no destructive actions on case-study pages (declared for consistency, unused, same as Phase 1's precedent) |

### Supplementary text/line colors (unchanged from Phase 1, reused verbatim)

| Token | Value | Usage on case-study pages |
|-------|-------|------|
| `--ink` | `#e8ebef` | H1, prose inline-link text, cross-link text |
| `--body` | `#aab2bd` | Standfirst, prose paragraphs |
| `--dim` | `#78818d` | Section headings, metric labels |
| `--faint` | `#565f6b` | Sub-heading ("trade-offs considered"), source annotations |
| `--hair` | `#1e242d` | Metrics-strip top/bottom border, cross-link block top border |
| `--hair2` | `#28303b` | Link underline default state |

---

## Copywriting Contract

Locked verbatim per `03-CONTEXT.md` where specified; **Claude's discretion (recommendation)** entries fill true gaps in the site's established factual/no-adjective voice ("no adjectives about self").

| Element | Copy |
|---------|------|
| Case-study H1 (mono artifact name) | `dynamodb/cellularization` / `elb/auto-weight-away` — verbatim from `src/data/systems.ts` `name` field, unchanged |
| Standfirst | **Claude's discretion (recommendation), one sentence per page, no adjectives about self:** `dynamodb/cellularization` → "Compute/storage segregation into isolated failure domains across US-EAST-1, extending AWS SDK Java v1/v2 for the new topology." `elb/auto-weight-away` → "Load-balancer management that pre-allocates surge capacity and automatically weighs out affected LBs — no human in the loop." (both are the existing `systems.ts` `description` field verbatim — reuse rather than write new copy, keeping the honesty gate trivially satisfiable) |
| Metrics strip (dynamodb/cellularization) | `+30% reliability` · `−20% p99 latency` — each with `source` attribute `resume-4.5 §AWS — DynamoDB cellularization (+30% reliability, −20% p99 latency)` |
| Metrics strip (elb/auto-weight-away) | `−10% peak-hour latency` · `90% ops automated` — each with `source` per `03-CONTEXT.md` decisions (`90% of capacity adjustments automated`) |
| Section headings | lowercase mono: `problem`, `approach`, `impact` — casing IS the brand (same doctrine as `selected systems`, never capitalize) |
| Sub-heading (within approach) | lowercase mono: `trade-offs considered` |
| Primary CTA / back-link | `back to work →` — links to `/#work` (per `03-CONTEXT.md` "quiet link back to `/#work`"); reuses the exact copy already shipped on `404.astro`'s `back to work →` link (pointing at `/` there; here at `/#work` since the origin is the systems list, not the whole home page) |
| Cross-link (to the other case study) | **Claude's discretion (recommendation):** `next: elb/auto-weight-away →` on the `dynamodb-cellularization` page, `next: dynamodb/cellularization →` on the `elb-auto-weight-away` page — mono, artifact-name format matches the H1/row-name idiom exactly, `next:` prefix (lowercase, no colon-adjacent adjective) signals sequence without narrative flourish |
| Systems-list row links (dynamodb, elb rows) | No copy change — existing row markup (`entry.name`/`entry.description`/`entry.metric`) becomes the link's visible content, wrapped in `<a href="/work/{slug}">`; the other two rows (`azure/health-snapshots`, `iot/contextual-widget`) stay unlinked plain text, per `03-CONTEXT.md` |
| Empty state | N/A (default) — case-study pages render only fixed, build-time content collection entries; no user-generated/dynamic content, no empty-state UI exists |
| Error state (malformed content-collection entry) | **Claude's discretion (recommendation):** not a runtime UI state — the honesty/schema gate rejects malformed entries at `astro build`/`astro check` time (per `03-CONTEXT.md` CASE-03), so there is no user-facing error copy to define; the "error" surfaces as a build failure, not a page |
| Error state (404, existing) | Unchanged from `01-UI-SPEC.md`/`src/pages/404.astro` — `404` + `this route doesn't exist.` + `back to work →`. No case-study-specific 404 variant needed. |
| Destructive confirmation | None (default) — no destructive actions exist on case-study pages (same as Phases 1–2: no forms, no deletions, no state-changing actions) |
| OG image text | Locked per `03-CONTEXT.md`: `Prateek Kumar` + `I build the infrastructure that intelligence runs on.` — nothing else competing on the image (no per-case-study OG variant is required; one shared static image serves all pages per STACK.md's "one bespoke, hand-designed OG image" recommendation and the sitemap's 3-page scope) |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|--------------|
| shadcn official | none — shadcn not initialized (project-wide decision, carried from Phases 1–2) | not applicable |
| third-party | none declared | not applicable |

No component registry is used in Phase 3 — consistent with the project's explicit "no UI framework, no component library" constraint (`STACK.md` "What NOT to Use"). Case-study pages, the metrics strip, and the OG image are 100% hand-authored `.astro`/CSS/SVG + a build-time `sharp` rasterization script, no runtime component dependency.

---

## Supplementary: Case-Study Page Structure & OG Image Composition (context for executor, not template-required)

### Case-study page section order (`src/pages/work/[slug].astro`)

1. Header (`SiteHeader` — unchanged, "every page needs header/footer" per `01-VERIFICATION.md`)
2. Article: mono H1 (artifact name) → serif standfirst → metrics strip → `problem` section → `approach` section (incl. `trade-offs considered` sub-heading) → `impact` section
3. Cross-link block: `back to work →` + `next: {other-slug} →`
4. Footer (`SiteFooter` — unchanged)

Both pages share one `[slug].astro` template driven by the content-collection schema; no per-page bespoke layout divergence.

### OG image composition — Claude's discretion (recommendation), 1200×630, one shared static asset

Reuses Fig. 01's aesthetic per `03-CONTEXT.md`/STACK.md, built from tokens already in `tokens.css` and `02-UI-SPEC.md` — no new colors:

| Element | Treatment |
|---------|-----------|
| Ground | `--bg` (`#0f1216`) full-bleed, with Fig. 01's `22px` dot-grid substrate at low opacity (reuse `02-UI-SPEC.md`'s dot-grid spacing exactly) |
| Motif | A simplified cell/route glyph in the right or lower-right third — 2–3 rounded rects (`--panel2` fill, matching Fig. 01's `cell0..3` node style) connected by one elbow route rendered in `--accent` at reduced weight — a quiet echo of Fig. 01, not a literal screenshot |
| Name | `Prateek Kumar` — serif (`--serif`), `--ink`, roughly the same relative weight/size as the site's H1 role at OG-image scale (large, upper-left or center-left, matching hero's left-aligned reading order) |
| Thesis | `I build the infrastructure that intelligence runs on.` — sans (`--sans` or serif — Claude's discretion, recommend serif to match the site's hero treatment where the thesis lives), `--body`, directly beneath the name, single line or soft-wrapped at a wide measure so it stays legible at social-preview thumbnail sizes |
| Accent budget | Motif route line + any single accent underline beneath the name — same "<5% of surface, never a fill" doctrine as the rest of the site |
| Build | Author as SVG in-repo (`src/assets/og/` or similar), rasterize to PNG via a small `sharp` node script at build time (or commit the PNG directly if simpler) — no new runtime dependency, `sharp` already available via `astro:assets` |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
