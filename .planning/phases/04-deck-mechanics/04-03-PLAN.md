---
phase: 04-deck-mechanics
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - .planning/phases/04-deck-mechanics/lighthouse-scores.md
autonomous: false
requirements: [DECK-01, DECK-02, DECK-03, DECK-04, DECK-05, DECK-06, DECK-07, DECK-08]

must_haves:
  truths:
    - "The production build is green and astro check reports 0 errors with the deck active"
    - "Lighthouse >=90 in all four categories against the LOCAL preview with the deck active"
    - "No deck JavaScript is reachable from any /work/* route"
    - "The no-JS fallback is intact: all v1 content in the DOM, panels hidden by transform only, one id=contact"
    - "A human confirms one-gesture-one-panel feel, deep-link cold-load, back/forward, reduced-motion, hint, and view-classic"
  artifacts:
    - path: ".planning/phases/04-deck-mechanics/lighthouse-scores.md"
      provides: "Recorded local Lighthouse scores (mobile + desktop), all four categories, with the deck active"
      contains: "performance"
  key_links:
    - from: "dist/index.html"
      to: "src/lib/nightsky/deck.ts (bundled)"
      via: "home page references the deck chunk; /work/* pages do not"
      pattern: "nightsky:panel-change"
  prohibitions:
    - "Do NOT run git push anywhere in this phase — the live site stays v1 until Phase 6. All commits remain local on main."
    - "Do NOT deploy, publish, or trigger the GitHub Actions Pages pipeline this phase."
---

<objective>
Prove the deck meets its acceptance surface without shipping it: run the full static + build gate, run Lighthouse against the LOCAL preview (mobile + desktop) with the deck active and record the scores in the phase directory, assert the deck JS is unreachable from `/work/*`, assert the no-JS fallback is intact, then hold a human-verify checkpoint for the feel-and-flow criteria that cannot be automated (one-gesture-one-panel, cold-load deep link, back/forward, reduced-motion, first-visit hint, view-classic) plus the flagged cross-device note.

Purpose: This is the phase's acceptance gate for DECK-01..08. It is deliberately a separate, non-autonomous plan because it pairs an automated gate with a blocking human checkpoint.

Output: `.planning/phases/04-deck-mechanics/lighthouse-scores.md` and a human sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deck-mechanics/04-CONTEXT.md
@.planning/phases/04-deck-mechanics/04-RESEARCH.md

@src/lib/nightsky/deck.ts
@src/styles/deck.css
@src/pages/index.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated gate — build, astro check, zero-hex, deck-not-on-/work, no-JS fallback, Lighthouse local</name>
  <files>.planning/phases/04-deck-mechanics/lighthouse-scores.md</files>
  <read_first>
    - .planning/phases/04-deck-mechanics/04-RESEARCH.md section "Lighthouse Local-Preview Invocation" (exact commands; preview serves http://localhost:4321/)
    - src/styles/tokens.css (the only file permitted to hold hex literals)
    - src/pages/work/[slug].astro (confirm it does not import PanelDeck — the structural reason deck JS never loads on /work/*)
  </read_first>
  <action>
    Run the complete automated acceptance gate. Commands run under Git Bash (Windows) with no PowerShell-specific syntax.

    1. Build + typecheck: `npm run build` (must succeed) and `npx astro check` (0 errors).

    2. Zero hex literals outside tokens.css: grep the deck source (`src/styles/deck.css`, `src/lib/nightsky/deck.ts`, `src/components/PanelDeck.astro`, `src/components/Panel.astro`, `src/components/DeckIndex.astro`, `src/data/panels.ts`) for the 3-to-8 digit hex-color pattern — expect 0.

    3. No-JS fallback intact (DECK-07): in `dist/index.html` assert exactly 7 `class="panel"`, exactly one `id="contact"`, all deep-link ids present (`id="hero"`, `id="fig-01"`, `id="systems"`, `id="experience"`, `id="patents"`, `id="skills"`), representative v1 content strings present (e.g. `request path through a cellularized region`, `all systems operational`), and the aria-live region present (`id="deck-live"`). Assert `src/styles/deck.css` never hides a panel via the CSS box-suppression or visibility properties (the negative grep in the verify block enforces this) — panels are transform-hidden only.

    4. Deck JS unreachable from /work/* (orchestrator acceptance criterion): use the `nightsky:panel-change` string as the deck-code sentinel. Assert the deck chunk exists for the home build and that NO `/work/*` HTML inlines or references it. For each `dist/work/*/index.html`, resolve every referenced `/_astro/*.js` chunk and confirm none contains the sentinel (see the verify loop), and that the HTML itself does not inline it.

    5. Lighthouse local (deck active): start the preview server in the background (`npm run preview` → http://localhost:4321/), then run Lighthouse mobile (default preset) and desktop preset headless against `http://localhost:4321/`. Extract the four category scores from each run and WRITE them to `.planning/phases/04-deck-mechanics/lighthouse-scores.md` (a small markdown table: run | performance | accessibility | best-practices | seo). Every category must be >= 0.90 (>=90). Stop the preview server when done. If any category is <90, record the shortfall and the specific audit(s) responsible — do NOT silently pass.

    Record a one-line PASS/FAIL summary per check at the top of `lighthouse-scores.md`.
  </action>
  <verify>
    <automated>npm run build && npx astro check</automated>
    <automated>test "$(grep -roE '#[0-9a-fA-F]{3,8}' src/styles/deck.css src/lib/nightsky/deck.ts src/components/PanelDeck.astro src/components/Panel.astro src/components/DeckIndex.astro src/data/panels.ts | wc -l | tr -d ' ')" = "0"</automated>
    <automated>test "$(grep -oE 'display:\s*none|visibility:\s*hidden' src/styles/deck.css | wc -l | tr -d ' ')" = "0"</automated>
    <automated>test "$(grep -o 'class="panel"' dist/index.html | wc -l | tr -d ' ')" = "7" && test "$(grep -o 'id="contact"' dist/index.html | wc -l | tr -d ' ')" = "1"</automated>
    <automated>test "$(grep -rl 'nightsky:panel-change' dist/work 2>/dev/null | wc -l | tr -d ' ')" = "0" && test "$(grep -rl 'nightsky:panel-change' dist/_astro 2>/dev/null | wc -l | tr -d ' ')" != "0"</automated>
    <automated>for html in dist/work/*/index.html; do for js in $(grep -oE '/_astro/[A-Za-z0-9_.-]+\.js' "$html"); do grep -q 'nightsky:panel-change' "dist${js}" && { echo "LEAK $html -> $js"; exit 1; }; done; done; echo OK</automated>
    <automated>grep -qi 'performance' .planning/phases/04-deck-mechanics/lighthouse-scores.md</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds and `npx astro check` reports 0 errors (deck active).
    - Zero hex literals across all deck source files (grep of the 3-8 digit hex pattern = 0).
    - `src/styles/deck.css` panel-hiding negative grep (`display:\s*none|visibility:\s*hidden`) = 0.
    - `dist/index.html`: 7 `class="panel"`, exactly 1 `id="contact"`, all six other deep-link ids present, key v1 content strings present, `id="deck-live"` present.
    - Deck sentinel `nightsky:panel-change` present in `dist/_astro` but absent from every `/work/*` HTML and every JS chunk those pages reference (leak loop prints OK).
    - `.planning/phases/04-deck-mechanics/lighthouse-scores.md` records mobile + desktop scores; every one of the four categories >= 90 on both runs.
  </acceptance_criteria>
  <done>Build + astro check green; zero hex; transform-only panel hiding; no-JS fallback intact; deck JS proven unreachable from /work/*; Lighthouse >=90 x4 recorded for mobile and desktop against the local preview.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — deck feel, cold-load, back/forward, reduced-motion, hint, view-classic</name>
  <action>
    Blocking human-verify gate (no autonomous action — the executor pauses here). Serve the production build locally (`npm run build && npm run preview`) and walk the operator through the how-to-verify checklist below covering DECK-01..08 feel-and-flow criteria that cannot be automated. Record the operator's response; on anything other than "approved", capture the failing input path / panel / behavior for gap closure. This is LOCAL-only verification — do not push or deploy.
  </action>
  <what-built>
    A live no-scroll panel deck on the home page (local preview only — NOT deployed): wheel/touch/keyboard/jump-index navigation, hash routing with cold-load deep links, inert + aria-live panels, a first-visit hint, a view-classic escape hatch, and reduced-motion instant transitions. The automated gate (task 1) already confirmed build/lint/hex/fallback/leak/Lighthouse.
  </what-built>
  <how-to-verify>
    Serve the production build locally: `npm run build && npm run preview`, then open http://localhost:4321/ and check:

    1. Wheel (DECK-01): one notch of a mouse wheel advances EXACTLY one panel; one trackpad swipe advances EXACTLY one panel (no skipping 2-3, no dead feel, no double-fire). Scroll up retreats one panel.
    2. Keyboard (DECK-03): ArrowDown/ArrowUp, PageDown/PageUp, Home, End each move panels; Tab into Fig. 01's panel and confirm arrow keys still page the deck (no collision) and Fig. 01's own controls still work; confirm a screen reader (or the DOM `#deck-live` text) announces "Panel N of M — Title" on each change; confirm hidden panels are not tab-reachable (inert).
    3. Jump index (DECK-04): the mono `NN / 07` index expands to the panel list; clicking/keyboard-activating an entry jumps directly to that panel and updates the index.
    4. Hash + history (DECK-05): each navigation updates the URL hash; browser Back/Forward move between panels; open a fresh tab at `http://localhost:4321/#patents` and confirm it cold-loads directly to Patents with NO flash of the hero panel first.
    5. First-visit hint (DECK-06): in a fresh profile / cleared localStorage, the `scroll · ↓ · swipe` hint shows, then disappears after the first navigation and does not return on reload.
    6. View-classic (DECK-07): the `view classic →` link restores normal scrolling (deck off) and persists across reload; the `deck view →` link returns to the deck. With JS disabled entirely, the page is the plain v1 scrolling layout.
    7. Reduced-motion (DECK-08): enable OS "reduce motion" and confirm panel changes are instant (no slide), while everything else still works.
    8. Touch (DECK-02): if a phone/tablet or device-emulation is available, vertical swipe pages one panel and horizontal swipe does NOT hijack (iOS edge-back intact). Real iPhone + mid-tier Android testing is a flagged checklist item — if hardware is unavailable this phase, note it as carried to Phase 6's checkpoint (do NOT block on physical devices).

    Reminder: this is LOCAL only. Do NOT push, deploy, or publish — the live site stays v1 until Phase 6.
  </how-to-verify>
  <resume-signal>Type "approved" if all criteria pass, or describe the specific input path / panel / behavior that failed.</resume-signal>
  <done>Operator confirms all eight input/flow criteria on the local preview (or logs specific failures for gap closure). No push/deploy performed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local build/preview → verification | Verification runs entirely against the local dist/preview; nothing is published |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Elevation/Deploy | accidental publish | medium | mitigate | Explicit no-push / no-deploy prohibition in must_haves.prohibitions; verification is local-preview only; no CI/Pages trigger this phase |
| T-04-01 | Tampering | hash resolution (re-verified) | low | mitigate | Deep-link cold-load test includes a nonexistent-hash spot check falling back to panel 0 (implemented in 04-02) |
| T-04-SC | Tampering | package installs | low | accept | Zero new dependencies this phase |
</threat_model>

<verification>
- Task 1 automated gate all green (build, astro check, zero-hex, transform-only, no-JS fallback, /work leak loop, Lighthouse >=90 x4 recorded).
- Human checkpoint: all eight input/flow criteria confirmed on the local preview.
- No `git push`, deploy, or Pages trigger anywhere in this phase.
</verification>

<success_criteria>
- All DECK-01..08 acceptance criteria pass: automated where possible, human-confirmed for feel/flow.
- Lighthouse >=90 in all four categories (mobile + desktop) against the local preview with the deck active, recorded in the phase dir.
- Deck JS provably absent from /work/*; no-JS fallback provably intact.
- The site is NOT deployed; all commits remain local on main.
</success_criteria>

## Artifacts This Phase Produces (Plan 04-03)

**New files:**
- `.planning/phases/04-deck-mechanics/lighthouse-scores.md` — recorded local Lighthouse scores (mobile + desktop; performance / accessibility / best-practices / seo) with a PASS/FAIL summary of every automated check.

**No source files are modified.** This plan only verifies and records. No new modules, components, exports, or CSS classes.

<output>
Create `.planning/phases/04-deck-mechanics/04-03-SUMMARY.md` when done.
</output>
