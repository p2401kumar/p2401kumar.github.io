---
phase: 05-night-sky-scene
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/shared/css-tokens.ts
  - src/lib/fig01/tokens.ts
  - src/data/constellations.ts
autonomous: true
requirements: [CONST-01]

must_haves:
  truths:
    - "The generic getComputedStyle/parseHex/rgba logic lives once in src/lib/shared/css-tokens.ts; fig01/tokens.ts re-exports/wraps it and keeps the FigTokens shape and fig01-specific token names"
    - "Fig. 01 does not regress: astro check reports 0 errors, zero hex literals remain in src/lib/fig01, and the requestAnimationFrame occurrence count in src/lib/fig01 is unchanged (2)"
    - "src/data/constellations.ts defines exactly 4 typed constellations (aws, microsoft, samsung, education-patents) with normalized star coords, magnitudes, link pairs, a data-only display label, a source annotation, and a panelIds mapping"
    - "Every constellation source annotation traces to strings already validated in experience.ts / patents.ts / systems.ts — nothing invented (honesty gate)"
    - "The panelIds mapping is the single source of truth for panel→constellation highlighting; at most one constellation maps to any panel; hero and contact map to none"
  artifacts:
    - path: "src/lib/shared/css-tokens.ts"
      provides: "Generic RgbTriple, parseHex, readToken, getComputedStyle-once cache helper, and rgba() — consumed by both fig01 and nightsky token bridges"
      contains: "rgba"
    - path: "src/lib/fig01/tokens.ts"
      provides: "Thinned fig01 token bridge — FigTokens shape + fig01 token names, delegating parse/format to shared/css-tokens.ts; behavior identical"
      contains: "FigTokens"
    - path: "src/data/constellations.ts"
      provides: "Typed 4-constellation data module (CONST-01) with honesty-gate source annotations and panelIds mapping"
      contains: "panelIds"
  key_links:
    - from: "src/lib/fig01/tokens.ts"
      to: "src/lib/shared/css-tokens.ts"
      via: "fig01 token bridge delegates hex parsing + rgba formatting to the shared module"
      pattern: "css-tokens"
    - from: "src/data/constellations.ts"
      to: "src/data/panels.ts"
      via: "panelIds values reference panel ids from the shipped manifest (fig-01, systems, experience, patents, skills)"
      pattern: "panelIds"
  prohibitions:
    - "Do NOT run git push anywhere in this phase — the live site stays v1 until Phase 6. All commits remain local on main."
    - "Do NOT deploy, publish, or trigger the GitHub Actions Pages pipeline this phase."
    - "Do NOT change any observable Fig. 01 behavior, token values, or the FigTokens field set — this is a pure refactor; the fig01 non-regression gate must stay green."
    - "Do NOT invent any constellation source fact — every source annotation must reuse a string already validated in experience.ts / patents.ts / systems.ts."
---

<objective>
Lay the two spike-independent foundations both downstream engines depend on. First, extract the generic getComputedStyle/parseHex/rgba logic out of fig01/tokens.ts into a new shared module src/lib/shared/css-tokens.ts (locked in 05-CONTEXT.md's Scene-architecture decision — "extract NOW, both engines consume it, fig01 must not regress"), and thin fig01/tokens.ts to a wrapper that keeps its FigTokens shape and fig01-specific token names while delegating the mechanics. Second, author src/data/constellations.ts: the typed 4-constellation data module (CONST-01) with normalized coordinates, magnitudes, link pairs, data-only labels, honesty-gate source annotations, and the authoritative panel→constellation mapping. Neither task depends on the Milky Way spike, so this plan runs in parallel with 05-01 in Wave 1.

Purpose: The shared token bridge unblocks nightsky/tokens.ts without duplicating hex-parsing (single-source-of-truth doctrine); the constellation data module unblocks the constellation render (05-05) and locks the honesty gate early.

Output: `src/lib/shared/css-tokens.ts`, a thinned `src/lib/fig01/tokens.ts`, and `src/data/constellations.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-night-sky-scene/05-CONTEXT.md
@.planning/phases/05-night-sky-scene/05-RESEARCH.md
@.planning/phases/05-night-sky-scene/05-UI-SPEC.md

@src/lib/fig01/tokens.ts
@src/data/panels.ts
@src/data/experience.ts
@src/data/patents.ts
@src/data/systems.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract src/lib/shared/css-tokens.ts and thin fig01/tokens.ts (fig01 non-regression gate)</name>
  <files>src/lib/shared/css-tokens.ts, src/lib/fig01/tokens.ts</files>
  <read_first>
    - src/lib/fig01/tokens.ts (the current 74-line module — RgbTriple, parseHex, readToken, getTokens's caching, rgba; only the FigTokens interface + the getTokens() field list are fig01-specific)
    - .planning/phases/05-night-sky-scene/05-RESEARCH.md "Standard Stack" row for src/lib/shared/css-tokens.ts and "Don't Hand-Roll" (RGB hex parsing) — the extraction contract
    - .planning/phases/05-night-sky-scene/05-CONTEXT.md Scene-architecture decision (the extraction is LOCKED; fig01 must not regress: zero hex, astro check, single-rAF greps re-run)
  </read_first>
  <action>
    Create src/lib/shared/css-tokens.ts exporting the generic, engine-agnostic pieces currently in fig01/tokens.ts: the RgbTriple interface, parseHex (3- and 6-digit hex support), rgba(triple, alpha), and a generic reader that reads getComputedStyle(document.documentElement) once and returns a parsed triple for a given custom-property name. Provide a small reusable helper so each engine can build its own cached token set from a list of property names (e.g. a readToken(styles, name) used by callers, plus a way to obtain the root computed styles once) — keep the getComputedStyle-once caching benefit intact. Do NOT bake fig01's token name list into the shared module.

    Rewrite src/lib/fig01/tokens.ts as a thin wrapper: keep the exported FigTokens interface and the fig01-specific token name list, keep the getTokens() public signature and its identity-stable cache behavior, and re-export RgbTriple and rgba from the shared module so existing importers (render.ts, interactions.ts, index.ts) compile unchanged. The observable behavior of getTokens()/rgba() must be identical — no token added or removed, no value changed.

    Re-run the fig01 non-regression gate after the change: astro check 0 errors, build green, zero hex literals in src/lib/fig01, and the requestAnimationFrame occurrence count in src/lib/fig01 unchanged at the baseline of 2 (render.ts's tick + initial call — this task does not touch render.ts).
  </action>
  <verify>
    <automated>npm run build && npx astro check</automated>
    <automated>test "$(grep -roE '#[0-9a-fA-F]{3,8}' src/lib/fig01 src/lib/shared | wc -l | tr -d ' ')" = "0"</automated>
    <automated>test "$(grep -roE 'requestAnimationFrame' src/lib/fig01 | wc -l | tr -d ' ')" = "2"</automated>
    <automated>test "$(grep -oE 'export (interface FigTokens|function getTokens)' src/lib/fig01/tokens.ts | wc -l | tr -d ' ')" = "2"</automated>
    <automated>test -f src/lib/shared/css-tokens.ts && test "$(grep -oE 'export function rgba' src/lib/shared/css-tokens.ts | wc -l | tr -d ' ')" != "0"</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/shared/css-tokens.ts exists and exports the generic RgbTriple + parseHex + rgba + a getComputedStyle-once reader; it contains NO fig01-specific token name list.
    - src/lib/fig01/tokens.ts still exports `interface FigTokens` and `function getTokens`, delegates parse/format to the shared module, and keeps identical observable behavior.
    - `npm run build` succeeds and `npx astro check` reports 0 errors.
    - Zero hex literals across src/lib/fig01 and src/lib/shared (grep = 0).
    - requestAnimationFrame occurrence count in src/lib/fig01 = 2 (unchanged baseline).
  </acceptance_criteria>
  <done>Shared css-tokens module extracted; fig01/tokens.ts thinned to a wrapper with identical behavior; fig01 non-regression gate green (astro check, zero hex, rAF count, build).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author src/data/constellations.ts — 4-constellation typed data module with honesty gate + panelIds mapping (CONST-01)</name>
  <files>src/data/constellations.ts</files>
  <read_first>
    - .planning/phases/05-night-sky-scene/05-UI-SPEC.md "Constellation data — honesty gate" table (id → label → source → panelIds) AND "Constellation placement" table (per-cluster x/y fraction bounds, 5–8 stars each, 4–6 link segments, never a mesh)
    - .planning/phases/05-night-sky-scene/05-CONTEXT.md Constellations decision (typed module fields: normalized coords, magnitudes, link pairs, display label, source annotation, panelIds mapping; at most one brightened; hero/contact → none)
    - src/data/experience.ts, src/data/patents.ts, src/data/systems.ts (the ONLY permitted sources for the `source` annotations — reuse their validated strings)
    - src/data/panels.ts (the panel ids the panelIds mapping references)
  </read_first>
  <action>
    Create src/data/constellations.ts as a typed, build-time data module (compiled into the static bundle like panels.ts). Define an exported interface for a constellation: an id (union of 'aws' | 'microsoft' | 'samsung' | 'education-patents'), a data-only `label` string, a `source` honesty-gate annotation, an array of `panelIds` this constellation highlights for, and a graph — normalized star positions (x/y as 0–1 viewport fractions within the UI-SPEC placement bounds for that cluster), a per-star magnitude/brightness band marker, and an array of link pairs (index-to-index) forming a sparse path + 1–2 cross-links (4–6 segments total, never fully connected). Also export a magnitude/band enum or numeric convention consistent with the UI-SPEC star bands, and a typed helper or constant that resolves a panel id to its constellation id (or none) — this mapping IS the single source of truth (05-05's render reads it; it never re-derives).

    Author the 4 clusters using the UI-SPEC placement table: microsoft (left-upper x:0.06–0.20, y:0.10–0.35), samsung (left-mid x:0.06–0.20, y:0.40–0.65), aws (right-upper near the Milky Way inner edge x:0.80–0.95, y:0.15–0.45), education-patents (right-lower x:0.80–0.95, y:0.50–0.75) — 6 stars each recommended, all coords strictly inside the outer margins (never inside the content-safe column x:0.20–0.80). panelIds mapping: aws→[fig-01, systems], microsoft→[experience], samsung→[skills], education-patents→[patents]; hero and contact map to no constellation.

    Honesty gate: each `source` annotation must reuse a string already validated in experience.ts / patents.ts / systems.ts (employer/education names + patent facts only). Do NOT invent dates, titles, metrics, or any fact not already present in those modules. The `label` is data-only (never rendered as canvas text this phase).
  </action>
  <verify>
    <automated>npm run build && npx astro check</automated>
    <automated>test "$(grep -oE "'aws'|'microsoft'|'samsung'|'education-patents'" src/data/constellations.ts | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test "$(grep -oE 'panelIds' src/data/constellations.ts | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test "$(grep -oE 'source' src/data/constellations.ts | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test "$(grep -roE '#[0-9a-fA-F]{3,8}' src/data/constellations.ts | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - src/data/constellations.ts exports a typed 4-constellation module (aws, microsoft, samsung, education-patents) with normalized star coords, magnitudes, link pairs (4–6 sparse segments per cluster, never a mesh), a data-only label, a source annotation, and a panelIds mapping.
    - All star coordinates fall inside the UI-SPEC outer-margin bounds for their cluster (none inside the content-safe column x:0.20–0.80).
    - panelIds mapping matches: aws→[fig-01,systems], microsoft→[experience], samsung→[skills], education-patents→[patents]; hero/contact → none; at most one constellation per panel.
    - Every source annotation reuses a validated string from experience.ts / patents.ts / systems.ts (no invented facts).
    - astro check 0 errors; build green; zero hex in the data module.
  </acceptance_criteria>
  <done>The typed constellation data module exists with a validated honesty gate and the authoritative panelIds mapping; type-checks and builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time data authoring → static bundle | constellations.ts is developer-authored, compiled in; no user/network input crosses here |
| local build → verification | All work is local; nothing is published |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-ID | Information Disclosure | constellation labels/source annotations | medium | mitigate | Honesty gate: every source reuses a string already validated in experience.ts/patents.ts/systems.ts; labels are employer/education names + patent facts only; nothing invented (matches the shipped `source:` provenance pattern) |
| T-05-02 | Tampering | fig01 token refactor regression | low | mitigate | Pure refactor; fig01 non-regression gate (astro check, zero hex, rAF count = 2, build) re-run; FigTokens shape + getTokens signature preserved |
| T-05-SC | Tampering (supply chain) | package installs | low | accept | Zero new dependencies in this plan |
| T-05-DEPLOY | Elevation / accidental deploy | GitHub Pages pipeline | medium | mitigate | Explicit no-push / no-deploy prohibition; local-only |
</threat_model>

<verification>
- fig01 non-regression gate green: astro check 0 errors, build green, zero hex in src/lib/fig01 and src/lib/shared, rAF count in src/lib/fig01 = 2.
- src/data/constellations.ts type-checks and builds; honesty-gate source annotations all trace to existing data modules.
- No `git push`, deploy, or Pages trigger anywhere in this plan.
</verification>

<success_criteria>
- The shared css-tokens bridge exists (single source of truth for hex parsing/rgba), consumed identically by fig01 with no regression.
- CONST-01's typed constellation data module exists with a validated honesty gate and the authoritative panel→constellation mapping.
- The site is NOT deployed; all commits remain local on main.
</success_criteria>

## Artifacts This Phase Produces (Plan 05-02)

**New files:**
- `src/lib/shared/css-tokens.ts` — generic getComputedStyle/parseHex/rgba token bridge shared by fig01 and nightsky. Exports: `RgbTriple`, `parseHex`, `rgba`, a getComputedStyle-once root-styles reader + `readToken` helper.
- `src/data/constellations.ts` — typed 4-constellation data module (CONST-01). Exports: the constellation interface/type, the `constellations` array, and the panel→constellation resolution mapping/helper.

**Modified files:**
- `src/lib/fig01/tokens.ts` — thinned to a wrapper over `shared/css-tokens.ts`; keeps `FigTokens` + `getTokens()` public contract and all fig01 token names; behavior identical (non-regression).

**New exports downstream plans consume:** `shared/css-tokens.ts` (→ 05-03 nightsky/tokens.ts), `constellations.ts` data + mapping (→ 05-05 constellation render).

<output>
Create `.planning/phases/05-night-sky-scene/05-02-SUMMARY.md` when done.
</output>
