---
phase: 05-night-sky-scene
plan: 05
type: execute
wave: 4
depends_on: [05-04, 05-02]
files_modified:
  - src/lib/nightsky/constellations.ts
  - src/lib/nightsky/scene.ts
autonomous: true
requirements: [CONST-01, CONST-02, CONST-03]

must_haves:
  truths:
    - "The 4 constellations from src/data/constellations.ts render as star nodes + sparse links on the scene canvas, always distinguishable from the ambient starfield"
    - "The active panel's constellation brightens while others dim, driven ONLY by the nightsky:panel-change document event over ~400ms ease-out; at most one constellation is brightened; hero/contact leave all at ambient"
    - "Under prefers-reduced-motion the brighten/dim still occurs (essential wayfinding) but collapses to an instant state change with a single repaint — no tween, no firing"
    - "A quiet link-firing beam travels one random ambient link every ~6–10s, one at a time, scheduled by setTimeout (never per-frame dice), reusing the Fig. 01 beam MATH pattern reimplemented locally"
    - "constellations.ts imports the data module but NEVER imports deck.ts or any fig01 module; firing is suppressed while the scene is paused or under reduced motion"
  artifacts:
    - path: "src/lib/nightsky/constellations.ts"
      provides: "Constellation graph render, panel-reactive brighten/dim tween, and setTimeout-scheduled link-firing (beam math mirrored, not imported)"
      contains: "nightsky:panel-change"
  key_links:
    - from: "src/lib/nightsky/constellations.ts"
      to: "src/data/constellations.ts"
      via: "reads the typed constellation graph + panelIds mapping (single source of truth)"
      pattern: "constellations"
    - from: "src/lib/nightsky/scene.ts"
      to: "src/lib/nightsky/constellations.ts"
      via: "drawFrame calls the constellation draw/advance hook after fireflies; the pause gate suppresses firing; renderStaticFrame draws constellations at their instant state"
      pattern: "constellation"
    - from: "src/lib/nightsky/constellations.ts"
      to: "document nightsky:panel-change event"
      via: "an independent document listener (separate from scene.ts's pause listener) sets the brighten/dim target from detail.id"
      pattern: "panel-change"
  prohibitions:
    - "Do NOT run git push anywhere in this phase — the live site stays v1 until Phase 6. All commits remain local on main."
    - "Do NOT deploy, publish, or trigger the GitHub Actions Pages pipeline this phase."
    - "Do NOT import deck.ts or any fig01 module from constellations.ts — subscribe to the nightsky:panel-change document event by literal name; reimplement the beam math locally."
    - "Do NOT roll per-frame dice to decide firing — schedule the next firing with setTimeout (6–10s); suppress it while paused or under reduced motion."
    - "Do NOT brighten more than one constellation at a time; hero/contact map to none (all ambient), not dimmed."
    - "Do NOT render any constellation LABEL as canvas text — labels are data-only this phase."
---

<objective>
Bring the constellations to life. Implement nightsky/constellations.ts: render the 4 career-chapter constellations (from src/data/constellations.ts) as star nodes + sparse links on the scene canvas, always visually distinct from the ambient starfield; brighten the active panel's constellation and dim the others over ~400ms ease-out, driven ONLY by the nightsky:panel-change document event (an independent listener, not deck.ts); and add a quiet link-firing beam that travels one random ambient link every ~6–10s (one at a time, setTimeout-scheduled, reusing the Fig. 01 beam MATH pattern reimplemented locally — never imported). Integrate the constellation draw + tween into scene.ts's drawFrame (Layer 2), wire firing suppression to the pause gate, and honor reduced motion (brighten/dim still occurs but instantly, with a single repaint; no tween, no firing).

Purpose: CONST-01 (the named constellations rendered), CONST-02 (panel-reactive brighten/dim via the event contract), CONST-03 (occasional quiet link-firing, sparse decorative motion).

Output: `src/lib/nightsky/constellations.ts` and the scene.ts drawFrame/pause integration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-night-sky-scene/05-CONTEXT.md
@.planning/phases/05-night-sky-scene/05-RESEARCH.md
@.planning/phases/05-night-sky-scene/05-UI-SPEC.md

@src/data/constellations.ts
@src/lib/nightsky/scene.ts
@src/lib/nightsky/tokens.ts
@src/lib/nightsky/deck.ts
@src/lib/fig01/model.ts
@src/lib/fig01/render.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render constellations + panel-reactive brighten/dim (CONST-01, CONST-02), integrated into scene.ts drawFrame</name>
  <files>src/lib/nightsky/constellations.ts, src/lib/nightsky/scene.ts</files>
  <behavior>
    - Constellation stars render at radius 2.5–4px (always larger than the ambient field's largest 2px band) so each cluster is findable at rest; links render as 0.75–1px hairlines.
    - Ambient state: star alpha 0.40–0.55, link alpha 0.12–0.20, no halo. Brightened: star 0.85–1.00, link 0.45–0.55, radial halo ~1.5× radius. Dimmed: star 0.25–0.35, link 0.08–0.12.
    - The nightsky:panel-change listener resolves detail.id → constellation id via the data module's panelIds mapping; that constellation targets Brightened, others Dimmed; if the panel maps to none (hero/contact), all target Ambient. At most one Brightened.
    - Normal motion: alphas tween to target over ~400ms ease-out inside the running loop. Reduced motion: alphas snap to target instantly and a single repaint is requested (no tween).
    - constellations.ts uses --star for all stars/links/halos (never --milkyway, never --accent); reads colors via nightsky/tokens.ts.
  </behavior>
  <read_first>
    - src/data/constellations.ts (the graph shape + panelIds mapping to consume — do not re-derive the mapping)
    - .planning/phases/05-night-sky-scene/05-UI-SPEC.md "Ambient/brightened/dimmed alpha states" table, "Constellation placement" table, and the Spacing rows (constellation star radius 2.5–4px, link stroke 0.75–1px)
    - .planning/phases/05-night-sky-scene/05-CONTEXT.md Constellations decision (brighten = alpha/glow lift over ~400ms ease-out, driven ONLY by the CustomEvent; at most one; no imports from deck.ts) and "Reduced motion" UI-SPEC row (brighten/dim still occurs but instant)
    - src/lib/fig01/interactions.ts createRedraw (lines ~303–309 — the reduced-motion "repaint once after a state change" pattern to mirror for the instant brighten/dim repaint)
    - src/lib/nightsky/scene.ts (drawFrame Layer-2 section + the renderStaticFrame path + the repaint hook to call)
  </read_first>
  <action>
    Implement src/lib/nightsky/constellations.ts. Export an init function (e.g. initConstellations) that takes what it needs from scene.ts (the sky tokens, a viewport size accessor, and a requestRepaint callback for the reduced-motion instant path) and returns a handle exposing: a draw(ctx, W, H, ts) that renders every constellation's stars + links at their current alpha (converting the data module's 0–1 fractional coords to pixels), an advance(ts) that eases current alphas toward their per-constellation target (ambient/brightened/dimmed) over ~400ms, and a teardown. Register an INDEPENDENT document listener for the literal 'nightsky:panel-change' event (type detail inline as CustomEvent&lt;{ id: string }&gt;; do NOT import deck.ts) that maps detail.id → constellation id via the data module's panelIds mapping and sets targets (one Brightened, others Dimmed; none → all Ambient). Under reduced motion (matchMedia), the listener snaps alphas to target and calls requestRepaint instead of relying on the tween.

    Integrate into scene.ts: initialize the constellation handle inside initNightSky (passing a requestRepaint that calls renderStaticFrame), call constellations.advance(ts) + constellations.draw(visibleCtx, W, H, ts) at the end of drawFrame's Layer-2 section (after fireflies), and also draw them (at instant state) inside renderStaticFrame so the reduced-motion static frame includes constellations. Add the constellation teardown to initNightSky's returned teardown. Use --star exclusively for constellation colors; zero hex.
  </action>
  <verify>
    <automated>npm run build && npx astro check</automated>
    <automated>test "$(grep -oE "nightsky:panel-change" src/lib/nightsky/constellations.ts | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test "$(grep -oE "from ['\"][^'\"]*(deck|fig01)[^'\"]*['\"]" src/lib/nightsky/constellations.ts | wc -l | tr -d ' ')" = "0"</automated>
    <automated>test "$(grep -oiE 'panelIds|constellation' src/lib/nightsky/constellations.ts | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test "$(grep -roE '#[0-9a-fA-F]{3,8}' src/lib/nightsky/constellations.ts | wc -l | tr -d ' ')" = "0"</automated>
    <automated>test "$(grep -oiE 'constellation' src/lib/nightsky/scene.ts | wc -l | tr -d ' ')" != "0"</automated>
  </verify>
  <acceptance_criteria>
    - The 4 constellations render as stars (2.5–4px) + hairline links from the data module coords, always distinct from the ambient field.
    - Brighten/dim is driven ONLY by the literal 'nightsky:panel-change' event resolved through the data module's panelIds mapping; at most one Brightened; hero/contact → all Ambient; ~400ms ease-out.
    - Under reduced motion, brighten/dim is instant with a single repaint (no tween); constellations appear in renderStaticFrame.
    - constellations.ts imports the data module but no deck/fig01 module; uses --star only; zero hex.
    - scene.ts drawFrame + renderStaticFrame call the constellation draw/advance hook; build + astro check green.
  </acceptance_criteria>
  <done>Constellations render and respond to panel changes (brighten one, dim the rest) via the event contract over 400ms (instant + repaint under reduced motion), integrated into the single-rAF scene without any deck/fig01 import.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Quiet link-firing beam — Fig. 01 beam math mirrored locally, setTimeout-scheduled, pause/reduced-motion suppressed (CONST-03)</name>
  <files>src/lib/nightsky/constellations.ts, src/lib/nightsky/scene.ts</files>
  <behavior>
    - At most ONE firing beam exists at a time, on a random link of the AMBIENT (non-highlighted) sky; the next firing is scheduled by setTimeout with a 6000 + random()*4000 ms delay — never a per-frame probability check.
    - The beam travels a straight star-to-star segment: a t-parameterized head + gradient tail (head radius ~1.5px, tail ~40px, speed ~0.18px/ms, stroke ~1.2px), reusing the Fig. 01 beam visual treatment (globalCompositeOperation 'lighter', head brighter than tail) reimplemented locally with a straight-line point-at-distance sampler (no elbow routes) — the fig01 modules are NOT imported.
    - Firing is suppressed while the scene is paused (tab hidden / fig-01 active) and under reduced motion; the scheduler is torn down with the scene.
  </behavior>
  <read_first>
    - src/lib/fig01/model.ts pAt (lines ~205–218 — the point-at-distance sampler to mirror as a straight-line version) and src/lib/fig01/render.ts drawBeams (the head+tail gradient visual treatment, 'lighter' composite) — MIRROR these, do NOT import them
    - .planning/phases/05-night-sky-scene/05-UI-SPEC.md Spacing rows for the firing beam (head 1.5px, tail 40px, speed 0.18px/ms, stroke 1.2px, cadence 6000 + random()*4000 ms) and the "Constellation firing" motion row
    - .planning/phases/05-night-sky-scene/05-RESEARCH.md "Code Examples" (pointAtDistance straight-line sketch) and "Anti-Patterns" (no per-frame dice — use setTimeout-scheduled sparse events)
    - src/lib/nightsky/scene.ts (the pause gate + drawFrame; how to expose a firing-suppressed setter the pause gate toggles)
  </read_first>
  <action>
    In constellations.ts, add the link-firing subsystem. Reimplement a straight-line point-at-distance sampler locally (mirror fig01/model.ts's pAt for a single segment — no 3-segment elbow). Maintain at most one active beam { link, distance }; on each frame that a beam exists, advance its distance by dt * speed and draw the head dot + gradient tail using --star with a 'lighter' composite (mirror fig01/render.ts drawBeams treatment), removing the beam when it reaches the segment end. Schedule firings with setTimeout using a 6000 + random()*4000 ms delay: on fire, if not suppressed, pick a random link from the AMBIENT (non-highlighted) constellations, spawn the single beam, then schedule the next firing. Expose a setFiringSuppressed(boolean) that, when true, clears any pending timeout and prevents new firings. Draw/advance the beam inside the existing constellation draw/advance hook so it rides the single rAF loop.

    In scene.ts, call constellations.setFiringSuppressed(true) whenever the loop is paused (tab hidden / fig-01 active) or under reduced motion, and setFiringSuppressed(false) when it resumes; ensure renderStaticFrame never draws a firing beam. Add the firing scheduler teardown to the constellation teardown already wired in Task 1.
  </action>
  <verify>
    <automated>npm run build && npx astro check</automated>
    <automated>test "$(grep -oE 'setTimeout' src/lib/nightsky/constellations.ts | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test "$(grep -oiE "setFiringSuppressed|suppress" src/lib/nightsky/constellations.ts | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test "$(grep -oiE "setFiringSuppressed|suppress" src/lib/nightsky/scene.ts | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test "$(grep -oE "from ['\"][^'\"]*(deck|fig01)[^'\"]*['\"]" src/lib/nightsky/constellations.ts | wc -l | tr -d ' ')" = "0"</automated>
    <automated>test "$(grep -roE '#[0-9a-fA-F]{3,8}' src/lib/nightsky/constellations.ts | wc -l | tr -d ' ')" = "0"</automated>
    <automated>test "$(grep -oE 'requestAnimationFrame' src/lib/nightsky/scene.ts | wc -l | tr -d ' ')" = "2"</automated>
  </verify>
  <acceptance_criteria>
    - At most one firing beam at a time on an ambient link; the next firing is setTimeout-scheduled (6000 + random()*4000 ms); no per-frame probability check.
    - The beam uses a locally-reimplemented straight-line point-at-distance sampler + head/tail 'lighter' treatment mirroring Fig. 01 — no fig01/deck import.
    - Firing is suppressed while paused (hidden / fig-01 active) and under reduced motion; renderStaticFrame never draws a beam.
    - Single rAF preserved (2 requestAnimationFrame occurrences in scene.ts); zero hex; build + astro check green.
  </acceptance_criteria>
  <done>A quiet, single, setTimeout-scheduled link-firing beam fires every 6–10s on the ambient sky using locally-mirrored Fig. 01 beam math, fully suppressed while paused or under reduced motion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time constellation data → canvas render | Developer-authored data rendered as shapes; no user/network input; labels are data-only, never drawn |
| deck → constellations (event) | Independent document listener on the internally-dispatched nightsky:panel-change event; no import coupling |
| local build → verification | All work is local; nothing is published |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Tampering (XSS via labels) | constellation label text | low | mitigate | Labels are DATA-ONLY and never rendered as canvas text or innerHTML this phase; the canvas is aria-hidden; no label string reaches any DOM sink |
| T-05-08 | Denial of Service (battery) | link-firing scheduling | medium | mitigate | setTimeout-scheduled sparse firing (6–10s), at most one beam, no per-frame dice; suppressed while paused/reduced-motion (Anti-Pattern avoidance) |
| T-05-06 | Tampering | module-boundary coupling | low | mitigate | constellations.ts imports the data module only; no deck/fig01 import (grep gate); beam math reimplemented locally |
| T-05-SC | Tampering (supply chain) | package installs | low | accept | No new dependencies in this plan |
| T-05-DEPLOY | Elevation / accidental deploy | GitHub Pages pipeline | medium | mitigate | Explicit no-push / no-deploy prohibition; local-only |
</threat_model>

<verification>
- Build + astro check green; single rAF preserved in scene.ts (2 occurrences).
- Constellations render + brighten/dim via the literal nightsky:panel-change event (one brightened, 400ms; instant + repaint under reduced motion).
- Link-firing is setTimeout-scheduled, one-at-a-time, ambient-only, suppressed while paused/reduced-motion; beam math mirrored locally.
- constellations.ts imports the data module only; no deck/fig01 import; labels never drawn; zero hex.
- No `git push`, deploy, or Pages trigger anywhere in this plan.
</verification>

<success_criteria>
- CONST-01: the 4 named career-chapter constellations render from the typed data module, always distinguishable from the ambient field.
- CONST-02: the active panel's constellation brightens while others dim, driven only by the nightsky:panel-change contract; at most one brightened.
- CONST-03: a quiet single link-firing beam fires occasionally on the ambient sky, reusing the Fig. 01 beam pattern reimplemented locally, suppressed while paused/reduced-motion.
- The site is NOT deployed; all commits remain local on main.
</success_criteria>

## Artifacts This Phase Produces (Plan 05-05)

**New files:**
- `src/lib/nightsky/constellations.ts` — constellation render + panel-reactive brighten/dim + link-firing. Exports: `initConstellations(...)` returning a handle (draw, advance, setFiringSuppressed, teardown); a locally-reimplemented straight-line point-at-distance sampler.

**Modified files:**
- `src/lib/nightsky/scene.ts` — initializes the constellation handle in initNightSky, calls its draw/advance in drawFrame + renderStaticFrame, and toggles setFiringSuppressed from the pause gate.

**New exports downstream consume:** the fully-rendered scene (constellations + firing) is now visually complete for 05-06's scrim/contrast verification + final gate.

<output>
Create `.planning/phases/05-night-sky-scene/05-05-SUMMARY.md` when done.
</output>
