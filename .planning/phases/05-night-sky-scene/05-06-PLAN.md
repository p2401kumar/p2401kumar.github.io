---
phase: 05-night-sky-scene
plan: 06
type: execute
wave: 5
depends_on: [05-05]
files_modified:
  - src/styles/deck.css
  - scripts/verify-contrast.mjs
  - .planning/phases/05-night-sky-scene/contrast-evidence.md
  - .planning/phases/05-night-sky-scene/frame-cost-audit.md
  - .planning/phases/05-night-sky-scene/lighthouse-scores.md
autonomous: false
requirements: [SKY-03, SKY-05]

must_haves:
  truths:
    - "A content scrim (vertical gradient, peak opacity <=0.38, no blur) sits behind panel content per the UI-SPEC stops, preserving the dark aesthetic while lifting text legibility over the scene"
    - "All text over the sky passes WCAG 1.4.3 at WORST-CASE brightness points: the brightest sky pixels under text regions (Milky Way taper, bright stars, constellation glow) are sampled and each yields >=4.5:1 vs --ink (>=3:1 for large text)"
    - "scripts/verify-contrast.mjs implements the WCAG relative-luminance + contrast-ratio formulas, self-tests in node, and samples worst-case canvas readback in the browser; recorded evidence lives in the phase dir"
    - "The full gate battery is green: astro check, build, zero-hex outside tokens.css, fig01 non-regression, nightsky single-rAF, no scene JS on /work/*, local Lighthouse >=90 x4 on both presets, and a bounded frame-cost audit"
    - "A human sees the assembled night sky (starfield, Milky Way, camper glow, fireflies, panel-reactive constellations, firing) on the local preview and signs off before Phase 6"
  artifacts:
    - path: "scripts/verify-contrast.mjs"
      provides: "WCAG worst-case contrast verifier — node --selftest (formula) + browser canvas-readback sampling"
      contains: "relativeLuminance"
    - path: ".planning/phases/05-night-sky-scene/contrast-evidence.md"
      provides: "Recorded worst-case contrast ratios per sampled text region (all >=4.5, or >=3 for large text)"
      contains: "4.5"
    - path: ".planning/phases/05-night-sky-scene/frame-cost-audit.md"
      provides: "Bounded per-frame element/draw-call count + TBT reasoning for SKY-03 idle-CPU"
      contains: "per-frame"
    - path: ".planning/phases/05-night-sky-scene/lighthouse-scores.md"
      provides: "Recorded local Lighthouse scores (mobile + desktop, all four categories) with deck + scene active"
      contains: "performance"
  key_links:
    - from: "src/styles/deck.css"
      to: "src/styles/tokens.css"
      via: "the scrim gradient reads rgb(from var(--bg) …) relative color syntax — no new token, zero hex"
      pattern: "--bg"
    - from: "scripts/verify-contrast.mjs"
      to: ".planning/phases/05-night-sky-scene/contrast-evidence.md"
      via: "the verifier's worst-case sampling output is recorded as SKY-05 evidence"
      pattern: "contrast"
  prohibitions:
    - "Do NOT run git push anywhere in this phase — the live site stays v1 until Phase 6. All commits remain local on main."
    - "Do NOT deploy, publish, or trigger the GitHub Actions Pages pipeline this phase. Lighthouse runs against the LOCAL preview only."
    - "Do NOT add any hex literal outside tokens.css — the scrim uses rgb(from var(--bg) …) relative color syntax."
    - "Do NOT silently pass a shortfall — if any Lighthouse category is <90 or any contrast sample is <4.5 (or <3 large), record the shortfall and the responsible audit/region, do not round up."
---

<objective>
Land the scrim, prove the contrast floor objectively, run the full acceptance battery, and hold the scene's human sign-off. Add the content scrim to deck.css per the UI-SPEC stops (vertical gradient via rgb(from var(--bg) …), peak opacity <=0.38, no blur), then implement scripts/verify-contrast.mjs — the WCAG relative-luminance + contrast-ratio verifier that self-tests in node and samples the brightest sky pixels under text regions via canvas readback in the browser — and record the worst-case ratios as SKY-05 evidence. Run the complete gate battery (astro check, build, zero-hex boundary-aware, fig01 non-regression, nightsky single-rAF, no scene JS reachable from /work/*, local Lighthouse >=90 x4 mobile + desktop recorded, and a bounded frame-cost audit for SKY-03). Finally, hold a blocking human-verify checkpoint so the user SEES their assembled night sky on the local preview before Phase 6.

Purpose: SKY-05 (worst-case contrast via a scrim) and SKY-03's finalized idle-CPU evidence, plus the phase's overall acceptance gate. Non-autonomous because it pairs the automated battery with a blocking human checkpoint.

Output: scrim in deck.css, `scripts/verify-contrast.mjs`, `contrast-evidence.md`, `frame-cost-audit.md`, `lighthouse-scores.md`, and a human sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-night-sky-scene/05-CONTEXT.md
@.planning/phases/05-night-sky-scene/05-RESEARCH.md
@.planning/phases/05-night-sky-scene/05-UI-SPEC.md

@src/styles/deck.css
@src/styles/tokens.css
@src/lib/nightsky/scene.ts
@src/lib/nightsky/constellations.ts
@src/components/NightSky.astro
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scrim in deck.css + scripts/verify-contrast.mjs + recorded worst-case contrast evidence (SKY-05)</name>
  <files>src/styles/deck.css, scripts/verify-contrast.mjs, .planning/phases/05-night-sky-scene/contrast-evidence.md</files>
  <read_first>
    - .planning/phases/05-night-sky-scene/05-UI-SPEC.md "Scrim implementation (SKY-05)" — the exact gradient stops (0/18/45/70/92/100% at 0/0.28/0.38/0.38/0.15/0), the .panel::before + .panel > * z-index rules, the peak-0.38 ceiling, and the worst-case (not average) verification requirement
    - .planning/phases/05-night-sky-scene/05-RESEARCH.md "Worst-case contrast verification (SKY-05)" code sketch (relativeLuminance, contrastRatio, worstCaseContrastInRegion) and "Don't Hand-Roll" (use the standard WCAG formula, not a hand-rolled one)
    - src/styles/deck.css (where the scrim lands; existing html.deck-active .panel > * rule to extend with z-index) and src/styles/tokens.css (--bg and --ink source values)
    - scripts/render-og.mjs (the existing .mjs node-script convention in this repo to match)
  </read_first>
  <action>
    Add the scrim to deck.css: a `.panel::before` pseudo-element (position:absolute; inset:0; z-index:0; pointer-events:none) with the UI-SPEC vertical linear-gradient built from rgb(from var(--bg) r g b / …) relative-color-syntax stops (0% 0 · 18% 0.28 · 45% 0.38 · 70% 0.38 · 92% 0.15 · 100% 0), and ensure `.panel > *` carries position:relative; z-index:1 so content always paints above the scrim. No blur filter, no new token, zero hex. Confirm the scrim never hides a panel (it is a decorative pseudo-element only).

    Create scripts/verify-contrast.mjs (ESM node script matching the render-og.mjs convention). It exports/contains relativeLuminance(r,g,b) and contrastRatio(l1,l2) implementing the standard WCAG 2.2 SC 1.4.3 formulas verbatim, plus worstCaseContrastInRegion(imageData-like, textLuminance) that scans a region's pixels and returns the WORST (lowest) ratio. Provide a `--selftest` mode that runs in plain node with NO new dependency: assert contrastRatio against known fixtures (pure black vs pure white == 21 within tolerance; --ink over --bg — parse those two token values from tokens.css — yields a ratio, printed) and exit non-zero if the formula fixtures fail. Provide a browser mode (documented in a header comment; runnable via the gstack /browse skill or a DevTools console paste) that grabs the live scene canvas getImageData under each text region's bounding box (post-scrim), samples the brightest sky pixels beneath the actual worst-case spots — the Milky Way taper edge, a Bright-band star, and a constellation glow while brightened — and computes the worst ratio vs --ink's luminance.

    Run the browser-mode sampling against the local preview and record the results in contrast-evidence.md: the sampled regions, their worst-case ratios, and PASS/FAIL vs the 4.5:1 (normal) / 3:1 (large) thresholds. If any region is below threshold, record the region + ratio and the scrim adjustment needed — do not round up.
  </action>
  <verify>
    <automated>npm run build && npx astro check</automated>
    <automated>node scripts/verify-contrast.mjs --selftest</automated>
    <automated>test "$(grep -oE 'relativeLuminance' scripts/verify-contrast.mjs | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test "$(grep -oE '::before' src/styles/deck.css | wc -l | tr -d ' ')" != "0"</automated>
    <automated>test "$(grep -roE '#[0-9a-fA-F]{3,8}' src/styles/deck.css | wc -l | tr -d ' ')" = "0"</automated>
    <automated>test -f .planning/phases/05-night-sky-scene/contrast-evidence.md && test "$(grep -oiE '4\.5|ratio' .planning/phases/05-night-sky-scene/contrast-evidence.md | wc -l | tr -d ' ')" != "0"</automated>
  </verify>
  <acceptance_criteria>
    - deck.css gains a `.panel::before` scrim with the UI-SPEC stops (peak <=0.38) via rgb(from var(--bg) …); `.panel > *` sits above it (z-index:1); no blur, no hex, no panel hidden.
    - scripts/verify-contrast.mjs implements the WCAG relativeLuminance + contrastRatio + worst-case-region functions; `node scripts/verify-contrast.mjs --selftest` exits 0 (formula fixtures pass) with zero new deps.
    - contrast-evidence.md records worst-case ratios for the Milky Way taper, a bright star, and a brightened constellation glow under text regions; each >=4.5 (or >=3 large), with any shortfall explicitly recorded.
    - Build + astro check green.
  </acceptance_criteria>
  <done>The scrim is in place and SKY-05 is proven objectively: a self-testing WCAG verifier plus recorded worst-case contrast evidence (>=4.5:1) at the brightest points under text.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Full gate battery — build/check, zero-hex, fig01 non-regression, nightsky single-rAF, /work leak, Lighthouse local x2, frame-cost audit</name>
  <files>.planning/phases/05-night-sky-scene/frame-cost-audit.md, .planning/phases/05-night-sky-scene/lighthouse-scores.md</files>
  <read_first>
    - .planning/phases/04-deck-mechanics/04-03-PLAN.md Task 1 (the proven gate-battery command shapes: zero-hex grep, /work leak loop, Lighthouse local invocation) and .planning/phases/04-deck-mechanics/lighthouse-scores.md (the 100/95/100/100 baseline the scene must not regress below 90)
    - .planning/phases/05-night-sky-scene/05-CONTEXT.md "Quality gates" (astro check, build, zero hex outside tokens.css, fig01 non-regression greps, local Lighthouse mobile+desktop, idle-CPU frame-cost audit method) and .planning/phases/05-night-sky-scene/05-RESEARCH.md "Pitfall 4" (frame-cost audit method: count Layer-2 elements/frame, multiply by proven-cheap primitive cost, reason to bounded conclusion)
    - src/lib/nightsky/scene.ts + src/lib/nightsky/constellations.ts (to count exact Layer-2 draw work per frame: twinkle subset size, firefly count, <=1 beam, constellation tween)
  </read_first>
  <action>
    Run the complete automated acceptance gate under Git Bash (Windows, grep -o | wc -l occurrence counting; no PowerShell syntax):

    1. Build + typecheck: `npm run build` (succeeds) and `npx astro check` (0 errors) with deck + scene active.

    2. Zero-hex outside tokens.css: grep the 3–8 digit hex pattern across all scene + touched source (src/lib/nightsky, src/lib/shared, src/components/NightSky.astro, src/data/constellations.ts, src/styles/deck.css) — expect 0.

    3. Fig. 01 non-regression: zero hex in src/lib/fig01 (=0), requestAnimationFrame count in src/lib/fig01 (=2), astro check 0 errors — the shared-tokens extraction must not have regressed Fig. 01.

    4. Nightsky single-rAF: requestAnimationFrame count in src/lib/nightsky/scene.ts (=2); confirm no requestAnimationFrame in starfield.ts (=0).

    5. Scene JS unreachable from /work/*: reuse the Phase-4 leak loop with the home-only interactive sentinel `nightsky:panel-change` (present in dist/_astro, absent from every dist/work/*/index.html and every /_astro/*.js chunk those pages reference) — since the deck and the scene engine are both home-only and share that contract, this transitively proves no scene JS leaked into /work. Additionally confirm src/pages/work/[slug].astro does not import NightSky.

    6. Local Lighthouse (deck + scene active): start the preview (`npm run preview` → http://localhost:4321/) in the background, run Lighthouse mobile (default) and desktop presets headless against the local URL, extract the four category scores per run, and WRITE them to lighthouse-scores.md (a table: run | performance | accessibility | best-practices | seo). Every category must be >=0.90. Stop the preview when done. Record any shortfall + the responsible audit; do not silently pass.

    7. Frame-cost audit (SKY-03): from the code, count the exact Layer-2 per-frame work — twinkle subset size (~5–8% of the star count, ~40 at the reference density), firefly count (<=15), at most one firing beam, and the constellation tween — multiply by the per-primitive cost class proven cheap in fig01/render.ts (simple arcs/gradients, no shadowBlur/filter:blur), and reason to a bounded conclusion. Attempt a DevTools/`browse` performance sample if the tooling starts; regardless, WRITE frame-cost-audit.md recording the element counts, the Lighthouse TBT figure, whichever live sample was obtainable (explicitly noting which), and the bounded-cost conclusion vs the SKY-03 <10%/5min floor (final live confirmation deferred to Phase 6 per CONTEXT.md if tooling can't measure locally).

    Record a one-line PASS/FAIL summary per check at the top of lighthouse-scores.md.
  </action>
  <verify>
    <automated>npm run build && npx astro check</automated>
    <automated>test "$(grep -roE '#[0-9a-fA-F]{3,8}' src/lib/nightsky src/lib/shared src/components/NightSky.astro src/data/constellations.ts src/styles/deck.css | wc -l | tr -d ' ')" = "0"</automated>
    <automated>test "$(grep -roE '#[0-9a-fA-F]{3,8}' src/lib/fig01 | wc -l | tr -d ' ')" = "0" && test "$(grep -roE 'requestAnimationFrame' src/lib/fig01 | wc -l | tr -d ' ')" = "2"</automated>
    <automated>test "$(grep -roE 'requestAnimationFrame' src/lib/nightsky/scene.ts | wc -l | tr -d ' ')" = "2" && test "$(grep -roE 'requestAnimationFrame' src/lib/nightsky/starfield.ts | wc -l | tr -d ' ')" = "0"</automated>
    <automated>test "$(grep -rl 'nightsky:panel-change' dist/work 2>/dev/null | wc -l | tr -d ' ')" = "0" && test "$(grep -rl 'nightsky:panel-change' dist/_astro 2>/dev/null | wc -l | tr -d ' ')" != "0"</automated>
    <automated>for html in dist/work/*/index.html; do for js in $(grep -oE '/_astro/[A-Za-z0-9_.-]+\.js' "$html"); do grep -q 'nightsky:panel-change' "dist${js}" && { echo "LEAK $html -> $js"; exit 1; }; done; done; echo OK</automated>
    <automated>grep -qi 'performance' .planning/phases/05-night-sky-scene/lighthouse-scores.md && grep -qi 'per-frame' .planning/phases/05-night-sky-scene/frame-cost-audit.md</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds; `npx astro check` reports 0 errors with deck + scene active.
    - Zero hex across all scene + touched source (grep = 0); tokens.css is the only hex-bearing file.
    - Fig. 01 non-regression holds: src/lib/fig01 hex = 0, rAF = 2, astro check clean.
    - Nightsky single-rAF: scene.ts rAF = 2, starfield.ts rAF = 0.
    - `nightsky:panel-change` present in dist/_astro, absent from every /work HTML + referenced JS chunk (leak loop prints OK); /work/[slug].astro does not import NightSky.
    - lighthouse-scores.md records mobile + desktop, all four categories >=90 (or a recorded shortfall); frame-cost-audit.md records the bounded per-frame conclusion + TBT.
  </acceptance_criteria>
  <done>The full battery is green: build/check, zero-hex, fig01 non-regression, nightsky single-rAF, no scene JS on /work, Lighthouse >=90 x4 on both presets recorded, and a bounded frame-cost audit for SKY-03.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — see the assembled night sky before Phase 6</name>
  <action>
    Blocking human-verify gate (the executor pauses here — no autonomous action). Serve the production build locally (`npm run build && npm run preview`) and walk the operator through the scene sign-off checklist below. Record the operator's response; on anything other than "approved", capture the specific visual/behavioral issue for gap closure. LOCAL-only — do not push or deploy.
  </action>
  <what-built>
    The complete persistent night-sky scene behind the deck on the home page (local preview only — NOT deployed): a dense pre-rendered starfield + Milky Way band, a silhouetted camper with a single warm copper glow, sparse drifting fireflies, and the four named career constellations that brighten with the active panel and occasionally fire a quiet link — all behind the working deck and its scrim, paused while Fig. 01 is active and under reduced motion. The automated battery (task 2) already confirmed build/check/hex/fig01-non-regression/single-rAF/no-/work-leak/Lighthouse/frame-cost, and task 1 recorded the worst-case contrast evidence.
  </what-built>
  <how-to-verify>
    Serve the production build locally: `npm run build && npm run preview`, then open http://localhost:4321/ and check:

    1. Scene present (SKY-01/SKY-02): behind every panel there is a dense starfield + a vivid Milky Way band (bright core in the right margin, content over darker sky); a camper silhouette sits at the horizon (bottom-left) with a single warm copper glow. The Milky Way shows no obvious banding/rings.
    2. Fireflies + twinkle (SKY-03): a handful (<=15) of copper fireflies drift slowly low in the frame; a small subset of stars twinkle gently — nothing else moves per frame; it reads calm, not busy.
    3. Constellations + brighten/dim (CONST-01/CONST-02): navigate the deck — on the fig-01 and systems panels the AWS constellation brightens; experience → Microsoft; skills → Samsung; patents → Education & Patents; hero and contact leave all at ambient. Only one is ever brightened; the others visibly dim; the transition is a smooth ~400ms fade.
    4. Link-firing (CONST-03): watch the ambient sky for ~10–20s — occasionally a single quiet beam travels one constellation link; never two at once, never a busy mesh.
    5. Pause rules (SKY-04): navigate to the Fig. 01 panel — the ambient scene animation pauses while Fig. 01 runs; leave it and the scene resumes. Switch the OS to "reduce motion" and reload — the scene renders as one static frame (stars + Milky Way + silhouette + constellations at rest, the active one already-brightened), with NO fireflies, twinkle, or firing. Background a tab and return — no runaway animation.
    6. Contrast/legibility (SKY-05): read the text on every panel — it stays comfortably legible over the scene through the scrim, including any panel whose column grazes the Milky Way taper; the look stays dark and calm (the scrim is subtle, not a heavy wash).
    7. Deck intact: wheel/keyboard/jump navigation still works exactly as in Phase 4; the scene never intercepts pointer input (pointer-events:none).

    Reminder: this is LOCAL only. Do NOT push, deploy, or publish — the live site stays v1 until Phase 6.
  </how-to-verify>
  <resume-signal>Type "approved" if the scene looks and behaves right across all seven checks, or describe the specific visual/behavioral issue (which element / panel / motion state) that failed.</resume-signal>
  <done>Operator confirms the assembled night sky on the local preview (or logs specific failures for gap closure). No push/deploy performed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local build/preview → verification | The whole battery + Lighthouse + contrast sampling run against the local dist/preview; nothing is published |
| tokens.css → scrim/canvas | Colors confined to tokens.css; the scrim reads rgb(from var(--bg) …); no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-09 | Information Disclosure (illegible text = failed accessibility) | scrim/contrast over the scene | high | mitigate | SKY-05 verified at WORST-CASE brightness points via a self-testing WCAG script + recorded evidence (>=4.5:1), not averages; scrim peak <=0.38 preserves the dark look |
| T-05-01 | Elevation / accidental deploy | GitHub Pages pipeline | medium | mitigate | Explicit no-push / no-deploy prohibition; Lighthouse + all verification run against the LOCAL preview only; no CI/Pages trigger this phase |
| T-05-SC | Tampering (supply chain) | package installs | low | accept | Zero new dependencies in this plan (verify-contrast.mjs is a zero-dep node/browser script) |
</threat_model>

<verification>
- Task 1: scrim in deck.css (UI-SPEC stops, no blur, no hex); `node scripts/verify-contrast.mjs --selftest` exits 0; contrast-evidence.md records worst-case ratios >=4.5.
- Task 2: build + astro check green; zero-hex; fig01 non-regression (hex=0, rAF=2); nightsky single-rAF (scene.ts=2, starfield.ts=0); /work leak loop OK; Lighthouse >=90 x4 mobile+desktop recorded; frame-cost audit recorded.
- Task 3: human confirms the assembled scene across all seven checks on the local preview.
- No `git push`, deploy, or Pages trigger anywhere in this plan.
</verification>

<success_criteria>
- SKY-05: all text over the sky passes WCAG 1.4.3 at worst-case brightness via a subtle scrim, proven by a self-testing verifier + recorded evidence.
- SKY-03: a bounded frame-cost audit (element counts × cheap-primitive cost + TBT) documents the idle-CPU floor (final live confirmation deferred to Phase 6 per CONTEXT.md).
- The whole phase's acceptance surface is green: build/check, zero-hex, fig01 non-regression, single-rAF, no /work leak, Lighthouse >=90 x4 local, contrast evidence, and a human scene sign-off.
- The site is NOT deployed; all commits remain local on main.
</success_criteria>

## Artifacts This Phase Produces (Plan 05-06)

**New files:**
- `scripts/verify-contrast.mjs` — zero-dep WCAG worst-case contrast verifier (node `--selftest` formula mode + browser canvas-readback sampling mode).
- `.planning/phases/05-night-sky-scene/contrast-evidence.md` — recorded worst-case contrast ratios per sampled text region (SKY-05 evidence).
- `.planning/phases/05-night-sky-scene/frame-cost-audit.md` — bounded per-frame element/draw-call count + TBT reasoning (SKY-03 evidence).
- `.planning/phases/05-night-sky-scene/lighthouse-scores.md` — local Lighthouse scores (mobile + desktop, four categories) with deck + scene active + a PASS/FAIL summary per battery check.

**Modified files:**
- `src/styles/deck.css` — the `.panel::before` content scrim (UI-SPEC stops, rgb(from var(--bg) …), peak 0.38) + `.panel > *` z-index:1.

**No new source modules/exports beyond the scrim CSS + the verification script.** This plan verifies, proves the floors, and records evidence.

<output>
Create `.planning/phases/05-night-sky-scene/05-06-SUMMARY.md` when done.
</output>
