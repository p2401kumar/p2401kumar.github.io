---
phase: 06-integration-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-integration-launch/06-01-integration-evidence.md
  - .planning/phases/06-integration-launch/06-01-fig01-audit.md
  - .planning/phases/06-integration-launch/06-01-lighthouse-scores-local.md
  - .planning/phases/06-integration-launch/06-01-LAUNCH-READINESS.md
autonomous: true
requirements: [INTG-01, INTG-02, INTG-03, INTG-04]

must_haves:
  truths:
    - "All 7 panels render the v1 section components unforked, hidden via transform (never a CSS box-suppression property), each legible over the scene at >=4.5:1 vs --ink on both viewports (INTG-01)"
    - "Cold deep-link to /#fig-01 initializes Fig. 01 with a nonzero canvas backing store — no 0x0 init — and the fig-01 panel active (INTG-02, Pitfall 11)"
    - "The full v1 Fig. 01 interaction checklist (send, fault -> weigh-away -> ~8s self-heal, keyboard proxies, sr-only degraded narration) re-passes while embedded in the deck (INTG-02)"
    - "Resizing while Fig. 01 is inactive then activating re-measures the canvas with no 0x0 and no stale DPR backing store (INTG-02)"
    - "Classic mode and reduced-motion both keep Fig. 01 functional (static frame path; fault still narrates) (INTG-02)"
    - "Exactly one canvas animation runs at a time — the scene is paused while Fig. 01 is active (INTG-02)"
    - "Both case studies cold-load at their /work/* URLs, browser Back restores the deck at #systems, the sitemap lists exactly the real routes, and scene/deck JS is unreachable from /work/* and /404 (INTG-03)"
    - "The full local regression battery is green and a decision-ready launch-readiness pack exists for the user's go/no-go (INTG-04 pre-flight)"
  artifacts:
    - .planning/phases/06-integration-launch/06-01-integration-evidence.md
    - .planning/phases/06-integration-launch/06-01-fig01-audit.md
    - .planning/phases/06-integration-launch/06-01-lighthouse-scores-local.md
    - .planning/phases/06-integration-launch/06-01-LAUNCH-READINESS.md
  key_links:
    - "PanelDeck.astro pre-paint script excludes non-hero hashes -> deck.ts's synchronous hash resolution paints #fig-01 first -> fig01 init reads a nonzero .fig-stage box (transform-hidden panels keep their layout box) = nonzero canvas"
    - "interactions.ts MutationObserver on .panel[data-state] pauses the fig01 rAF when inactive; the ResizeObserver on .fig-stage is NOT gated on panelActive, so it re-layouts even while inactive -> activate lands on the correct size"
    - "scene.ts subscribes nightsky:panel-change by literal name; detail.id==='fig-01' sets fig01Active -> updateRunState stops the scene loop while Fig. 01 is active"
---

<objective>
Produce the complete LOCAL proof that the assembled site (deck + night-sky scene + all 7 v1 panels + case studies) is launch-ready, and aggregate it into a single decision-ready go/no-go pack for the user. This plan verifies INTG-01, INTG-02, INTG-03 to green and pre-flights INTG-04's Lighthouse floor locally. It ships NOTHING: no `git push`, no deploy, no Pages trigger, and it must not touch `.planning/config.json`.

Purpose: The 06-02 deploy is gated on an explicit human go decision. That decision must be anticlimactic — every gate except live Lighthouse is proven green here first (06-CONTEXT.md "Specific Ideas").
Output: `06-01-integration-evidence.md`, `06-01-fig01-audit.md`, `06-01-lighthouse-scores-local.md`, `06-01-LAUNCH-READINESS.md`, plus committed sign-off screenshots.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-integration-launch/06-CONTEXT.md

# The v1 Fig. 01 checklist this phase re-passes (the 5 truths + FIG-01..07)
@.planning/milestones/v1.0-phases/02-fig01-signature-figure/02-VERIFICATION.md

# Evidence-format precedent + non-regression baselines (contrast 9.27/8.07;
# local Lighthouse mobile 98/100/100/100, desktop 100x4; rAF invariants)
@.planning/phases/05.1-celestial-extras/lighthouse-scores.md

# As-built DOM contract + resize/DPR/pause ground truth (READ these live in
# each task — do NOT trust selectors/line numbers from any summary):
@src/components/Figure01.astro
@src/lib/fig01/render.ts
@src/lib/fig01/interactions.ts

# Additional as-built files each task Reads directly (paths named in actions):
#   src/lib/fig01/index.ts       (DOM selector contract header)
#   src/lib/nightsky/deck.ts     (hash routing, popstate/hashchange, applyPanelStates, view-classic)
#   src/lib/nightsky/scene.ts    (pause machine: nightsky:panel-change -> fig01Active)
#   src/components/PanelDeck.astro (pre-paint speculative deck-active, boot)
#   src/components/Panel.astro   (thin slot wrapper; emitId)
#   src/pages/index.astro        (panel composition — v1 components wrapped, unforked)
#   src/pages/work/[slug].astro  (case-study template; crosslinks)
#   src/styles/deck.css          (transform-only hiding)
#   scripts/verify-contrast.mjs  (--selftest / --cdp / --moon)
#   astro.config.mjs             (site, sitemap filter)
</context>

<tasks>

<task type="auto">
  <name>Task 1: INTG-01 panel provenance + transform-only hiding + contrast non-regression; INTG-03 routing, sitemap, leak gate, 404</name>
  <files>.planning/phases/06-integration-launch/06-01-integration-evidence.md</files>
  <action>
Read the real files first: src/pages/index.astro, src/components/Panel.astro, src/styles/deck.css, src/pages/work/[slug].astro, src/components/SystemsList.astro, astro.config.mjs. Then prove INTG-01 and INTG-03 against the assembled build and record every result (with the exact command output) into 06-01-integration-evidence.md as two sections.

INTG-01 (spot-verify markup provenance — panels wrap the v1 section components, no duplicated content):
- Assert index.astro composes exactly 7 Panel wrappers, each wrapping the ORIGINAL v1 component unforked: Hero, Figure01, SystemsList, ExperienceSection, PatentsSection, SkillsSection, ContactSection. Record the import + usage lines as evidence.
- Assert Panel.astro is a thin slot wrapper (single slot, zero content, zero layout styling of its own) and that no per-panel content fork files exist under src/components/ (only Panel.astro and PanelDeck.astro carry "panel" in the name; there is no duplicated section/content file).
- Assert deck.css hides inactive panels ONLY via transform + opacity + pointer-events, with zero CSS box-suppression declarations (the negative grep in the verify block enforces this) and never a visibility/display hiding technique. Record that inactive panels keep their layout box — the mechanism that keeps Fig. 01's canvas non-zero when its panel is inactive.
- Legibility over the scene: re-run the SKY-05 contrast battery as a NON-REGRESSION check (this is already the shipped gate). Build, start a local preview, run `node scripts/verify-contrast.mjs --selftest` (must pass) and `--cdp` at 1440x900 and 1280x800; parse the JSON report and assert every panel's `failing` array is empty and the global worst vs --ink is >=4.5 at BOTH viewports. Record the two global-worst numbers against the 9.27 / 8.07 baseline (drift within noise is fine; a drop below 4.5 is a hard fail). Also run `--moon` at both viewports (must exit 0: moonPeak < mwPeak) as the SKY-07 non-regression.

INTG-03 (case studies + routing + sitemap + leak + 404):
- Cold-load routing (scripted headless Chrome over CDP — reuse the node-WebSocket harness pattern already in scripts/verify-contrast.mjs, or a scratchpad node script): for each case study route (/work/dynamodb-cellularization and /work/elb-auto-weight-away) navigate cold and assert the article renders standalone with v1 chrome (h1 systemId, standfirst, metrics, back-to-work crosslink) and that neither the deck nor the scene mounts (no `.deck-active` on <html>, no `#nightsky-canvas` present).
- Browser Back restores the deck at #systems: navigate cold to `/`, drive the deck to the systems panel (dispatch the deck ArrowDown keydown until `[data-panel-id="systems"][data-state="active"]`; location.hash becomes `#systems`), then navigate to `/work/dynamodb-cellularization`, then history.back(); assert the home page cold-restores with the systems panel active (hash `#systems`, `[data-panel-id="systems"]` has data-state="active"). This is the INTG-03 gate.
- Record a routing-sanity OBSERVATION (do NOT silently pass): the case-study "back to work ->" crosslink targets `/#work`, and `#work` is the id on SystemsList.astro inside the systems panel, but the deck manifest hash for that panel is `systems` — so `/#work` resolves via deck.ts resolveIndexFromHash to the fallback index 0 (hero) in deck mode. Note this in the evidence as a fix-forward UX item for the user's go/no-go (it is NOT a launch blocker: cold-loadable URLs + browser Back + sitemap all pass).
- Sitemap: after build, assert dist/sitemap-index.xml and dist/sitemap-0.xml exist and that sitemap-0.xml lists EXACTLY the three real routes (home `/`, and the two `/work/*` case studies) and only those — /404 must be absent (astro.config.mjs filters it). Assert the count of `<loc>` entries is exactly 3.
- Leak gate (re-run the shipped check): assert the deck sentinel string `nightsky:panel-change` appears in a home `/_astro/*.js` chunk but NEVER in any `dist/work/*/index.html` nor any `/_astro` chunk they reference; assert dist/404.html likewise references no deck/scene chunk. 404 sanity: assert dist/404.html builds and renders the site chrome without the deck/scene.

NEVER run `git push`. Do NOT modify `.planning/config.json`.
  </action>
  <verify>
    <automated>
set -e
npm run build >/dev/null 2>&1
# INTG-01: v1 components wrapped unforked in index.astro
for c in Hero Figure01 SystemsList ExperienceSection PatentsSection SkillsSection ContactSection; do grep -q "<$c" src/pages/index.astro || { echo "MISSING wrapped component $c"; exit 1; }; done
# INTG-01: transform-only hiding — zero CSS box-suppression in deck.css
test "$(grep -oiE 'display:[[:space:]]*none' src/styles/deck.css | wc -l | tr -d ' ')" -eq 0
test "$(grep -oiE 'visibility:[[:space:]]*hidden' src/styles/deck.css | wc -l | tr -d ' ')" -eq 0
test "$(grep -oE 'transform:[[:space:]]*translateY' src/styles/deck.css | wc -l | tr -d ' ')" -ge 2
# INTG-03: sitemap lists exactly the 3 real routes, /404 absent
test -f dist/sitemap-0.xml && test -f dist/sitemap-index.xml
test "$(grep -oE '<loc>' dist/sitemap-0.xml | wc -l | tr -d ' ')" -eq 3
test "$(grep -oE '/404/?</loc>' dist/sitemap-0.xml | wc -l | tr -d ' ')" -eq 0
grep -q '/work/dynamodb-cellularization' dist/sitemap-0.xml
grep -q '/work/elb-auto-weight-away' dist/sitemap-0.xml
# INTG-03: leak gate — deck sentinel absent from every /work/* HTML and from 404
test "$(grep -orE 'nightsky:panel-change' dist/work | wc -l | tr -d ' ')" -eq 0
test "$(grep -oE 'nightsky:panel-change' dist/404.html | wc -l | tr -d ' ')" -eq 0
# Contrast + routing evidence recorded (scripted-browser sections written by the task)
grep -q 'INTG-01' .planning/phases/06-integration-launch/06-01-integration-evidence.md
grep -q 'INTG-03' .planning/phases/06-integration-launch/06-01-integration-evidence.md
grep -qiE 'worst.*(4\.[5-9]|[5-9]\.|[1-9][0-9]\.)' .planning/phases/06-integration-launch/06-01-integration-evidence.md
echo "TASK 1 PASS"
    </automated>
  </verify>
  <done>INTG-01 and INTG-03 are proven and recorded in 06-01-integration-evidence.md: 7 v1 components wrapped unforked; transform-only hiding (zero box-suppression); contrast --cdp both viewports >=4.5 vs --ink with 0 failing regions plus --moon PASS; both case studies cold-load scene/deck-free; browser Back restores #systems; sitemap lists exactly the 3 real routes; deck sentinel unreachable from /work/* and /404; the /#work crosslink resolution recorded as a fix-forward observation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: INTG-02 — the embedded Fig. 01 audit (Pitfall 11): cold deep-link 0x0, full v1 interaction checklist headless, resize-while-inactive, classic, reduced-motion, one-active-animation</name>
  <files>.planning/phases/06-integration-launch/06-01-fig01-audit.md</files>
  <behavior>
    - Cold-load /#fig-01: after load, `#fig01-canvas`.width > 0 AND .height > 0 (no 0x0 init), and `[data-panel-id="fig-01"]` has data-state="active".
    - send: clicking `#send` prepends a "request dispatched" line to `#fig01-log`.
    - fault: clicking `#fault` narrates "fault injected" + "weighed away" to the log (class hl), disables `#fault`, and the faulted node's `.node-proxy` aria-label gains the "degraded, rerouting" suffix.
    - self-heal: after ~8s the log gains "recovered · weight restored" (class ok) and `#fault` is re-enabled.
    - keyboard proxies: focusing a `.node-proxy` shows the tooltip (`#fig01-tip` opacity=1, non-empty); clicking a proxy prepends "request dispatched". All 10 proxies carry non-empty aria-labels.
    - resize-while-inactive -> activate: cold-load /#hero (fig-01 inactive); resize the viewport; activate fig-01; `#fig01-canvas`.width === round(clientWidth * min(devicePixelRatio,2)) and both dimensions > 0 (re-measured, not 0x0, not stale DPR).
    - classic mode: with view-preference classic, `<html>` is classic-active (not deck-active) and Fig. 01 is interactive (send updates the log); dist/index.html (no-JS output) contains the full figure chrome + 10 proxy buttons.
    - reduced-motion: under prefers-reduced-motion, two `#fig01-canvas` captures ~400ms apart are identical (static frame, no loop) AND clicking `#fault` still narrates (reduced-motion redraw path works).
    - one-active-animation: while fig-01 is active, two `#nightsky-canvas` captures ~500ms apart are identical (scene paused) while two `#fig01-canvas` captures during a dispatched beam differ (fig01 animating); on the hero panel, two `#nightsky-canvas` captures differ (scene running).
  </behavior>
  <action>
Read src/components/Figure01.astro (DOM contract: .fig root, .fig-stage, #fig01-canvas, #fig01-tip, #fig01-log, #send, #fault, .node-proxy[data-node]), src/lib/fig01/render.ts (layout(): W = .fig-stage getBoundingClientRect width, H = canvas.clientHeight (CSS 380px / 300px mobile); getDpr = min(devicePixelRatio,2); ResizeObserver-driven re-layout), src/lib/fig01/interactions.ts (MutationObserver on .panel[data-state] sets panelActive; updateRunState gate; scheduleHeal setTimeout HEAL_DELAY_MS=8000; createRedraw -> renderStaticFrame under reduced motion), src/lib/nightsky/scene.ts (nightsky:panel-change -> fig01Active -> updateRunState stops the scene loop), src/components/PanelDeck.astro (pre-paint speculation only for empty/#hero hash; deck.ts resolves other hashes synchronously), and src/lib/nightsky/deck.ts (goTo/applyPanelStates/keyboard).

Write a scratchpad node CDP driver (reuse the launchChrome/connectCdp/node-WebSocket harness pattern already in scripts/verify-contrast.mjs; save under the scratchpad dir, do NOT add it to the repo or to package.json). Build once and serve a local preview (npm run build && npx astro preview -> http://localhost:4321/). Drive the assembled page headlessly and assert each behavior above via Runtime.evaluate (dispatch clicks/focus on #send/#fault/.node-proxy, read #fig01-log.textContent, read canvas.width/height/clientWidth, read `[data-panel-id]` data-state, read devicePixelRatio, and capture canvas frames via canvas.toDataURL() for the static/animating pixel-diffs). For reduced-motion use CDP Emulation.setEmulatedMedia (prefers-reduced-motion: reduce) or the `--force-prefers-reduced-motion` chrome flag (the sanctioned precedent). For the resize test use CDP Emulation.setDeviceMetricsOverride (or relaunch at a second width) while fig-01 is inactive, then activate and assert the re-measured backing store.

Anchor the 0x0 audit in what render.ts ACTUALLY does: because inactive deck panels hide via transform (they keep their layout box), .fig-stage has a nonzero width even when the fig-01 panel is inactive, and canvas.clientHeight is the fixed CSS height — so layout() computes a nonzero backing store. The cold deep-link case is the real risk (pre-paint speculation excludes non-hero hashes), so assert the POST-load init correctness: canvas nonzero at init on a cold /#fig-01. The resize case proves the ResizeObserver re-layout (not gated on panelActive) keeps the size correct across an inactive->active transition.

Record the full result as a checklist table in 06-01-fig01-audit.md that maps 1:1 to the v1 Fig. 01 checklist in 02-VERIFICATION.md (the 5 observable truths + FIG-01..07), each marked PASS with the concrete assertion evidence, PLUS the four deck-reality additions (cold-load 0x0, resize-while-inactive, classic, one-active-animation). Capture screenshots for the readiness pack: cold /#fig-01 (canvas painted), post-fault degraded state, and post-resize+activate. If ANY behavior fails, record the exact failing path and stop — do not paper over it.

NEVER run `git push`. Do NOT modify `.planning/config.json`.
  </action>
  <verify>
    <automated>
set -e
# The scratchpad CDP audit script must exit 0 with every sub-check PASS.
# (Executor names the script; it builds+previews, drives headless Chrome, asserts all behaviors, prints "FIG01 EMBEDDED AUDIT PASS".)
node "$SCRATCHPAD/fig01-embedded-audit.mjs" | tee "$SCRATCHPAD/fig01-audit.out"
grep -q 'FIG01 EMBEDDED AUDIT PASS' "$SCRATCHPAD/fig01-audit.out"
# No-JS build output carries the full figure chrome + 10 proxy buttons (classic/no-JS floor)
npm run build >/dev/null 2>&1
test "$(grep -oE 'class="node-proxy' dist/index.html | wc -l | tr -d ' ')" -eq 10
grep -q 'role="img"' dist/index.html
grep -q 'id="fig01-log"' dist/index.html
# Evidence table written and maps the v1 checklist + deck-reality additions
EV=.planning/phases/06-integration-launch/06-01-fig01-audit.md
grep -q 'INTG-02' "$EV"
grep -qiE '0x0|no zero|nonzero canvas' "$EV"
grep -qiE 'resize' "$EV"
grep -qiE 'one[- ]active|scene paused' "$EV"
grep -qiE 'reduced[- ]motion' "$EV"
grep -qiE 'classic' "$EV"
echo "TASK 2 PASS"
    </automated>
  </verify>
  <done>06-01-fig01-audit.md records the full embedded Fig. 01 checklist re-pass: cold /#fig-01 inits a nonzero canvas; send/fault/weigh-away/~8s self-heal/keyboard-proxy/sr-only narration all work embedded; resize-while-inactive then activate re-measures with no 0x0 and no stale DPR; classic mode and reduced-motion keep the figure functional; exactly one canvas animates at a time (scene paused while fig-01 active). No-JS output carries the full figure chrome + 10 proxy buttons. Screenshots captured. Every check maps to the v1 02-VERIFICATION checklist.</done>
</task>

<task type="auto">
  <name>Task 3: Full local regression battery + local Lighthouse both presets + the launch-readiness go/no-go pack</name>
  <files>.planning/phases/06-integration-launch/06-01-lighthouse-scores-local.md, .planning/phases/06-integration-launch/06-01-LAUNCH-READINESS.md</files>
  <action>
Re-run the full shipped gate battery against the assembled tree and record it in 06-01-lighthouse-scores-local.md following the format of 05.1-celestial-extras/lighthouse-scores.md (a PASS/FAIL table + a scores table). Gates:
- `npm run build` (exit 0); `npx astro check` (0 errors / 0 warnings / 0 hints).
- Zero hex literals outside tokens.css, boundary-aware `#[0-9a-fA-F]{3,8}\b` over src/lib/nightsky (incl. meteors.ts), src/lib/fig01, src/lib/shared, NightSky.astro, src/data/constellations.ts, deck.css — expect 0 real (document the known `#dec` id-selector false positives excluded by the `\b` terminator, per the 05.1 precedent).
- Single-rAF invariants (count `requestAnimationFrame` per file with grep -o | wc -l): src/lib/fig01/render.ts = 2; nightsky total = 2 (both in scene.ts); starfield.ts = 0; meteors.ts = 0.
- Zero deck/fig01 imports in the nightsky scene modules (scene/constellations/starfield/meteors/tokens): 0 import statements (event-name subscription + mirrored math only).
- Local Lighthouse: start the preview (npm run build && npx astro preview -> http://localhost:4321/), run `npx lighthouse` mobile (default preset) and desktop (--preset=desktop) headless, extract all four category scores from each, and record them. Every category must be >=90 on BOTH presets. Baseline for reference: mobile 98/100/100/100, desktop 100x4 (05.1). If any category is <90, record the shortfall and the responsible audit(s) — do NOT silently pass; a local <90 is a planning-time flag for the user (the authoritative INTG-04 gate is the LIVE run in 06-02).
- Sign-off screenshots for the readiness pack: hero, a constellation-brightened panel (experience — Microsoft cluster), the crescent moon region, and the reduced-motion static frame (mirroring the 05-06 / 05.1 screenshot set), plus the Fig. 01 screenshots captured in Task 2.

Then write 06-01-LAUNCH-READINESS.md as the single decision-ready go/no-go pack FOR THE USER (06-CONTEXT.md "Specific Ideas" — no digging required). It MUST contain:
1. What changes live: the live site flips from the v1 single-scroll editorial page to the v2 night-sky panel deck (this REPLACES the current live professional site). One paragraph, plain language.
2. The aggregate gate table: pull every gate result from Task 1 (INTG-01/03), Task 2 (INTG-02), and this task's battery + local Lighthouse into one table with PASS/FAIL and the key numbers (contrast worst-case both viewports, rAF counts, Lighthouse four-category scores both presets). Mark live Lighthouse as the ONE gate deferred to 06-02.
3. Screenshot index: a table listing each committed screenshot (hero / constellation / moon / reduced-motion / fig-01 cold-load / fig-01 degraded) with a one-line caption.
4. Rollback plan (state it explicitly, do not execute it): if the user ever wants to undo the launch — revert the deploy commits + push (v1 remains in git history), the v1.0 tag is already present, and the legacy-2021 branch is preserved on origin. Include the exact rollback commands as reference (revert + fast-forward push; never --force).
5. Real-device touch checklist (carried from Phase 4's checkpoint item 8 by explicit prior agreement — NOT automatable, the user's physical devices): list the concrete checks the user runs against the LIVE site post-deploy (rollback available): on a real iPhone (Safari) and a mid-tier Android (Chrome) — vertical swipe pages exactly one panel (no skip, no dead feel, no double-fire from momentum tail); horizontal/edge swipe does NOT hijack navigation (iOS edge-back intact); jump-index tap jumps to the panel; deep-link cold-load (e.g. /#patents) opens the right panel with no flash of hero; view-classic escape hatch works; Fig. 01 send/fault are tappable and the log narrates.
6. The 5-minute on-device idle-CPU confirmation carried from Phase 5 (05-06 SKY-03 deferral) — note it as a live post-deploy check the user can run.

NEVER run `git push`. Do NOT modify `.planning/config.json`.
  </action>
  <verify>
    <automated>
set -e
npm run build >/dev/null 2>&1
npx astro check 2>&1 | grep -qE '0 error'
# rAF single-driver invariants
test "$(grep -oE 'requestAnimationFrame' src/lib/fig01/render.ts | wc -l | tr -d ' ')" -eq 2
test "$(grep -oE 'requestAnimationFrame' src/lib/nightsky/scene.ts | wc -l | tr -d ' ')" -eq 2
test "$(grep -oE 'requestAnimationFrame' src/lib/nightsky/starfield.ts | wc -l | tr -d ' ')" -eq 0
test "$(grep -oE 'requestAnimationFrame' src/lib/nightsky/meteors.ts | wc -l | tr -d ' ')" -eq 0
# Zero cross-boundary imports in scene modules
test "$(grep -orE "import[^\n]*(nightsky/deck|lib/fig01)" src/lib/nightsky/scene.ts src/lib/nightsky/constellations.ts src/lib/nightsky/starfield.ts src/lib/nightsky/meteors.ts | wc -l | tr -d ' ')" -eq 0
# Boundary-aware zero-hex (word-terminated) across the styled/scene surface
test "$(grep -ornE '#[0-9a-fA-F]{3,8}\b' src/lib/nightsky src/lib/fig01 src/styles/deck.css | wc -l | tr -d ' ')" -eq 0
# Lighthouse recorded, all four categories >=90 both presets (numbers written by the task)
LH=.planning/phases/06-integration-launch/06-01-lighthouse-scores-local.md
grep -qiE 'mobile' "$LH" && grep -qiE 'desktop' "$LH"
# Readiness pack complete: all six required sections present
RD=.planning/phases/06-integration-launch/06-01-LAUNCH-READINESS.md
grep -qiE 'changes live|replaces' "$RD"
grep -qiE 'gate table|PASS' "$RD"
grep -qiE 'screenshot' "$RD"
grep -qiE 'rollback' "$RD"
grep -qiE 'real[- ]device|iPhone|Android' "$RD"
grep -qiE 'live Lighthouse' "$RD"
echo "TASK 3 PASS"
    </automated>
  </verify>
  <done>The full local regression battery is green and recorded in 06-01-lighthouse-scores-local.md (build, astro check 0/0/0, boundary-aware zero-hex, rAF 2/2/0/0, zero cross-boundary imports, local Lighthouse >=90 x4 both presets). 06-01-LAUNCH-READINESS.md is a complete, decision-ready go/no-go pack: what-changes-live, aggregate gate table, screenshot index, explicit rollback plan, the carried real-device touch checklist, and the live 5-min idle-CPU note. Live Lighthouse is the only gate marked deferred to 06-02. No push, no deploy, config.json untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local working tree -> origin/main (GitHub) | A push here IS the deploy (deploy.yml fires on push to main) and replaces the live professional site. This plan must NEVER cross it. |
| planning artifacts -> config | `.planning/config.json` governs autonomous behavior; this plan must not mutate it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Elevation/Integrity | any `git push` in 06-01 | critical | mitigate | No push, deploy, or Pages trigger anywhere in this plan; the deploy is exclusively 06-02 Task 2, after the 06-02 Task 1 human gate. Every verify block is local-only. |
| T-06-03 | Denial of Service (credibility) | shipping a regressed site | high | mitigate | Full local battery + embedded Fig. 01 audit + contrast non-regression must be green and recorded BEFORE the go/no-go, so 06-02's deploy is anticlimactic. Any shortfall recorded, never silently passed. |
| T-06-04 | Tampering | `.planning/config.json` | low | mitigate | Plan explicitly does not touch config.json; autonomous behavior is unchanged. |
| T-06-SC | Tampering | transient `npx lighthouse` audit tool | low | mitigate | Lighthouse runs as a transient audit tool only (Phase 4/5 precedent); no new dependency is added to package.json; verify-contrast.mjs is plain node + built-in WebSocket. |
</threat_model>

<verification>
- Local-only: `git status` shows no push; origin/main is untouched by this plan (all commits, if any, are local docs commits made by the GSD workflow, not pushes).
- INTG-01: 7 v1 components wrapped unforked; transform-only hiding (0 box-suppression); contrast --cdp both viewports >=4.5 vs --ink, 0 failing regions; --moon PASS.
- INTG-02: the embedded Fig. 01 audit script exits 0 with the full v1 checklist + deck-reality additions PASS; no-JS output carries the figure chrome + 10 proxies.
- INTG-03: both /work/* cold-load scene/deck-free; browser Back restores #systems; sitemap lists exactly 3 real routes; deck sentinel unreachable from /work/* and /404.
- INTG-04 pre-flight: local Lighthouse >=90 x4 on both presets recorded; the authoritative live run is 06-02.
- The launch-readiness pack contains all six required sections.
</verification>

<success_criteria>
- All three evidence files + the launch-readiness pack exist, are committed, and record green gates (or explicitly-recorded shortfalls, never silent passes).
- The user has a single decision-ready document to make the 06-02 go/no-go call without digging.
- Nothing was pushed or deployed; `.planning/config.json` is untouched.
</success_criteria>

<output>
Create `.planning/phases/06-integration-launch/06-01-SUMMARY.md` when done.
</output>
