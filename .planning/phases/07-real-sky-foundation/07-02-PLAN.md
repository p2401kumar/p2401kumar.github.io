---
phase: 07-real-sky-foundation
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - .planning/phases/07-real-sky-foundation/spike-glass/harness.html
  - .planning/phases/07-real-sky-foundation/spike-glass/soak.mjs
  - .planning/phases/07-real-sky-foundation/07-SPIKE-GLASS.md
autonomous: true
requirements: [IMG-05]
user_setup: []

must_haves:
  truths:
    - "A standalone harness renders the committed sky master + the REAL live scene.ts engine + 4 mock backdrop-filter blur(12px) surfaces at production geometry"
    - "A 60s CDP soak measures idle CPU for scene-alone (baseline) and scene+glass, and the MARGINAL delta is computed and recorded (same Performance.getMetrics methodology as 05-06's 5.6% baseline)"
    - "07-SPIKE-GLASS.md records a PASS / PASS-WITH-LADDER / FAIL verdict against the marginal <=2-3% (total <10%) budget — the Phase-8 glass re-scope gate"
  artifacts:
    - .planning/phases/07-real-sky-foundation/spike-glass/harness.html
    - .planning/phases/07-real-sky-foundation/spike-glass/soak.mjs
    - .planning/phases/07-real-sky-foundation/07-SPIKE-GLASS.md
  key_links:
    - "The verdict gates 07-03 and all of Phase 8: FAIL STOPS the chain (07-CONTEXT.md designated autonomous-mode stop); PASS-WITH-LADDER enumerates the exact mitigations Phase 8 must apply"
    - "The harness backdrop is 07-01's committed public/sky master (blur CPU cost is content-independent, so the real master is the faithful choice)"
---

<objective>
Spike 2 (the milestone re-scope trigger): measure the idle-CPU cost of `backdrop-filter: blur(12px)` glass surfaces sitting over the permanently-animating night-sky canvas, so Phase 8 knows — BEFORE it builds glass — whether "full glass over an animating scene" fits the CPU floor or needs a structural rethink (glass over static-only regions).

Per 07-CONTEXT.md D-Spike2: this is the designated autonomous-mode stop. It runs FIRST (spike-first pattern), and its verdict is a hard gate on every later plan.

This plan touches IMG-05 only insofar as it validates that the surviving animated authored overlay (constellations/meteors/twinkle) can coexist with future glass within budget; its primary purpose is the ROADMAP "Research gate" glass re-scope decision.

Purpose: produce the PASS / PASS-WITH-LADDER / FAIL verdict that unblocks (or halts) the chain.
Output: `spike-glass/harness.html`, `spike-glass/soak.mjs`, `07-SPIKE-GLASS.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/07-real-sky-foundation/07-CONTEXT.md
@.planning/phases/07-real-sky-foundation/07-RESEARCH.md

# The zero-dep CDP harness to reuse VERBATIM (launchChrome/connectCdp/Cdp, findChrome):
@scripts/verify-contrast.mjs
# The live engine the harness must exercise (not a re-implementation):
@src/lib/nightsky/scene.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the standalone glass harness + CDP soak driver</name>
  <files>.planning/phases/07-real-sky-foundation/spike-glass/harness.html, .planning/phases/07-real-sky-foundation/spike-glass/soak.mjs</files>
  <action>
Create `spike-glass/harness.html` per 07-RESEARCH.md §3.2: a fixed full-viewport stack of (bottom) an `<img>` pointing at 07-01's committed master `/sky/milky-way-2560.avif`, (middle) `#scene` canvas driven by the REAL `initNightSky()` from src/lib/nightsky/scene.ts (import the actual engine — mirror v2's spike-milkyway.html precedent of exercising real engine code, never a mock), and (top) 4 mock `.glass` surfaces at production-like geometry with `backdrop-filter: blur(12px) saturate(150%)` and background `rgb(255 255 255 / 0.07)`: header (top, height 64px), footer (bottom, height 88px), jump-index (bottom-right, 160x220), content panel (centered, 880 x 60vh). The 4 glass divs must be toggleable via a query flag or a `display:none` class so the same page serves both runs.

Create `spike-glass/soak.mjs`: reuse verify-contrast.mjs's `findChrome()` / `launchChrome()` / `connectCdp()` / `Cdp` class VERBATIM (zero new dependency — node 22 built-in WebSocket + headless "new" mode; software rasterization is conservative vs real GPU, so a passing number is a safe lower bound). Serve the harness locally (astro preview OR a tiny static file server over the built dist so `/sky/` resolves). Inject into the page: `Performance.getMetrics` sampling at soak start + end (CPU% = (TaskDuration_end - TaskDuration_start) / wallClockSeconds), a `PerformanceObserver('longtask')` counter (target 0), and a requestAnimationFrame frame counter for realized fps (target 60.0). Track the `LayoutDuration` delta separately (target 0.000s — blur is a compositor op, not layout). Implement the two-run protocol per 07-RESEARCH.md §3.2: (1) BASELINE = 4 glass divs display:none, 60s soak; (2) GLASS = 4 glass divs visible, 60s soak. Record for BOTH runs: wall-clock, TaskDuration delta -> CPU%, ScriptDuration delta, LayoutDuration delta, long-task count, realized fps. Then compute MARGINAL = GLASS CPU% - BASELINE CPU%.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-real-sky-foundation/spike-glass/harness.html && test -f .planning/phases/07-real-sky-foundation/spike-glass/soak.mjs && test "$(grep -o 'blur(12px)' .planning/phases/07-real-sky-foundation/spike-glass/harness.html | wc -l | tr -d ' ')" = "1"</automated>
  </verify>
  <done>harness.html renders the committed master + real scene engine + 4 mock blur(12px) glass surfaces at production geometry; soak.mjs reuses the verify-contrast CDP primitives and implements the baseline-vs-glass two-run protocol with marginal-delta computation; no new npm dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Run the 60s soaks, compute the marginal delta, write the verdict</name>
  <files>.planning/phases/07-real-sky-foundation/07-SPIKE-GLASS.md</files>
  <action>
Run both 60s soaks (baseline, then glass). Expect the baseline to be equal-or-LOWER than 05-06's 5.6% (Layer 0 generation is now near-empty per §5, less canvas work/frame). Compute MARGINAL = glass CPU% - baseline CPU%.

Write `07-SPIKE-GLASS.md` with the full 05-06-shaped table for BOTH runs (wall-clock, CPU%, ScriptDuration delta, LayoutDuration delta, long-task count, fps), the explicit marginal delta, and ONE verdict keyed off the marginal (not either absolute number):
  - PASS: marginal <= 2-3% AND total < 10% -> full glass proceeds in Phase 8.
  - PASS-WITH-LADDER: over the marginal target but tunable under the floor -> enumerate EXACTLY which GLASS.md §1.4 mitigations were needed, in priority order (minimize overlap area -> throttle canvas redraw to ~15-20fps -> remove inactive panels from the paint tree via display:none not opacity:0 -> keep blur <=16px where canvas is present -> cap simultaneous glass surfaces at <=4), so Phase 8 applies them as measured, not re-derived.
  - FAIL: cannot tune under the floor -> STOP protocol below.

**FAIL protocol (state verbatim in 07-SPIKE-GLASS.md and honor it): STOP — do not proceed to any later plan. Do not attempt mitigations and silently re-measure. Return the FAIL verdict with the measured numbers as evidence; the orchestrator presents the structural rethink (glass over static-only regions) to the user. This is a designated autonomous-mode stop (07-CONTEXT.md).**
  </action>
  <verify>
    <automated>test -f .planning/phases/07-real-sky-foundation/07-SPIKE-GLASS.md && test "$(grep -oE 'PASS|PASS-WITH-LADDER|FAIL' .planning/phases/07-real-sky-foundation/07-SPIKE-GLASS.md | head -1 | wc -l | tr -d ' ')" = "1"</automated>
  </verify>
  <done>07-SPIKE-GLASS.md records both soak tables, the computed marginal delta, and a single PASS / PASS-WITH-LADDER / FAIL verdict. On PASS-WITH-LADDER the exact mitigation ladder subset is enumerated. On FAIL the STOP protocol is invoked (chain halts, verdict + numbers returned to the orchestrator, no later plan runs).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local preview -> headless Chrome (CDP) | Verification-time only; the harness is decorative and takes no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03 | Denial of Service | 60s CDP soak leaves a headless Chrome process | low | mitigate | Reuse verify-contrast.mjs's proc.kill()/tmp-profile cleanup in a finally block (already proven) |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | NO installs — node 22 built-in WebSocket + local Chrome only; package.json untouched (07-RESEARCH.md Package Legitimacy Audit) |
</threat_model>

<verification>
- Both soaks run to completion; baseline and glass CPU% recorded; marginal delta computed.
- 07-SPIKE-GLASS.md contains exactly one verdict tier.
- No new npm dependency: `git diff --exit-code -- package.json package-lock.json`.
- On FAIL: the plan STOPS and reports; no source files are modified and no later plan is entered.
</verification>

<success_criteria>
A defensible PASS / PASS-WITH-LADDER / FAIL glass-CPU verdict recorded with the marginal delta and full soak tables; the FAIL-STOP protocol wired as the chain gate; no push; no new dependency.
</success_criteria>

<output>
Create `.planning/phases/07-real-sky-foundation/07-02-SUMMARY.md` when done.
</output>
