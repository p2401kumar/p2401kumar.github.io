---
phase: 07-real-sky-foundation
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - src/components/NightSky.astro
  - src/layouts/BaseLayout.astro
  - src/pages/index.astro
  - src/lib/nightsky/starfield.ts
  - src/lib/nightsky/scene.ts
  - src/lib/nightsky/constellations.ts
  - scripts/verify-contrast.mjs
autonomous: true
requirements: [IMG-01, IMG-02, IMG-05]
user_setup: []

must_haves:
  truths:
    - "The composited Milky-Way photo renders full-viewport behind everything on the home page as a static picture/img (never canvas-drawn), present in no-JS classic mode, LCP-preloaded, zero CLS (IMG-01, IMG-02)"
    - "The procedural sky wash + dither + full starfield + procedural Milky Way are gone from Layer 0; the photo shows through the now-transparent canvas; the drawn crescent moon, career constellations (panel-reactive brighten), meteors, and the ~40-star twinkle subset all survive on top (IMG-05)"
    - "Constellation stars render at 3.0-4.5px with a faint resting halo (1.3x radius, alpha 0.12-0.18); the moon stays dimmer than the photo's brightest galactic-core pixel at both check widths"
    - "verify-contrast.mjs composites the real photo as the bottom sampling layer; worst-case text-over-scene contrast stays >= 4.5:1 vs --ink"
  artifacts:
    - src/components/NightSky.astro
    - src/layouts/BaseLayout.astro
    - src/pages/index.astro
    - src/lib/nightsky/starfield.ts
    - src/lib/nightsky/constellations.ts
    - scripts/verify-contrast.mjs
  key_links:
    - "The picture srcset EXACTLY mirrors the index.astro preload imagesrcset -> single fetch, no double-download (07-RESEARCH.md pitfall 2)"
    - ".sky-photo is the FIRST child of .nightsky-host (below #nightsky-canvas by DOM order) -> photo bottom, transparent overlay on top, no new z-index"
    - "The photo-aware verify-contrast composite keeps the honesty gate honest per commit (07-CONTEXT.md: no silent unverified contrast)"
---

<objective>
Integration core (gated by 07-02's PASS/PASS-WITH-LADDER — do NOT run if Spike 2 returned FAIL): deliver the real sky photo behind the scene, perform the overlay surgery so the authored elements survive on top of the photo instead of the procedural sky, upgrade the constellation stars for legibility against real star noise, and evolve verify-contrast.mjs to sample over the actual composited photo.

Purpose: land IMG-01 (photo renders behind everything), IMG-02 (static/LCP-discoverable/no-JS/zero-CLS delivery), and IMG-05 (authored overlay survives on the real sky) in one coherent, contrast-verified commit.
Output: photo delivery in NightSky/BaseLayout/index; surgery in starfield/scene/constellations; photo-aware verify-contrast.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/07-real-sky-foundation/07-RESEARCH.md
@.planning/phases/07-real-sky-foundation/07-UI-SPEC.md

# The exact surgery/delivery surfaces (read as live):
@src/components/NightSky.astro
@src/layouts/BaseLayout.astro
@src/pages/index.astro
@src/lib/nightsky/starfield.ts
@src/lib/nightsky/scene.ts
@src/lib/nightsky/constellations.ts
@scripts/verify-contrast.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Photo delivery — LCP-discoverable, no-JS, zero-CLS picture below the canvas</name>
  <files>src/layouts/BaseLayout.astro, src/pages/index.astro, src/components/NightSky.astro</files>
  <action>
BaseLayout.astro: add a named head slot inside `<head>`, after the two existing font preloads (07-RESEARCH.md section 4.1) so only pages that need it inject a preload; work/[slug].astro and 404.astro pass nothing and stay unaffected.

index.astro: pass a preload link through the head slot — rel=preload, as=image, href=/sky/milky-way-1920.avif, imagesrcset "/sky/milky-way-1920.avif 1920w, /sky/milky-way-2560.avif 2560w", imagesizes=100vw, fetchpriority=high. The imagesrcset/imagesizes MUST byte-for-byte mirror the picture avif source's srcset/sizes below (mismatch = double-fetch, 07-RESEARCH.md pitfall 2).

NightSky.astro: add a `.sky-photo` div as the FIRST child of `.nightsky-host` (BEFORE `#nightsky-canvas`) so it paints at the bottom by DOM order — NO new z-index values (07-RESEARCH.md section 4.3). Inside it a `<picture>` with an avif `<source>` (srcset "/sky/milky-way-1920.avif 1920w, /sky/milky-way-2560.avif 2560w", sizes 100vw), a webp `<source>` (same pattern, .webp), and an `<img src="/sky/milky-way-1920.avif" alt="" role="presentation" fetchpriority="high" loading="eager" decoding="async">`. Read the LQIP at build time in the component frontmatter (readFileSync of public/sky/milky-way-lqip.txt, per 07-RESEARCH.md section 4.2) and set it as the `.sky-photo` wrapper background (center/cover) so it paints instantly behind the img; also set the wrapper background-color to var(--bg) so a failed load degrades to --bg, never a broken-image glyph (07-UI-SPEC error-state).

Styling (in NightSky.astro's scoped style block, present before first paint — no JS-injected sizing): `.sky-photo`, its picture, and img all position:absolute, inset:0, width:100%, height:100%; img object-fit:cover. Zero CLS is structural (the host is already position:fixed, never in flow — 07-RESEARCH.md section 4.2). Object-position responsive ladder via media queries (07-UI-SPEC): under 640px -> 12% 25%; 640-1023px -> 55% 32%; 1024-1799px -> 72% 38%; 1800px and up -> 78% 34%. LQIP-to-full fade: a NO-JS-SAFE CSS keyframe fade on the img (opacity 0 to 1 over 400ms, cubic-bezier(0.16,1,0.3,1), matching deck.css) that ENDS at opacity:1 so the photo is visible without any script (IMG-02 no-JS clause is a hard gate — never a JS/load-event-gated opacity that leaves the img invisible under no-JS); the LQIP wrapper background sits behind it throughout. Under prefers-reduced-motion:reduce, animation:none plus opacity:1 instant (07-UI-SPEC "stop, don't dampen"). NOTE: if the opacity fade regresses LCP past budget at 07-04's checkpoint, the fade yields — the LCP floor wins over the fade nicety; the acceptable fallback is 07-RESEARCH.md section 4.2's LQIP-as-img-background with no opacity fade.

The photo is NEVER drawn into the canvas — plain DOM only (IMG-02; 07-RESEARCH.md Canvas2D-8-bit pitfall).
  </action>
  <verify>
    <automated>npx astro check && npm run build && for f in dist/sky/milky-way-1920.avif dist/sky/milky-way-2560.avif dist/sky/milky-way-1920.webp dist/sky/milky-way-2560.webp; do test -f "$f" || exit 1; done && test "$(grep -o 'slot=.head.' src/pages/index.astro | wc -l | tr -d ' ')" = "1" && test "$(grep -o 'name=.head.' src/layouts/BaseLayout.astro | wc -l | tr -d ' ')" = "1"</automated>
  </verify>
  <done>astro check 0 errors, build green, all 4 masters copied to dist/sky/; index.astro injects the preload via the head slot with imagesrcset mirroring the picture srcset; .sky-photo is the first child of .nightsky-host; the photo renders in a no-JS load (opacity ends at 1 with zero script); zero new z-index values added.</done>
</task>

<task type="auto">
  <name>Task 2: Overlay surgery + constellation legibility upgrade</name>
  <files>src/lib/nightsky/starfield.ts, src/lib/nightsky/scene.ts, src/lib/nightsky/constellations.ts</files>
  <action>
starfield.ts (per 07-RESEARCH.md section 5.1-5.3 function map):
- REMOVE the full-canvas sky-wash gradient fill AND the ground-fill rect from generateLayer0 (both fillRect calls that paint an opaque sky) so Layer 0 is transparent except the authored elements and the photo shows through. Per 07-UI-SPEC Seam Contract "Ground-fill consistency" resolution 1 (preferred): remove the canvas ground-fill rect entirely — the photo already blends to --bg and the root --bg sits beneath the fixed host, so the ground reads correctly with nothing drawn.
- REMOVE the dither pass and its row-slice helper. <!-- planner-discipline-allow: ditherRows -->
- REMOVE the entire procedural Milky Way composite and its now-dead helpers plus constants (haze/fine/coarse dust, centerline, dust-dot, the MW_ constants). <!-- planner-discipline-allow: queueMilkyWay -->
- REMOVE the Faintest + Dim magnitude bands' bake (87% of stars, never twinkle-eligible). KEEP generating the twinkleStars metadata for Mid+Bright candidates ONLY, confined to the horizontal margins OUTSIDE the content column (mirror scene.ts's contentColumnEdges formula — a hard margin include/exclude, replacing the smoothstep alpha cap, per section 5.2), pushing x/y/radius/baseAlpha/color WITHOUT any canvas fill() for them. Retire the SKY-05 governor's alpha-cap/attenuation halves (no baked field left to cap); keep only the margin inclusion-gating (re-scoped as above).
- KEEP drawMoon unchanged (the final Layer-0 unit) — it already composites source-over, correct over a photo.
- KEEP the requestIdle/drainQueue chunking machinery in place (cheap when near-empty; Phase 9 may reuse it). starfield.ts must keep ZERO requestAnimationFrame calls.

scene.ts: seedTwinkles is metadata-agnostic and needs NO change (section 5.3) — twinkle count still lands ~40 at 1440x900 (07-04 measures it; if the margin pool is too sparse, raising TWINKLE_SUBSET_FRACTION is the one-line fix, not dropping the target). The single-rAF invariant holds: scene.ts stays at exactly 2 requestAnimationFrame.

constellations.ts (07-UI-SPEC Overlay Harmony): raise STAR_RADIUS_MID from 2.75 to 3.0 and STAR_RADIUS_BRIGHT from 4 to 4.5 (upper half of the 2.5-4px range, so drawn stars read categorically larger than ~1-2px photo star specks). Add a faint RESTING halo: set the ambient state's halo alpha from 0 to 0.15 (within 0.12-0.18) and introduce an ambient halo radius scale of 1.3x (kept visibly quieter than the brightened 1.5x / 0.20-0.30 halo) used for the resting-state halo draw. Dimmed halo stays 0. Link stroke unchanged.
  </action>
  <verify>
    <automated>npx astro check && npm run build && test "$(grep -o 'requestAnimationFrame' src/lib/nightsky/scene.ts | wc -l | tr -d ' ')" = "2" && test "$(grep -o 'requestAnimationFrame' src/lib/nightsky/starfield.ts | wc -l | tr -d ' ')" = "0" && test "$(grep -o 'queueMilkyWay' src/lib/nightsky/starfield.ts | wc -l | tr -d ' ')" = "0" && test "$(grep -o 'ditherRows' src/lib/nightsky/starfield.ts | wc -l | tr -d ' ')" = "0" && test "$(grep -o '3.0' src/lib/nightsky/constellations.ts | wc -l | tr -d ' ')" != "0" && test "$(grep -o '4.5' src/lib/nightsky/constellations.ts | wc -l | tr -d ' ')" != "0"</automated>
  </verify>
  <done>astro check 0 errors, build green; scene.ts holds exactly 2 requestAnimationFrame, starfield.ts 0; the procedural MW composite and dither pass are gone (grep counts 0); Layer 0 is transparent except the authored elements; constellation radii are 3.0/4.5 with a resting 1.3x halo; no deck/fig01 import appears in any nightsky scene module.</done>
</task>

<task type="auto">
  <name>Task 3: Photo-aware verify-contrast.mjs (drawImage composite + moon-ceiling retarget)</name>
  <files>scripts/verify-contrast.mjs</files>
  <action>
Extend the verifier's in-page sampler to composite the REAL photo as the new bottom layer (07-RESEARCH.md section 6, Option A — analytic drawImage, NOT screenshot sampling; the pre-blur case is mathematically exact and needs zero new dependency).

samplePageOnce: before the existing scrim/DOM compositing over the `#nightsky-canvas` pixel, add one earlier step — query `.sky-photo img`, and if complete/decoded, draw it into a same-backing-store offscreen canvas using the object-fit:cover source-rect math from section 6.2 (drawImage does not understand CSS object-fit — replicate the ~10-line cover crop helper), with willReadFrequently:true. Then in the per-pixel composite, read the photo pixel FIRST (bottom), composite the canvas pixel over it via source-over using the canvas pixel's own alpha (Layer 0 is now mostly transparent, so most pixels show ~100% photo), THEN continue the existing scrim + DOM-layer chain unchanged. Document in the script header that this handles the photo (pre-blur) but intentionally NOT backdrop-filter blur — that's Phase 8's screenshot-sampling gate (section 6.3).

sampleMoonOnce (--moon mode): the Milky-Way-core comparator (sample point B) currently reads from the canvas, but the MW core now lives on the PHOTO. Retarget it to read the photo's brightest galactic-core pixel — composite the `.sky-photo img` via the same object-fit:cover drawImage and take the peak luminance over the UI-SPEC sample-B box (around x:0.90*cssWidth, y:0.50*cssHeight, and/or the x:0.80-0.98 / y:0.30-0.60 core box). moonPeak stays the canvas moon-box peak. Assert moonPeak < mwPeak strictly at both 1280x800 and 1440x900 (07-UI-SPEC Overlay Harmony: a moon brighter than the vivid photo core is not physically plausible; fix by darkening the grade, never by brightening the moon past its 0.55 ceiling).

Keep --selftest green (existing WCAG/scrim/moon fixtures unchanged); the new photo-composite path may add a fixture but must not regress the current ones.
  </action>
  <verify>
    <automated>node scripts/verify-contrast.mjs --selftest</automated>
  </verify>
  <done>--selftest passes; the sampler composites the real photo as the bottom layer via object-fit:cover drawImage; the --moon comparator reads the photo core for sample B; the honesty gate is satisfied for this commit — a --cdp run at both 1280x800 and 1440x900 over the integrated preview reports worst-case >= 4.5:1 vs --ink, and --moon at both widths reports moonPeak < mwPeak (record the numbers for the 07-04 battery).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static asset -> browser | The photo is a plain public/ asset served by GitHub Pages; no server logic, no user input |
| Local preview -> headless Chrome (CDP) | Verification-time contrast sampling only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Information Disclosure | Silent contrast regression under text over the new photo backdrop | medium | mitigate | Photo-aware verify-contrast composite + a --cdp run both viewports before this commit (07-CONTEXT.md: no silent unverified contrast for even one commit) |
| T-07-05 | Tampering | Preload/picture srcset mismatch causing a wasteful double-fetch | low | mitigate | imagesrcset mirrors the picture srcset exactly; confirmed at 07-04 via the Lighthouse "Preload LCP image" audit |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | NO installs — no package.json change; all delivery is native picture/img + existing sharp-built masters |
</threat_model>

<verification>
- astro check 0 errors; build green; all 4 masters land in dist/sky/.
- Single-rAF invariant: scene.ts=2, starfield.ts=0 (meteors.ts=0, fig01/render.ts=2 unchanged).
- Procedural MW composite + dither pass removed (grep counts 0); Layer 0 transparent except authored elements.
- No deck/fig01 import in any nightsky scene module.
- verify-contrast --selftest passes; photo-aware --cdp both viewports >= 4.5:1 vs --ink; --moon both viewports moonPeak < mwPeak.
- No push; no new npm dependency (git diff --exit-code package.json package-lock.json).
</verification>

<success_criteria>
The real sky photo renders full-viewport behind everything (static, LCP-preloaded, no-JS, zero-CLS); the authored overlay (moon, constellations with brighten + resting halo, meteors, ~40-star twinkle) survives on top of the photo; contrast over the photo is verified >= 4.5:1 and the moon stays dimmer than the photo core — all in one commit.
</success_criteria>

<output>
Create `.planning/phases/07-real-sky-foundation/07-03-SUMMARY.md` when done.
</output>
