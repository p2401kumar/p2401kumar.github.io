---
phase: 07-real-sky-foundation
plan: 04
type: execute
wave: 4
depends_on: ["07-03"]
files_modified:
  - src/components/SiteFooter.astro
  - src/styles/deck.css
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [IMG-04]
user_setup: []

must_haves:
  truths:
    - "The exact CC BY 4.0 credit line renders in the footer in BOTH deck and classic modes, with NOIRLab and CC BY 4.0 as links (IMG-04)"
    - "The full gate battery is green: astro check 0, build green, zero-hex, single-rAF (scene=2/starfield=0/meteors=0/fig01=2), no deck/fig01 imports in scene modules, contrast >= 4.5:1 both viewports, moon dimmer than the photo core both viewports, banding selftest passes"
    - "The dedicated post-photo LCP checkpoint passes: Lighthouse mobile AND desktop presets score >= 90 in every category and LCP lands within the 1.5-2.8s budget (ROADMAP #6) — a category < 90 or LCP blowout STOPS the phase"
    - "IMG-01..05 are marked Complete in REQUIREMENTS.md and the ROADMAP Phase 7 checkboxes are ticked"
  artifacts:
    - src/components/SiteFooter.astro
    - src/styles/deck.css
    - .planning/phases/07-real-sky-foundation/lcp-checkpoint.md
  key_links:
    - "The 2-line footer must still clear deck.css's .panel bottom padding (112px) in deck mode — bump the padding if the taller footer would occlude content"
    - "The LCP checkpoint is the phase's final blocking gate: it BLOCKS completion and must pass before any Phase 8 glass work compounds (07-CONTEXT.md)"
---

<objective>
Phase close-out: add the CC BY 4.0 photo credit line to the shared footer (IMG-04, both modes), run the full carried-floor gate battery plus the dedicated post-photo LCP checkpoint that BLOCKS the phase from completing hot, and mark IMG-01..05 complete.

Per 07-CONTEXT.md D-LCP: the LCP checkpoint runs immediately after integration and is a hard blocking gate — a category < 90 or an LCP blowout = STOP and report, do not proceed to Phase 8.

Purpose: land IMG-04 and prove the whole phase against every floor before it locks.
Output: credit line in SiteFooter/deck.css; the green battery + LCP evidence; REQUIREMENTS/ROADMAP/STATE updates.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@.planning/phases/07-real-sky-foundation/07-UI-SPEC.md
@.planning/phases/07-real-sky-foundation/07-RESEARCH.md

# The footer to extend + the deck padding to re-check:
@src/components/SiteFooter.astro
@src/styles/deck.css
# The gate scripts the battery runs:
@scripts/verify-contrast.mjs
@scripts/verify-banding.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: CC BY 4.0 credit line in the shared footer (both modes)</name>
  <files>src/components/SiteFooter.astro, src/styles/deck.css</files>
  <action>
SiteFooter.astro (07-UI-SPEC Credit Line): restructure the footer from a single flex row into a flex COLUMN (gap 8px). Wrap the existing `.st` status span + `#clock` in a `.footer-row` div (visual result of that row unchanged), and add a new `.credit` sibling line BELOW it, left-aligned (ragged-left, matching the status line — do not center).

Credit copy is LOCKED verbatim (07-CONTEXT.md / 07-UI-SPEC): `Sky: NOIRLab/NSF/AURA/E. Slawik/M. Zamani, CC BY 4.0`. The word NOIRLab is a link to https://noirlab.edu/public/images/noirlab2430b/ ; the phrase CC BY 4.0 is a link to https://creativecommons.org/licenses/by/4.0/ . Every other word (`Sky:`, `/NSF/AURA/E. Slawik/M. Zamani,`) is plain text. Both links get target=_blank rel="noopener noreferrer". Full unabridged line — no shortened form (honesty gate).

Credit typography/color (tokens only, zero new hex/token): font-family var(--mono), font-size 11.5px, line-height 1.4, color var(--faint). Links: color var(--dim), border-bottom 1px var(--hair2); on hover color var(--ink), border-color var(--accent) — this is the existing site-wide link pattern (global.css + SiteHeader nav precedent), not a new one.

Add the credit UNCONDITIONALLY (planner decision, per 07-RESEARCH.md pitfall 9: simplest and least likely to be silently missed; SiteFooter is shared across home + work/[slug] + 404). It is present in BOTH modes: deck mode's footer is fixed chrome, classic mode's flows normally — the same 2-line markup serves both.

deck.css: the 2-line footer is taller. VERIFY the `.panel` bottom padding (currently 112px) still clears the taller fixed footer in deck mode; if the measured footer height plus its margin/padding now exceeds 112px, bump that padding value so the footer never occludes panel content (07-UI-SPEC Credit Line "Both modes" — a mechanical clearance check).
  </action>
  <verify>
    <automated>npx astro check && npm run build && test "$(grep -o 'NOIRLab/NSF/AURA/E. Slawik/M. Zamani' src/components/SiteFooter.astro | wc -l | tr -d ' ')" = "1" && test "$(grep -o 'creativecommons.org/licenses/by/4.0' src/components/SiteFooter.astro | wc -l | tr -d ' ')" = "1" && test "$(grep -o 'NOIRLab' dist/index.html | wc -l | tr -d ' ')" != "0"</automated>
  </verify>
  <done>astro check 0, build green; the exact credit line + both links render in the footer; it appears in the built home page HTML (both deck and classic mode share this markup); deck.css .panel padding still clears the 2-line footer.</done>
</task>

<task type="auto">
  <name>Task 2: Full gate battery + blocking LCP checkpoint + requirements close-out</name>
  <files>.planning/phases/07-real-sky-foundation/lcp-checkpoint.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
Run the full carried-floor battery and record results. Gates (all must pass):
- astro check 0 errors; npm run build green.
- Zero hex outside tokens.css: NightSky.astro, SiteFooter.astro, deck.css all report 0 hex color literals.
- Single-rAF invariant unchanged: scene.ts=2, starfield.ts=0, meteors.ts=0, fig01/render.ts=2.
- No deck/fig01 import in any nightsky scene module (scene.ts/starfield.ts/constellations.ts/meteors.ts).
- Contrast: node scripts/verify-contrast.mjs --cdp at 1440x900 AND 1280x800 over the built preview — worst-case over-sky text >= 4.5:1 vs --ink at both.
- Moon: node scripts/verify-contrast.mjs --moon at 1440x900 AND 1280x800 — moonPeak < mwPeak (photo core) at both.
- Banding: node scripts/verify-banding.mjs --selftest passes AND the committed masters re-check clean.

Screenshots (via the existing CDP/browse pattern): capture DPR1 and DPR2, including the band-over-margin composition (the Milky-Way core sitting in the right margin, not the content column), the reduced-motion static frame, and classic mode. Save under the phase dir as evidence.

**LCP CHECKPOINT (blocking gate, 07-CONTEXT.md D-LCP):** run Lighthouse at BOTH the mobile and desktop presets against `astro preview` (the Phase 4/5 npx-lighthouse convention per 07-RESEARCH.md Architectural Responsibility Map; if npx lighthouse cannot resolve in this environment, fall back to a CDP LargestContentfulPaint PerformanceObserver read via the soak-harness pattern + a manual Lighthouse pass). Record every category score and the LCP number in lcp-checkpoint.md, comparing LCP against the 1.5-2.8s research budget. **Gate: if ANY category scores < 90 on either preset, OR the LCP number blows the budget, STOP — do not mark the phase complete, do not advance to Phase 8; report the failing category/LCP number to the orchestrator with the recorded evidence.** Also confirm Lighthouse's "Preload Largest Contentful Paint image" audit passes with no duplicate/wasted flag (single fetch per master — 07-RESEARCH.md pitfall 2) and CLS reads 0 / matches the pre-photo baseline.

Only after ALL gates AND the LCP checkpoint pass: mark IMG-01, IMG-02, IMG-03, IMG-04, IMG-05 Complete in REQUIREMENTS.md (checkboxes + the status table rows), tick the ROADMAP Phase 7 requirement/criteria boxes, and update STATE.md's position.
  </action>
  <verify>
    <automated>npx astro check && npm run build && test "$(grep -o 'requestAnimationFrame' src/lib/nightsky/scene.ts | wc -l | tr -d ' ')" = "2" && test "$(grep -o 'requestAnimationFrame' src/lib/nightsky/starfield.ts | wc -l | tr -d ' ')" = "0" && test "$(grep -oE '#[0-9a-fA-F]{3,8}' src/components/NightSky.astro src/components/SiteFooter.astro src/styles/deck.css | wc -l | tr -d ' ')" = "0" && node scripts/verify-contrast.mjs --selftest && node scripts/verify-banding.mjs --selftest && test -f .planning/phases/07-real-sky-foundation/lcp-checkpoint.md</automated>
  </verify>
  <done>Every battery gate is green; --cdp both viewports >= 4.5:1; --moon both viewports moonPeak < mwPeak; screenshots (DPR1/DPR2, band-over-margin, reduced-motion, classic) captured; lcp-checkpoint.md records Lighthouse mobile+desktop categories all >= 90 and LCP within 1.5-2.8s; IMG-01..05 marked Complete; ROADMAP/STATE updated. If the LCP gate fails, the phase is STOPPED and reported instead.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual sign-off — seam invisibility, composition, overlay harmony</name>
  <action>Human checkpoint: pause for the craft-critical visual confirmation described below (seam invisibility, band-over-margin composition, overlay harmony, both modes). Automated gates (contrast/moon/banding/LCP) have already passed. Wait for the resume-signal before locking the phase.</action>
  <what-built>The fully integrated real-sky home page: composited Milky-Way photo behind the deck, the authored overlay (moon, constellations with resting halo + panel brighten, meteors, twinkle) on top, the bottom seam blended into --bg, and the CC BY 4.0 credit line in the footer. Automated gates (contrast, moon dimness, banding, LCP) have already passed; this confirms the craft-critical visual judgments Claude cannot make.</what-built>
  <how-to-verify>
    1. Open the captured DPR1 and DPR2 screenshots. Confirm the Milky-Way band core sits in the RIGHT margin and the diagonal reads like "the same campsite on a clearer night" (07-UI-SPEC Crop + Placement) — an upgrade, not a redesign.
    2. Confirm the bottom seam between photo sky and drawn ground is INVISIBLE at every viewport (craft-critical — 07-CONTEXT.md Specific Ideas); no visible band where the photo fades to --bg.
    3. Confirm the drawn constellations/moon/meteors still read as the brightest, authored things over the busier real starfield (overlay harmony); the moon reads dimmer than the photo core.
    4. Confirm the reduced-motion static frame and classic (no-deck) mode both render the photo + camper + credit line correctly.
    5. Confirm the footer credit line renders on two lines with working NOIRLab / CC BY 4.0 links, no footer overlap in deck mode.
  </how-to-verify>
  <resume-signal>Type "approved" to lock the phase, or describe the seam/composition/harmony issue to fix (re-grade the master via build-sky.mjs or adjust object-position) and re-verify.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Outbound credit links -> external sites | target=_blank links to noirlab.edu + creativecommons.org |
| Local preview -> headless Chrome | LCP/contrast/screenshot verification only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-06 | Tampering | Reverse-tabnabbing via target=_blank credit links | low | mitigate | rel="noopener noreferrer" on both outbound links (07-UI-SPEC Credit Line) |
| T-07-07 | Denial of Service | LCP regression from the full-viewport photo threatens the >=90 mobile floor | medium | mitigate | preload + fetchpriority + LQIP (07-03) proven by the blocking LCP checkpoint here; STOP-and-report on failure before Phase 8 compounds |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | NO installs — credit line is native markup + tokens; Lighthouse runs via npx per Phase 4/5 precedent, no package.json change (git diff --exit-code package.json package-lock.json) |
</threat_model>

<verification>
- astro check 0; build green; zero-hex 0 across NightSky.astro/SiteFooter.astro/deck.css.
- Single-rAF: scene=2, starfield=0, meteors=0, fig01/render=2; no deck/fig01 imports in scene modules.
- Credit line exact copy + both links present, in home HTML (both modes), all pages.
- Contrast --cdp both viewports >= 4.5:1; --moon both viewports moonPeak < mwPeak; --banding selftest passes.
- LCP checkpoint: Lighthouse mobile+desktop all categories >= 90, LCP within 1.5-2.8s, preload-LCP audit clean, CLS 0/baseline — recorded in lcp-checkpoint.md. Failure = STOP.
- Human visual checkpoint approved.
- IMG-01..05 Complete; ROADMAP/STATE updated; no push; no new dependency.
</verification>

<success_criteria>
IMG-04 credit line shipped in both modes; every carried floor green; the blocking LCP checkpoint passed (both presets >= 90, LCP in budget); the phase's real-sky foundation is proven end-to-end and locked, with IMG-01..05 marked Complete.
</success_criteria>

<output>
Create `.planning/phases/07-real-sky-foundation/07-04-SUMMARY.md` when done.
</output>
