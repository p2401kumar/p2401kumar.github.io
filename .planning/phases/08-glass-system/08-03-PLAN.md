---
phase: 08-glass-system
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - .planning/phases/08-glass-system/08-03-glass-reproof.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [GLS-04]
user_setup: []
must_haves:
  truths:
    - "Glass + scene idle CPU stays under the 10% total floor on the REAL preview page (60s CDP idle soak), 60fps, 0 long tasks, recorded against Spike-2's +0.47pp projection"
    - "Lighthouse >= 90 across all four categories on both mobile and desktop presets with TBT ~0 (glass adds compositing, no JS)"
    - "The full carry-forward regression battery re-passes: astro check/build, zero-hex incl. the 13 new tokens, single-rAF counts unchanged, no deck/fig01 imports in scene modules, banding + screenshot-contrast + moon, sitemap"
    - "Leak gate holds: /work/* and /404 carry no matched glass rules (body has no has-sky class); index does"
    - "GLS-01..04 marked Complete; ROADMAP Phase 8 ticked [x]; STATE advanced"
  artifacts:
    - .planning/phases/08-glass-system/08-03-glass-reproof.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
  key_links:
    - "prefers-reduced-transparency emulation toggles glass off/on ON THE SAME real page -> the honest marginal re-blur delta vs Spike-2's projection"
    - "body.has-sky presence on index vs absence on /work + /404 is the leak-gate assertion"
---

<objective>
Re-prove the glass CPU budget on the real page (GLS-04), re-run the full carry-forward regression battery over the composited build, and close Phase 8. Spike 2 projected +0.47pp marginal / 4.52% total for 4 glass surfaces over the animating scene; this plan confirms that projection holds on the actual preview page now that glass is live, then locks the requirements and roadmap.

Purpose: the glass CPU cost was measured in a spike harness, not the production page — GLS-04 requires the real-page re-proof before the phase is trusted. And every carried floor (zero-hex, single-rAF, leak gate, contrast, banding, moon) must still hold with glass composited on top.

Output: a re-proof/battery evidence file + requirement/roadmap/state closeout.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-glass-system/08-CONTEXT.md
@.planning/phases/08-glass-system/08-UI-SPEC.md
@.planning/phases/08-glass-system/08-02-glass-gate.md

# Spike-2 measured budget + soak methodology to reproduce (05-06/07-02).
@.planning/phases/07-real-sky-foundation/07-SPIKE-GLASS.md
@.planning/phases/07-real-sky-foundation/spike-glass/soak.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: GLS-04 real-page idle-CPU re-proof + Lighthouse both presets</name>
  <files>.planning/phases/08-glass-system/08-03-glass-reproof.md</files>
  <action>
Run a 60s CDP idle soak on the REAL preview page (not the spike harness) using the 05-06/07-02 methodology: launch headless Chrome at 1440x900 DPR1 via Emulation.setDeviceMetricsOverride (reuse the launchChrome/Cdp/connectCdp pattern already proven in soak.mjs and verify-contrast.mjs), navigate the built preview, apply the 15s unmeasured warm-up, then measure Performance.getMetrics TaskDuration delta -> main-thread CPU% over 60s. Get the marginal HONESTLY on the same page by toggling glass with CDP Emulation.setEmulatedMedia: a BASELINE run under prefers-reduced-transparency:reduce (glass collapses to solid, zero backdrop-filter) and a GLASS run under no-preference (glass live). Marginal = glass CPU% - baseline CPU%. Also record realized fps (target 60.0), long-task count (target 0), and LayoutDuration delta (target 0.000s — blur is a compositor op, not layout). The executor may add a small `--url`/real-page mode to soak.mjs or run an inline CDP soak (Claude's discretion — it is dev-time measurement tooling, not shipped code; no new dependency, package.json untouched).

Gate: total (glass run) < 10%, 60fps held, 0 long tasks. Expect ~baseline+0.5pp marginal, consistent with Spike-2's +0.47pp projection; record the delta vs that projection explicitly. If the real-page total somehow breaches 10% or fps drops, apply the measured mitigation ladder in Spike-2's stated order (the GLASS.md 1.4 levers: overlap-area reduction -> 15-20fps canvas throttle under glass -> spawn-density bias -> blur cap toward 12px), re-measure, and record which lever was needed — none is expected.

Then re-run Lighthouse on the final build at BOTH presets (mobile default + `--preset=desktop`) against the preview, asserting all four categories >= 90 with TBT ~0 ms (glass adds compositing but no JS, so scores should hold at the 07-04 baseline of mobile 99 / desktop 100x4). Record every number (soak table + Lighthouse table) into 08-03-glass-reproof.md alongside Spike-2's projection for comparison.
  </action>
  <verify>
    <automated>test -f .planning/phases/08-glass-system/08-03-glass-reproof.md && test "$(grep -oiE 'marginal|60\.0|long task|Lighthouse' .planning/phases/08-glass-system/08-03-glass-reproof.md | wc -l | tr -d ' ')" != "0" && test "$(grep -oiE '10%|< 10|total' .planning/phases/08-glass-system/08-03-glass-reproof.md | wc -l | tr -d ' ')" != "0" && test "$(grep -o '\"sharp\"' package.json | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <done>08-03-glass-reproof.md records the real-page 60s idle soak (baseline vs glass via reduced-transparency emulation): marginal ~+0.5pp, total < 10%, 60.0 fps, 0 long tasks, LayoutDuration delta 0.000s — compared to Spike-2's +0.47pp/4.52% projection; Lighthouse >= 90 x4 on both presets with TBT ~0; no dependency added.</done>
</task>

<task type="auto">
  <name>Task 2: Full carry-forward regression battery + leak gate</name>
  <files>.planning/phases/08-glass-system/08-03-glass-reproof.md</files>
  <action>
Re-run the full carried-floor battery over the composited build and append the results table to 08-03-glass-reproof.md. Every gate ASSERTS (test "$(grep -o ... | wc -l | tr -d ' ')" = N against source, never grep -c against minified output):

- astro check 0 errors/warnings/hints; npm run build green (4 pages).
- Zero-hex outside tokens.css across the modified source (deck.css / SiteHeader / SiteFooter / BaseLayout / index.astro via the standard #[0-9a-fA-F]{3,8} count == 0; DeckIndex.astro via the identifier-boundary PCRE regex == 0 to skip its #deck-* ids). PLUS assert exactly 13 --glass-* token declarations exist in tokens.css and their values are rgb()/px/%/decimal (no new hex added inside tokens.css).
- Single-rAF invariant unchanged (glass is CSS-only, adds zero JS): scene.ts requestAnimationFrame == 2, starfield.ts == 0, meteors == 0, fig01 render == 2.
- Scene modules import zero deck/fig01 (grep the nightsky lib for deck/fig01 imports == 0, doc-comment mentions excluded).
- verify-contrast.mjs --selftest passes (scrim 0.38 sync check green — scrim untouched this phase); verify-banding.mjs --selftest + the committed masters pass (glass never touched the photo/masters); --cdp-screenshot >= 4.5 at both widths (the screenshot-sampled contrast gate is now the contrast floor); --moon dimness passes at both widths (glass is a DOM layer above the canvas; --moon reads canvas pixels directly, so 0.21/0.19 < mwPeak must be unchanged).
- LEAK GATE: build, then assert the has-sky scoping is airtight — dist/index.html body carries has-sky, while dist/work/*/index.html and dist/404.html bodies do NOT (so no chrome-glass rule can match there), and no panel-glass reaches /work (deck.css is imported only by PanelDeck, index-only). Also confirm /work + /404 keep zero scene/sky JS refs (carried from 07-04's leak gate). Case studies stay editorial.
- Sitemap exactly 3 routes.
- package.json / package-lock / .planning/config.json untouched; zero installs.

Any failure is fixed in the offending source (not by weakening the gate) and the battery re-run green before proceeding.
  </action>
  <verify>
    <automated>npx astro check && npm run build && test "$(grep -o 'requestAnimationFrame' src/lib/nightsky/scene.ts | wc -l | tr -d ' ')" = "2" && test "$(grep -o 'requestAnimationFrame' src/lib/nightsky/starfield.ts | wc -l | tr -d ' ')" = "0" && test "$(grep -oE '\-\-glass-[a-z0-9-]+:' src/styles/tokens.css | wc -l | tr -d ' ')" = "13" && test "$(grep -oiE '#[0-9a-fA-F]{3,8}' src/styles/deck.css src/components/SiteHeader.astro src/components/SiteFooter.astro src/layouts/BaseLayout.astro src/pages/index.astro | wc -l | tr -d ' ')" = "0" && test "$(grep -oP '#[0-9a-fA-F]{3,8}(?![0-9A-Za-z_-])' src/components/DeckIndex.astro | wc -l | tr -d ' ')" = "0" && test "$(grep -o 'has-sky' dist/index.html | wc -l | tr -d ' ')" != "0" && test "$(cat dist/work/*/index.html dist/404.html | grep -o 'has-sky' | wc -l | tr -d ' ')" = "0" && node scripts/verify-contrast.mjs --selftest && node scripts/verify-banding.mjs --selftest && (npm run preview >/dev/null 2>&1 & PREV=$!; sleep 5; node scripts/verify-contrast.mjs --cdp-screenshot --url http://localhost:4321/ --width 1280 --height 800 >/dev/null 2>&1; C=$?; node scripts/verify-contrast.mjs --moon --url http://localhost:4321/ --width 1280 --height 800 >/dev/null 2>&1; M=$?; kill $PREV 2>/dev/null; test $C = 0 && test $M = 0)</automated>
  </verify>
  <done>Full battery green over the composited build: astro check 0 / build green; single-rAF unchanged (2/0/0/2); 13 glass tokens, zero hex outside tokens.css (DeckIndex #deck-* excluded); selftest + banding + --cdp-screenshot (>=4.5 both widths) + --moon all pass; leak gate airtight (index has has-sky, /work + /404 do not); sitemap 3 routes; package.json/lock/config.json untouched. Results appended to 08-03-glass-reproof.md.</done>
</task>

<task type="auto">
  <name>Task 3: Close out — requirements, roadmap, state, summaries</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
Mark GLS-01, GLS-02, GLS-03, GLS-04 Complete in REQUIREMENTS.md (both the checkbox list and the traceability table Status column: Pending -> Complete). Update ROADMAP.md: tick Phase 8's plan list, set Plans to 3/3, flip the Phase 8 progress-table Status to Complete with today's date, and mark the "Phase 8: Glass System" bullet in the v3.0 section [x]. Use scoped Edit replacements only — never rewrite the whole ROADMAP file.

Update STATE.md: advance current_phase to 8 complete / ready to plan Phase 9 (Living Sky), set stopped_at accordingly, append the Phase 8 decisions to Accumulated Context (the locked --glass-* token values and final tier assignments after any gate escalation, the active-panel-only scoping that held the 4-surface budget, the real-page GLS-04 marginal vs Spike-2's +0.47pp, the KEEP-scrim-0.38 decision, and that the screenshot-sampled --cdp-screenshot mode is now the contrast gate). Record the Phase 8 plan durations in the velocity table.

Note for Phase 9 in STATE: ambient animation must fit in the idle-CPU budget that glass leaves (the measured real-page total from Task 1), and the screenshot-sampled contrast floor is now the arbiter any ambient light source must not breach. Ensure each 08-0N-SUMMARY.md exists (self-check per the execute-plan workflow). This is a docs-only task — no source or config changes; .planning/config.json stays untouched.
  </action>
  <verify>
    <automated>test "$(grep -oE 'GLS-0[1-4]' .planning/REQUIREMENTS.md | wc -l | tr -d ' ')" != "0" && test "$(grep -c 'Complete' .planning/REQUIREMENTS.md)" != "0" && test "$(grep -o '8. Glass System' .planning/ROADMAP.md | wc -l | tr -d ' ')" != "0" && test -f .planning/phases/08-glass-system/08-01-SUMMARY.md && test -f .planning/phases/08-glass-system/08-02-SUMMARY.md && git diff --quiet .planning/config.json</automated>
  </verify>
  <done>GLS-01..04 Complete in REQUIREMENTS (checkboxes + traceability rows); ROADMAP Phase 8 ticked [x] with 3/3 plans and a Complete status row; STATE advanced to Phase 8 complete with the Phase 8 decisions logged and a Phase 9 budget note; all three 08-0N-SUMMARY.md files exist; .planning/config.json untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser compositor -> idle CPU budget | glass blur cost lives in the compositor; the soak measures it dev-time via CDP |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-03 | Denial of Service | glass compositing CPU over the permanently-animating scene | medium | mitigate | GLS-04 60s real-page idle soak re-proves < 10% total, 60fps, 0 long tasks vs Spike-2's +0.47pp; blur 12px default / 16px hard ceiling; exactly 4 simultaneous surfaces via active-panel scoping; measured mitigation ladder available but not expected |
| T-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero new dependencies; package.json / package-lock / .planning/config.json untouched and asserted in verify; no install task |
</threat_model>

<verification>
- Real-page 60s idle soak: total < 10%, 60.0 fps, 0 long tasks, marginal ~Spike-2's projection.
- Lighthouse >= 90 x4 on both presets, TBT ~0.
- Battery green: astro check/build, 13 tokens + zero-hex, single-rAF 2/0/0/2, no scene->deck/fig01 imports, selftest + banding + --cdp-screenshot + --moon, sitemap 3, leak gate airtight.
- GLS-01..04 Complete; ROADMAP Phase 8 [x] 3/3; STATE advanced; config.json untouched.
</verification>

<success_criteria>
Glass holds the <10% idle CPU floor on the real page with 60fps and zero long tasks, Lighthouse stays >=90 x4, every carried floor re-passes over the composited build with an airtight leak gate, and GLS-01..04 are closed with the roadmap and state advanced to Phase 9.
</success_criteria>

<output>
Create `.planning/phases/08-glass-system/08-03-SUMMARY.md` when done.
</output>
