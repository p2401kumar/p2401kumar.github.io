---
phase: 09-living-sky
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/nightsky/idle-queue.ts
  - src/lib/nightsky/clouds.ts
  - src/lib/nightsky/starfield.ts
  - src/lib/nightsky/scene.ts
  - src/lib/nightsky/parallax.ts
  - src/components/NightSky.astro
autonomous: true
requirements: [AMB-01, AMB-02, AMB-04]

must_haves:
  truths:
    - "Two cloud layers (far ~3px/s, near ~6px/s) drift slowly across the lower sky and wrap seamlessly with no seam or repeat-jump."
    - "Cloud alpha is attenuated x0.15 under the content column (80px smoothstep at each edge), so over-text contrast is unaffected."
    - "The ~40-star twinkle subset reads as 2-oscillator scintillation, with a bounded chromatic nudge on only the 3 brightest."
    - "On panel change the camper nudges 18px and clouds nudge ~9px, both settle back to rest; the canvas is NEVER CSS-transformed; reduced-motion is an instant no-motion jump."
  artifacts:
    - src/lib/nightsky/idle-queue.ts
    - src/lib/nightsky/clouds.ts
    - src/lib/nightsky/parallax.ts
  key_links:
    - "scene.ts drawFrame calls cloudsHandle.advance/draw as Layer 1.5 (after the Layer-0 blit, before the twinkle loop)."
    - "clouds.ts and parallax.ts each subscribe to the LITERAL 'nightsky:panel-change' event; neither imports deck.ts."
    - "starfield.ts imports requestIdle/drainQueue from idle-queue.ts (behavior-preserving extraction)."
---

<objective>
Build the three tick-native ambient systems that ride scene.ts's single rAF: two drifting cloud layers (AMB-01), the 2-oscillator scintillation upgrade with a chromatic nudge (AMB-04), and panel-change parallax on the camper + clouds (AMB-02). The `--aurora` token and aurora module are NOT in this plan (09-02).

Purpose: Make the sky drift and shimmer and gain physical depth on panel change, entirely inside the existing single-rAF + pause machine, with clouds attenuated to contrast-safe alphas under the text column by construction.

Output: idle-queue.ts (shared, extracted from starfield.ts), clouds.ts, parallax.ts, the scintillation waveform upgrade inside scene.ts, and the .camper parallax CSS in NightSky.astro.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-living-sky/09-CONTEXT.md
@.planning/phases/09-living-sky/09-RESEARCH.md
@.planning/phases/09-living-sky/09-UI-SPEC.md

# Live code this plan extends or mirrors
@src/lib/nightsky/scene.ts
@src/lib/nightsky/starfield.ts
@src/lib/nightsky/constellations.ts
@src/lib/nightsky/meteors.ts
@src/lib/nightsky/tokens.ts
@src/components/NightSky.astro
@src/styles/deck.css
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract idle-queue.ts, build clouds.ts (2 layers + wraparound blit + column governor + parallax offset), wire into scene.ts</name>
  <files>src/lib/nightsky/idle-queue.ts, src/lib/nightsky/clouds.ts, src/lib/nightsky/starfield.ts, src/lib/nightsky/scene.ts</files>
  <behavior>
    - Extraction preserves behavior: starfield.ts generates identical Layer-0 output after importing the idle scheduler instead of owning it inline.
    - Clouds are visible in the lower-sky margins and drift; under the content column their contribution is attenuated to a near-negligible alpha.
    - clouds.draw performs a bounded per-frame blit set (target ~4 sprite drawImage calls + one masked-buffer blit); no per-frame gradient/path recomputation of the sprites themselves.
    - Draw order: Layer-0 blit -> clouds -> twinkle (clouds sit behind the crisp point features).
    - The static (reduced-motion / paused) path renders both cloud layers ONCE at offset 0 and peak margin alpha.
  </behavior>
  <action>
Extract the idle scheduler, then build clouds as a handle-shaped module and wire it into the single tick.

1. Create `src/lib/nightsky/idle-queue.ts`: move `requestIdle` and `drainQueue` (and the `IdleDeadlineLike`/`WorkUnit` types) out of `starfield.ts` VERBATIM and `export` them. Keep the exact feature-detect-requestIdleCallback-with-a-timeout-shim behavior — this is the Safari-first-class fallback path, do not change its semantics. In `starfield.ts`, delete the two local functions and their local types and add `import { requestIdle, drainQueue, type WorkUnit } from './idle-queue';` — every other line of starfield.ts stays byte-for-byte identical (this is a pure move; Layer-0 output must not change).

2. Create `src/lib/nightsky/clouds.ts` exporting `interface CloudsHandle { advance(ts: number, dt: number): void; draw(ctx: CanvasRenderingContext2D, cssWidth: number, cssHeight: number): void; drawStatic(ctx: CanvasRenderingContext2D, cssWidth: number, cssHeight: number): void; resize(cssWidth: number, cssHeight: number): void; setFarShed(shed: boolean): void; teardown(): void; }` and `initClouds(options: { tokens: SkyTokens; getViewport: () => { width: number; height: number } | null }): CloudsHandle`. Mirror the constellations.ts / meteors.ts handle idiom. This module owns NO rAF and NO timers — all cadence is driven from `advance()`.

   Sprite generation (mirror starfield.ts's chunked generation via the shared idle queue; regenerate ONLY on `resize(w,h)`, never per tick): generate TWO detached offscreen sprite canvases via `drainQueue`, each `spriteWidth = cssWidth` wide (author the rightmost ~120px of blob content repeated at the left edge so the tile wraps losslessly). Fill color is `tokens.milkyway` composited with the DEFAULT source-over compositing at the alphas below (never the additive blend mode that caused the 05-06 near-white failure — globalCompositeOperation stays at its source-over default at every blob/fill call site). `tokens.skyHorizon` is an allowed cooler OUTER falloff stop only. Every radial-gradient blob uses at least 4 stops (never a bare 0%/100% pair) to avoid 8-bit banding on the large slow-alpha gradients.
     - Far ("wisps"): band center 0.72H, spread 0.64H-0.80H. Elongated horizontal streaks (aspect 6:1-10:1), 4-6 blobs across one tile width, gradient falloff over 65-70% of blob radius, PEAK margin alpha 0.08 (tuning range 0.05-0.10).
     - Near ("banks"): band center 0.78H, spread 0.71H-0.85H (base grazing the 0.85H horizon). Cluster groups of 3-4 rounder overlapping blobs (aspect 2:1-3:1), 3-5 clusters across one tile width, falloff over 55-65% of radius, PEAK margin alpha 0.13 (tuning range 0.10-0.16, HARD ceiling 0.18).
   Keep the last-good sprites on screen while a regeneration is in flight (swap only when the new pair drains) so a resize never shows a blank cloud band.

   Continuous drift: `farOffset += 3 * dt/1000`, `nearOffset += 6 * dt/1000` (fixed CSS-px/s, matching the firefly px/s convention). Wraparound blit per layer: draw the sprite twice at integer-rounded `x = round(((offset % spriteWidth)+spriteWidth)%spriteWidth) - spriteWidth` and `x + spriteWidth` (integer rounding avoids the sub-pixel bilinear-resample path).

   Column governor (contrast-by-construction, applied LIVE at blit time, NOT baked — clouds drift but the column is fixed): draw both layers into ONE reusable offscreen buffer canvas sized to the union band (top 0.64H, height (0.85-0.64)*cssHeight), each layer at its own globalAlpha; then apply the viewport-fixed column mask with `ctx.globalCompositeOperation = 'destination-in'` filling a HORIZONTAL alpha stencil gradient across the buffer width — stencil alpha 1.0 across the margins, 0.15 across the content column, with an 80px smoothstep ramp at each column boundary (mirror the `contentColumnEdges(cssWidth)` pad/half formula already duplicated in scene.ts/starfield.ts: pad clamp(18, 4vw, 32), half min(880, width-2*pad)/2). Use `rgba(tokens.milkyway, a)` for the stencil stops (only the alpha channel is load-bearing under destination-in; this keeps it token-sourced and zero-hex). Reset compositing to its source-over default, then blit the masked buffer to the visible canvas at (0, 0.64H). Result: far peak 0.08 -> ~0.012 under text, near peak 0.13 -> ~0.0195 under text.

   Parallax cloud-offset hook (AMB-02 mid layer): register clouds.ts's OWN independent `document.addEventListener('nightsky:panel-change', onPanelChange)` (mirror constellations.ts's independent listener — never import deck.ts; deck.ts documents the detail shape { index, id, total }). Null-check `detail`; derive `direction = Math.sign(detail.index - lastPanelIndex) || 1`; store `lastPanelIndex`. Record a nudge: peak `9 * -direction` px (advance/index-increasing nudges LEFT, per the locked direction metaphor), starting 60ms after the event, easing back to 0 over 480ms with `cubic-bezier(0.16,1,0.3,1)` ease-out. `advance()` adds the current eased nudge value ON TOP of each layer's continuous drift offset (do not mutate the persistent drift). Under `matchMedia('(prefers-reduced-motion: reduce)').matches`, apply the settle instantly (nudge target is 0, so instant == no visible motion) and never animate.

   `drawStatic(ctx,w,h)`: render both layers at offset 0, peak margin alpha, column-governed, with the nudge contribution forced to 0 (the reduced-motion "beautiful still").
   `setFarShed(shed)`: when true, `draw()`/`drawStatic()` skip the far layer (the mobile-ladder tier-1 seam; wired by 09-03 — default false here).
   `resize(w,h)`: regenerate both sprites + recompute the buffer/column geometry for the new size.
   `teardown()`: remove the panel-change listener and any motion listener, drop the sprites/buffer.

3. Wire into `src/lib/nightsky/scene.ts`: `import { initClouds, type CloudsHandle } from './clouds';` add module-scope `let cloudsHandle: CloudsHandle | null = null;`. In `initNightSky`, after tokens are read, `cloudsHandle = initClouds({ tokens: skyTokens, getViewport: () => (layer0 ? { width: layer0.cssWidth, height: layer0.cssHeight } : null) });`. In `adoptLayer0`, call `cloudsHandle?.resize(result.cssWidth, result.cssHeight);` BEFORE `renderStaticFrame()`. In `drawFrame`, insert Layer 1.5 immediately AFTER the `visibleCtx.drawImage(layer0.canvas, ...)` blit and BEFORE the twinkle loop: `if (cloudsHandle) { cloudsHandle.advance(ts, dt); cloudsHandle.draw(visibleCtx, w, h); }`. In `renderStaticFrame`, insert `cloudsHandle?.drawStatic(visibleCtx, layer0.cssWidth, layer0.cssHeight);` immediately after the Layer-0 blit and before the constellations draw. In `teardown`, `cloudsHandle?.teardown(); cloudsHandle = null;`. Update the now-stale renderStaticFrame header comment (which still claims "Layer 0 bakes EVERY star") to note clouds are drawn once here; the twinkle subset stays ABSENT under reduced motion (unchanged).
  </action>
  <verify>
    <automated>npm run build && npx astro check && test "$(grep -o "lighter" src/lib/nightsky/clouds.ts | wc -l)" -eq 0 && test "$(grep -o "requestAnimationFrame" src/lib/nightsky/clouds.ts | wc -l)" -eq 0 && test "$(grep -oE "setTimeout|setInterval" src/lib/nightsky/clouds.ts | wc -l)" -eq 0 && test "$(grep -oE "#[0-9a-fA-F]{3,8}" src/lib/nightsky/clouds.ts src/lib/nightsky/idle-queue.ts | wc -l)" -eq 0 && test "$(grep -o "requestAnimationFrame" src/lib/nightsky/scene.ts | wc -l)" -eq 2 && test "$(grep -oE "requestIdle|drainQueue" src/lib/nightsky/starfield.ts | wc -l)" -ge 1</automated>
  </verify>
  <done>Build green and astro check 0 errors. clouds.ts contains zero additive-composite/rAF/timer/hex occurrences; scene.ts still owns exactly 2 rAF calls; starfield.ts imports the extracted scheduler. Clouds drift, wrap seamlessly, and are governed to ~0.012/~0.0195 under the column.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Scintillation upgrade — 2-oscillator waveform + chromatic nudge on the 3 brightest (scene.ts)</name>
  <files>src/lib/nightsky/scene.ts, src/lib/nightsky/starfield.ts</files>
  <behavior>
    - The twinkle subset keeps its ~40-star count and margin containment; no new stars.
    - Each star's alpha is the sum of the retuned slow oscillator and a new fast oscillator; the combined envelope stays within roughly today's total range (more textured, not brighter or busier).
    - Exactly the 3 largest-radius pool entries carry a bounded saturation nudge, pulsing in phase with their slow oscillator; the nudge never becomes a saturated color flash.
    - Reduced motion is unaffected here: the drawn twinkle subset remains ABSENT in the static frame (photo stars provide the base).
  </behavior>
  <action>
Upgrade the existing Layer-2a twinkle waveform in place (do NOT widen the pool, do NOT change margin containment).

1. Retune the primary oscillator amplitude constants: `TWINKLE_AMPLITUDE_MIN` 0.15 -> 0.12, `TWINKLE_AMPLITUDE_MAX` 0.25 -> 0.18 (trimmed to make room for the secondary term). Add secondary-oscillator constants: `TWINKLE_SEC_PERIOD_MIN_MS = 400`, `TWINKLE_SEC_PERIOD_MAX_MS = 700`, `TWINKLE_SEC_AMPLITUDE_MIN = 0.04`, `TWINKLE_SEC_AMPLITUDE_MAX = 0.07`, and `CHROMATIC_S_DELTA = 0.08`.

2. Extend the `TwinkleStar` interface with `secPeriodMs`, `secPhase`, `secAmplitude`, and OPTIONAL chromatic fields `chromatic?: { baseH: number; baseS: number; baseL: number }`. In `seedTwinkles`, assign the secondary term for every star (uniform-random period/amplitude within the new ranges, random phase). After building the subset, select the 3 largest-`radius` entries ONCE (deterministic per generation — never reselected per frame) and populate their `chromatic` field: derive base HSL from the star's own color via a local `rgbToHsl` helper; if the star is effectively neutral (S near 0) assign a hue from the existing cool/warm pair (210 or 35, matching starfield's jitterStarColor — alternate the 3 across cool/warm). Export `hslToRgb` from `starfield.ts` (it already exists there) so scene.ts reuses it verbatim rather than re-deriving; add the small `rgbToHsl` helper locally in scene.ts.

3. In `drawFrame` Layer 2a, compute `alpha = clamp01(s.baseAlpha + s.amplitude*Math.sin(ts/s.periodMs + s.phase) + s.secAmplitude*Math.sin(ts/s.secPeriodMs + s.secPhase))`. For the 3 chromatic stars, when `chromaticNudgeEnabled` (a new module-scope `let chromaticNudgeEnabled = true;` seam that 09-03's ladder will gate), compute `S = clamp(s.chromatic.baseS + CHROMATIC_S_DELTA*Math.sin(ts/s.periodMs + s.phase), 0, 0.3)` (SAME period/phase as the primary oscillator so color and brightness pulse together) and set the fill color to `hslToRgb(s.chromatic.baseH, S, s.chromatic.baseL)`; otherwise use the star's stored color unchanged. Keep it source-over; no new draw calls, no new arcs.
  </action>
  <verify>
    <automated>npm run build && npx astro check && test "$(grep -o "TWINKLE_SEC_AMPLITUDE_MIN" src/lib/nightsky/scene.ts | wc -l)" -ge 1 && test "$(grep -o "CHROMATIC_S_DELTA" src/lib/nightsky/scene.ts | wc -l)" -ge 1 && test "$(grep -o "chromaticNudgeEnabled" src/lib/nightsky/scene.ts | wc -l)" -ge 1 && test "$(grep -o "export function hslToRgb" src/lib/nightsky/starfield.ts | wc -l)" -eq 1 && test "$(grep -o "TWINKLE_SUBSET_FRACTION" src/lib/nightsky/scene.ts | wc -l)" -ge 1</automated>
  </verify>
  <done>Build green, astro check clean. Secondary oscillator + chromatic constants present, the chromatic-enable seam exists, hslToRgb is exported from starfield, and the subset fraction (count) is unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Parallax on the camper — parallax.ts + .camper CSS keyframes in NightSky.astro</name>
  <files>src/lib/nightsky/parallax.ts, src/components/NightSky.astro</files>
  <behavior>
    - On advance (index up) the camper translates left ~18px then settles to rest; on retreat it mirrors right; the settled state is always translate3d(0,0,0).
    - The animation targets ONLY .camper; the canvas host is never transformed.
    - Under reduced motion no nudge class is applied and the CSS @media gate forces animation:none — an instant, zero-motion result.
    - Rapid repeated panel changes restart the nudge cleanly (reflow-restart trick).
  </behavior>
  <action>
Add the DOM/CSS "ground" parallax layer. This is compositor-only, event-driven, and NEVER sheds (locked).

1. Create `src/lib/nightsky/parallax.ts` exporting `initParallax(): () => void`. Module-scope `let lastPanelIndex = 0;`. Register an independent `document.addEventListener('nightsky:panel-change', onPanelChange)` (mirror constellations.ts's independent listener — never import deck.ts). `onPanelChange`: null-check `(e as CustomEvent<{ index: number; id: string; total: number }>).detail`; `direction = Math.sign(detail.index - lastPanelIndex) || 1`; update `lastPanelIndex`. If `matchMedia('(prefers-reduced-motion: reduce)').matches`, RETURN before touching classList (avoid a wasted compositor frame; the @media gate already neutralizes it). Otherwise select `document.querySelector('.camper')`, null-check, remove both `parallax-nudge-fwd`/`parallax-nudge-back` classes, read `void camper.offsetWidth` to force reflow (restart on rapid changes), then add `direction >= 0 ? 'parallax-nudge-fwd' : 'parallax-nudge-back'`. Return a teardown that removes the listener. This module owns zero timers and zero rAF.

2. In `src/components/NightSky.astro`'s scoped `<style>` block (co-located with the existing `.camper` rule so Astro scoping + keyframe renaming stay consistent — do NOT put this in deck.css, where `.camper` is not defined and the runtime-added class would not match): add `@keyframes camper-parallax-nudge-fwd` (0% translate3d(0,0,0); 35% translate3d(-18px,0,0); 100% translate3d(0,0,0)) and `@keyframes camper-parallax-nudge-back` (35% keyframe at +18px). Add `.camper.parallax-nudge-fwd { animation: camper-parallax-nudge-fwd 420ms cubic-bezier(0.16, 1, 0.3, 1) 1; }` and the `-back` sibling — 18px peak (locked; ceiling 24px), 420ms synced with the deck's own panel transition. Add a `@media (prefers-reduced-motion: reduce)` block setting `animation: none` on both nudge classes. Do NOT add any transform to `#nightsky-canvas` or `.nightsky-host` (locked canvas-never-transformed invariant).

3. In NightSky.astro's existing bare `<script>`, `import { initParallax } from "../lib/nightsky/parallax";` and call it once, retaining its teardown alongside the existing `teardownNightSky` (single boot path, no ClientRouter).
  </action>
  <verify>
    <automated>npm run build && npx astro check && test "$(grep -oE "requestAnimationFrame|setTimeout|setInterval" src/lib/nightsky/parallax.ts | wc -l)" -eq 0 && test "$(grep -oE "from ['\"][^'\"]*deck" src/lib/nightsky/parallax.ts | wc -l)" -eq 0 && test "$(grep -o "prefers-reduced-motion" src/lib/nightsky/parallax.ts | wc -l)" -ge 1 && test "$(grep -o "camper-parallax-nudge-fwd" src/components/NightSky.astro | wc -l)" -ge 1 && test "$(grep -o "prefers-reduced-motion" src/components/NightSky.astro | wc -l)" -ge 1</automated>
  </verify>
  <done>Build green, astro check clean. parallax.ts has zero rAF/timers and never imports deck.ts, has a reduced-motion guard; NightSky.astro carries the .camper nudge keyframes + reduced-motion @media; no canvas transform introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deck.ts -> document event -> ambient listeners | The same-origin `nightsky:panel-change` CustomEvent is the only "input" clouds.ts/parallax.ts consume; dispatched solely by deck.ts's trusted goTo(). |
| tick -> compositor | Cloud blits and the .camper transform must never re-blur glass beyond the panel's own transition window. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering / DoS | parallax.ts + clouds.ts panel-change listeners | low | mitigate | Null-check `detail`/`detail.index` before use; a missing/malformed detail no-ops, never throws (mirrors scene.ts's existing optional-chaining pattern). |
| T-09-02 | Denial of Service (main-thread) | clouds.ts | medium | mitigate | Handle shape with zero owned timers/rAF — cadence rides advance(); grep gate asserts zero additive-composite/rAF/timer occurrences so ambient can never outlive the pause machine. |
| T-09-03 | Denial of Service (unbounded state) | cloud parallax offset | low | mitigate | Bounded nudge-and-settle-to-0 (never a persistent camera offset), so repeated panel changes cannot accumulate an unbounded drift. |
| T-09-SC | Tampering (supply chain) | npm installs | n/a | accept | Zero package installs this plan (Floors: package.json untouched) — Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
Plan-level defense-in-depth (the true arbiter is 09-03's closing battery):
- `npm run build` green; `npx astro check` 0 errors.
- New-module invariant greps (grep -o | wc -l convention): zero additive-composite in clouds.ts; zero rAF/timers in clouds.ts and parallax.ts; zero hex in clouds.ts/idle-queue.ts/parallax.ts; scene.ts rAF count == 2; parallax.ts has no deck import.
- Early contrast smoke (clouds live, aurora not yet present): `npm run build`, start `npm run preview`, then `node scripts/verify-contrast.mjs --cdp-screenshot --width 1440 --height 900` and `--width 1280 --height 800` — both should exit 0. Capture evidence PNGs of both viewports (clouds visible in the lower-sky margins, attenuated under the column) into `.planning/phases/09-living-sky/evidence/`.
- Confirm `getComputedStyle(document.querySelector('#nightsky-canvas')).transform === 'none'` at rest and during a panel-change nudge (spot check).
</verification>

<success_criteria>
- AMB-01: two pre-rendered cloud sprite layers wraparound-blit inside the existing tick, lower-sky biased, column-governed to contrast-safe alphas.
- AMB-04: the ~40-star twinkle subset is a 2-oscillator scintillation with a bounded chromatic nudge on the 3 brightest; count and margin containment unchanged.
- AMB-02: camper (18px) + clouds (~9px) nudge and settle on panel change; canvas never transformed; reduced-motion is an instant no-motion jump.
- Single-rAF preserved (scene.ts == 2; clouds.ts/parallax.ts == 0); zero new dependencies; no push.
</success_criteria>

<output>
Create `.planning/phases/09-living-sky/09-01-SUMMARY.md` when done.
</output>
