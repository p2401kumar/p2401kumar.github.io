---
phase: 10-integration-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/10-integration-launch/10-01-fig01-audit.md
  - .planning/phases/10-integration-launch/10-01-integration-evidence.md
  - .planning/phases/10-integration-launch/10-01-battery.md
  - .planning/phases/10-integration-launch/10-01-LAUNCH-READINESS.md
  - public/og/og-default.png
autonomous: true
requirements: [FLR-01, FLR-02, FLR-03, LNC-01]

must_haves:
  truths:
    - "The embedded Fig. 01 36-check CDP audit re-passes against the FULLY COMPOSITED page (photo + glass + ambient behind it) — including one-active-animation, now proven with clouds AND aurora ALSO paused: the whole #nightsky-canvas hash is identical across a pair while fig-01 is active, while fig-01's own canvas animates during a dispatched beam (FLR-02)"
    - "Deck mechanics re-pass: one-gesture-one-panel (wheel/keys/jump), hash routing + back/forward, /#work alias -> systems, cold deep-links, view-classic escape + return (FLR-02)"
    - "No-JS classic mode renders the resume photo (static <img>/CSS) + all content readable, the Fig. 01 10-proxy keyboard floor + log present in dist/index.html, and glass declares nothing (@supports / prefers-reduced-transparency degrade to the opaque baseline) (FLR-02, FLR-03)"
    - "Case studies cold-load scene-free (zero nightsky / sky / ambient leak), sitemap lists exactly 3 real routes, /#work alias resolves to systems, the CC BY 4.0 credit line renders in both deck and classic modes (FLR-02)"
    - "The full gate suite is green and lands in the Phase-9 reference families: every verifier mode (selftest, agreement-selftest, cdp-screenshot both viewports, moon, aurora), banding (selftest + live crops), single-rAF / zero-hex / cross-boundary-import greps, ambient soak <10% total, local Lighthouse >=90 x4 both presets (FLR-01 local pre-flight, FLR-02)"
    - "Reduced-motion renders exactly one static frame, keyboard operability holds, and the honesty gate (every displayed number traces to the resume) remains intact across the composited stack (FLR-03)"
    - "A decision-ready go/no-go pack exists with before/after hero shots (v2-live vs v3-local), the aggregate gate table, the screenshot index, the rollback plan citing tags v1.0/v2.0, the carried real-device checklist incl. the v3 additions, and the fix-forward list — and NOTHING is pushed or deployed (LNC-01 rollback path documented)"
  artifacts:
    - .planning/phases/10-integration-launch/10-01-fig01-audit.md
    - .planning/phases/10-integration-launch/10-01-integration-evidence.md
    - .planning/phases/10-integration-launch/10-01-battery.md
    - .planning/phases/10-integration-launch/10-01-LAUNCH-READINESS.md
  key_links:
    - "Fig. 01 pause contract under full ambient: scene.ts subscribes nightsky:panel-change by literal name; detail.id==='fig-01' sets fig01Active -> updateRunState stops the SINGLE ambient rAF that drives clouds/aurora/parallax/scintillation together, so the whole #nightsky-canvas freezes while Fig. 01 animates its own canvas = one-active-animation holds with the full ambient set behind it"
    - "/#work alias: deck.ts HASH_ALIASES Map(['work','systems']) -> resolveIndexFromHash maps #work to the systems panel (the v2 fix-forward observation, now CLOSED — verify against the live source, not a summary)"
    - "The boundary-aware zero-hex grep over deck.css + the nightsky/fig01 modules is the enforcement that the new --glass-* tokens and the --aurora hue token live ONLY in tokens.css (they are hex values; any leak outside tokens.css trips the grep)"
---

<objective>
Produce the complete LOCAL proof that the fully composited v3.0 "Real Sky" site (real astrophotography photo + frosted glass chrome + living ambient scene + deck + all 7 v1 panels + case studies) is launch-ready, and aggregate it into a single decision-ready go/no-go pack for the user. This plan re-runs the entire carry-forward regression battery verbatim against the assembled whole, re-passes the embedded Fig. 01 36-check audit with the full ambient behind it, and pre-flights the Lighthouse floor locally. It verifies FLR-02 and FLR-03 to green, pre-flights FLR-01 locally, and documents the LNC-01 rollback path in the pack.

It ships NOTHING: no `git push`, no deploy, no Pages trigger. The ONLY sanctioned product edit in this plan is the optional OG asset swap (OG-03); every other product value — glass, scrim, photo, ambient, Fig. 01 — is LOCKED and verified read-only. It must not touch `package.json`, `package-lock.json`, or `.planning/config.json`.

Purpose: the 10-02 deploy is gated on an explicit human go decision. That decision must be anticlimactic — every gate except LIVE Lighthouse is proven green here first (10-CONTEXT.md "Specific Ideas": the user decides in 60 seconds, before/after hero shots + gate table lead, everything else is appendix).
Output: `10-01-fig01-audit.md`, `10-01-integration-evidence.md`, `10-01-battery.md`, `10-01-LAUNCH-READINESS.md`, plus committed sign-off screenshots.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-integration-launch/10-CONTEXT.md

# The PROVEN Phase-6 pattern this plan mirrors (audit technique, evidence
# shape, gate table) — the v2.0 integration-launch precedent:
@.planning/milestones/v2.0-phases/06-integration-launch/06-01-PLAN.md
@.planning/milestones/v2.0-phases/06-integration-launch/06-01-LAUNCH-READINESS.md

# The all-green Phase-9 reference numbers the battery re-run must land in
# (soak 5.49% total, Lighthouse 99/100/100/100 + 100x4, contrast/aurora/moon,
# single-rAF, banding, leak gate) — the reference families for every gate:
@.planning/phases/09-living-sky/battery/battery-summary.md
@.planning/phases/09-living-sky/09-03-SUMMARY.md

# As-built contracts each task Reads DIRECTLY (never trust selectors/line
# numbers from any summary — Read these live in the task):
@src/lib/fig01/index.ts
#   src/components/Figure01.astro     (DOM contract: .fig-stage, #fig01-canvas, #fig01-tip, #fig01-log, #send, #fault, .node-proxy[data-node])
#   src/lib/fig01/render.ts           (layout()/getDpr/stopAnimationLoop; requestAnimationFrame x2)
#   src/lib/fig01/interactions.ts     (MutationObserver panelActive gate; scheduleHeal 8s; createRedraw/renderStaticFrame reduced-motion path)
#   src/lib/nightsky/scene.ts         (single ambient rAF x2; nightsky:panel-change -> fig01Active -> updateRunState; computeAmbientTier)
#   src/lib/nightsky/deck.ts          (HASH_ALIASES Map(['work','systems']); resolveIndexFromHash; goTo/applyPanelStates; view-classic)
#   src/components/PanelDeck.astro    (pre-paint speculative deck-active; boot; DeckIndex sibling)
#   src/components/DeckIndex.astro    (#deck-index / #deck-index-count / .deck-jump a[data-panel-index] / .deck-view-classic / #deck-live aria-live)
#   src/components/Panel.astro        (thin <div class="panel" data-panel-id> wrapper — zero layout CSS of its own)
#   src/components/NightSky.astro     (.nightsky-host > .sky-photo <picture>/<img> + #nightsky-canvas + .camper)
#   src/components/SiteFooter.astro   (the CC BY 4.0 credit line: NOIRLab + CC BY 4.0 links, both modes)
#   src/pages/index.astro             (7 Panel wrappers unforked; sky-photo LCP preload slot)
#   src/pages/work/[slug].astro       (case-study template; /#work back crosslink)
#   src/styles/deck.css               (transform-only hiding; glass usage via var(--glass-*))
#   scripts/verify-contrast.mjs       (--selftest / --agreement-selftest / --cdp-screenshot / --moon / --aurora)
#   scripts/verify-banding.mjs        (--selftest; live crops)
#   .planning/phases/09-living-sky/ambient-soak/ambient-soak.mjs   (reusable ambient A/B soak driver)
#   .planning/phases/09-living-sky/battery/capture-battery.mjs     (CDP captures/probes driver)
#   .github/workflows/deploy.yml      (push-to-main = deploy — DO NOT trigger it in this plan)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The composited-surface audit — embedded Fig. 01 36-check re-run (full ambient behind it) + deck mechanics + no-JS classic + case-study/SEO/leak/sitemap/alias/credit</name>
  <files>.planning/phases/10-integration-launch/10-01-fig01-audit.md, .planning/phases/10-integration-launch/10-01-integration-evidence.md</files>
  <behavior>
    - Cold-load /#fig-01 on the composited build: after load, `#fig01-canvas`.width > 0 AND .height > 0 (no 0x0 init) and `[data-panel-id="fig-01"]` has data-state="active".
    - send / fault -> weigh-away / ~8s self-heal / 10 keyboard proxies + sr-only degraded narration all re-pass embedded (the full v1 checklist, mapped 1:1).
    - resize-while-inactive -> activate re-measures: `#fig01-canvas`.width === round(clientWidth * min(devicePixelRatio,2)), both dims > 0 (no stale DPR store).
    - one-active-animation WITH FULL AMBIENT: while fig-01 is active, two `#nightsky-canvas` captures ~2s apart are byte/hash-identical (clouds + aurora + parallax + scintillation all frozen because they share the single ambient rAF the pause machine stops), while two `#fig01-canvas` captures during a dispatched beam differ; on the hero panel the `#nightsky-canvas` pair differs (ambient running).
    - reduced-motion: under prefers-reduced-motion, two full screenshots ~2s apart are byte-identical (one static frame — photo + moon + constellations + clouds + aurora present) AND clicking `#fault` still narrates (reduced-motion redraw path works).
    - classic mode: with view-preference classic, `<html>` is classic-active (not deck-active), Fig. 01 is interactive (send updates the log), and the resume photo + credit line are present.
    - deck mechanics: one gesture (ArrowDown / wheel / jump-list tap) advances exactly one panel; location.hash tracks the active panel; back/forward restores; cold deep-link to /#patents opens patents with no flash of hero; /#work resolves to the systems panel (alias); view-classic escape then deck-view return both work.
  </behavior>
  <action>
Read the real files first (never trust a summary): src/components/Figure01.astro, src/lib/fig01/render.ts, src/lib/fig01/interactions.ts, src/lib/nightsky/scene.ts, src/lib/nightsky/deck.ts, src/components/PanelDeck.astro, src/components/DeckIndex.astro, src/components/Panel.astro, src/components/NightSky.astro, src/components/SiteFooter.astro, src/pages/index.astro, src/pages/work/[slug].astro. Confirm the DOM selector contract and the pause/alias wiring against the source as it stands now.

Write a scratchpad node CDP driver (reuse the launchChrome/connectCdp/node-WebSocket harness already proven in scripts/verify-contrast.mjs and .planning/phases/09-living-sky/battery/capture-battery.mjs — save it under the scratchpad dir; do NOT add it to the repo or to package.json). Build once and serve the composited build (`npm run build && npm run preview` -> http://localhost:4321/). Drive the page headlessly and assert each behavior above via Runtime.evaluate: dispatch clicks/focus on #send/#fault/.node-proxy, read #fig01-log.textContent, read canvas.width/height/clientWidth, read `[data-panel-id]` data-state and location.hash, read devicePixelRatio; capture #nightsky-canvas and #fig01-canvas frames via toDataURL() for the static/animating pixel-diffs. For reduced-motion use CDP Emulation.setEmulatedMedia (prefers-reduced-motion: reduce). For the resize test use Emulation.setDeviceMetricsOverride while fig-01 is inactive, then activate and assert the re-measured backing store.

The v3 twist on one-active-animation: in v2 the paused surface was the procedural scene; NOW the single ambient rAF drives clouds + aurora + parallax + scintillation together, and the pause machine must freeze ALL of them when fig-01 is active. Assert the whole #nightsky-canvas hash is identical across a ~2s pair while fig-01 is active (use the canvas-dataURL-hash instrument the Phase-9 battery used, since the camper-glow CSS pulse legitimately animates outside reduced motion — the pause contract is the CANVAS), and differs on the hero panel. This is the composited-parity check that v2's audit could not make.

Record two evidence files:
1. `10-01-fig01-audit.md` — a checklist table mapping 1:1 to the v1 Fig. 01 checklist (the 5 observable truths + FIG-01..07) plus the four deck-reality additions (cold-load no-0x0, resize-while-inactive, classic, one-active-animation-with-full-ambient), each PASS with the concrete assertion evidence. Capture screenshots for the pack: cold /#fig-01 painted, post-fault degraded, post-resize+activate.
2. `10-01-integration-evidence.md` — three sections: (a) DECK MECHANICS (one-gesture-one-panel, hash + back/forward, cold deep-links, /#work alias -> systems, view-classic escape + return); (b) NO-JS CLASSIC (from dist/index.html: resume photo <picture>/<img> present, all content readable, the Fig. 01 role="img" chrome + exactly 10 .node-proxy buttons + #fig01-log present, and glass declares nothing — the glass rules are @supports/prefers-reduced-transparency-gated in deck.css and never inline, so the no-JS/no-@supports floor is the opaque baseline); (c) CASE-STUDY + SEO (both /work/* cold-load standalone with zero deck/scene, browser Back restores #systems, sitemap lists EXACTLY the 3 real routes with /404 absent, leak gate = 0 nightsky/sky/ambient refs in dist/work + dist/404.html, the CC BY 4.0 credit line present in both modes).

If ANY behavior fails, record the exact failing path and STOP — do not paper over it. NEVER run `git push`; do NOT modify package.json, package-lock.json, or `.planning/config.json`.
  </action>
  <verify>
    <automated>
set -e
npm run build >/dev/null 2>&1
# The scratchpad CDP audit exits 0 with every sub-check PASS and prints the sentinel.
node "$SCRATCHPAD/fig01-composited-audit.mjs" | tee "$SCRATCHPAD/fig01-audit.out"
grep -q 'FIG01 COMPOSITED AUDIT PASS' "$SCRATCHPAD/fig01-audit.out"
# No-JS floor: dist/index.html carries the resume photo, the figure chrome, and exactly 10 proxies
test "$(grep -oE 'class="node-proxy' dist/index.html | wc -l | tr -d ' ')" -eq 10
grep -q 'id="fig01-log"' dist/index.html
grep -q 'role="img"' dist/index.html
test "$(grep -oE '<picture>' dist/index.html | wc -l | tr -d ' ')" -ge 1
# Credit line present (both modes share the footer)
grep -q 'CC BY 4.0' dist/index.html
# /#work alias is WIRED in source (v2 fix-forward now closed) — assert the mapping literal exists
test "$(grep -oE "\['work', ?'systems'\]" src/lib/nightsky/deck.ts | wc -l | tr -d ' ')" -ge 1
# Sitemap lists exactly the 3 real routes, /404 absent
test -f dist/sitemap-0.xml
test "$(grep -oE '<loc>' dist/sitemap-0.xml | wc -l | tr -d ' ')" -eq 3
test "$(grep -oE '/404/?</loc>' dist/sitemap-0.xml | wc -l | tr -d ' ')" -eq 0
# Leak gate: deck sentinel + sky refs unreachable from /work/* and /404
test "$(grep -orE 'nightsky:panel-change' dist/work | wc -l | tr -d ' ')" -eq 0
test "$(grep -oE 'nightsky:panel-change' dist/404.html | wc -l | tr -d ' ')" -eq 0
test "$(grep -orE '/sky/milky-way' dist/work | wc -l | tr -d ' ')" -eq 0
# Evidence files written with the required sections
FA=.planning/phases/10-integration-launch/10-01-fig01-audit.md
grep -qiE 'one[- ]active|scene paused|ambient.*paused' "$FA"
grep -qiE 'resize' "$FA"
grep -qiE 'reduced[- ]motion' "$FA"
grep -qiE 'classic' "$FA"
IE=.planning/phases/10-integration-launch/10-01-integration-evidence.md
grep -qiE 'deck mechanics|one[- ]gesture' "$IE"
grep -qiE 'no[- ]js|classic' "$IE"
grep -qiE 'leak gate|sitemap' "$IE"
grep -qiE '#work|alias' "$IE"
echo "TASK 1 PASS"
    </automated>
  </verify>
  <done>10-01-fig01-audit.md records the embedded Fig. 01 checklist re-pass on the composited build: cold /#fig-01 inits a nonzero canvas; send/fault/weigh-away/~8s self-heal/keyboard-proxy/sr-only all work embedded; resize-while-inactive then activate re-measures with no 0x0 / no stale DPR; classic + reduced-motion keep the figure functional; and one-active-animation holds with the FULL ambient set frozen (whole #nightsky-canvas hash identical while fig-01 active). 10-01-integration-evidence.md records deck mechanics, the no-JS classic floor (photo + 10 proxies + log + glass-declares-nothing), and the case-study/SEO surface (both /work/* scene-free, Back -> #systems, sitemap 3 routes, leak gate 0, /#work alias -> systems, credit line both modes). Any failure recorded and stopped, never papered over. No push; config/package untouched.</done>
</task>

<task type="auto">
  <name>Task 2: The full carry-forward gate suite + local Lighthouse both presets (one consolidated battery table vs the Phase-9 reference families) + OG-03 opportunistic refresh</name>
  <files>.planning/phases/10-integration-launch/10-01-battery.md, public/og/og-default.png</files>
  <action>
Re-run the entire shipped gate battery against the composited build and record it in `10-01-battery.md` as ONE consolidated PASS/FAIL table, each row carrying the measured number AND the Phase-9 reference family it must land in (from battery-summary.md). Reuse the committed drivers — do NOT rewrite them and do NOT touch the locked source. Serve the build with `npm run build && npm run preview` -> http://localhost:4321/.

Gates (each a row in the table):
- `npm run build` exit 0; `npx astro check` -> 0 errors / 0 warnings / 0 hints.
- Verifier modes: `node scripts/verify-contrast.mjs --selftest` (PASS), `--agreement-selftest` (PASS), `--cdp-screenshot` at 1440x900 AND 1280x800 (every panel's failing[] empty, worst >=4.5 vs --ink — reference worsts @1440: experience 15.06 / patents 15.55 / skills 15.57 / header 6.23 / hero 13.55), `--moon` both viewports (moonPeak < mwPeak — ref 0.2212<0.4748 @1440, 0.2580<0.4695 @1280), `--aurora` both viewports (auroraPeak < mwPeak, full-cycle window — ref 0.1035<0.4748 @1440, 0.0384<0.4695 @1280).
- Banding: `node scripts/verify-banding.mjs --selftest` (clean passes, banded control fails) + live gradient crops (cloud + aurora bands off the reduced-motion still at peak alpha — runs=1 gaps=0 each).
- Ambient soak: run .planning/phases/09-living-sky/ambient-soak/ambient-soak.mjs (60s, glass live both runs, ambient A/B via reduced-motion emulation) and record TOTAL idle CPU — MUST be < 10% (reference family 5.49% total, marginal ~0 vs 08's 6.10%), with 60fps and 0 long tasks. If it lands outside the 5.49% family but still < 10%, record the delta and one line of rationale; a total >= 10% is a hard fail.
- Structural greps (single-driver invariants; count with grep -o | wc -l, NEVER grep -c): src/lib/nightsky/scene.ts requestAnimationFrame == 2; clouds.ts / aurora.ts / parallax.ts each == 0; meteors.ts == 0; starfield.ts == 0; src/lib/fig01/render.ts == 2. Zero deck/fig01 imports in the scene modules (scene/constellations/starfield/meteors/clouds/aurora/parallax/tokens) == 0. Boundary-aware zero-hex `#[0-9a-fA-F]{3,8}\b` over src/lib/nightsky + src/lib/fig01 + src/styles/deck.css + NightSky.astro == 0 (the enforcement that --glass-*/--aurora hex tokens live in tokens.css only; tokens.css itself is EXCLUDED because it legitimately holds them).
- Local Lighthouse: `npx lighthouse` mobile (default preset) AND desktop (--preset=desktop) headless against the preview; extract all four category scores + LCP/CLS/TBT from each and record them. Every category >= 90 on BOTH presets (reference: mobile 99/100/100/100 LCP 1.9s TBT 0ms CLS 0.003; desktop 100x4 LCP 0.5s). A local < 90 is a planning-time flag for the user (the authoritative FLR-01 gate is the LIVE run in 10-02) — record the shortfall + responsible audit(s), never silently pass.
- Sign-off screenshots for the pack (via the scratchpad CDP driver): hero (v3-local), a tier-2 panel (experience — Microsoft cluster brightened), the jump index, the aurora+moon region, the reduced-motion static still (the marquee proof-of-craft — may re-capture or reuse 09's battery/reduced-motion-still-1440x900.png), mobile 375w, and classic mode.

OG-03 (opportunistic, planner's discretion, honesty gate applies): the static OG image at public/og/og-default.png still shows the v1/v2-era look. IF a trivial capture suffices — headless Chrome under prefers-reduced-motion at 1200x630 over the composited hero (a REAL capture of the real site), written to public/og/og-default.png, then `npm run build` + assert the SEO meta still resolves og:image to /og/og-default.png and the new PNG is exactly 1200x630 — refresh it inside this task and record the before/after. IF it turns into real design work (framing, legibility, letterboxing, crop composition), do NOT force it: leave public/og/og-default.png untouched and record OG-03 as DEFERRED with one line of rationale (OG-03 stays in Future Requirements). This is the ONLY sanctioned product edit in the whole plan.

NEVER run `git push`; do NOT modify package.json, package-lock.json, or `.planning/config.json`.
  </action>
  <verify>
    <automated>
set -e
npm run build >/dev/null 2>&1
npx astro check 2>&1 | grep -qE '0 error'
# Single-rAF single-driver invariants (grep -o | wc -l — never grep -c)
test "$(grep -oE 'requestAnimationFrame' src/lib/nightsky/scene.ts | wc -l | tr -d ' ')" -eq 2
test "$(grep -oE 'requestAnimationFrame' src/lib/fig01/render.ts | wc -l | tr -d ' ')" -eq 2
test "$(grep -oE 'requestAnimationFrame' src/lib/nightsky/clouds.ts | wc -l | tr -d ' ')" -eq 0
test "$(grep -oE 'requestAnimationFrame' src/lib/nightsky/aurora.ts | wc -l | tr -d ' ')" -eq 0
test "$(grep -oE 'requestAnimationFrame' src/lib/nightsky/parallax.ts | wc -l | tr -d ' ')" -eq 0
test "$(grep -oE 'requestAnimationFrame' src/lib/nightsky/meteors.ts | wc -l | tr -d ' ')" -eq 0
test "$(grep -oE 'requestAnimationFrame' src/lib/nightsky/starfield.ts | wc -l | tr -d ' ')" -eq 0
# Zero cross-boundary imports in the scene modules
test "$(grep -orE 'from .*(nightsky/deck|lib/fig01)' src/lib/nightsky | wc -l | tr -d ' ')" -eq 0
# Boundary-aware zero-hex outside tokens.css across the styled/scene surface
test "$(grep -ornE '#[0-9a-fA-F]{3,8}\b' src/lib/nightsky src/lib/fig01 src/styles/deck.css src/components/NightSky.astro | wc -l | tr -d ' ')" -eq 0
# Battery record written with the key gate rows + numbers
BT=.planning/phases/10-integration-launch/10-01-battery.md
grep -qiE 'selftest' "$BT"
grep -qiE 'cdp-screenshot|contrast' "$BT"
grep -qiE 'aurora' "$BT" && grep -qiE 'moon' "$BT"
grep -qiE 'banding' "$BT"
grep -qiE 'soak' "$BT"
grep -qiE 'mobile' "$BT" && grep -qiE 'desktop' "$BT"
grep -qiE 'OG-03|og-default' "$BT"
# OG asset is a valid 1200x630 PNG IF it was refreshed (else untouched — either is acceptable)
test -f public/og/og-default.png
echo "TASK 2 PASS"
    </automated>
  </verify>
  <done>10-01-battery.md records the full carry-forward gate suite as one consolidated table, every row green and inside its Phase-9 reference family: build + astro check 0/0/0, all verifier modes (selftest / agreement-selftest / cdp-screenshot both viewports failing[] empty / moon / aurora), banding (selftest + live crops), ambient soak < 10% total (5.49% family) at 60fps 0 long tasks, single-rAF 2/2/0..0, zero cross-boundary imports, boundary-aware zero-hex 0, and local Lighthouse >= 90 x4 both presets with LCP/CLS/TBT. Sign-off screenshots captured. OG-03 either refreshed as a real 1200x630 reduced-motion hero capture (build + meta verified) or recorded DEFERRED with one line of rationale. No push; package/config untouched.</done>
</task>

<task type="auto">
  <name>Task 3: The launch-readiness go/no-go pack (10-01-LAUNCH-READINESS.md) — decision-ready, 60-second call, NO push anywhere</name>
  <files>.planning/phases/10-integration-launch/10-01-LAUNCH-READINESS.md</files>
  <action>
Write `10-01-LAUNCH-READINESS.md` as the single decision-ready go/no-go pack FOR THE USER (10-CONTEXT.md: lead with before/after hero shots + the gate table so the call takes 60 seconds; everything else is appendix). Mirror the 06-01-LAUNCH-READINESS.md shape. Frame it honestly per 10-CONTEXT "Specific Ideas": v2.0 Night Sky has been live for ~1 day; v3.0 is the same substance with a REAL sky (photo + glass + living ambient) — the user knew this trajectory, no drama. It MUST contain:

1. What changes live: `p2401kumar.github.io` flips from the v2.0 procedural night sky to the v3.0 real-sky composite — the real NOIRLab Milky Way photo behind everything, frosted-glass panel/chrome/jump-index, and the living ambient scene (drifting clouds, panel-change parallax, breathing aurora, scintillation). Content unchanged and unforked; the two case studies stay scene-free; view-classic + no-JS fall back to the v1 page. One paragraph, plain language. State the push is ~62 local commits ahead of origin/main (first v3 commit `3fbbcd2 docs: start milestone v3.0 Real Sky`).

2. Before/after hero shots: the v2-LIVE hero (a read-only screenshot of the CURRENT live site https://p2401kumar.github.io/ — captured, never modified) beside the v3-local hero (from Task 2). This is the lead artifact for the 60-second call.

3. The aggregate gate table: pull every result from Task 1 (fig01 audit + deck + no-JS + case-study/SEO) and Task 2 (battery + local Lighthouse) into one PASS/FAIL table with the key numbers (contrast worsts both viewports, aurora/moon peaks, soak total, single-rAF counts, Lighthouse four categories both presets, LCP/CLS/TBT). Mark LIVE Lighthouse as the ONE gate deferred to 10-02. Note the v2 fix-forward items now CLOSED in v3 (/#work alias wired; cold-/#fig-01 pause seed — confirm which of the two v2 carried notes are resolved by the current source and say so).

4. Screenshot index: a table listing every committed screenshot (before/after hero, tier-2 panel, jump index, aurora+moon, reduced-motion still, mobile 375w, classic mode, fig-01 cold-load, fig-01 degraded) with a one-line caption. FEATURE the reduced-motion still as the proof-of-craft artifact (10-CONTEXT "Specific Ideas").

5. Rollback plan (state explicitly, do NOT execute): revert the v3 deploy range + fast-forward push (v2 remains in git history); tags **v1.0** and **v2.0** on origin mark both prior generations (`git checkout v2.0` restores the shipped Night Sky); the revert-and-push itself re-triggers deploy.yml and republishes the prior output. Include the exact reference commands (revert + FF push; NEVER --force).

6. Real-device touch checklist (carried from v2 by standing agreement — the user runs it on their phones post-deploy, rollback available): swipe pages exactly one panel, iOS edge-swipe/back intact, jump-index tap, deep-link cold-load, view-classic escape, Fig. 01 send/fault tappable + narrating — PLUS the v3 additions: photo loads fast on cellular, glass renders (iOS Safari -webkit-backdrop-filter path), the panel-change parallax nudge feels right, no scroll jank with ambient live, and a 5-minute idle battery/warmth check (the <10% soak's real-world confirmation).

7. Fix-forward list (known, recorded, NOT launch blockers): anything known-cosmetic — e.g. the aurora lateral-softening lever deliberately NOT taken (09-03 carried note (b): side edges vertical as specified, the offscreen-pass lever stays documented), plus the OG-03 disposition from Task 2 if it was deferred, plus any /#work / cold-/#fig-01 residue if not fully closed.

8. Verdict: N of M gates green locally; the only open gate is LIVE Lighthouse (structurally requires the deploy). Saying **go** = FF push of the ~62 commits -> Actions Pages deploy -> live Lighthouse both presets -> live smoke -> the real-device checklist at leisure. The user's options at the 10-02 gate: **launch now** / **fix-listed-items-then-launch** / **hold** (the orchestrator asks in chat). Saying no costs nothing — everything stays local, the live site remains v2.0.

This plan pushes NOTHING. Assert it: origin/main stays behind local main (no push occurred). NEVER run `git push`; do NOT modify package.json, package-lock.json, or `.planning/config.json`.
  </action>
  <verify>
    <automated>
set -e
RD=.planning/phases/10-integration-launch/10-01-LAUNCH-READINESS.md
# All eight required sections present
grep -qiE 'changes live|flips|replaces' "$RD"
grep -qiE 'before/after|v2-live|v3-local' "$RD"
grep -qiE 'gate table|PASS' "$RD"
grep -qiE 'screenshot' "$RD"
grep -qiE 'rollback' "$RD"
grep -qiE 'v1\.0' "$RD" && grep -qiE 'v2\.0' "$RD"
grep -qiE 'real[- ]device|iPhone|cellular|parallax' "$RD"
grep -qiE 'fix[- ]forward|aurora lateral|OG-03' "$RD"
grep -qiE 'live Lighthouse' "$RD"
grep -qiE 'launch now|hold' "$RD"
# PROOF this plan pushed nothing: origin/main is still an ancestor of, and NOT equal to, local main
git fetch origin >/dev/null 2>&1 || true
git merge-base --is-ancestor origin/main main
test "$(git rev-parse origin/main)" != "$(git rev-parse main)"
echo "TASK 3 PASS"
    </automated>
  </verify>
  <done>10-01-LAUNCH-READINESS.md is a complete, decision-ready go/no-go pack: what-changes-live (v2.0 night sky -> v3.0 real sky), before/after hero shots (v2-live vs v3-local), the aggregate gate table with numbers, the screenshot index featuring the reduced-motion still, the explicit rollback plan citing tags v1.0/v2.0, the carried real-device checklist incl. the v3 additions, the fix-forward list, and the verdict with the three go/no-go options. LIVE Lighthouse is the only gate marked deferred to 10-02. Verified that this plan pushed nothing (origin/main still behind local main). package/config untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local working tree -> origin/main (GitHub) | A push here IS the deploy (deploy.yml fires on push to main) and REPLACES the live v2.0 professional site. This plan must NEVER cross it — every verify block is local-only. |
| locked product values -> product source | glass / scrim / photo / ambient / Fig. 01 are LOCKED; this plan verifies them read-only. The single sanctioned product edit is the optional OG asset swap (a real capture, honesty-gated). |
| planning artifacts -> config | `package.json` / `package-lock.json` / `.planning/config.json` govern deps + autonomous behavior; this plan must not mutate them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Elevation/Integrity | any `git push` in 10-01 | critical | mitigate | No push, deploy, or Pages trigger anywhere in this plan; the deploy is exclusively 10-02 Task 2, after the 10-02 Task 1 human gate. Task 3 verify ASSERTS origin/main is still behind local main. |
| T-10-03 | Denial of Service (credibility) | shipping a regressed composite | high | mitigate | The full carry-forward battery + embedded Fig. 01 composited audit + contrast/aurora/moon/soak/Lighthouse must all be green and recorded (landing in the Phase-9 families) BEFORE the go/no-go, so 10-02's deploy is anticlimactic. Any shortfall recorded, never silently passed. |
| T-10-05 | Tampering | locked product values (glass/photo/ambient/Fig.01) | high | mitigate | Verification only; the sole sanctioned edit is the OG asset swap, and even that is honesty-gated (a real capture of the real site) and skippable (DEFERRED path). |
| T-10-04 | Tampering | `package.json` / `.planning/config.json` | low | mitigate | Plan explicitly does not touch them; no new dependency added; autonomous behavior unchanged. |
| T-10-SC | Tampering | transient `npx lighthouse` / node CDP drivers | low | mitigate | Lighthouse runs as a transient audit tool (Phase 4/5/7/8/9 precedent); the CDP/soak drivers are node built-ins reusing verify-contrast.mjs / capture-battery.mjs; zero installs, package.json/lock byte-identical. |
</threat_model>

<verification>
- Local-only: `git merge-base --is-ancestor origin/main main` holds and origin/main != main (no push occurred); package.json/lock and .planning/config.json byte-identical.
- FLR-02 (regression battery): embedded Fig. 01 36-check re-passes with full ambient paused; deck mechanics + no-JS classic (photo + 10 proxies + glass-declares-nothing) + case-study scene-free leak gate + sitemap 3 routes + /#work alias + credit line all green; every verifier mode + soak <10% + single-rAF + zero-hex recorded.
- FLR-03: reduced-motion one static frame, keyboard operability, honesty gate intact across the composite.
- FLR-01 pre-flight: local Lighthouse >=90 x4 both presets recorded with LCP/CLS/TBT; the authoritative live run is 10-02.
- LNC-01 (rollback documented): the pack states the explicit rollback path (revert + FF push; tags v1.0/v2.0) — deploy itself is gated in 10-02.
- The launch-readiness pack contains all eight required sections and leads with before/after hero shots + gate table.
</verification>

<success_criteria>
- All four evidence/pack files exist, are committed, and record green gates inside the Phase-9 reference families (or explicitly-recorded shortfalls, never silent passes).
- The user has a single decision-ready document to make the 10-02 go/no-go call in 60 seconds without digging.
- Nothing was pushed or deployed; the only product edit (if taken) is the honesty-gated OG capture; package.json/lock and .planning/config.json are untouched.
</success_criteria>

<output>
Create `.planning/phases/10-integration-launch/10-01-SUMMARY.md` when done.
</output>
