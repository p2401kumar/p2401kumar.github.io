---
phase: 11-bolder-sky
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/components/NightSky.astro
  - src/styles/tokens.css
  - src/styles/deck.css
  - src/lib/nightsky/constellations.ts
  - src/lib/nightsky/scene.ts
  - src/lib/nightsky/aurora.ts
autonomous: true
requirements:
  - BOLD-03
  - BOLD-04
  - BOLD-05

must_haves:
  truths:
    - "No camper silhouette anywhere in the scene; a clean soft warm ambient glow low in the frame carries the copper brand warmth (a radial, no hard blob), fireflies unchanged."
    - "The content card reads as PREMIUM liquid glass — brighter frost, a specular highlight, a crisp bright top edge, rounded corners — not a flat gray box; BOTH tiers read as glass while holding contrast."
    - "Constellations still read as clearly AUTHORED over the brighter, busier photo; the moon and aurora stay under the (now brighter) Milky-Way-core luminance ceiling; fireflies + the warm glow carry copper."
  artifacts:
    - "src/components/NightSky.astro — camper SVG removed; the copper glow reshaped into a clean soft warm ambient radial."
    - "src/styles/tokens.css — reworked --glass-* liquid-glass token values (both tiers, values live here ONLY)."
    - "src/styles/deck.css — .panel-card specular ::before highlight + rounded corners + bright edge; all degradation ladders preserved."
    - "src/lib/nightsky/{constellations,scene,aurora}.ts — overlay re-tuned to the brighter sky."
  key_links:
    - "--glass-* tokens (tokens.css) -> .panel-card rules (deck.css) -> the liquid-glass look — glass values live in tokens.css only, zero hex."
    - "@supports + prefers-reduced-transparency + @media print ladders + .fig solid exemption + active-panel-only scoping + -webkit dual-write + the lightningcss cssTarget prefix-order pin are ALL preserved."
    - "constellation/firefly alphas + aurora envelope -> moon/aurora luminance ceilings under the new brighter core (proven by --moon/--aurora in 11-03)."
---

<objective>
Cut the unrecognizable camper "smudge", upgrade the content-card glass to read as premium
liquid glass, and re-tune the drawn overlay to the brighter sky from 11-01 (BOLD-03, BOLD-04,
BOLD-05). This is a look rework of the shipped v3.0 engine — every v3.0 invariant holds.

Purpose: The v3.0 review confirmed the camper reads as an unidentifiable smudge and the glass
reads as a flat gray box. The approved pick is Direction A's warm ambient glow (no silhouette)
grafted with Direction C's true liquid-glass card. The overlay must be re-tuned so constellations
stay authored and moon/aurora stay under the brighter core ceiling.

Output: A camper-free scene with a clean warm ambient glow, a liquid-glass card (both tiers),
and a re-tuned overlay — all values in tokens.css, all v3.0 accessibility ladders and engine
invariants intact. Contrast is arbitrated + escalated in 11-03 (this plan sets the LOOK).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-bolder-sky/11-CONTEXT.md

# The camper SVG to cut + the glow to reshape (Layer 1)
@src/components/NightSky.astro
# --glass-* + --sky-* + --accent live here ONLY (zero-hex doctrine)
@src/styles/tokens.css
# The card-scoped .panel-card glass + the @supports/reduced-transparency/print ladders
@src/styles/deck.css
# Panel wrapper — the .panel-card surface the glass attaches to
@src/components/Panel.astro
# Overlay re-tune touch points
@src/lib/nightsky/constellations.ts
@src/lib/nightsky/scene.ts
@src/lib/nightsky/aurora.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cut the camper; keep a clean soft warm ambient glow</name>
  <files>src/components/NightSky.astro</files>
  <action>
Remove the camper silhouette (per 11-CONTEXT.md recipe item 3, BOLD-03). Delete the entire
`<svg class="camper-svg">...</svg>` block (the .camper-body rect, .camper-wheel circle, and
.camper-window rect) from the markup, and delete the `.camper-body, .camper-wheel { fill:
--sky-zenith }` and `.camper-window { fill: --accent }` style rules. This drops the literal
"campsite" narrative — user-directed, fine.

Keep the copper brand warmth alive as a CLEAN soft warm ambient glow low in the frame (mockup A
kept exactly this — a warm low-left radial, no hard silhouette). Reshape the existing
`.camper-glow` into that ambient glow: broaden its radius and soften its peak so it reads as a
warm wash (not a window-anchored ember), positioned low in the frame (mockup A's warm low-left
glow ~12% x / 92% y is the anchor; exact placement/softness is Claude's discretion). Continue to
use `rgb(from var(--accent) r g b / <alpha>)` — never a hex literal, never shadowBlur/filter:blur().
Fireflies already carry copper and stay unchanged. Keep `.camper-glow` a CHILD of the `.camper`
container and size/position it so the glow sits INSIDE the `.camper` box — 11-03 re-points the
visibility gate's lower-left assertion (from silhouette-edge to warm-glow luminance-presence) at the
`.camper` region, so the glow must be measurable there.

PRESERVE the ground-layer parallax: keep the `.camper` positioned container element and its
`.camper.parallax-nudge-fwd` / `.camper.parallax-nudge-back` keyframe rules so parallax.ts (which
toggles those classes on `nightsky:panel-change`) stays UNTOUCHED — only the SVG inside it is
removed and the glow is reshaped. Keep the reduced-motion branches (the glow's static opacity and
the nudge's `animation: none`). The canvas is never transformed.
  </action>
  <verify>
    <automated>test "$(grep -o 'camper-svg' src/components/NightSky.astro | wc -l | tr -d ' ')" = "0" && test "$(grep -o 'camper-body' src/components/NightSky.astro | wc -l | tr -d ' ')" = "0" && test "$(grep -o 'camper-window' src/components/NightSky.astro | wc -l | tr -d ' ')" = "0" && test "$(grep -oE 'rgb\(from var\(--accent\)' src/components/NightSky.astro | wc -l | tr -d ' ')" != "0" && npx astro check</automated>
    <human-check>No silhouette anywhere; a soft warm ambient glow sits low in the frame carrying the copper warmth; fireflies unchanged.</human-check>
  </verify>
  <done>The camper SVG + its fill rules are gone; a clean warm ambient radial glow remains (accent via relative-color, no hex); parallax classes preserved; astro check 0 errors.</done>
</task>

<task type="auto">
  <name>Task 2: Liquid-glass card — rework --glass-* tokens + .panel-card, ladders preserved</name>
  <files>src/styles/tokens.css, src/styles/deck.css</files>
  <action>
Restyle the (already card-scoped) card glass to read as PREMIUM liquid glass, not a flat gray box
(per 11-CONTEXT.md recipe item 4, BOLD-04; mockup Direction C's card). ALL values as `--glass-*`
tokens in tokens.css ONLY — zero hex, use `rgb(255 255 255 / <alpha>)` form.

In tokens.css, rework the tier-1 "optical" tokens toward the liquid-glass target: brighter frost
(--glass-bg white ~9-10%, up from 6%), blur ~20-24px (--glass-blur, up from 12px — the 16px note
in the file was a v3.0 ceiling; this milestone supersedes it, update the comment), saturate ~1.4
(--glass-saturate), brightness ~1.08 (--glass-brightness, up from 0.90), a crisp bright top edge
(--glass-edge white ~28-30%, up from 10%). Rework the tier-2 "protected" tokens so the dense-text
panels read as glass too: the v3.0 dark `rgb(15 18 23 / 0.55)` fill is the literal "gray box" —
make it lighter / tinted-but-luminous while still functioning as a scrim (the screenshot gate in
11-03 arbitrates the exact opacity; the LOOK target is "liquid glass", never "dark rectangle").

In deck.css §8, add the liquid-glass form to the `.panel-card` glass rule: rounded corners
(border-radius ~14-16px), a specular highlight via a `::before` soft white radial in the card's
top-left (pointer-events:none, positioned/overflow-clipped so it does not escape the card), and a
faint inner glow (inset box-shadow) — mirror the mockup C card treatment. Keep the bright top edge
as border-top from --glass-edge.

PRESERVE, exactly (these are the accessibility + build-correctness ladders):
- the `@supports (backdrop-filter...) or (-webkit-backdrop-filter...)` guard and the
  `@media (prefers-reduced-transparency: no-preference)` guard (degradation = transparent/scrim-only);
- the `@media print` strip (add the `::before` to the strip if it could print);
- the Fig. 01 `.fig` opaque `var(--panel)` exemption ("an instrument, not a window");
- active-panel-only scoping (`.panel[data-state="active"] > .panel-card` / classic equivalent) so
  only ONE card composites a blur pass (the <=4-surface budget);
- the `-webkit-backdrop-filter` + `backdrop-filter` DUAL-WRITE on every glass rule, webkit first,
  standard second (the lightningcss prefix-order fix — vite cssTarget pin keeps the pair from
  collapsing to last-wins). Every backdrop-filter must have a matching -webkit- sibling.
Chrome glass (header/footer/jump-index tokens) may stay as-is unless 11-03's contrast gate flags it.
  </action>
  <verify>
    <automated>test "$(grep -o '@supports' src/styles/deck.css | wc -l | tr -d ' ')" != "0" && test "$(grep -o 'prefers-reduced-transparency' src/styles/deck.css | wc -l | tr -d ' ')" != "0" && test "$(grep -o '@media print' src/styles/deck.css | wc -l | tr -d ' ')" != "0" && test "$(grep -oE 'backdrop-filter:' src/styles/deck.css | wc -l | tr -d ' ')" = "$(( $(grep -oE 'webkit-backdrop-filter:' src/styles/deck.css | wc -l | tr -d ' ') * 2 ))" && test "$(grep -rEn '#[0-9a-fA-F]{6}' src/styles/deck.css | wc -l | tr -d ' ')" = "0" && npm run build</automated>
    <human-check>The card reads as liquid glass — brighter frost, a top-left specular highlight, a crisp bright top edge, rounded corners; tier-2 dense panels read as glass, not a dark rectangle.</human-check>
  </verify>
  <done>--glass-* tokens reworked to the liquid-glass target (tokens.css only, zero hex); .panel-card gains specular ::before + rounded corners; @supports/reduced-transparency/print ladders + .fig exemption + active-only scoping + -webkit dual-write all preserved; build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Re-tune the overlay to the brighter sky; hold every engine invariant</name>
  <files>src/lib/nightsky/constellations.ts, src/lib/nightsky/scene.ts, src/lib/nightsky/aurora.ts</files>
  <action>
Re-tune the drawn overlay so it still reads correctly over the brighter, busier photo from 11-01
(per 11-CONTEXT.md recipe item 5, BOLD-05). Touch the alpha/brightness knobs only — no structural
engine change:

- constellations.ts: constellations must still read as clearly AUTHORED over the brighter photo —
  bump the star/link/halo alpha targets and/or halo scale in the HIGHLIGHT_STATES table (ambient /
  brightened / dimmed) enough that the lines stay legible against a busier background. All
  stars/links/halos/beams keep reading --star exclusively (never --milkyway, never --accent).
- scene.ts: fireflies keep the --accent copper tint (the ONLY Layer-2 accent use); confirm the
  twinkle/firefly alphas still read over the brighter field. The warm ambient glow is Layer-1 DOM
  (Task 1) — not here.
- aurora.ts: the aurora envelope stays under the core ceiling — a brighter MW core gives MORE
  headroom under the moon<core and aurora<core ceilings (good), so no envelope change is required
  unless 11-03's --aurora/--moon check flags it. Keep source-over compositing; do not touch the
  breathing period.

HOLD every v3.0 engine invariant: the single rAF stays in scene.ts (exactly 2
requestAnimationFrame; clouds/aurora/parallax/constellations/starfield/meteors = 0). The re-tune
is ALPHA-ONLY — it adds NO new composite-op changes: the aurora/cloud AREA fills stay source-over,
and the existing point-feature additive draws (the constellation firing beam + meteors — the ONLY
sanctioned additive uses) are unchanged (baseline count 2, aurora.ts stays additive-free). The
canvas / host are NEVER CSS-transformed; the `nightsky:panel-change` event-name coupling is
unchanged; reduced-motion still renders exactly one static frame. Moon stays dim. Meteors unchanged.
  </action>
  <verify>
    <automated>test "$(grep -o 'requestAnimationFrame' src/lib/nightsky/scene.ts | wc -l | tr -d ' ')" = "2" && test "$(grep -o 'requestAnimationFrame' src/lib/nightsky/constellations.ts | wc -l | tr -d ' ')" = "0" && test "$(grep -o 'requestAnimationFrame' src/lib/nightsky/aurora.ts | wc -l | tr -d ' ')" = "0" && test "$(grep -rEo "globalCompositeOperation[[:space:]]*=[[:space:]]*['\"]lighter['\"]" src/lib/nightsky | wc -l | tr -d ' ')" = "2" && test "$(grep -Eo "globalCompositeOperation[[:space:]]*=[[:space:]]*['\"]lighter['\"]" src/lib/nightsky/aurora.ts | wc -l | tr -d ' ')" = "0" && npx astro check</automated>
    <human-check>Constellations read as authored over the brighter photo; fireflies carry copper; moon stays dim; nothing regressed visually.</human-check>
  </verify>
  <done>Overlay alphas re-tuned for the brighter sky; single-rAF (2/0/0), no 'lighter' composite, no canvas transform, source-over only; astro check 0 errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author -> shipped CSS/JS | token + overlay changes affect the accessibility degradation ladders every visitor's browser resolves |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-04 | Tampering | glass degradation ladders (@supports / reduced-transparency / print) | high | mitigate | Preserve all three ladders + the -webkit dual-write + active-panel scoping; verify asserts each ladder token count is non-zero and the -webkit/standard pair count matches. A dropped ladder silently breaks accessibility fallbacks. |
| T-11-05 | Denial of Service | overlay alpha bump inflating idle CPU | medium | mitigate | Alpha-only re-tune (no new draws, no new rAF); single-rAF assertion holds; the ambient soak (<10%) is re-proven in 11-03. |
| T-11-06 | Elevation of Privilege | hex literal leaking outside tokens.css | low | mitigate | verify asserts zero 6-hex in deck.css; glass values live in tokens.css only; the glow uses relative-color from --accent. |
</threat_model>

<verification>
- `npm run build` succeeds; `npx astro check` reports 0 errors.
- Camper SVG + fill rules absent from NightSky.astro; a relative-color warm glow remains.
- @supports + reduced-transparency + print ladders present in deck.css; every backdrop-filter has a -webkit sibling; zero hex in deck.css.
- single-rAF (scene 2 / constellations 0 / aurora 0); no `'lighter'` composite assignment in src/lib/nightsky.
</verification>

<success_criteria>
- Camper cut; clean warm ambient glow low in the frame (human eyeball).
- Card reads as premium liquid glass, both tiers; all v3.0 ladders + engine invariants intact.
- Constellations authored over the brighter sky; moon dim; fireflies copper.
- Nothing pushed.
</success_criteria>

<output>
Create `.planning/phases/11-bolder-sky/11-02-SUMMARY.md` when done.
</output>
